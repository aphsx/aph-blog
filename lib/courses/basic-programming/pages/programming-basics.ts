import type { Page } from "@/lib/types";

export const programmingBasicsPages: Record<string, Page> = {
  // ─────────────────────────────────────────────────────────────
  "pb-variables": {
    slug: "pb-variables",
    title: { th: "ตัวแปร & ชนิดข้อมูล (Variables & Data Types)", en: "" },
    lead: { th: "กล่องเก็บข้อมูลและชนิดของข้อมูลแต่ละแบบ — รากฐานของทุกบรรทัดที่คุณจะเขียนต่อจากนี้", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "ตัวแปร (variable) คือ \"กล่องที่ตั้งชื่อไว้\" สำหรับเก็บข้อมูลในหน่วยความจำ (RAM) เพื่อนำกลับมาใช้ทีหลัง นี่คือสิ่งแรกที่ทุกโปรแกรมต้องใช้ บทย่อยนี้จะเจาะลึกตั้งแต่วิธีสร้าง ชนิดข้อมูลแต่ละแบบ การแปลงชนิด ไปจนถึงกับดักที่มือใหม่เจอบ่อย" },
        { t: "callout", title: "พิมพ์ตามไปด้วยทุกตัวอย่าง", c: "เปิด replit.com หรือ programiz.com/python-programming/online-compiler แล้วพิมพ์โค้ดทุกชิ้นเอง พร้อมลองเปลี่ยนค่าดูผลลัพธ์ การลงมือทำคือสิ่งที่ทำให้เข้าใจจริง" },

        { t: "h2", c: "สร้างตัวแปรอย่างไร" },
        { t: "p", c: "ใน Python สร้างตัวแปรด้วยการกำหนดค่าผ่านเครื่องหมาย = (อ่านว่า \"กำหนดให้เท่ากับ\" ไม่ใช่ \"เท่ากับ\" ทางคณิตศาสตร์) ฝั่งซ้ายคือชื่อ ฝั่งขวาคือค่า และไม่ต้องประกาศชนิดล่วงหน้า Python รู้ชนิดเองจากค่า" },
        { t: "code", lang: "python", c: "age = 25            # สร้างกล่องชื่อ age เก็บเลข 25\nname = \"Aphisit\"    # กล่อง name เก็บข้อความ\n\nprint(age)          # 25\nprint(name)         # Aphisit\n\nage = age + 1       # อ่านค่าเดิม (25) บวก 1 แล้วเก็บกลับ\nprint(age)          # 26" },
        { t: "p", c: "ลำดับการทำงานของ age = age + 1 สำคัญ: คอมพิวเตอร์คำนวณฝั่งขวาก่อน (25 + 1 = 26) แล้วค่อยเก็บผลลัพธ์กลับเข้ากล่อง age การเข้าใจจุดนี้ช่วยให้ไม่งงกับโค้ดที่อัปเดตตัวเอง" },

        { t: "h2", c: "ชนิดข้อมูลพื้นฐาน (Data Types)" },
        { t: "p", c: "ข้อมูลมีหลายชนิด แต่ละชนิดทำสิ่งที่ต่างกัน เช่นบวกตัวเลขได้ผลเป็นผลรวม แต่ \"บวก\" ข้อความได้ผลเป็นการต่อคำ จำ 5 ชนิดพื้นฐานนี้ให้ขึ้นใจ" },
        {
          t: "table",
          head: ["ชนิด", "ชื่อเต็ม", "ตัวอย่าง", "ใช้เก็บ"],
          rows: [
            ["int", "integer", "0, 25, -7, 1000000", "จำนวนเต็ม"],
            ["float", "floating point", "1.75, -0.5, 3.0", "เลขทศนิยม"],
            ["str", "string", '"hello", "ก", "123"', "ข้อความ"],
            ["bool", "boolean", "True, False", "ค่าจริง/เท็จ"],
            ["NoneType", "none", "None", "ไม่มีค่า / ยังไม่กำหนด"],
          ],
        },
        { t: "code", lang: "python", c: "age = 25            # int\nheight = 1.75       # float\nname = \"Aphisit\"    # str\nis_student = True   # bool (ขึ้นต้นตัวใหญ่: True / False)\nmiddle_name = None  # ยังไม่มีค่า\n\n# เช็คชนิดด้วยฟังก์ชัน type()\nprint(type(age))    # <class 'int'>\nprint(type(height)) # <class 'float'>\nprint(type(name))   # <class 'str'>" },
        { t: "callout", title: "int กับ float ต่างกันอย่างไร", c: "int เก็บจำนวนเต็ม (ไม่มีจุดทศนิยม) ส่วน float เก็บเลขมีจุดทศนิยม สังเกตว่า 3 เป็น int แต่ 3.0 เป็น float แม้ค่าจะเท่ากัน และผลของการหาร / จะเป็น float เสมอ (10 / 2 = 5.0 ไม่ใช่ 5)" },

        { t: "h3", c: "ข้อความ (String) เขียนได้หลายแบบ" },
        { t: "code", lang: "python", c: "a = 'เครื่องหมายคำพูดเดี่ยวก็ได้'\nb = \"หรือคู่ก็ได้ ผลเหมือนกัน\"\nc = \"\"\"ข้อความ\nหลายบรรทัด\nใช้สามตัว\"\"\"\n\n# ถ้าในข้อความมีเครื่องหมายคำพูด ให้ใช้ชนิดสลับกัน\nquote = 'เขาพูดว่า \"สวัสดี\"'\nprint(quote)   # เขาพูดว่า \"สวัสดี\"" },

        { t: "h2", c: "การแปลงชนิดข้อมูล (Type Conversion)" },
        { t: "p", c: "บ่อยมากที่เราต้องแปลงชนิด โดยเฉพาะข้อมูลที่รับจากผู้ใช้ผ่าน input() ซึ่งได้เป็น str เสมอ ถ้าจะนำไปคำนวณต้องแปลงเป็นตัวเลขก่อน" },
        { t: "code", lang: "python", c: "x = \"5\"             # นี่คือข้อความ ไม่ใช่ตัวเลข\nprint(x + x)        # 55   (ต่อข้อความ ไม่ใช่ 10!)\nprint(int(x) + 1)   # 6    (แปลงเป็น int ก่อนค่อยบวก)\n\nprint(int(\"42\"))    # 42    str -> int\nprint(float(\"3.5\")) # 3.5   str -> float\nprint(str(100))     # '100' int -> str\nprint(int(3.9))     # 3     float -> int (ตัดเศษทิ้ง ไม่ปัด!)\nprint(bool(0))      # False (0 = เท็จ, ตัวเลขอื่น = จริง)" },
        { t: "callout", title: "กับดักที่เจอบ่อยสุด", warn: true, c: "ลืมแปลง input() เป็นตัวเลข แล้วเอาไปบวกกัน เช่น a = input() ; b = input() ; print(a + b) ถ้าใส่ 2 กับ 3 จะได้ '23' ไม่ใช่ 5 ต้องครอบด้วย int() ตอนรับ: a = int(input())" },

        { t: "h2", c: "กฎและธรรมเนียมการตั้งชื่อตัวแปร" },
        { t: "p", c: "ชื่อตัวแปรที่ดีคือชื่อที่อ่านแล้วรู้เลยว่าเก็บอะไร ไม่ต้องเดา โค้ดจริงถูกอ่านบ่อยกว่าถูกเขียน การตั้งชื่อดีจึงสำคัญมาก" },
        {
          t: "table",
          head: ["กฎ", "ถูก", "ผิด"],
          rows: [
            ["ใช้ตัวพิมพ์เล็กคั่นด้วย _ (snake_case)", "user_age", "UserAge / userage"],
            ["สื่อความหมาย", "total_price", "x / tp / a1"],
            ["ห้ามขึ้นต้นด้วยตัวเลข", "item1", "1item"],
            ["ห้ามมีช่องว่างหรืออักขระพิเศษ", "first_name", "first name"],
            ["ห้ามใช้คำสงวน", "class_name", "class / for / if"],
          ],
        },
        { t: "callout", title: "ค่าคงที่ (Constant)", c: "ถ้าเป็นค่าที่ไม่ตั้งใจให้เปลี่ยน นิยมตั้งชื่อด้วยตัวพิมพ์ใหญ่ทั้งหมด เช่น PI = 3.14159 หรือ MAX_SCORE = 100 Python ไม่ได้บังคับ แต่เป็นสัญญาณบอกเพื่อนร่วมทีมว่า \"อย่าแก้ค่านี้\"" },

        { t: "h2", c: "กำหนดค่าหลายตัวพร้อมกัน" },
        { t: "code", lang: "python", c: "x, y, z = 1, 2, 3       # กำหนดทีเดียวหลายตัว\nprint(x, y, z)          # 1 2 3\n\na = b = c = 0           # กำหนดค่าเดียวกันให้หลายตัว\n\nx, y = y, x             # สลับค่ากันได้ในบรรทัดเดียว!\nprint(x, y)             # 2 1" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "ตัวแปรคือกล่องตั้งชื่อเก็บข้อมูลใน RAM สร้างด้วย ชื่อ = ค่า",
            "ชนิดพื้นฐาน: int, float, str, bool, None — เช็คด้วย type()",
            "แปลงชนิดด้วย int(), float(), str(), bool() — สำคัญมากกับ input()",
            "ตั้งชื่อแบบ snake_case ให้สื่อความหมาย เลี่ยงคำสงวน",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) สร้างตัวแปรเก็บชื่อ อายุ ส่วนสูง แล้ว print ออกมาเป็นประโยค  2) รับเลข 2 ตัวจาก input() แล้วหาผลรวม (ระวังต้อง int ก่อน)  3) ลองสลับค่าตัวแปร 2 ตัวด้วยบรรทัดเดียว  4) ทดลอง int(3.9), bool(0), bool(\"hi\") ดูผลลัพธ์ว่าได้อะไร" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: ตัวดำเนินการ (Operators) →", slug: "pb-operators", desc: "คำนวณ เปรียบเทียบ และเชื่อมตรรกะ" },
            { title: "← กลับหน้าภาพรวมคอร์ส", slug: "learn" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-operators": {
    slug: "pb-operators",
    title: { th: "ตัวดำเนินการ (Operators)", en: "" },
    lead: { th: "เครื่องหมายที่ใช้คำนวณ เปรียบเทียบ และเชื่อมเงื่อนไข — พร้อมลำดับความสำคัญที่ต้องระวัง", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "ตัวดำเนินการ (operator) คือสัญลักษณ์ที่ใช้กระทำกับค่า เช่น + คูณ เปรียบเทียบ แบ่งเป็นกลุ่มหลัก ๆ ที่ต้องรู้จักทั้งหมด" },

        { t: "h2", c: "ตัวดำเนินการเลขคณิต (Arithmetic)" },
        { t: "code", lang: "python", c: "print(7 + 3)   # 10   บวก\nprint(7 - 3)   # 4    ลบ\nprint(7 * 3)   # 21   คูณ\nprint(7 / 3)   # 2.333...  หาร (ได้ float เสมอ)\nprint(7 // 3)  # 2    หารปัดเศษลง (floor division)\nprint(7 % 3)   # 1    เศษจากการหาร (modulo)\nprint(2 ** 3)  # 8    ยกกำลัง (2 ยกกำลัง 3)" },
        {
          t: "table",
          head: ["ตัวดำเนินการ", "ความหมาย", "7 op 2 ได้"],
          rows: [
            ["+", "บวก", "9"],
            ["-", "ลบ", "5"],
            ["*", "คูณ", "14"],
            ["/", "หาร (float)", "3.5"],
            ["//", "หารปัดลง", "3"],
            ["%", "เศษ (modulo)", "1"],
            ["**", "ยกกำลัง", "49"],
          ],
        },
        { t: "callout", title: "% (modulo) มีประโยชน์เกินคาด", c: "ใช้เช็คเลขคู่/คี่: n % 2 == 0 คือคู่ ใช้เช็คหารลงตัว: n % 5 == 0 คือหาร 5 ลงตัว และใช้วนค่าเป็นวง เช่น (i + 1) % 7 ให้เลขวันในสัปดาห์ 0-6 เทคนิคนี้เจอบ่อยมากในข้อสอบ" },

        { t: "h2", c: "ตัวดำเนินการเปรียบเทียบ (Comparison)" },
        { t: "p", c: "ผลลัพธ์ของการเปรียบเทียบเป็น bool (True หรือ False) เสมอ ใช้เป็นเงื่อนไขในบทถัดไป" },
        { t: "code", lang: "python", c: "print(5 == 5)   # True   เท่ากันไหม (สองตัว! ไม่ใช่ = ตัวเดียว)\nprint(5 != 3)   # True   ไม่เท่ากัน\nprint(5 > 3)    # True   มากกว่า\nprint(5 < 3)    # False  น้อยกว่า\nprint(5 >= 5)   # True   มากกว่าหรือเท่ากับ\nprint(5 <= 4)   # False  น้อยกว่าหรือเท่ากับ\n\nprint(\"abc\" == \"abc\")  # True  เปรียบเทียบข้อความก็ได้" },
        { t: "callout", title: "= กับ == ต่างกันคนละเรื่อง", warn: true, c: "= (ตัวเดียว) คือ \"กำหนดค่า\" เช่น x = 5 ส่วน == (สองตัว) คือ \"เปรียบเทียบว่าเท่ากันไหม\" เช่น x == 5 สับสนสองตัวนี้คือบั๊กยอดฮิตของมือใหม่ทุกคน" },

        { t: "h2", c: "ตัวดำเนินการตรรกะ (Logical)" },
        { t: "p", c: "ใช้เชื่อมเงื่อนไขหลายอันเข้าด้วยกัน มี 3 ตัว: and, or, not" },
        { t: "code", lang: "python", c: "age = 20\nhas_id = True\n\nprint(age >= 18 and age < 60)  # True  (จริงทั้งคู่)\nprint(age < 13 or age > 65)    # False (ไม่จริงสักอัน)\nprint(not has_id)              # False (กลับค่า)\n\n# เชื่อมหลายเงื่อนไขได้\ncan_enter = age >= 18 and has_id and age < 100\nprint(can_enter)               # True" },
        {
          t: "table",
          head: ["A", "B", "A and B", "A or B"],
          rows: [
            ["True", "True", "True", "True"],
            ["True", "False", "False", "True"],
            ["False", "True", "False", "True"],
            ["False", "False", "False", "False"],
          ],
        },
        { t: "callout", title: "จำง่าย ๆ", c: "and = ต้องจริงทั้งหมดถึงจะจริง (เข้มงวด) · or = จริงแค่อันเดียวก็พอ (ใจดี) · not = กลับค่าจริงเป็นเท็จ เท็จเป็นจริง" },

        { t: "h2", c: "ตัวดำเนินการกำหนดค่าแบบย่อ (Augmented Assignment)" },
        { t: "p", c: "การอัปเดตค่าตัวเองเขียนย่อได้ ช่วยให้โค้ดสั้นลงและอ่านง่ายขึ้น เจอบ่อยมากในการนับและสะสมค่า" },
        { t: "code", lang: "python", c: "count = 10\ncount += 5    # เท่ากับ count = count + 5  -> 15\ncount -= 3    # -> 12\ncount *= 2    # -> 24\ncount //= 5   # -> 4\nprint(count)  # 4\n\ntext = \"Ha\"\ntext += \"ha\"  # ต่อข้อความก็ได้ -> 'Haha'\nprint(text)" },

        { t: "h2", c: "ลำดับความสำคัญ (Operator Precedence)" },
        { t: "p", c: "เมื่อมีหลายตัวดำเนินการในบรรทัดเดียว Python ทำตามลำดับเหมือนคณิตศาสตร์: วงเล็บ → ยกกำลัง → คูณ/หาร → บวก/ลบ → เปรียบเทียบ → ตรรกะ ถ้าไม่แน่ใจให้ใส่วงเล็บไว้ ปลอดภัยและอ่านง่ายกว่า" },
        { t: "code", lang: "python", c: "print(2 + 3 * 4)      # 14   (คูณก่อน: 2 + 12)\nprint((2 + 3) * 4)    # 20   (วงเล็บก่อน: 5 * 4)\nprint(10 - 2 ** 2)    # 6    (ยกกำลังก่อน: 10 - 4)\nprint(True or False and False)  # True (and ก่อน or)" },

        { t: "h2", c: "Membership (in) & Identity (is)" },
        { t: "p", c: "อีกสองกลุ่มที่ใช้บ่อยมาก: in เช็คว่า \"มีอยู่ในกลุ่มไหม\" และ is เช็คว่า \"เป็น object เดียวกันไหม\" (ต่างจาก == ที่เช็คค่าเท่ากัน)" },
        { t: "code", lang: "python", c: "# in / not in — เช็คสมาชิก (ใช้กับ list, str, dict)\nprint(\"a\" in \"cat\")           # True\nprint(3 in [1, 2, 3])         # True\nprint(\"x\" not in \"cat\")       # True\n\nfruits = [\"แอปเปิล\", \"กล้วย\"]\nif \"กล้วย\" in fruits:\n    print(\"มีกล้วย\")\n\n# is — เช็คว่าเป็น object เดียวกัน (ใช้กับ None เป็นหลัก)\nx = None\nprint(x is None)              # True  (วิธีมาตรฐานเช็ค None)\nprint(x == None)              # ได้ผลเหมือนกันแต่ไม่นิยม — ใช้ is" },
        { t: "callout", title: "== เทียบค่า, is เทียบ object", c: "ใช้ == เกือบทุกครั้งที่เทียบค่า ส่วน is ใช้เฉพาะกับ None (x is None) ตามธรรมเนียม Python — อย่าใช้ is เทียบตัวเลข/ข้อความทั่วไป เพราะผลอาจไม่เป็นอย่างที่คิด" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "เลขคณิต: + - * / // % ** — จำ % ไว้ใช้เช็คคู่/คี่และหารลงตัว",
            "เปรียบเทียบ: == != > < >= <= ได้ผลเป็น True/False (อย่าสับสน = กับ ==)",
            "ตรรกะ: and (จริงทั้งหมด), or (จริงอย่างน้อยหนึ่ง), not (กลับค่า)",
            "กำหนดค่าย่อ: += -= *= //= ช่วยอัปเดตค่าตัวเองสั้นลง",
            "membership: in / not in เช็คสมาชิก; identity: is ใช้เช็ค None",
            "ลำดับความสำคัญตามคณิตศาสตร์ — ไม่แน่ใจให้ใส่วงเล็บ",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) รับเลขจาก input() แล้วบอกว่าคู่หรือคี่ด้วย %  2) คำนวณว่า 137 วินาที = กี่นาทีกี่วินาที (ใช้ // และ %)  3) ลองทำนายผลของ 2 + 3 * 4 ** 2 ก่อนรันจริง  4) เขียนเงื่อนไขเช็คว่าปีหนึ่งเป็นปีอธิกสุรทินไหม (หาร 4 ลงตัว และไม่หาร 100 ลงตัว หรือหาร 400 ลงตัว)" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: รับข้อมูล & แสดงผล →", slug: "pb-io", desc: "input(), print() และ f-string" },
            { title: "← ตัวแปร & ชนิดข้อมูล", slug: "pb-variables" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-io": {
    slug: "pb-io",
    title: { th: "รับข้อมูล & แสดงผล (Input & Output)", en: "" },
    lead: { th: "คุยกับผู้ใช้ผ่าน input() และ print() พร้อม f-string สำหรับจัดรูปข้อความให้สวยงาม", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "โปรแกรมที่มีประโยชน์ต้องรับข้อมูลเข้าและแสดงผลออก หัวข้อนี้เจาะ input() กับ print() ให้ลึก รวมถึง f-string ซึ่งเป็นวิธีจัดรูปข้อความที่ใช้บ่อยที่สุดในงานจริง" },

        { t: "h2", c: "แสดงผลด้วย print()" },
        { t: "code", lang: "python", c: "print(\"Hello\")              # Hello\nprint(\"a\", \"b\", \"c\")        # a b c   (คั่นด้วยช่องว่างอัตโนมัติ)\nprint(\"a\", \"b\", sep=\"-\")    # a-b     (เปลี่ยนตัวคั่นด้วย sep)\nprint(\"ไม่ขึ้นบรรทัดใหม่\", end=\" \")  # คุม end (ปกติคือขึ้นบรรทัดใหม่)\nprint(\"ต่อท้ายเลย\")          # ไม่ขึ้นบรรทัดใหม่ ต่อท้ายเลย" },

        { t: "h2", c: "f-string — วิธีจัดรูปข้อความที่ดีที่สุด" },
        { t: "p", c: "f-string คือใส่ตัว f นำหน้าเครื่องหมายคำพูด แล้ววางตัวแปรไว้ในวงเล็บปีกกา {} Python จะแทนค่าให้อัตโนมัติ อ่านง่ายและทรงพลังมาก" },
        { t: "code", lang: "python", c: "name = \"Aphisit\"\nage = 25\nprint(f\"สวัสดี {name} อายุ {age} ปี\")\n# สวัสดี Aphisit อายุ 25 ปี\n\n# คำนวณในวงเล็บได้เลย\nprint(f\"ปีหน้าอายุ {age + 1} ปี\")   # ปีหน้าอายุ 26 ปี\n\n# จัดรูปทศนิยม: :.2f = 2 ตำแหน่ง\npi = 3.14159\nprint(f\"ค่า pi = {pi:.2f}\")          # ค่า pi = 3.14\n\n# จัดความกว้าง/หลักพัน\nprice = 1234567\nprint(f\"ราคา {price:,} บาท\")          # ราคา 1,234,567 บาท" },
        { t: "callout", title: "ทำไมต้อง f-string", c: "วิธีเก่าใช้ + ต่อข้อความ เช่น \"อายุ \" + str(age) + \" ปี\" ซึ่งต้องแปลงชนิดเองและอ่านยาก f-string สั้นกว่า ปลอดภัยกว่า และเป็นมาตรฐานที่ใช้กันทั่วไปในโค้ดสมัยใหม่" },

        { t: "h2", c: "รับข้อมูลด้วย input()" },
        { t: "p", c: "input() หยุดรอให้ผู้ใช้พิมพ์แล้วกด Enter ค่าที่ได้เป็นข้อความ (str) เสมอ — แม้ผู้ใช้พิมพ์ตัวเลข ถ้าจะคำนวณต้องแปลงชนิดก่อน" },
        { t: "code", lang: "python", c: "name = input(\"คุณชื่ออะไร: \")        # ได้เป็น str\nage = int(input(\"อายุเท่าไหร่: \"))   # รับแล้วแปลงเป็น int ทันที\nheight = float(input(\"สูงกี่เมตร: \"))# แปลงเป็น float\n\nprint(f\"สวัสดี {name}\")\nprint(f\"อีก 10 ปีคุณจะอายุ {age + 10} ปี\")" },
        { t: "callout", title: "input() ได้ str เสมอ — จำให้ขึ้นใจ", warn: true, c: "ถ้าลืมครอบ int() แล้วเอาไปบวก จะกลายเป็นการต่อข้อความ หรือถ้าเทียบ age > 18 โดย age เป็น str จะ error ติดนิสัยครอบ int()/float() ตั้งแต่ตอนรับเลย" },

        { t: "h2", c: "ตัวอย่างรวม: โปรแกรมคำนวณ BMI" },
        { t: "code", lang: "python", c: "print(\"=== คำนวณ BMI ===\")\nweight = float(input(\"น้ำหนัก (กก.): \"))\nheight = float(input(\"ส่วนสูง (เมตร): \"))\n\nbmi = weight / (height ** 2)\nprint(f\"ค่า BMI ของคุณคือ {bmi:.1f}\")\n# ถ้าใส่ 60 และ 1.70 -> ค่า BMI ของคุณคือ 20.8" },

        { t: "h2", c: "รับหลายค่าในบรรทัดเดียว (split / map)" },
        { t: "p", c: "บ่อยครั้งผู้ใช้พิมพ์หลายค่าคั่นด้วยช่องว่างในบรรทัดเดียว — ใช้ .split() หั่นเป็นลิสต์ แล้ว map() แปลงชนิดทีเดียว เทคนิคนี้เจอบ่อยมากเวลาแก้โจทย์/รับ input หลายค่า" },
        { t: "code", lang: "python", c: "# ผู้ใช้พิมพ์:  10 20 30\nparts = input(\"ใส่เลขคั่นด้วยช่องว่าง: \").split()   # ['10','20','30']\nnums = [int(x) for x in parts]                       # [10, 20, 30]\nprint(sum(nums))                                     # 60\n\n# รับ 2 ค่าแยกตัวแปรในบรรทัดเดียว\na, b = input(\"ใส่ 2 เลข: \").split()\na, b = int(a), int(b)\nprint(a + b)\n\n# สั้นสุดด้วย map\nx, y = map(int, input().split())   # แปลงทุกตัวเป็น int พร้อมแยกตัวแปร" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "print() แสดงผล ปรับ sep และ end ได้",
            "f-string (f\"...{ตัวแปร}...\") คือวิธีจัดรูปข้อความที่ดีที่สุด — คำนวณและจัดรูปทศนิยม/หลักพันได้",
            "input() รับข้อความจากผู้ใช้ ได้เป็น str เสมอ ต้องแปลงชนิดก่อนคำนวณ",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) รับชื่อและปีเกิด แล้ว print อายุโดยประมาณด้วย f-string  2) รับราคาสินค้าและจำนวน แล้วแสดงราคารวมแบบมีหลักพัน (:,)  3) เขียนโปรแกรมคำนวณพื้นที่สามเหลี่ยมจากฐานและสูง แสดงผลทศนิยม 2 ตำแหน่ง" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: เงื่อนไข (Conditionals) →", slug: "pb-conditionals", desc: "ให้โปรแกรมตัดสินใจด้วย if/elif/else" },
            { title: "← ตัวดำเนินการ", slug: "pb-operators" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-conditionals": {
    slug: "pb-conditionals",
    title: { th: "เงื่อนไข (Conditionals: if / elif / else)", en: "" },
    lead: { th: "ทำให้โปรแกรมตัดสินใจได้ — เลือกทำคำสั่งต่างกันตามสถานการณ์", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "เงื่อนไขทำให้โปรแกรม \"ฉลาด\" ขึ้น เพราะเลือกทำสิ่งที่ต่างกันได้ตามข้อมูลที่เจอ เช่นถ้าคะแนนถึงเกณฑ์ให้ผ่าน ไม่ถึงให้ตก หัวใจคือคำว่า if (ถ้า)" },

        { t: "h2", c: "if พื้นฐาน" },
        { t: "p", c: "if ตามด้วยเงื่อนไขและ : จากนั้นบรรทัดที่ย่อหน้าเข้าไปคือคำสั่งที่จะทำเมื่อเงื่อนไขเป็นจริง" },
        { t: "code", lang: "python", c: "age = 20\nif age >= 18:\n    print(\"คุณบรรลุนิติภาวะแล้ว\")\n    print(\"สามารถทำบัตรประชาชนได้\")  # ย่อหน้าเท่ากัน = อยู่ใน if เดียวกัน\nprint(\"จบโปรแกรม\")  # ไม่ย่อหน้า = ทำเสมอ ไม่ขึ้นกับ if" },
        { t: "callout", title: "การย่อหน้า (indentation) สำคัญที่สุดใน Python", warn: true, c: "Python ใช้การย่อหน้า (มาตรฐาน 4 ช่องว่าง) เพื่อบอกว่าคำสั่งไหนอยู่ในบล็อกไหน ต่างจากภาษาอื่นที่ใช้วงเล็บปีกกา {} ถ้าย่อหน้าผิดจะ error หรือทำงานผิด ห้ามผสม space กับ tab เด็ดขาด" },

        { t: "h2", c: "if / else — มีทางเลือกสำรอง" },
        { t: "code", lang: "python", c: "score = 45\nif score >= 50:\n    print(\"ผ่าน\")\nelse:               # ถ้าเงื่อนไข if เป็นเท็จ\n    print(\"ไม่ผ่าน\")\n# ออก: ไม่ผ่าน" },

        { t: "h2", c: "if / elif / else — หลายทางเลือก" },
        { t: "p", c: "elif (ย่อจาก else if) ใช้ตรวจเงื่อนไขถัดไปเมื่อเงื่อนไขก่อนหน้าเป็นเท็จ Python ตรวจจากบนลงล่าง เจออันแรกที่จริงก็ทำแล้วหยุด มีกี่ elif ก็ได้" },
        { t: "code", lang: "python", c: "score = 75\nif score >= 80:\n    grade = \"A\"\nelif score >= 70:       # มาถึงนี่แปลว่า score < 80 แล้ว\n    grade = \"B\"\nelif score >= 60:\n    grade = \"C\"\nelif score >= 50:\n    grade = \"D\"\nelse:\n    grade = \"F\"\nprint(f\"เกรด {grade}\")   # เกรด B" },
        { t: "callout", title: "ลำดับของเงื่อนไขสำคัญ", c: "เพราะ Python หยุดที่เงื่อนไขแรกที่จริง ต้องเรียงจากเข้มงวดที่สุดไปหลวมที่สุด ถ้าสลับเอา score >= 50 ขึ้นก่อน คนได้ 90 ก็จะได้เกรด D เพราะเข้าเงื่อนไขแรกที่จริงทันที" },

        { t: "h2", c: "เงื่อนไขซ้อนกัน (Nested if)" },
        { t: "code", lang: "python", c: "logged_in = True\nis_admin = False\n\nif logged_in:\n    if is_admin:\n        print(\"ยินดีต้อนรับผู้ดูแลระบบ\")\n    else:\n        print(\"ยินดีต้อนรับผู้ใช้ทั่วไป\")\nelse:\n    print(\"กรุณาเข้าสู่ระบบ\")\n# ออก: ยินดีต้อนรับผู้ใช้ทั่วไป" },
        { t: "p", c: "เงื่อนไขซ้อนกันลึกมาก ๆ จะอ่านยาก มักเขียนใหม่ด้วย and ให้แบนราบกว่าได้ เช่น if logged_in and is_admin:" },

        { t: "h2", c: "Truthy / Falsy — ค่าที่ถือว่าจริง/เท็จ" },
        { t: "p", c: "ใน if ค่าที่ไม่ใช่ bool ก็ใช้เป็นเงื่อนไขได้ Python มองบางค่าเป็น \"เท็จ\" (falsy) ได้แก่: 0, ค่าว่าง (\"\", [], {}), และ None ส่วนค่าอื่นเป็น \"จริง\" (truthy)" },
        { t: "code", lang: "python", c: "name = input(\"ชื่อ: \")\nif name:                 # ข้อความว่าง = เท็จ, มีตัวอักษร = จริง\n    print(f\"สวัสดี {name}\")\nelse:\n    print(\"คุณไม่ได้ใส่ชื่อ\")\n\nitems = []\nif not items:            # ลิสต์ว่าง = เท็จ, not = จริง\n    print(\"ตะกร้าว่าง\")" },

        { t: "h2", c: "เขียนสั้นด้วย ternary และ match" },
        { t: "code", lang: "python", c: "# ternary: เลือกค่าในบรรทัดเดียว\nage = 20\nstatus = \"ผู้ใหญ่\" if age >= 18 else \"เด็ก\"\nprint(status)   # ผู้ใหญ่\n\n# match (Python 3.10+): เทียบหลายค่าแบบสะอาด\nday = 3\nmatch day:\n    case 1: print(\"จันทร์\")\n    case 2: print(\"อังคาร\")\n    case 3: print(\"พุธ\")\n    case _: print(\"วันอื่น\")   # _ = กรณีอื่น ๆ ทั้งหมด" },

        { t: "h2", c: "เกร็ดเงื่อนไขที่ใช้จริงในงาน" },
        { t: "code", lang: "python", c: "score = 75\n\n# เทียบช่วงแบบลูกโซ่ (chained) — เขียนเหมือนคณิตศาสตร์ได้เลย\nif 0 <= score <= 100:          # = (0 <= score) and (score <= 100)\n    print(\"คะแนนอยู่ในช่วงที่ถูกต้อง\")\n\n# เช็คว่าอยู่ในกลุ่มไหม ด้วย in (สั้นกว่าเขียน or หลายอัน)\nday = \"Sat\"\nif day in (\"Sat\", \"Sun\"):       # แทน day==\"Sat\" or day==\"Sun\"\n    print(\"วันหยุด\")" },
        { t: "callout", title: "= กับ == สับสนคือบั๊กยอดฮิต", warn: true, c: "= คือ \"กำหนดค่า\" (x = 5) ส่วน == คือ \"เปรียบเทียบ\" (if x == 5) เขียน if x = 5 จะ error ทันที โชคดีที่ Python ฟ้อง — แต่ในเงื่อนไขซับซ้อนให้ระวังเสมอ และจำว่าเช็ค None ใช้ if x is None ไม่ใช่ ==" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "if ทำคำสั่งเมื่อเงื่อนไขจริง, else เป็นทางสำรอง, elif ตรวจเงื่อนไขถัดไป",
            "การย่อหน้าบอกขอบเขตของบล็อก — ผิดแล้ว error",
            "เรียงเงื่อนไขจากเข้มงวดไปหลวม เพราะหยุดที่อันแรกที่จริง",
            "ค่า falsy: 0, \"\", [], {}, None — ที่เหลือ truthy",
            "ternary และ match ช่วยเขียนเงื่อนไขให้สั้นและสะอาด",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) รับคะแนน แล้วแปลงเป็นเกรด A-F  2) รับเลข บอกว่าเป็นบวก ลบ หรือศูนย์  3) รับปี ค.ศ. บอกว่าเป็นปีอธิกสุรทินไหม  4) เขียนเครื่องคิดเลขง่าย: รับเลข 2 ตัวและเครื่องหมาย (+,-,*,/) แล้วคำนวณตามนั้น (ใช้ if/elif)" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: การวนซ้ำ (Loops) →", slug: "pb-loops", desc: "ทำคำสั่งซ้ำด้วย for และ while" },
            { title: "← รับข้อมูล & แสดงผล", slug: "pb-io" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-loop-choose": {
    slug: "pb-loop-choose",
    title: { th: "เลือก for หรือ while ให้ถูก", en: "" },
    lead: { th: "หน้าก่อนสอนว่า for กับ while เขียนยังไง หน้านี้ตอบคำถามที่ยากกว่า คือจะรู้ได้ยังไงว่าโจทย์ตรงหน้าต้องใช้ตัวไหน", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
              { t: "p", c: "คนที่เพิ่งเรียน loop มักติดปัญหาเดียวกันหมด: รู้ว่า for เขียนยังไง รู้ว่า while เขียนยังไง แต่พอเจอโจทย์จริงแล้วเลือกไม่ถูกว่าควรใช้ตัวไหน สุดท้ายก็ใช้ for ทุกอย่างเพราะคุ้นกว่า แล้วไปเจอทางตันทีหลังโดยไม่รู้ว่าทางตันนั้นเกิดจากการเลือกเครื่องมือผิดตั้งแต่แรก หน้านี้จะให้เกณฑ์ตัดสินที่ใช้ได้จริงกับทุกโจทย์" },

              { t: "h2", c: "คำถามเดียวที่ตัดสินได้: ใครเป็นคนควบคุมการขยับ" },
              { t: "p", c: "ลืมคำอธิบายแบบ \"for วนตามลำดับ while วนตามเงื่อนไข\" ไปก่อน เพราะมันไม่ช่วยตัดสินใจอะไร ให้ถามแทนว่า ใครขยับตัวนับ" },
              {
                t: "table",
                head: ["", "for", "while"],
                rows: [
                  ["ใครขยับตัวนับ", "ลำดับ (range หรือลิสต์) ขยับให้เรา", "เราขยับเองด้วยมือ"],
                  ["ขยับได้แค่ไหน", "ทีละหนึ่งก้าวตามลำดับที่กำหนดไว้ล่วงหน้า แทรกแซงไม่ได้", "อิสระ กระโดด ถอยหลัง หรือขยับหลายก้าวก็ได้"],
                  ["รู้จำนวนรอบก่อนเริ่มไหม", "รู้ (เท่ากับความยาวของลำดับ)", "ไม่รู้ก็ได้ ขึ้นกับว่าเงื่อนไขจะเท็จเมื่อไหร่"],
                  ["ต้องระวังอะไร", "แทบไม่มี ลืมขยับไม่ได้เพราะไม่ได้ขยับเอง", "ลืมขยับ = วนไม่จบตลอดกาล"],
                ],
              },
              { t: "p", c: "ข้อที่สำคัญที่สุดคือแถวที่สอง และมันไม่ใช่เรื่องสไตล์การเขียน แต่เป็นข้อจำกัดจริงของภาษา หลายคนคิดว่าถ้าอยากให้ for กระโดด ก็แก้ค่าตัวนับข้างในลูปสิ — ลองรันดูว่าเกิดอะไรขึ้น" },
              {
                t: "codeout",
                lang: "python",
                label: "พิสูจน์: แก้ตัวนับข้างใน for ไม่มีผลอะไรเลย",
                code: `nums = [10, 20, 30, 40, 50]

print("--- for: พยายามแก้ i ข้างในลูป")
for i in range(len(nums)):
    print("  รอบนี้ i =", i)
    i += 2                      # พยายามกระโดด -> ไม่มีผลอะไรเลย
print("  สรุป: วนครบ 5 รอบเหมือนเดิม บรรทัด i += 2 เหมือนไม่มีอยู่")

print("--- while: เราขยับเอง")
i = 0
while i < len(nums):
    print("  รอบนี้ i =", i)
    i += 2                      # กระโดดได้จริง
print("  สรุป: วน 3 รอบ กระโดดข้ามได้ตามสั่ง")`,
                out: `--- for: พยายามแก้ i ข้างในลูป
  รอบนี้ i = 0
  รอบนี้ i = 1
  รอบนี้ i = 2
  รอบนี้ i = 3
  รอบนี้ i = 4
  สรุป: วนครบ 5 รอบเหมือนเดิม บรรทัด i += 2 เหมือนไม่มีอยู่
--- while: เราขยับเอง
  รอบนี้ i = 0
  รอบนี้ i = 2
  รอบนี้ i = 4
  สรุป: วน 3 รอบ กระโดดข้ามได้ตามสั่ง`,
              },
              { t: "p", c: "เหตุผลคือ for หยิบค่าถัดไปจากลำดับมาใส่ตัวแปร i ใหม่ทุกรอบ ค่าที่เราเขียนทับลงไปเองจึงถูกโยนทิ้งทันทีเมื่อขึ้นรอบใหม่ พูดอีกแบบคือ i ใน for เป็นแค่ ที่พักค่า ไม่ใช่ตัวควบคุมการวน ส่วน i ใน while เป็นตัวควบคุมจริง ๆ เพราะเงื่อนไขอ่านค่าจากมันตรง ๆ" },
              { t: "callout", title: "เกณฑ์ตัดสินใน 1 บรรทัด", c: "ถ้าการขยับไปข้างหน้าของโจทย์ ไม่ใช่ทีละหนึ่งเสมอ (ต้องกระโดด ถอย หรือขยับตามเงื่อนไขที่รู้ตอนรัน) ให้ใช้ while — นอกนั้นใช้ for เพราะปลอดภัยกว่าและอ่านง่ายกว่า" },

              { t: "h2", c: "โจทย์เดียวกัน เขียนสองแบบ แล้วดูว่าอันไหนเหมาะ" },
              { t: "p", c: "งานแรก: บวกเลขทุกตัวในลิสต์ จำนวนรอบเท่ากับความยาวลิสต์เป๊ะ ๆ ไม่มีเงื่อนไขให้กระโดด" },
              {
                t: "code",
                lang: "python",
                c: `nums = [4, 8, 15]

# แบบ for — เหมาะ
total = 0
for n in nums:
    total += n

# แบบ while — ทำได้ แต่ต้องดูแลตัวนับเองโดยไม่ได้ประโยชน์อะไรเพิ่ม
total = 0
i = 0
while i < len(nums):
    total += nums[i]
    i += 1                # ถ้าลืมบรรทัดนี้ โปรแกรมค้างทันที`,
              },
              { t: "p", c: "งานที่สอง: นับกลุ่มตัวอักษรซ้ำที่ติดกัน เช่น aabbbcc ต้องได้ a2 b3 c2 งานนี้หนึ่งรอบของการทำงานคือ หนึ่งกลุ่ม ซึ่งยาวไม่เท่ากัน จึงต้องกระโดดข้ามทั้งกลุ่มในรอบเดียว" },
              {
                t: "code",
                lang: "python",
                c: `s = "aabbbcc"

# แบบ while — เหมาะ เพราะกระโดดข้ามทั้งกลุ่มได้
i = 0
while i < len(s):
    ch = s[i]
    count = 0
    while i < len(s) and s[i] == ch:
        count += 1
        i += 1            # กระโดดทีละกลุ่ม ไม่ใช่ทีละตัว
    print(ch, count)

# แบบ for — ทำได้ แต่ต้องเพิ่มตัวแปรจำสถานะ แล้วโค้ดจดกลุ่มจะซ้ำสองที่
# (ที่หนึ่งในลูป อีกที่หลังลูปจบ เพราะกลุ่มสุดท้ายไม่มีตัวที่ต่างมาปิดท้าย)
# -> ลืมชุดหลังลูปคือบั๊กที่พบบ่อยที่สุดของโจทย์แนวนี้`,
              },
              { t: "callout", title: "สัญญาณว่ากำลังฝืนใช้ for", c: "ถ้าเขียน for แล้วพบว่าต้องเพิ่มตัวแปรจำสถานะหลายตัว หรือต้องก๊อปโค้ดชุดเดิมไปวางซ้ำหลังลูปอีกที นั่นคือสัญญาณว่าโจทย์นี้ควรใช้ while ตั้งแต่แรก โค้ดจะสั้นลงและบั๊กหายไปเอง" },

              { t: "h2", c: "break, continue และ else ที่คนใช้ผิดกันเกือบทั้งหมด" },
              { t: "p", c: "สามคำนี้มักถูกสอนแค่ว่าทำอะไร แต่ไม่ได้บอกว่าควรหยิบมาใช้เมื่อไหร่ ซึ่งเป็นส่วนที่ทำให้โค้ดอ่านง่ายขึ้นหรือแย่ลงจริง ๆ" },
              {
                t: "table",
                head: ["คำสั่ง", "ใช้เมื่อ", "ประโยชน์ที่ได้"],
                rows: [
                  ["break", "ได้คำตอบแล้วไม่ต้องดูต่อ เช่นค้นหาแล้วเจอ", "ไม่เสียเวลาวนของที่เหลือ และสื่อชัดว่า \"จบภารกิจ\""],
                  ["continue", "รอบนี้ไม่เข้าเกณฑ์ ข้ามไปเลย", "ทำให้โค้ดตื้นลง ไม่ต้องเอาเนื้อหางานทั้งก้อนไปห่อใน if"],
                  ["else ของ loop", "อยากรู้ว่าวนจบแบบ ไม่เคย break เลยหรือเปล่า", "แทนตัวแปรธง (flag) เช่น found = False ได้ทั้งตัว"],
                ],
              },
              { t: "p", c: "เรื่อง continue ให้ดูรูปร่างของโค้ดสองแบบนี้เทียบกัน สังเกตว่าแบบขวาเนื้องานหลักไม่ต้องย่อหน้าลึก อ่านง่ายกว่าเวลาเงื่อนไขคัดออกมีหลายข้อ" },
              {
                t: "code",
                lang: "python",
                c: `# แบบไม่ใช้ continue — เนื้องานถูกห่อไว้ในสุด ยิ่งมีเงื่อนไขคัดออกมาก ยิ่งลึก
for user in users:
    if user.active:
        if user.age >= 18:
            send_email(user)

# แบบใช้ continue เป็นตัวคัดออกด้านหน้า (guard) — เนื้องานอยู่ระดับตื้นสุด
for user in users:
    if not user.active:
        continue
    if user.age < 18:
        continue
    send_email(user)`,
              },
              { t: "h3", c: "else ของ loop ทำงานอย่างไรจริง ๆ" },
              { t: "p", c: "จุดที่คนเข้าใจผิดกันมากที่สุด: else ของ loop ไม่ใช่ \"ทำเมื่อ loop จบ\" เฉย ๆ เพราะถ้าเป็นแบบนั้นก็เขียนต่อท้ายลูปธรรมดาก็ได้ ไม่ต้องมี else ให้เปลืองคำสั่ง ความหมายจริงคือ ทำเมื่อวนจบโดยไม่เคยเจอ break — ถ้าออกจากลูปด้วย break บล็อก else จะถูกข้ามไปทั้งก้อน นี่คือเหตุผลทั้งหมดที่คำสั่งนี้มีอยู่" },
              {
                t: "codeout",
                lang: "python",
                label: "for-else กับสองเคสที่ต่างกันแค่มี break หรือไม่มี",
                code: `def find_odd(nums):
    for n in nums:
        if n % 2 == 1:
            print("  เจอเลขคี่:", n)
            break
    else:
        print("  วนจบโดยไม่เคยเจอ -> ไม่มีเลขคี่เลย")


print("nums = [4, 8, 16]  (ไม่มีเลขคี่)")
find_odd([4, 8, 16])

print("nums = [4, 15, 16]  (มีเลขคี่)")
find_odd([4, 15, 16])`,
                out: `nums = [4, 8, 16]  (ไม่มีเลขคี่)
  วนจบโดยไม่เคยเจอ -> ไม่มีเลขคี่เลย
nums = [4, 15, 16]  (มีเลขคี่)
  เจอเลขคี่: 15`,
              },
              { t: "p", c: "เคสที่สองไม่พิมพ์บรรทัด \"ไม่มีเลขคี่เลย\" เพราะออกจากลูปด้วย break พูดให้จำง่ายคือ else ของ loop แปลว่า \"วนจนหมดแล้วไม่เจอสิ่งที่หา\" ซึ่งเป็นคู่กับ break เสมอ ถ้าในลูปไม่มี break อยู่เลย การเขียน else ก็ไม่มีความหมาย (มันจะทำงานทุกครั้ง เหมือนเขียนต่อท้ายลูปธรรมดา)" },
              { t: "callout", title: "ถ้ายังไม่ชินกับ else ของ loop", warn: true, c: "ไม่ต้องรีบใช้ก็ได้ เพราะการเขียนเป็นฟังก์ชันแล้ว return ทันทีที่เจอ ให้ผลเหมือนกันและคนอ่านเข้าใจง่ายกว่าในโค้ดจริง แต่ต้องรู้ว่ามันหมายถึงอะไร เพราะจะเจอในโค้ดคนอื่น และถ้าเข้าใจผิดว่า \"มันรันทุกครั้งที่ loop จบ\" จะอ่านโค้ดนั้นผิดทั้งก้อน" },

              { t: "h2", c: "เดินบนข้อมูล: มี 4 ท่า เลือกให้ถูกท่า" },
              { t: "p", c: "พอตัดสินใจได้ว่าใช้ for แล้ว ยังต้องเลือกอีกชั้นว่าจะเดินแบบไหน ท่าที่ผิดไม่ได้ทำให้โค้ดพัง แต่ทำให้โค้ดยาวและพลาดง่ายขึ้นโดยไม่จำเป็น" },
              {
                t: "table",
                head: ["ท่า", "ใช้เมื่อ", "หมายเหตุ"],
                rows: [
                  ["for x in a", "ต้องการแค่ค่า ไม่สนตำแหน่ง", "ท่ามาตรฐาน ใช้บ่อยสุด เลือกอันนี้ก่อนเสมอถ้าทำได้"],
                  ["for i, x in enumerate(a)", "ต้องการทั้งตำแหน่งและค่า", "ดีกว่า for i in range(len(a)) แล้วไปหยิบ a[i] เอง เพราะสั้นกว่าและพลาดยากกว่า"],
                  ["for i in range(len(a))", "ต้องใช้ index ไปทำอย่างอื่น เช่นเทียบกับ a[i-1] หรือเขียนค่าลง a[i]", "เหลือไว้ใช้เฉพาะกรณีนี้ ไม่ใช่ท่าเริ่มต้น"],
                  ["for x, y in zip(a, b)", "มีข้อมูลสองชุดที่คู่กันตำแหน่งต่อตำแหน่ง", "ระวังกับดักด้านล่าง"],
                ],
              },
              { t: "p", c: "กับดักของ zip ที่ต้องรู้ก่อนใช้: มันหยุดที่ลิสต์ที่สั้นกว่า โดยไม่แจ้งเตือนอะไรเลย" },
              {
                t: "codeout",
                lang: "python",
                label: "zip กินข้อมูลหายเงียบ ๆ เมื่อสองลิสต์ยาวไม่เท่ากัน",
                code: `names = ["Aph", "Bee", "Cha"]
scores = [80, 95]                 # สั้นกว่า names หนึ่งช่อง

for name, score in zip(names, scores):
    print(name, score)

print("ได้", len(list(zip(names, scores))), "คู่ จากชื่อทั้งหมด", len(names), "คน")
print("-> Cha หายไปเงียบ ๆ ไม่มี error ไม่มีคำเตือน")`,
                out: `Aph 80
Bee 95
ได้ 2 คู่ จากชื่อทั้งหมด 3 คน
-> Cha หายไปเงียบ ๆ ไม่มี error ไม่มีคำเตือน`,
              },
              { t: "p", c: "พฤติกรรมนี้ไม่ใช่บั๊กของ Python และบางครั้งเราก็ต้องการแบบนี้จริง ๆ แต่ถ้าเราคิดว่าสองลิสต์ยาวเท่ากันแน่ ๆ แล้วมันไม่เท่า ข้อมูลจะหายไปโดยที่โปรแกรมยังทำงานปกติ ซึ่งหาเจอยากมาก ทางที่ปลอดภัยคือเช็ค len(a) == len(b) ก่อน zip เมื่อความยาวควรเท่ากันตามตรรกะของงาน" },

              { t: "h2", c: "สรุปหัวข้อนี้" },
              {
                t: "ul",
                c: [
                  "คำถามตัดสิน for หรือ while คือ \"การขยับไปข้างหน้าเป็นทีละหนึ่งเสมอหรือไม่\" ไม่ใช่ \"รู้จำนวนรอบไหม\" อย่างเดียว",
                  "แก้ค่าตัวนับข้างใน for ไม่มีผล เพราะมันถูกเขียนทับด้วยค่าถัดไปของลำดับทุกรอบ ถ้าต้องกระโดดต้องใช้ while",
                  "ต้องเพิ่มตัวแปรจำสถานะหลายตัว หรือต้องก๊อปโค้ดไปวางซ้ำหลังลูป = สัญญาณว่าเลือก for ผิด",
                  "break ใช้เมื่อได้คำตอบแล้ว, continue ใช้คัดออกด้านหน้าเพื่อให้โค้ดตื้นลง",
                  "else ของ loop แปลว่า \"วนจบโดยไม่เคย break\" ไม่ใช่ \"ทำเมื่อ loop จบ\" — ถ้าในลูปไม่มี break การเขียน else ก็ไร้ความหมาย",
                  "เดินบนข้อมูลให้เริ่มจาก for x in a ก่อน แล้วขยับไป enumerate หรือ range(len(a)) เฉพาะเมื่อจำเป็น",
                  "zip หยุดที่ลิสต์สั้นกว่าแบบเงียบ ๆ — เช็คความยาวก่อนถ้ามันควรเท่ากัน",
                ],
              },
              { t: "callout", title: "แบบฝึกหัด", c: "1) เขียนโค้ดพิมพ์เลขคี่จาก 1 ถึง 99 สองแบบ ทั้ง for และ while แล้วตอบว่าแบบไหนเหมาะกว่าเพราะอะไร  2) รับตัวเลขเรื่อย ๆ จนผู้ใช้พิมพ์ 0 — ข้อนี้ใช้ for ได้ไหม ทำไม  3) เขียนฟังก์ชันหาว่าคำหนึ่งมีสระซ้ำติดกันไหม โดยต้องกระโดดข้ามทั้งกลุ่มที่ซ้ำ  4) เขียนค้นหาชื่อในลิสต์ด้วย for-else แล้วเขียนอีกรอบด้วยฟังก์ชัน + return เทียบกันว่าอันไหนอ่านง่ายกว่า  5) แก้โค้ดนี้ให้ปลอดภัย: for a, b in zip(list1, list2) เมื่อสองลิสต์ควรยาวเท่ากัน" },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-loops": {
    slug: "pb-loops",
    title: { th: "การวนซ้ำ (Loops: for & while)", en: "" },
    lead: { th: "ทำคำสั่งซ้ำ ๆ โดยไม่ต้องเขียนซ้ำ — for, while, range, break/continue และ pattern ที่เจอบ่อย", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "loop ใช้สั่งให้คอมพิวเตอร์ทำงานซ้ำ ๆ ซึ่งเป็นสิ่งที่มันถนัดที่สุด แทนที่จะ copy โค้ดเดิม 100 รอบ เราเขียนครั้งเดียวแล้วบอกให้วน มี 2 แบบหลัก: for (รู้ว่าจะวนกี่รอบ/วนตามข้อมูล) และ while (วนจนกว่าเงื่อนไขจะเท็จ)" },

        { t: "h2", c: "for loop กับ range()" },
        { t: "p", c: "for ใช้วนตามลำดับของค่า วิธีที่พบบ่อยคือใช้คู่กับ range() ที่สร้างลำดับตัวเลขให้" },
        { t: "code", lang: "python", c: "for i in range(5):       # 0,1,2,3,4 (เริ่ม 0 ไม่รวม 5)\n    print(i)\n\nfor i in range(1, 6):    # 1,2,3,4,5 (เริ่ม 1 ไม่รวม 6)\n    print(i)\n\nfor i in range(0, 10, 2): # 0,2,4,6,8 (ก้าวทีละ 2)\n    print(i)\n\nfor i in range(5, 0, -1): # 5,4,3,2,1 (นับถอยหลัง)\n    print(i)" },
        {
          t: "table",
          head: ["เขียนแบบ", "ได้ลำดับ"],
          rows: [
            ["range(5)", "0 1 2 3 4"],
            ["range(2, 7)", "2 3 4 5 6"],
            ["range(0, 10, 2)", "0 2 4 6 8"],
            ["range(10, 0, -2)", "10 8 6 4 2"],
          ],
        },

        { t: "h2", c: "วนตามข้อมูลโดยตรง" },
        { t: "p", c: "for วนตามลิสต์ ข้อความ หรือข้อมูลอื่นได้โดยตรง ไม่ต้องใช้ index — อ่านง่ายและเป็นวิธีที่ Python แนะนำ" },
        { t: "code", lang: "python", c: "fruits = [\"แอปเปิล\", \"กล้วย\", \"ส้ม\"]\nfor fruit in fruits:\n    print(f\"ฉันชอบ{fruit}\")\n\nfor ch in \"Hello\":       # วนทีละตัวอักษร\n    print(ch)\n\n# ต้องการทั้งลำดับและค่า ใช้ enumerate\nfor i, fruit in enumerate(fruits):\n    print(f\"{i+1}. {fruit}\")   # 1. แอปเปิล ..." },

        { t: "h2", c: "while loop" },
        { t: "p", c: "while วนตราบใดที่เงื่อนไขยังเป็นจริง เหมาะเมื่อไม่รู้จำนวนรอบล่วงหน้า เช่นวนจนกว่าผู้ใช้จะพิมพ์คำว่าออก" },
        { t: "code", lang: "python", c: "count = 5\nwhile count > 0:\n    print(count)\n    count -= 1          # ต้องเปลี่ยนค่า ไม่งั้นวนไม่จบ!\nprint(\"ปล่อยจรวด!\")\n\n# วนรับข้อมูลจนกว่าจะถูก\npassword = \"\"\nwhile password != \"1234\":\n    password = input(\"รหัสผ่าน: \")\nprint(\"เข้าสู่ระบบสำเร็จ\")" },
        { t: "callout", title: "ระวัง infinite loop", warn: true, c: "ถ้าเงื่อนไข while ไม่มีวันเป็นเท็จ โปรแกรมจะวนไม่จบจนเครื่องค้าง ต้องมีบรรทัดที่ค่อย ๆ ทำให้เงื่อนไขเป็นเท็จเสมอ (เช่น count -= 1) ถ้าโปรแกรมค้างให้กด Ctrl+C เพื่อหยุด" },

        { t: "h2", c: "break และ continue" },
        { t: "code", lang: "python", c: "# break = ออกจาก loop ทันที\nfor i in range(100):\n    if i == 5:\n        break\n    print(i)        # 0 1 2 3 4 (หยุดที่ 5)\n\n# continue = ข้ามไปรอบถัดไป (ไม่ทำบรรทัดที่เหลือ)\nfor i in range(10):\n    if i % 2 == 0:\n        continue\n    print(i)        # 1 3 5 7 9 (ข้ามเลขคู่)" },

        { t: "h2", c: "loop ซ้อน loop (Nested Loops)" },
        { t: "p", c: "วาง loop ไว้ใน loop ใช้กับตาราง ตัวคูณ หรือข้อมูล 2 มิติ ระวังเรื่องประสิทธิภาพ: loop ซ้อน 2 ชั้นทำงาน n×n รอบ (จะเรียนเรื่องนี้ลึกในบท Big-O)" },
        { t: "code", lang: "python", c: "# สูตรคูณแม่ 2 ถึง 4\nfor row in range(2, 5):\n    for col in range(1, 13):\n        print(f\"{row}x{col}={row*col}\", end=\"  \")\n    print()   # ขึ้นบรรทัดใหม่เมื่อจบแต่ละแม่" },

        { t: "h2", c: "Pattern การวนที่เจอบ่อย" },
        { t: "code", lang: "python", c: "nums = [4, 8, 15, 16, 23, 42]\n\n# 1) สะสมผลรวม\ntotal = 0\nfor n in nums:\n    total += n\nprint(total)        # 108\n\n# 2) นับจำนวนที่เข้าเงื่อนไข\ncount = 0\nfor n in nums:\n    if n > 15:\n        count += 1\nprint(count)        # 3\n\n# 3) หาค่ามากที่สุดเอง\nbiggest = nums[0]\nfor n in nums:\n    if n > biggest:\n        biggest = n\nprint(biggest)      # 42" },

        { t: "h2", c: "zip — วนสองลิสต์พร้อมกัน (ใช้บ่อยในงาน)" },
        { t: "p", c: "เมื่อมีข้อมูล 2 ชุดที่คู่กัน (เช่นชื่อกับคะแนน) ใช้ zip วนพร้อมกันทีละคู่ — สะอาดกว่าวนด้วย index มาก" },
        { t: "code", lang: "python", c: "names = [\"Aph\", \"Bee\", \"Cha\"]\nscores = [80, 95, 70]\n\nfor name, score in zip(names, scores):\n    print(f\"{name} ได้ {score} คะแนน\")\n# Aph ได้ 80 คะแนน ...\n\n# วน dict ด้วย .items() ก็ได้ทั้ง key และ value\nprices = {\"กาแฟ\": 50, \"ชา\": 40}\nfor item, price in prices.items():\n    print(f\"{item} ราคา {price} บาท\")" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "for วนตามลำดับ — ใช้กับ range() หรือวนตามลิสต์/ข้อความโดยตรง",
            "enumerate() ให้ทั้งลำดับ+ค่า; zip() วนหลายลิสต์พร้อมกัน; .items() วน dict",
            "while วนจนกว่าเงื่อนไขจะเท็จ — ต้องมีจุดที่ทำให้เงื่อนไขเปลี่ยน เลี่ยง infinite loop",
            "break ออกจาก loop, continue ข้ามไปรอบถัดไป",
            "loop ซ้อน loop ใช้กับตาราง/2 มิติ แต่ระวังประสิทธิภาพ",
            "จำ pattern: สะสมผลรวม, นับ, หาค่ามาก/น้อยสุด",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) print เลข 1-100 ที่หาร 3 ลงตัว  2) รับเลข n แล้วหาผลรวม 1 ถึง n  3) หา factorial ของ n ด้วย loop  4) รับตัวเลขไปเรื่อย ๆ จนผู้ใช้พิมพ์ 0 แล้วบอกผลรวมและค่าเฉลี่ย  5) วาดสามเหลี่ยมดาว (* ทีละแถวเพิ่มขึ้น) ด้วย loop ซ้อน" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: ฟังก์ชัน (Functions) →", slug: "pb-functions", desc: "จัดกลุ่มคำสั่งให้ใช้ซ้ำได้" },
            { title: "← เงื่อนไข", slug: "pb-conditionals" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-functions": {
    slug: "pb-functions",
    title: { th: "ฟังก์ชัน (Functions)", en: "" },
    lead: { th: "จัดกลุ่มคำสั่งให้เป็นชุดที่เรียกใช้ซ้ำได้ — parameter, return, ค่า default, *args/**kwargs และ lambda", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "ฟังก์ชันคือการรวมคำสั่งหลายบรรทัดเป็นชุดที่ตั้งชื่อไว้ เรียกใช้กี่ครั้งก็ได้ ทำให้โค้ดสั้น ไม่ซ้ำซ้อน และแก้ที่เดียวพอ ถือเป็นทักษะที่ยกระดับโค้ดของคุณจาก \"สคริปต์\" เป็น \"โปรแกรม\" จริง ๆ" },

        { t: "h2", c: "นิยามและเรียกใช้ฟังก์ชัน" },
        { t: "code", lang: "python", c: "def greet():                 # def = define นิยามฟังก์ชัน\n    print(\"สวัสดีครับ\")\n    print(\"ยินดีต้อนรับ\")\n\ngreet()                      # เรียกใช้ (อย่าลืมวงเล็บ)\ngreet()                      # เรียกซ้ำได้เรื่อย ๆ" },
        { t: "callout", title: "สังเกตวงเล็บ", c: "greet หมายถึงตัวฟังก์ชันเอง ส่วน greet() (มีวงเล็บ) คือการสั่งให้ \"ทำงาน\" ลืมวงเล็บคือบั๊กที่เจอบ่อย โปรแกรมจะไม่รันฟังก์ชันให้" },

        { t: "h2", c: "parameter — รับค่าเข้า" },
        { t: "p", c: "parameter คือตัวแปรที่ฟังก์ชันรับเข้ามาทำงาน ทำให้ฟังก์ชันยืดหยุ่น ใช้กับข้อมูลต่างกันได้" },
        { t: "code", lang: "python", c: "def greet(name):             # name คือ parameter\n    print(f\"สวัสดี {name}\")\n\ngreet(\"Aphisit\")             # สวัสดี Aphisit  (\"Aphisit\" คือ argument)\ngreet(\"Mali\")                # สวัสดี Mali\n\ndef add(a, b):               # รับได้หลายตัว\n    print(a + b)\nadd(3, 5)                    # 8" },
        { t: "callout", title: "parameter vs argument", c: "parameter คือชื่อตัวแปรตอน \"นิยาม\" ฟังก์ชัน (name) ส่วน argument คือค่าจริงที่ส่งตอน \"เรียกใช้\" (\"Aphisit\") คนมักเรียกรวม ๆ ว่า \"พารามิเตอร์\" ก็เข้าใจกันได้" },

        { t: "h2", c: "return — ส่งค่ากลับ" },
        { t: "p", c: "ความแตกต่างสำคัญ: print แค่ \"แสดง\" ออกจอ แต่ return \"ส่งค่ากลับ\" ให้นำไปใช้ต่อได้ ฟังก์ชันที่คำนวณควรใช้ return เพื่อให้นำผลไปคิดต่อ" },
        { t: "code", lang: "python", c: "def add(a, b):\n    return a + b             # ส่งผลลัพธ์กลับ จบฟังก์ชันทันที\n\nresult = add(3, 5)           # เก็บค่าที่ส่งกลับ\nprint(result)                # 8\nprint(add(10, 20) * 2)       # 60  (นำผลไปคำนวณต่อได้)\n\n# return หลายค่าได้ (ได้เป็น tuple)\ndef min_max(nums):\n    return min(nums), max(nums)\nlow, high = min_max([3, 1, 7])\nprint(low, high)             # 1 7" },
        {
          t: "details",
          summary: "print vs return ต่างกันอย่างไร (กดดูตัวอย่าง)",
          c: [
            { t: "code", lang: "python", c: "def add_print(a, b):\n    print(a + b)      # แค่แสดง\n\ndef add_return(a, b):\n    return a + b      # ส่งค่ากลับ\n\nx = add_print(2, 3)   # แสดง 5 บนจอ แต่ x = None\ny = add_return(2, 3)  # ไม่แสดงอะไร แต่ y = 5\n\nprint(x)              # None  (ฟังก์ชันไม่ได้ return อะไร)\nprint(y * 10)         # 50    (นำไปคำนวณต่อได้)" },
            { t: "p", c: "สรุป: ถ้าต้องการนำผลไปใช้ต่อ ให้ใช้ return ถ้าแค่อยากแสดงผลให้คนดู ใช้ print ฟังก์ชันคำนวณเกือบทั้งหมดควร return" },
          ],
        },

        { t: "h2", c: "ค่า default" },
        { t: "p", c: "กำหนดค่าเริ่มต้นให้ parameter ได้ ถ้าผู้เรียกไม่ส่งค่ามาจะใช้ค่า default แทน ทำให้ฟังก์ชันเรียกง่ายขึ้น" },
        { t: "code", lang: "python", c: "def greet(name, greeting=\"สวัสดี\"):\n    return f\"{greeting} {name}\"\n\nprint(greet(\"Aphisit\"))               # สวัสดี Aphisit\nprint(greet(\"Aphisit\", \"หวัดดี\"))     # หวัดดี Aphisit\nprint(greet(name=\"Mali\", greeting=\"ดีจ้า\"))  # ระบุชื่อ parameter ได้" },

        { t: "h2", c: "รับจำนวนไม่จำกัด: *args และ **kwargs" },
        { t: "code", lang: "python", c: "def total(*nums):            # *args รับกี่ตัวก็ได้ (เป็น tuple)\n    return sum(nums)\nprint(total(1, 2, 3))        # 6\nprint(total(1, 2, 3, 4, 5))  # 15\n\ndef profile(**info):         # **kwargs รับเป็นคู่ key=value (เป็น dict)\n    for key, value in info.items():\n        print(f\"{key}: {value}\")\nprofile(name=\"Aphisit\", age=25)" },

        { t: "h2", c: "ฟังก์ชันสั้น ๆ ด้วย lambda" },
        { t: "p", c: "lambda คือฟังก์ชันบรรทัดเดียวไม่มีชื่อ มักใช้กับการเรียงลำดับหรือ filter ข้อมูล" },
        { t: "code", lang: "python", c: "square = lambda x: x ** 2\nprint(square(5))             # 25\n\n# ใช้บ่อยกับ sorted() เพื่อบอกว่าเรียงตามอะไร\npeople = [{\"name\": \"A\", \"age\": 30}, {\"name\": \"B\", \"age\": 20}]\nresult = sorted(people, key=lambda p: p[\"age\"])\nprint(result[0][\"name\"])     # B  (อายุน้อยสุดมาก่อน)" },

        { t: "h2", c: "เขียน docstring อธิบายฟังก์ชัน" },
        { t: "code", lang: "python", c: "def bmi(weight, height):\n    \"\"\"คำนวณ BMI จากน้ำหนัก(กก.) และส่วนสูง(เมตร)\"\"\"\n    return weight / (height ** 2)\n\nprint(bmi.__doc__)   # แสดงคำอธิบายของฟังก์ชัน" },

        { t: "h2", c: "Type Hints — บอกชนิดข้อมูล (มาตรฐานสมัยใหม่)" },
        { t: "p", c: "เราเขียน \"ใบ้\" ชนิดข้อมูลของ parameter และค่าที่ return ได้ ไม่บังคับตอนรัน แต่ช่วยให้ editor เตือน bug และคนอ่านเข้าใจง่าย — โค้ดในงานจริงสมัยนี้นิยมใส่" },
        { t: "code", lang: "python", c: "def bmi(weight: float, height: float) -> float:\n    return weight / (height ** 2)\n\ndef greet(name: str, times: int = 1) -> str:\n    return (f\"สวัสดี {name} \") * times\n\n# name: str = parameter ชนิด str, -> str = ฟังก์ชันคืน str" },
        { t: "callout", title: "กับดัก: อย่าใช้ list/dict เป็นค่า default", warn: true, c: "เขียน def f(items=[]) เป็นบั๊กคลาสสิก เพราะ list ถูกสร้างครั้งเดียวตอนนิยามฟังก์ชัน แล้วใช้ร่วมกันทุกครั้งที่เรียก ค่าจะค้างข้ามการเรียก! วิธีแก้: ใช้ items=None แล้วสร้าง list ใหม่ข้างใน (if items is None: items = []) — เรื่องนี้จะเข้าใจลึกขึ้นในคอร์สเชิงลึก" },

        { t: "h2", c: "สรุปหัวข้อนี้" },
        {
          t: "ul",
          c: [
            "def นิยามฟังก์ชัน เรียกใช้ด้วยชื่อตามด้วยวงเล็บ ()",
            "parameter รับค่าเข้า ทำให้ฟังก์ชันยืดหยุ่น",
            "return ส่งค่ากลับเพื่อนำไปใช้ต่อ (ต่างจาก print ที่แค่แสดง)",
            "ค่า default ทำให้ parameter ไม่บังคับส่ง",
            "*args/**kwargs รับจำนวนอาร์กิวเมนต์ไม่จำกัด",
            "lambda คือฟังก์ชันบรรทัดเดียว เหมาะกับ sorted/filter",
            "type hint (name: str -> int) ช่วยเตือน bug; ห้ามใช้ list/dict เป็นค่า default",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) เขียนฟังก์ชัน is_even(n) คืน True/False  2) เขียน max_of_three(a,b,c) คืนค่ามากสุด  3) เขียน greet(name, lang=\"th\") ที่ทักทายต่างภาษาตาม lang  4) เขียนฟังก์ชันรับลิสต์ตัวเลข แล้ว return ทั้งผลรวมและค่าเฉลี่ย  5) ใช้ sorted + lambda เรียงรายชื่อตามความยาวของชื่อ" },
        {
          t: "links",
          c: [
            { title: "ถัดไป: ข้อผิดพลาด & การแก้บั๊ก →", slug: "pb-errors", desc: "อ่าน error ให้เป็น และ try/except" },
            { title: "← การวนซ้ำ", slug: "pb-loops" },
          ],
        },
      ],
      en: [],
    },
  },

  // ─────────────────────────────────────────────────────────────
  "pb-errors": {
    slug: "pb-errors",
    title: { th: "ข้อผิดพลาด & การแก้บั๊ก (Errors & Debugging)", en: "" },
    lead: { th: "ขอบเขตตัวแปร (scope), อ่าน error ให้เป็น, จัดการด้วย try/except และเทคนิคแก้บั๊กที่ใช้จริง", en: "" },
    group: "บทที่ 2: พื้นฐานการเขียนโปรแกรม",
    blocks: {
      th: [
        { t: "p", c: "การเจอ error เป็นเรื่องปกติของทุกคนที่เขียนโปรแกรม ไม่ใช่ความล้มเหลว มืออาชีพต่างจากมือใหม่ตรงที่ \"อ่าน error เป็น\" และหาสาเหตุได้เร็ว หัวข้อนี้ปิดท้ายบทที่ 2 ด้วยทักษะเอาตัวรอดที่สำคัญที่สุด" },

        { t: "h2", c: "ขอบเขตของตัวแปร (Scope)" },
        { t: "p", c: "ตัวแปรที่สร้างในฟังก์ชัน (local) ใช้ได้แค่ในฟังก์ชันนั้น ส่วนตัวแปรนอกฟังก์ชัน (global) เข้าถึงได้ทั่วไป เข้าใจเรื่องนี้ช่วยแก้บั๊ก \"ทำไมตัวแปรหาไม่เจอ\" ได้มาก" },
        { t: "code", lang: "python", c: "total = 100          # global — ใช้ได้ทั้งไฟล์\n\ndef calc():\n    x = 10           # local — มีอยู่แค่ในฟังก์ชันนี้\n    return total + x # อ่าน global ได้\n\nprint(calc())        # 110\n# print(x)           # NameError! x ไม่มีอยู่นอกฟังก์ชัน" },
        { t: "callout", title: "ทำไมต้องมี scope", c: "เพื่อไม่ให้ตัวแปรชื่อซ้ำกันในคนละฟังก์ชันชนกัน แต่ละฟังก์ชันมี \"พื้นที่ส่วนตัว\" ของมัน ทำให้เขียนฟังก์ชันได้อย่างมั่นใจว่าไม่ไปแก้ค่าของที่อื่นโดยบังเอิญ" },

        { t: "h2", c: "ประเภทของ error ที่เจอบ่อย" },
        {
          t: "table",
          head: ["Error", "สาเหตุ", "ตัวอย่าง"],
          rows: [
            ["SyntaxError", "พิมพ์ผิดไวยากรณ์", "ลืม : ท้าย if, วงเล็บไม่ครบ"],
            ["NameError", "ใช้ชื่อที่ยังไม่มี/พิมพ์ผิด", "เรียก prnt() แทน print()"],
            ["TypeError", "ใช้ชนิดข้อมูลผิด", '"3" + 5 (str บวก int)'],
            ["ValueError", "ชนิดถูกแต่ค่าผิด", 'int("abc")'],
            ["IndexError", "เข้าถึง index เกินขนาด", "list[10] บนลิสต์ 3 ตัว"],
            ["KeyError", "เข้าถึง key ที่ไม่มีใน dict", 'd["missing"]'],
            ["ZeroDivisionError", "หารด้วยศูนย์", "10 / 0"],
            ["IndentationError", "ย่อหน้าผิด", "ย่อหน้าไม่ตรงกัน"],
          ],
        },

        { t: "h2", c: "อ่าน error message ให้เป็น" },
        { t: "p", c: "เมื่อโปรแกรม error Python จะพิมพ์ข้อความบอกว่าเกิดที่บรรทัดไหนและเพราะอะไร อ่านจาก \"ล่างขึ้นบน\" บรรทัดสุดท้ายคือชนิดและคำอธิบาย ส่วนเหนือขึ้นไปบอกตำแหน่ง" },
        { t: "code", lang: "text", c: "Traceback (most recent call last):\n  File \"main.py\", line 3, in <module>\n    print(10 / 0)\nZeroDivisionError: division by zero\n\n# อ่านว่า: บรรทัด 3 ในไฟล์ main.py หารด้วยศูนย์" },

        { t: "h2", c: "จัดการ error ด้วย try / except" },
        { t: "p", c: "บาง error เราคาดเดาได้ว่าจะเกิด (เช่นผู้ใช้พิมพ์ตัวอักษรแทนตัวเลข) ใช้ try/except ดักไว้ ไม่ให้โปรแกรมพังกลางคัน" },
        { t: "code", lang: "python", c: "try:\n    num = int(input(\"ใส่ตัวเลข: \"))\n    print(f\"100 หาร {num} = {100 / num}\")\nexcept ValueError:\n    print(\"กรุณาใส่ตัวเลขเท่านั้น\")\nexcept ZeroDivisionError:\n    print(\"หารด้วยศูนย์ไม่ได้\")\nelse:\n    print(\"คำนวณสำเร็จ\")     # ทำเมื่อไม่มี error\nfinally:\n    print(\"จบการทำงาน\")      # ทำเสมอ ไม่ว่าจะ error หรือไม่" },
        { t: "callout", title: "อย่า except เปล่า ๆ แบบกว้างเกินไป", warn: true, c: "การเขียน except: เฉย ๆ (จับทุก error) ทำให้บั๊กถูกซ่อนและหายาก ควรระบุชนิด error ที่คาดไว้ เช่น except ValueError: เพื่อให้รู้ว่ากำลังจัดการอะไรอยู่" },

        { t: "h2", c: "โยน error เองด้วย raise" },
        { t: "p", c: "นอกจากดัก error เรายัง \"โยน\" error เองได้เมื่อเจอสถานการณ์ที่ไม่ควรดำเนินต่อ — ดีกว่าคืนค่าแปลก ๆ (เช่น -1) ที่ผู้เรียกอาจลืมเช็ค ทำให้ bug ปรากฏทันทีตรงจุดเกิดเหตุ" },
        { t: "code", lang: "python", c: "def set_age(age):\n    if age < 0:\n        raise ValueError(f\"อายุติดลบไม่ได้: {age}\")   # หยุดและบอกสาเหตุชัด\n    return age\n\n# set_age(-5)   ->  ValueError: อายุติดลบไม่ได้: -5\n\n# ใช้คู่กับ try/except ของผู้เรียกได้\ntry:\n    set_age(-5)\nexcept ValueError as e:\n    print(f\"ผิดพลาด: {e}\")" },
        { t: "callout", title: "fail fast — พังเร็ว พังชัด", c: "การ raise ตั้งแต่เจอข้อมูลผิดที่ \"ขอบ\" ของระบบ ดีกว่าปล่อยให้ค่าผิดไหลไปพังที่อื่นซึ่งหาสาเหตุยากกว่ามาก นี่คือหลักการ defensive programming ที่จะลงลึกในคอร์สเชิงลึก" },

        { t: "h2", c: "เทคนิคแก้บั๊กที่ใช้จริงทุกวัน" },
        {
          t: "ol",
          c: [
            "อ่าน error message ให้จบ — มันบอกบรรทัดและสาเหตุตรง ๆ",
            "แทรก print() ดูค่าตัวแปรในจุดต่าง ๆ ว่าตรงกับที่คิดไหม (วิธีที่ง่ายและทรงพลังที่สุด)",
            "ตัดโค้ดให้เหลือน้อยที่สุดที่ยังเกิดบั๊ก เพื่อจำกัดวงหาสาเหตุ",
            "ตรวจ assumption: ชนิดข้อมูลถูกไหม (ใช้ type()), ค่าว่างไหม, index เกินไหม",
            "อธิบายโค้ดให้คนอื่น (หรือเป็ดยาง) ฟังทีละบรรทัด มักเจอจุดผิดเอง",
          ],
        },
        { t: "code", lang: "python", c: "# ตัวอย่างใช้ print debug: ทำไมผลรวมผิด?\ndef sum_list(nums):\n    total = 0\n    for n in nums:\n        print(f\"กำลังบวก {n}, total ตอนนี้ = {total}\")  # debug\n        total += n\n    return total\n\nsum_list([1, 2, 3])\n# เห็นค่าทีละขั้น ช่วยจับจุดที่ผิดได้" },

        { t: "h2", c: "สรุปหัวข้อนี้ & จบบทที่ 2" },
        {
          t: "ul",
          c: [
            "ตัวแปร local อยู่แค่ในฟังก์ชัน, global เข้าถึงได้ทั่วไป",
            "รู้จัก error ยอดฮิต: NameError, TypeError, ValueError, IndexError, KeyError",
            "อ่าน error จากล่างขึ้นบน บรรทัดสุดท้ายบอกชนิดและสาเหตุ",
            "try/except ดัก error ที่คาดได้ ไม่ให้โปรแกรมพัง — ระบุชนิดให้ชัด",
            "เทคนิคแก้บั๊ก: อ่าน error, print ดูค่า, ตัดโค้ด, ตรวจ assumption",
          ],
        },
        { t: "callout", title: "แบบฝึกหัด", c: "1) เขียนโปรแกรมหารเลข 2 ตัวที่ดักทั้ง ValueError และ ZeroDivisionError  2) ลองสร้าง error แต่ละชนิดในตารางดูว่าหน้าตา message เป็นอย่างไร  3) แก้บั๊กโปรแกรมที่ให้มาด้วยการแทรก print ดูค่า  4) เขียนฟังก์ชันรับ input จนกว่าผู้ใช้จะใส่ตัวเลขถูกต้อง (ใช้ while + try/except)" },
        { t: "callout", title: "จบบทที่ 2 — เขียนโปรแกรมเป็นแล้ว!", warn: false, c: "ตอนนี้คุณมีเครื่องมือครบสำหรับเขียนโปรแกรมเชิงตรรกะ: ตัวแปร, operator, เงื่อนไข, loop, ฟังก์ชัน และจัดการ error ได้ บทถัดไปจะเรียนวิธีเก็บข้อมูลหลายค่าอย่างเป็นระบบ" },
        {
          t: "links",
          c: [
            { title: "บทที่ 3: โครงสร้างข้อมูล →", slug: "ds-list", desc: "เริ่มที่ List/Array — เก็บข้อมูลหลายค่าในตัวแปรเดียว" },
            { title: "← ฟังก์ชัน", slug: "pb-functions" },
          ],
        },
      ],
      en: [],
    },
  },
};
