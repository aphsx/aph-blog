import type { Page } from "@/lib/types";

export const graphBfsPages: Record<string, Page> = {
  "lc75-intro-graph-bfs": {
    slug: "lc75-intro-graph-bfs",
    title: "Graphs — BFS (ลุยเป็นชั้น)",
    lead: "traverse graph และ grid (ตาราง) ทีละชั้นด้วย deque เหมาะกับหาระยะสั้นสุดหรือจำนวนก้าวน้อยสุด",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "หมวดก่อนหน้าเราลุยลึกด้วย DFS มาแล้ว คราวนี้มารู้จักคู่หูของมันคือ BFS (Breadth-First Search) หรือการลุยเป็นชั้น แทนที่จะพุ่งลึกไปทางเดียวจนสุด BFS จะแผ่ออกไปทีละวงรอบตัว เหมือนคลื่นน้ำที่กระเพื่อมออกจากจุดที่โยนก้อนหินลงไป จุดนี้ทำให้ BFS เก่งเรื่องหา shortest path (ระยะทางสั้นที่สุด) เป็นพิเศษ" },

      { t: "h2", c: "BFS ทำงานยังไง" },
      { t: "p", c: "BFS traverse (เดินไล่) graph โดยเยี่ยม node (โหนด) ที่อยู่ ใกล้ จุดเริ่มก่อน แล้วค่อยขยายไปไกลขึ้นทีละชั้น ชั้นแรกคือเพื่อนบ้าน (neighbor) ตรงของจุดเริ่ม (ห่าง 1 ก้าว) ชั้นถัดไปคือเพื่อนบ้านของเพื่อนบ้าน (ห่าง 2 ก้าว) ไปเรื่อย ๆ เพราะมันแผ่ออกเป็นวงตามระยะทาง พอ BFS ไปถึงเป้าหมายครั้งแรก เรารับประกันได้เลยว่านั่นคือ shortest path (เส้นทางที่สั้นที่สุด, ก้าวน้อยที่สุด) นี่คือเหตุผลที่โจทย์แนว หาจำนวนก้าวน้อยสุด ระยะสั้นสุด เวลาน้อยสุด มักใช้ BFS" },
      { t: "p", c: "เครื่องมือคู่กับ BFS คือ queue (คิว) แบบเข้าก่อนออกก่อน เราใช้ deque จาก collections เพราะมัน pop (ดึง) ของจากหัว queue ได้เร็ว O(1) หลักการคือ push (ใส่) node ที่จะเริ่มลง queue แล้ว iterate (วน) pop ออกจากหัว queue มาประมวลผล พร้อม append (ต่อท้าย) เพื่อนบ้านที่ยังไม่ visited (เคยเยือน) เข้าท้าย queue ทำจน queue หมด และเหมือน DFS เราต้องมี visited กันเดินซ้ำเสมอ" },
      { t: "code", lang: "python", c: `from collections import deque

# template BFS บนกราฟ
def bfs(start, graph):
    queue = deque([start])
    visited = {start}              # ทำเครื่องหมายตั้งแต่ใส่คิว
    steps = 0
    while queue:
        # ดึงทั้งชั้นออกมาพร้อมกัน เพื่อนับจำนวนก้าว (ชั้น)
        for _ in range(len(queue)):
            node = queue.popleft() # ดึงจากหัวคิว
            # ... ทำอะไรกับ node ตรงนี้ ...
            for nxt in graph[node]:
                if nxt not in visited:
                    visited.add(nxt)   # ทำเครื่องหมายทันทีที่ใส่คิว
                    queue.append(nxt)  # โยนเข้าท้ายคิว
        steps += 1                 # จบหนึ่งชั้น = ขยับไปอีกหนึ่งก้าว` },

      { t: "h2", c: "BFS บน grid สองมิติ" },
      { t: "p", c: "โจทย์ BFS เจอบ่อยมากในรูป grid (ตาราง) สองมิติ เช่นแผนที่ เขาวงกต ทุ่งนา เรามองแต่ละช่อง (r, c) เป็น node และช่องที่อยู่ติดกัน 4 ทิศ (บน ล่าง ซ้าย ขวา) เป็น neighbor โดยไม่ต้องสร้าง adjacency list (ลิสต์เพื่อนบ้าน) เลย แค่คำนวณพิกัด neighbor สด ๆ ตอนเดิน" },
      { t: "code", lang: "python", c: `# เพื่อนบ้าน 4 ทิศบน grid: บน ล่าง ซ้าย ขวา
directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

rows, cols = len(grid), len(grid[0])
for dr, dc in directions:
    nr, nc = r + dr, c + dc
    # เช็คว่าไม่หลุดขอบตารางก่อนเสมอ
    if 0 <= nr < rows and 0 <= nc < cols:
        # ... ช่อง (nr, nc) เป็นเพื่อนบ้านที่ใช้ได้ ...
        pass` },
      { t: "p", c: "อีกเทคนิคที่หมวดนี้ใช้คือ multi-source BFS คือเริ่มจากหลายจุดพร้อมกัน แค่ push จุดเริ่มทุกจุดเข้า queue ตั้งแต่ต้น แล้ว BFS แผ่ออกพร้อมกันหมด เหมาะกับโจทย์ที่มีต้นตอลามหลายจุด เช่นไฟไหม้ หรือของเน่าที่ลามพร้อมกันหลายที่" },
      { t: "callout", title: "DFS หรือ BFS ดี", c: "ถ้าโจทย์ถามแค่ ไปถึงไหม เชื่อมกันไหม นับกลุ่ม ใช้ได้ทั้งคู่ แต่ถ้าถาม shortest / สั้นที่สุด น้อยที่สุด ให้เลือก BFS เพราะมันแผ่เป็นชั้นตามระยะทาง เจอเป้าหมายครั้งแรกคือคำตอบที่สั้นสุดทันที ส่วน DFS ไม่การันตีเรื่องนี้" },
      { t: "callout", c: "หมวดนี้มี 2 ข้อ ทางออกใกล้สุดในเขาวงกต และส้มเน่า พร้อมแล้วกดถัดไปเริ่มข้อแรกได้เลย" },
    ],
  },

  "lc75-p47": {
    slug: "lc75-p47",
    title: "ข้อ 47 · LC1926 — ทางออกใกล้สุดในเขาวงกต 🟡",
    lead: "โจทย์ถามก้าวน้อยสุด = สัญญาณ BFS เก็บ steps ไปกับพิกัดในคิว ช่องติดขอบช่องแรกคือคำตอบ",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์ ให้ตาราง maze ที่ช่อง . คือทางเดินได้ และ + คือกำแพง พร้อมตำแหน่งทางเข้า entrance = [row, col] ให้หาจำนวนก้าวน้อยที่สุดที่จะเดินจากทางเข้าไปถึง ทางออก โดยทางออกคือช่องว่างที่อยู่ ติดขอบ ตาราง (แถวบนสุด ล่างสุด คอลัมน์ซ้ายสุด ขวาสุด) และต้องไม่ใช่ช่องทางเข้าเอง เดินได้ 4 ทิศ ถ้าไปถึงทางออกไม่ได้เลยให้คืน -1" },
      { t: "ul", c: [
        "maze = [[+,+,.,+],[.,.,.,+],[+,+,+,.]], entrance = [1,2] → 1 (เดินขึ้นไปช่อง (0,2) ที่ติดขอบบน)",
        "ทางเข้าอยู่กลางและถูกกำแพงล้อมจนออกไม่ได้ → -1",
      ] },
      { t: "callout", c: "ทางเข้าเองไม่นับเป็นทางออก แม้จะติดขอบก็ตาม ต้องเช็คเงื่อนไขติดขอบกับช่องใหม่ที่เดินไปเท่านั้น" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "ใช้ BFS บน grid เพราะโจทย์ถามจำนวนก้าว น้อยที่สุด นี่คือสัญญาณชัดเจนว่าใช้ BFS ไม่ใช่ DFS เนื่องจาก BFS แผ่ตามระยะทาง ช่องแรกที่ไปถึงและเป็นทางออก ย่อมเป็นทางออกที่ใกล้สุดโดยอัตโนมัติ" },
      { t: "p", c: "เราเก็บจำนวนก้าวไปพร้อมกับพิกัดในคิวเลย (r, c, steps) พอดึงช่องไหนออกมาก็รู้ทันทีว่ามันห่างจากทางเข้ากี่ก้าว และเพื่อประหยัด memory เราเขียนทับช่องที่เยี่ยมแล้วด้วย + (ทำให้มันกลายเป็นกำแพง) แทนการสร้าง set visited แยก" },
      { t: "ol", c: [
        "อ่านขนาดตาราง และพิกัดทางเข้า ตั้ง directions 4 ทิศ",
        "ใส่ (start_r, start_c, 0) ลงคิว แล้วปิดช่องเริ่มด้วย + เพื่อกันเดินซ้ำ",
        "วนจนคิวหมด ดึง (r, c, steps) ออกจากหัวคิว",
        "ลองเพื่อนบ้าน 4 ทิศ ช่องใหม่ต้องอยู่ในตารางและเป็น . (เดินได้)",
        "ถ้าช่องใหม่ติดขอบ คืน steps + 1 ทันที (นี่คือทางออกที่ใกล้สุด)",
        "ถ้าไม่ติดขอบ ปิดช่องด้วย + แล้วใส่ (nr, nc, steps + 1) เข้าคิว",
        "ถ้าเดินจนคิวหมดไม่เจอทางออก คืน -1",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "นับทางเข้าเป็นทางออกเพราะมันก็ติดขอบ ต้องปิดช่องเริ่มไว้ตั้งแต่ต้นและเช็คเงื่อนไขติดขอบกับช่อง ใหม่ เท่านั้น อีกจุดคือลืมทำเครื่องหมายช่องตอนใส่คิว ทำให้ช่องเดียวถูกใส่ซ้ำหลายรอบ" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "รันบน maze = [[+,+,.,+],[.,.,.,+],[+,+,+,.]], entrance = [1,2]" },
      { t: "table", head: ["ดึงออก (r,c,steps)", "เพื่อนบ้านที่เดินได้", "ผล"], rows: [
        ["(1,2,0)", "(0,2) เป็น . และ (1,1) เป็น .", "(0,2) ติดขอบบน → คืน 0+1 = 1"],
      ] },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `from collections import deque

def nearest_exit(maze, entrance):
    rows, cols = len(maze), len(maze[0])
    start_r, start_c = entrance
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

    queue = deque([(start_r, start_c, 0)])   # (แถว, คอลัมน์, จำนวนก้าว)
    maze[start_r][start_c] = "+"             # ทำเครื่องหมายว่าเยี่ยมแล้ว (ปิดทาง)

    while queue:
        r, c, steps = queue.popleft()
        for dr, dc in directions:
            nr, nc = r + dr, c + dc
            # ต้องอยู่ในตาราง และเป็นทางเดินได้
            if 0 <= nr < rows and 0 <= nc < cols and maze[nr][nc] == ".":
                # ถ้าช่องใหม่อยู่ติดขอบ นี่คือทางออก
                if nr == 0 or nr == rows - 1 or nc == 0 or nc == cols - 1:
                    return steps + 1
                maze[nr][nc] = "+"           # ปิดทางกันเดินซ้ำ
                queue.append((nr, nc, steps + 1))
    return -1                                # เดินจนทั่วแล้วไม่เจอทางออก

maze = [["+", "+", ".", "+"],
        [".", ".", ".", "+"],
        ["+", "+", "+", "."]]
print(nearest_exit(maze, [1, 2]))            # 1` },
        { t: "p", c: "เพราะโจทย์ถามจำนวนก้าว น้อยที่สุด นี่คือสัญญาณชัดเจนว่าใช้ BFS เราเก็บจำนวนก้าวไปพร้อมกับพิกัดในคิวเลย (r, c, steps) พอดึงช่องไหนออกมาก็รู้ทันทีว่ามันห่างจากทางเข้ากี่ก้าว เมื่อเราเดินไปเจอช่องว่างที่ติดขอบเป็นครั้งแรก เพราะ BFS แผ่ตามระยะ ช่องนั้นย่อมเป็นทางออกที่ใกล้สุด คืน steps + 1 ได้เลย" },
        { t: "p", c: "ทริกประหยัด memory คือแทนที่จะสร้าง set visited แยก เราเขียนทับช่องที่เยี่ยมแล้วด้วย + (ทำให้มันกลายเป็นกำแพง) เพื่อกันเดินซ้ำ จุดที่ต้องระวังคือทางเข้า ไม่ นับเป็นทางออกแม้จะติดขอบ โค้ดจึงเช็คเงื่อนไขติดขอบเฉพาะกับช่อง ใหม่ (nr, nc) ที่ไม่ใช่ช่องเริ่ม และปิดช่องเริ่มตั้งแต่ต้น" },
        { t: "p", c: "Time O(rows × cols) เยี่ยมแต่ละช่องมากสุดหนึ่งครั้ง · Space O(rows × cols) จากคิวในกรณีแย่สุด" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เจอคำว่า ก้าวน้อยสุด / ระยะสั้นสุด บน grid ให้นึกถึง BFS เก็บ steps ไปกับพิกัดในคิว แล้วคืนทันทีที่เจอเป้าหมายครั้งแรก และใช้เขียนทับ grid แทน visited ได้เมื่อได้รับอนุญาตให้แก้ input" },
    ],
  },

  "lc75-p48": {
    slug: "lc75-p48",
    title: "ข้อ 48 · LC994 — ส้มเน่า 🟡",
    lead: "multi-source BFS โยนส้มเน่าทุกลูกเข้าคิวพร้อมกัน แผ่ทีละชั้นนับเป็นนาที ปิดท้ายเช็คส้มสดที่เหลือ",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์ ให้ตาราง grid ที่แต่ละช่องเป็น 0 (ว่าง) 1 (ส้มสด) หรือ 2 (ส้มเน่า) ทุก ๆ หนึ่งนาที ส้มเน่าจะทำให้ส้มสดที่อยู่ ติดกัน 4 ทิศ เน่าตามไปด้วย ให้หาว่าใช้เวลากี่นาทีจนไม่เหลือส้มสดเลย ถ้ามีส้มสดที่ไม่มีวันเน่า (ไม่มีส้มเน่าเข้าถึง) ให้คืน -1" },
      { t: "ul", c: [
        "grid = [[2,1,1],[1,1,0],[0,1,1]] → 4 นาที",
        "grid = [[2,1,1],[0,1,1],[1,0,1]] → -1 (ส้มมุมล่างซ้ายถูกช่องว่างตัดขาด)",
        "grid = [[0,2]] → 0 (ไม่มีส้มสดตั้งแต่แรก)",
      ] },
      { t: "callout", c: "มีจุดเริ่มหลายจุด (ส้มเน่าทุกลูกลามพร้อมกัน) และมีเคสพิเศษที่ต้องคืน 0 กับ -1 ให้ครบ" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "ใช้ multi-source BFS จุดที่ทำให้ข้อนี้ต่างจากข้อก่อนคือมันมีจุดเริ่ม หลายจุดพร้อมกัน (ส้มเน่าทุกลูกลามในเวลาเดียวกัน) เราจึงไม่ได้เริ่มจากจุดเดียว แต่โยนส้มเน่าทุกลูกเข้าคิวตั้งแต่ต้น แล้ว BFS แผ่ออกพร้อมกันหมด คลื่นการเน่าจึงขยายทีละวงพอดีกับเวลาจริง" },
      { t: "p", c: "การนับนาทีต้องประมวลผล ทีละชั้น ใช้ for _ in range(len(queue)) ก่อนดึงของ เพื่อจัดการเฉพาะส้มที่เน่าอยู่ ณ ต้นนาทีนี้ให้ครบก่อน ค่อยขึ้นนาทีใหม่ และใช้ตัวแปร fresh นับส้มสดที่เหลือ ทุกครั้งที่ทำให้ลูกหนึ่งเน่าก็ลด fresh ลง สุดท้ายถ้า fresh ยังเหลือแปลว่าเน่าไม่ทั่ว" },
      { t: "ol", c: [
        "ไล่ทั้ง grid โยนตำแหน่งส้มเน่าทุกลูกเข้าคิว และนับจำนวนส้มสด (fresh)",
        "ถ้า fresh เป็น 0 ตั้งแต่แรก คืน 0 ทันที (ไม่ต้องรอเวลา)",
        "วน while queue และ fresh > 0 บวก minutes หนึ่งในแต่ละรอบ",
        "ดึงส้มเน่าทั้งชั้นนี้ออก (for _ in range(len(queue))) ทำให้เพื่อนบ้านที่เป็นส้มสดเน่าตาม",
        "แต่ละลูกที่เน่าใหม่ ตั้งเป็น 2 ลด fresh แล้วใส่เข้าคิวเป็นแหล่งเน่าของนาทีถัดไป",
        "จบลูป ถ้า fresh เป็น 0 คืน minutes ไม่งั้นคืน -1",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ไม่ล็อกจำนวนช่องด้วย len(queue) ก่อนดึง ทำให้ส้มที่เพิ่งเน่าในนาทีนี้ถูกนับรวมในชั้นเดียวกัน นับนาทีเกิน อีกจุดคือลืมเคสไม่มีส้มสดตั้งแต่แรกที่ต้องคืน 0 ไม่ใช่ -1" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "รันบน grid = [[2,1,1],[1,1,0],[0,1,1]] (เริ่มมีส้มเน่าที่ (0,0), fresh = 6)" },
      { t: "table", head: ["นาที", "ส้มที่เน่าใหม่", "fresh เหลือ"], rows: [
        ["1", "(0,1), (1,0)", "4"],
        ["2", "(0,2), (1,1)", "2"],
        ["3", "(2,1)", "1"],
        ["4", "(2,2)", "0 → คืน 4"],
      ] },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `from collections import deque

def oranges_rotting(grid):
    rows, cols = len(grid), len(grid[0])
    queue = deque()
    fresh = 0

    # หาตำแหน่งส้มเน่าทั้งหมด (จุดเริ่มหลายจุด) และนับส้มสด
    for r in range(rows):
        for c in range(cols):
            if grid[r][c] == 2:
                queue.append((r, c))
            elif grid[r][c] == 1:
                fresh += 1

    if fresh == 0:
        return 0                     # ไม่มีส้มสดตั้งแต่แรก ใช้ 0 นาที

    minutes = 0
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

    while queue and fresh > 0:
        minutes += 1                 # ผ่านไปอีกหนึ่งนาที
        for _ in range(len(queue)):  # แผ่ทั้งชั้น (ส้มเน่า ณ นาทีนี้) พร้อมกัน
            r, c = queue.popleft()
            for dr, dc in directions:
                nr, nc = r + dr, c + dc
                if 0 <= nr < rows and 0 <= nc < cols and grid[nr][nc] == 1:
                    grid[nr][nc] = 2         # ส้มสดเน่าตาม
                    fresh -= 1
                    queue.append((nr, nc))   # กลายเป็นแหล่งเน่าในนาทีถัดไป

    return minutes if fresh == 0 else -1     # ถ้ายังเหลือส้มสด แปลว่าเน่าไม่ถึง

print(oranges_rotting([[2, 1, 1], [1, 1, 0], [0, 1, 1]]))  # 4
print(oranges_rotting([[2, 1, 1], [0, 1, 1], [1, 0, 1]]))  # -1
print(oranges_rotting([[0, 2]]))                           # 0` },
        { t: "p", c: "จุดที่ทำให้ข้อนี้ต่างจากข้อก่อนคือมันมีจุดเริ่ม หลายจุดพร้อมกัน (ส้มเน่าทุกลูกลามในเวลาเดียวกัน) เทคนิคคือ multi-source BFS เราไม่ได้เริ่มจากจุดเดียว แต่โยนส้มเน่าทุกลูกเข้าคิวตั้งแต่ต้น แล้ว BFS แผ่ออกพร้อมกันหมด คลื่นการเน่าจึงขยายทีละวงพอดีกับเวลาจริง" },
        { t: "p", c: "การนับนาทีต้องประมวลผล ทีละชั้น การเขียน for _ in range(len(queue)) ก่อนดึงของ ทำให้เราจัดการเฉพาะส้มที่เน่าอยู่ ณ ต้นนาทีนี้ให้ครบก่อน ค่อยขึ้นนาทีใหม่ ถ้าไม่ล็อกจำนวนไว้ก่อน ส้มที่เพิ่งเน่าในนาทีนี้จะถูกนับรวมทำให้นับนาทีเกิน เราใช้ตัวแปร fresh นับส้มสดที่เหลือ ทุกครั้งที่ทำให้ลูกหนึ่งเน่าก็ลด fresh ลง" },
        { t: "p", c: "edge case สำคัญสามอย่าง หนึ่ง ไม่มีส้มสดตั้งแต่แรก ต้องคืน 0 (ไม่ใช่ -1) จึงเช็ค fresh == 0 ก่อนเริ่มวน สอง มีส้มสดที่ถูกช่องว่างตัดขาด สุดท้าย fresh ยังมากกว่า 0 จึงคืน -1 สาม เงื่อนไข while queue and fresh > 0 ช่วยหยุดทันทีเมื่อส้มสดหมด ไม่นับนาทีเกินโดยเปล่าประโยชน์ · Time O(rows × cols) เยี่ยมแต่ละช่องมากสุดหนึ่งครั้ง · Space O(rows × cols) จากคิวในกรณีที่ส้มเน่าเยอะ" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เมื่อมีต้นตอลามพร้อมกันหลายจุด (ไฟ น้ำ ของเน่า) ใช้ multi-source BFS โยนทุกต้นตอเข้าคิวก่อนเริ่ม แล้วประมวลผลทีละชั้นเพื่อนับเวลา อย่าลืมเคสว่างเปล่าและเคสเข้าไม่ถึง" },
    ],
  },
};
