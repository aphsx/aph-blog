import type { Page } from "@/lib/types";

export const graphBfsPages: Record<string, Page> = {
  "lc75-graph-bfs": {
    slug: "lc75-graph-bfs",
    title: "Graphs — BFS (ลุยเป็นชั้น)",
    lead: "สำรวจกราฟและ grid ทีละชั้นด้วย deque เหมาะกับหาระยะสั้นสุดหรือจำนวนก้าวน้อยสุด",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "หัวข้อก่อนหน้าเราลุยลึกด้วย DFS มาแล้ว คราวนี้มารู้จักคู่หูของมันคือ BFS (Breadth-First Search) หรือการลุยเป็นชั้น แทนที่จะพุ่งลึกไปทางเดียวจนสุด BFS จะแผ่ออกไปทีละวงรอบตัว เหมือนคลื่นน้ำที่กระเพื่อมออกจากจุดที่โยนก้อนหินลงไป จุดนี้ทำให้ BFS เก่งเรื่องหาระยะทางสั้นที่สุดเป็นพิเศษ" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "BFS สำรวจกราฟโดยเยี่ยม node ที่อยู่ ใกล้ จุดเริ่มก่อน แล้วค่อยขยายไปไกลขึ้นทีละชั้น ชั้นแรกคือเพื่อนบ้านตรงของจุดเริ่ม (ห่าง 1 ก้าว) ชั้นถัดไปคือเพื่อนบ้านของเพื่อนบ้าน (ห่าง 2 ก้าว) ไปเรื่อย ๆ เพราะมันแผ่ออกเป็นวงตามระยะทาง พอ BFS ไปถึงเป้าหมายครั้งแรก เรารับประกันได้เลยว่านั่นคือเส้นทางที่สั้นที่สุด (ก้าวน้อยที่สุด) นี่คือเหตุผลที่โจทย์แนว หาจำนวนก้าวน้อยสุด ระยะสั้นสุด เวลาน้อยสุด มักใช้ BFS" },
      { t: "p", c: "เครื่องมือคู่กับ BFS คือ queue (คิว) แบบเข้าก่อนออกก่อน เราใช้ deque จาก collections เพราะมันดึงของจากหัวคิวได้เร็ว O(1) หลักการคือ เอา node ที่จะเริ่มใส่คิว แล้ววนดึงออกจากหัวคิวมาประมวลผล พร้อมโยนเพื่อนบ้านที่ยังไม่เยี่ยมเข้าท้ายคิว ทำจนคิวหมด และเหมือน DFS เราต้องมี visited กันเดินซ้ำเสมอ" },
      { t: "code", lang: "python", c: `from collections import deque

# template BFS บนกราฟ
def bfs(start, graph):
    queue = deque([start])
    visited = {start}              # ทำเครื่องหมายตั้งแต่ใส่คิว
    steps = 0
    while queue:
        # ดึงทั้งชั้นออกมาพร้อมกัน เพื่อนับจำนวนก้าว (ชั้น)
        for _ in range(len(queue)):
            node = queue.popleft() # ดึงจากหัวคิว
            # ... ทำอะไรกับ node ตรงนี้ ...
            for nxt in graph[node]:
                if nxt not in visited:
                    visited.add(nxt)   # ทำเครื่องหมายทันทีที่ใส่คิว
                    queue.append(nxt)  # โยนเข้าท้ายคิว
        steps += 1                 # จบหนึ่งชั้น = ขยับไปอีกหนึ่งก้าว` },
      { t: "h3", c: "BFS บน grid สองมิติ" },
      { t: "p", c: "โจทย์ BFS เจอบ่อยมากในรูปตาราง (grid) สองมิติ เช่นแผนที่ เขาวงกต ทุ่งนา เรามองแต่ละช่อง (r, c) เป็น node และช่องที่อยู่ติดกัน 4 ทิศ (บน ล่าง ซ้าย ขวา) เป็นเพื่อนบ้าน โดยไม่ต้องสร้าง adjacency list เลย แค่คำนวณพิกัดเพื่อนบ้านสด ๆ ตอนเดิน" },
      { t: "code", lang: "python", c: `# เพื่อนบ้าน 4 ทิศบน grid: บน ล่าง ซ้าย ขวา
directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

rows, cols = len(grid), len(grid[0])
for dr, dc in directions:
    nr, nc = r + dr, c + dc
    # เช็คว่าไม่หลุดขอบตารางก่อนเสมอ
    if 0 <= nr < rows and 0 <= nc < cols:
        # ... ช่อง (nr, nc) เป็นเพื่อนบ้านที่ใช้ได้ ...
        pass` },
      { t: "callout", title: "DFS หรือ BFS ดี", c: "ถ้าโจทย์ถามแค่ ไปถึงไหม เชื่อมกันไหม นับกลุ่ม ใช้ได้ทั้งคู่ แต่ถ้าถาม สั้นที่สุด น้อยที่สุด ให้เลือก BFS เพราะมันแผ่เป็นชั้นตามระยะทาง เจอเป้าหมายครั้งแรกคือคำตอบที่สั้นสุดทันที ส่วน DFS ไม่การันตีเรื่องนี้" },

      { t: "h2", c: "ข้อ 1 — ทางออกใกล้สุดในเขาวงกต (LC1926) 🟡" },
      { t: "p", c: "ให้ตาราง maze ที่ช่อง . คือทางเดินได้ และ + คือกำแพง พร้อมตำแหน่งทางเข้า entrance = [row, col] ให้หาจำนวนก้าวน้อยที่สุดที่จะเดินจากทางเข้าไปถึง ทางออก โดยทางออกคือช่องว่างที่อยู่ ติดขอบ ตาราง (แถวบนสุด ล่างสุด คอลัมน์ซ้ายสุด ขวาสุด) และต้องไม่ใช่ช่องทางเข้าเอง เดินได้ 4 ทิศ ถ้าไปถึงทางออกไม่ได้เลยให้คืน -1 เช่นเขาวงกตขนาด 3 แถวที่มีทางออกใกล้ ๆ อาจตอบ 1 ก้าว ถ้าทางเข้าอยู่กลางและถูกกำแพงล้อมจนออกไม่ได้ก็ตอบ -1" },
      { t: "callout", title: "คำใบ้", c: "BFS จากทางเข้า นับก้าวทีละชั้น ช่องแรกที่ไปถึงแล้วอยู่ติดขอบ (และไม่ใช่ทางเข้า) คือทางออกที่ใกล้สุด คืนจำนวนก้าวตอนนั้นได้เลย" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import deque

def nearest_exit(maze, entrance):
    rows, cols = len(maze), len(maze[0])
    start_r, start_c = entrance
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

    queue = deque([(start_r, start_c, 0)])   # (แถว, คอลัมน์, จำนวนก้าว)
    maze[start_r][start_c] = "+"             # ทำเครื่องหมายว่าเยี่ยมแล้ว (ปิดทาง)

    while queue:
        r, c, steps = queue.popleft()
        for dr, dc in directions:
            nr, nc = r + dr, c + dc
            # ต้องอยู่ในตาราง และเป็นทางเดินได้
            if 0 <= nr < rows and 0 <= nc < cols and maze[nr][nc] == ".":
                # ถ้าช่องใหม่อยู่ติดขอบ นี่คือทางออก
                if nr == 0 or nr == rows - 1 or nc == 0 or nc == cols - 1:
                    return steps + 1
                maze[nr][nc] = "+"           # ปิดทางกันเดินซ้ำ
                queue.append((nr, nc, steps + 1))
    return -1                                # เดินจนทั่วแล้วไม่เจอทางออก

maze = [["+", "+", ".", "+"],
        [".", ".", ".", "+"],
        ["+", "+", "+", "."]]
print(nearest_exit(maze, [1, 2]))            # 1` },
        { t: "p", c: "เพราะโจทย์ถามจำนวนก้าว น้อยที่สุด นี่คือสัญญาณชัดเจนว่าใช้ BFS เราเก็บจำนวนก้าวไปพร้อมกับพิกัดในคิวเลย (r, c, steps) พอดึงช่องไหนออกมาก็รู้ทันทีว่ามันห่างจากทางเข้ากี่ก้าว เมื่อเราเดินไปเจอช่องว่างที่ติดขอบเป็นครั้งแรก เพราะ BFS แผ่ตามระยะ ช่องนั้นย่อมเป็นทางออกที่ใกล้สุด คืน steps + 1 ได้เลย" },
        { t: "p", c: "ทริกประหยัด memory คือแทนที่จะสร้าง set visited แยก เราเขียนทับช่องที่เยี่ยมแล้วด้วย + (ทำให้มันกลายเป็นกำแพง) เพื่อกันเดินซ้ำ จุดที่ต้องระวังคือทางเข้า ไม่ นับเป็นทางออกแม้จะติดขอบ โค้ดจึงเช็คเงื่อนไขติดขอบเฉพาะกับช่อง ใหม่ (nr, nc) ที่ไม่ใช่ช่องเริ่ม และปิดช่องเริ่มตั้งแต่ต้น อีกจุดคืออย่าลืมตั้งค่าเริ่ม steps ให้ถูก การเก็บก้าวในคิวช่วยไม่ให้พลาดตรงนี้ · Time O(rows x cols) เยี่ยมแต่ละช่องมากสุดหนึ่งครั้ง · Space O(rows x cols) จากคิวในกรณีแย่สุด" },
      ] },

      { t: "h2", c: "ข้อ 2 — ส้มเน่าลาม (LC994) 🟡" },
      { t: "p", c: "ให้ตาราง grid ที่แต่ละช่องเป็น 0 (ว่าง) 1 (ส้มสด) หรือ 2 (ส้มเน่า) ทุก ๆ หนึ่งนาที ส้มเน่าจะทำให้ส้มสดที่อยู่ ติดกัน 4 ทิศ เน่าตามไปด้วย ให้หาว่าใช้เวลากี่นาทีจนไม่เหลือส้มสดเลย ถ้ามีส้มสดที่ไม่มีวันเน่า (ไม่มีส้มเน่าเข้าถึง) ให้คืน -1 เช่น grid = [[2,1,1],[1,1,0],[0,1,1]] ตอบ 4 นาที ส่วน grid = [[2,1,1],[0,1,1],[1,0,1]] ตอบ -1 เพราะมีส้มมุมล่างซ้ายที่โดนช่องว่างตัดขาด และถ้าไม่มีส้มสดตั้งแต่แรก ตอบ 0" },
      { t: "callout", title: "คำใบ้", c: "นี่คือ BFS หลายจุดเริ่มพร้อมกัน (multi-source) เอาส้มเน่า ทุกลูก ใส่คิวตั้งแต่แรกพร้อมกัน แล้วแผ่ออกทีละนาที (ทีละชั้น) จำนวนชั้นที่แผ่คือจำนวนนาที ปิดท้ายเช็คว่ายังเหลือส้มสดไหม" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import deque

def oranges_rotting(grid):
    rows, cols = len(grid), len(grid[0])
    queue = deque()
    fresh = 0

    # หาตำแหน่งส้มเน่าทั้งหมด (จุดเริ่มหลายจุด) และนับส้มสด
    for r in range(rows):
        for c in range(cols):
            if grid[r][c] == 2:
                queue.append((r, c))
            elif grid[r][c] == 1:
                fresh += 1

    if fresh == 0:
        return 0                     # ไม่มีส้มสดตั้งแต่แรก ใช้ 0 นาที

    minutes = 0
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

    while queue and fresh > 0:
        minutes += 1                 # ผ่านไปอีกหนึ่งนาที
        for _ in range(len(queue)):  # แผ่ทั้งชั้น (ส้มเน่า ณ นาทีนี้) พร้อมกัน
            r, c = queue.popleft()
            for dr, dc in directions:
                nr, nc = r + dr, c + dc
                if 0 <= nr < rows and 0 <= nc < cols and grid[nr][nc] == 1:
                    grid[nr][nc] = 2         # ส้มสดเน่าตาม
                    fresh -= 1
                    queue.append((nr, nc))   # กลายเป็นแหล่งเน่าในนาทีถัดไป

    return minutes if fresh == 0 else -1     # ถ้ายังเหลือส้มสด แปลว่าเน่าไม่ถึง

print(oranges_rotting([[2, 1, 1], [1, 1, 0], [0, 1, 1]]))  # 4
print(oranges_rotting([[2, 1, 1], [0, 1, 1], [1, 0, 1]]))  # -1
print(oranges_rotting([[0, 2]]))                           # 0` },
        { t: "p", c: "จุดที่ทำให้ข้อนี้ต่างจากข้อก่อนคือมันมีจุดเริ่ม หลายจุดพร้อมกัน (ส้มเน่าทุกลูกลามในเวลาเดียวกัน) เทคนิคคือ multi-source BFS เราไม่ได้เริ่มจากจุดเดียว แต่โยนส้มเน่าทุกลูกเข้าคิวตั้งแต่ต้น แล้ว BFS แผ่ออกพร้อมกันหมด คลื่นการเน่าจึงขยายทีละวงพอดีกับเวลาจริง" },
        { t: "p", c: "การนับนาทีต้องประมวลผล ทีละชั้น การเขียน for _ in range(len(queue)) ก่อนดึงของ ทำให้เราจัดการเฉพาะส้มที่เน่าอยู่ ณ ต้นนาทีนี้ให้ครบก่อน ค่อยขึ้นนาทีใหม่ ถ้าไม่ล็อกจำนวนไว้ก่อน ส้มที่เพิ่งเน่าในนาทีนี้จะถูกนับรวมทำให้นับนาทีเกิน เราใช้ตัวแปร fresh นับส้มสดที่เหลือ ทุกครั้งที่ทำให้ลูกหนึ่งเน่าก็ลด fresh ลง" },
        { t: "p", c: "edge case สำคัญสามอย่าง หนึ่ง ไม่มีส้มสดตั้งแต่แรก ต้องคืน 0 (ไม่ใช่ -1) จึงเช็ค fresh == 0 ก่อนเริ่มวน สอง มีส้มสดที่ถูกช่องว่างตัดขาด สุดท้าย fresh ยังมากกว่า 0 จึงคืน -1 สาม เงื่อนไข while queue and fresh > 0 ช่วยหยุดทันทีเมื่อส้มสดหมด ไม่นับนาทีเกินโดยเปล่าประโยชน์ · Time O(rows x cols) เยี่ยมแต่ละช่องมากสุดหนึ่งครั้ง · Space O(rows x cols) จากคิวในกรณีที่ส้มเน่าเยอะ" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Heap / Priority Queue →", slug: "lc75-heap", desc: "คิวที่ดึงค่ามากสุดหรือน้อยสุดออกมาก่อนเสมอ" },
        { title: "← Graphs — DFS", slug: "lc75-graph-dfs" },
      ] },
    ],
  },
};
