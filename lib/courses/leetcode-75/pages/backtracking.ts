import type { Page } from "@/lib/types";

export const backtrackingPages: Record<string, Page> = {
  "lc75-backtracking": {
    slug: "lc75-backtracking",
    title: "Backtracking — ลองทุกทางแล้วถอยกลับ",
    lead: "เทคนิคไล่ลองทุกทางเลือกแบบมีระบบ ลงลึกไปทีละก้าว แล้วถอยกลับ (undo) เพื่อลองทางอื่น",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "Backtracking (การย้อนรอย) คือเทคนิคสำหรับโจทย์ที่ต้อง สร้างคำตอบทีละชิ้น และมีหลายทางเลือกในแต่ละก้าว เช่น หาทุกชุดค่าที่เป็นไปได้ ทุกการจัดเรียง หรือทุกวิธีเลือกของ ไอเดียหลักคือ ลองเลือกทางหนึ่ง แล้วลงลึกต่อไปเรื่อย ๆ ถ้าเจอทางตัน หรือสำรวจครบแล้ว ก็ ถอยกลับ มายกเลิกการเลือกล่าสุด (undo) เพื่อไปลองทางอื่นแทน มันคือการค้นหาแบบเป็นระบบที่รับประกันว่าจะไม่พลาดคำตอบไหนเลย" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "ลองนึกภาพว่าเรากำลังเดินอยู่ในเขาวงกตที่แตกเป็นทางแยกเยอะมาก ทุกครั้งที่เจอทางแยก เราเลือกเดินทางหนึ่งไปก่อน ถ้าสุดทางแล้วไม่เจอทางออก เราก็เดินย้อนกลับมาที่ทางแยกเดิม แล้วลองอีกทาง Backtracking ทำแบบเดียวกันเป๊ะ ๆ แต่ทำด้วย recursion (ฟังก์ชันเรียกตัวเอง)" },
      { t: "p", c: "เราสามารถวาดการทำงานเป็น recursion tree ได้ แต่ละ node คือ สถานะปัจจุบัน (คำตอบที่สร้างไปแล้วบางส่วน) และแต่ละกิ่งที่แตกออกไปคือ ทางเลือก หนึ่งที่เราลอง สมมติต้องสร้างสตริงยาว 2 ตัวจากตัวอักษร a และ b ต้นไม้จะเป็นแบบนี้:" },
      { t: "code", lang: "python", c: `                   ""            <- ยังไม่เลือกอะไร
                 /    \\
              "a"      "b"        <- เลือกตัวแรก
             /   \\    /   \\
          "aa" "ab" "ba" "bb"     <- เลือกตัวที่สอง (คำตอบครบ)` },
      { t: "p", c: "แต่ละใบ (leaf) ของต้นไม้คือคำตอบที่สมบูรณ์หนึ่งชุด การเดินลงจากรากไปหาใบคือการ ลงลึก (explore) และเมื่อกลับขึ้นมาเพื่อไปกิ่งข้าง ๆ คือการ ถอยกลับ (backtrack)" },
      { t: "p", c: "หัวใจของ backtracking ทุกข้อจะมีโครงเดียวกัน จำ template สามจังหวะนี้ไว้: choose (เลือก) → explore (ลงลึก) → unchoose (ถอย)" },
      { t: "code", lang: "python", c: `def backtrack(path, choices):
    if เงื่อนไขว่าคำตอบครบแล้ว:
        result.append(path[:])   # เก็บสำเนาคำตอบ (สำคัญ! ต้อง copy)
        return
    for choice in choices:       # ลองทุกทางเลือกที่ทำได้
        path.append(choice)      # 1) choose  — เลือกทางนี้
        backtrack(path, ...)     # 2) explore — ลงลึกต่อ
        path.pop()               # 3) unchoose — ถอย เอาออกเพื่อลองทางอื่น` },
      { t: "callout", title: "ทำไมต้อง undo", c: "เพราะเราใช้ตัวแปร path ตัวเดียวร่วมกันทุกกิ่ง ถ้าลงลึกไปทางหนึ่งแล้วไม่ถอยกลับมาลบของที่เพิ่งใส่ พอไปกิ่งข้าง ๆ path จะยังมีของเก่าค้างอยู่ ทำให้คำตอบเพี้ยน การ pop() หลัง explore คือการคืนสภาพให้เหมือนก่อนเลือก เพื่อให้กิ่งถัดไปเริ่มจากสถานะที่ถูกต้อง" },
      { t: "callout", title: "ระวังตอนเก็บคำตอบ", warn: true, c: "เวลาเก็บ path ลง result ต้องเก็บสำเนา (path[:] หรือ list(path)) ไม่ใช่ตัว path เอง เพราะ path จะถูกแก้ต่อเรื่อย ๆ ถ้าเก็บตัวจริงไป คำตอบทุกชุดใน result จะกลายเป็นลิสต์ว่างเปล่าตอนจบ" },

      { t: "h2", c: "ข้อ 1 — ชุดตัวอักษรจากเลขบนแป้นโทรศัพท์ (LC17) 🟡" },
      { t: "p", c: "แป้นโทรศัพท์แบบเก่ามีตัวอักษรกำกับบนปุ่มตัวเลข: 2=abc, 3=def, 4=ghi, 5=jkl, 6=mno, 7=pqrs, 8=tuv, 9=wxyz ให้สตริงของตัวเลข (2-9) มา แล้วคืนทุกชุดตัวอักษรที่กดได้ เช่น digits = \"23\" ตอบ ['ad','ae','af','bd','be','bf','cd','ce','cf'] (เลข 2 ให้ a/b/c เลข 3 ให้ d/e/f จับคู่กันครบทุกแบบ) ถ้า digits เป็นสตริงว่างให้คืนลิสต์ว่าง" },
      { t: "callout", title: "คำใบ้", c: "แต่ละหลักของตัวเลขคือหนึ่งก้าวใน recursion ที่หลักนั้นเราต้องลองตัวอักษรทุกตัวที่ปุ่มนั้นมี พอเลือกครบทุกหลัก (ความยาว path เท่ากับความยาว digits) ก็ได้คำตอบหนึ่งชุด" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def letter_combinations(digits):
    if not digits:
        return []                # ไม่มีเลขก็ไม่มีชุดตัวอักษร
    mapping = {
        "2": "abc", "3": "def", "4": "ghi", "5": "jkl",
        "6": "mno", "7": "pqrs", "8": "tuv", "9": "wxyz",
    }
    result = []
    path = []

    def backtrack(i):
        if i == len(digits):          # เลือกครบทุกหลักแล้ว
            result.append("".join(path))
            return
        for ch in mapping[digits[i]]: # ลองทุกตัวอักษรบนปุ่มนี้
            path.append(ch)           # choose
            backtrack(i + 1)          # explore หลักถัดไป
            path.pop()                # unchoose ถอยไปลองตัวอื่น

    backtrack(0)
    return result

print(letter_combinations("23"))
# ['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf']
print(letter_combinations(""))   # []` },
        { t: "p", c: "เราเดินตามหลักของ digits ทีละตัวด้วยตัวชี้ i ที่หลัก i เราดูว่าปุ่มนั้นมีตัวอักษรอะไรบ้าง (mapping[digits[i]]) แล้ววนลองทุกตัว แต่ละตัวคือหนึ่งกิ่งของต้นไม้ เมื่อ i เดินไปถึงความยาวของ digits แปลว่าเราเลือกครบทุกหลักแล้ว จึงต่อ path เป็นสตริงเก็บลง result" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมเช็คกรณี digits ว่าง ถ้าไม่ดักไว้ ฟังก์ชันจะคืน [''] (ลิสต์ที่มีสตริงว่างหนึ่งตัว) แทนที่จะเป็น [] อีกจุดคือลืม path.pop() ทำให้ตัวอักษรค้างสะสม" },
        { t: "p", c: "Time O(4^n · n) เมื่อ n คือจำนวนหลัก แต่ละหลักแตกได้มากสุด 4 กิ่ง (ปุ่ม 7 กับ 9 มี 4 ตัว) จึงมีคำตอบได้ถึง 4^n ชุด และการต่อสตริงแต่ละชุดใช้เวลา O(n) · Space O(n) จากความลึกของ recursion และขนาด path (ไม่นับพื้นที่เก็บ result)" },
      ] },

      { t: "h2", c: "ข้อ 2 — เลือก k ตัวจาก 1-9 ให้ผลรวมเท่ากับ n (LC216) 🟡" },
      { t: "p", c: "ให้จำนวน k กับ n มา ให้หาทุกชุดที่ประกอบด้วยเลข k ตัวที่เลือกจาก 1 ถึง 9 โดยห้ามเลือกเลขซ้ำในชุดเดียวกัน และผลรวมของทั้งชุดต้องเท่ากับ n เช่น k = 3, n = 7 ตอบ [[1,2,4]] (มีชุดเดียวที่เลือก 3 ตัวไม่ซ้ำแล้วรวมได้ 7) อีกตัวอย่าง k = 3, n = 9 ตอบ [[1,2,6],[1,3,5],[2,3,4]] แต่ละชุดเรียงจากน้อยไปมากและไม่ซ้ำกัน" },
      { t: "callout", title: "คำใบ้", c: "เพื่อไม่ให้เลือกเลขซ้ำและไม่ให้ได้ชุดเดิมสลับลำดับ ให้ไล่เลือกจากน้อยไปมากเสมอ โดยส่งค่า start บอกว่าเลขถัดไปต้องเริ่มจากตัวไหน และตัดกิ่งทิ้งได้ทันทีถ้าเลขที่จะเลือกมากกว่าค่าที่เหลือ (remaining)" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def combination_sum3(k, n):
    result = []
    path = []

    def backtrack(start, remaining):
        if len(path) == k:            # เลือกครบ k ตัวแล้ว
            if remaining == 0:        # ผลรวมพอดี = คำตอบที่ใช้ได้
                result.append(path[:])
            return
        for num in range(start, 10):  # เลือกเลขจาก start ถึง 9
            if num > remaining:       # ตัดกิ่ง: ตัวนี้และตัวถัด ๆ ใหญ่เกิน
                break
            path.append(num)              # choose
            backtrack(num + 1, remaining - num)  # explore: ตัวถัดไปเริ่มที่ num+1
            path.pop()                    # unchoose

    backtrack(1, n)
    return result

print(combination_sum3(3, 7))  # [[1, 2, 4]]
print(combination_sum3(3, 9))  # [[1, 2, 6], [1, 3, 5], [2, 3, 4]]
print(combination_sum3(4, 1))  # []` },
        { t: "p", c: "เราสร้างชุดคำตอบทีละตัว โดยพารามิเตอร์ start คุมไม่ให้ย้อนไปเลือกเลขที่เล็กกว่าตัวล่าสุด ทำให้ชุดที่ได้เรียงจากน้อยไปมากเสมอ จึงไม่มีทางเลือกเลขซ้ำและไม่ได้ชุดที่เป็นเพียงการสลับลำดับของกันและกัน ส่วน remaining คือค่าที่ยังต้องเติมให้ครบ n เมื่อเลือกเลข num เราก็ลบมันออกจาก remaining แล้วลงลึกต่อ" },
        { t: "p", c: "จุดที่ทำให้เร็วขึ้นคือการตัดกิ่ง (pruning) เมื่อ num > remaining เราหยุดทั้ง loop ด้วย break ได้เลย เพราะ range เรียงจากน้อยไปมาก ถ้าตัวนี้ใหญ่เกินไปแล้ว ตัวถัด ๆ ยิ่งใหญ่กว่า จึงไม่มีทางเป็นคำตอบ เงื่อนไขสำคัญตอนเก็บคำตอบคือต้องครบสองอย่างพร้อมกัน: จำนวนพอดี k ตัว และผลรวมเหลือ 0 พอดี" },
        { t: "p", c: "Time O(C(9,k) · k) จำนวนชุดที่เป็นไปได้มากสุดคือการเลือก k ตัวจาก 9 (มีเพดานตายตัวเพราะเลือกได้แค่ 1-9) และการ copy แต่ละชุดใช้ O(k) · Space O(k) จากความลึก recursion และขนาด path" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Dynamic Programming — 1 มิติ →", slug: "lc75-dp-1d", desc: "แก้ปัญหาย่อยที่ซ้ำกันแล้วเก็บคำตอบไว้ใช้ซ้ำ" },
        { title: "← Binary Search", slug: "lc75-binary-search" },
      ] },
    ],
  },
};
