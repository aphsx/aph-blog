import type { Page } from "@/lib/types";

export const treeDfsPages: Record<string, Page> = {
  "lc75-tree-dfs": {
    slug: "lc75-tree-dfs",
    title: "Binary Tree — DFS (ลุยลึก)",
    lead: "รู้จัก binary tree ตั้งแต่ศูนย์ แล้วใช้ recursion ลุยลงลึกทีละกิ่ง (DFS) เพื่อแก้โจทย์ต้นไม้ 6 ข้อ",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "จนถึงตอนนี้เราเล่นกับข้อมูลที่เรียงเป็นแถวยาว ๆ (ลิสต์, สตริง, linked list) มาตลอด หน้านี้เราจะเจอโครงสร้างข้อมูลแบบใหม่ที่แตกกิ่งก้านเหมือนต้นไม้ ชื่อว่า binary tree มันโผล่ในโจทย์สัมภาษณ์บ่อยมาก และเครื่องมือหลักที่ใช้ตะลุยมันคือ recursion ถ้ายังไม่เคยรู้จัก tree เลยก็ไม่เป็นไร เราจะเริ่มจากศูนย์" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "Binary tree คือโครงสร้างข้อมูลที่ประกอบด้วยกล่องเล็ก ๆ เรียกว่า node ต่อกันแบบแตกกิ่งลงล่าง แต่ละ node เก็บค่าหนึ่งค่า และชี้ไปหา node ลูกได้มากสุด 2 ตัว (นี่คือที่มาของคำว่า binary = สอง) เราเรียกลูกสองตัวนั้นว่าลูกซ้าย (left child) และลูกขวา (right child)" },
      { t: "ul", c: [
        "root = node บนสุด เป็นจุดเริ่มต้นของต้นไม้ (ต้นไม้ทั้งต้นเข้าถึงได้ผ่าน root)",
        "node = กล่องแต่ละกล่องที่เก็บค่าและลิงก์ไปหาลูก",
        "left child / right child = node ลูกทางซ้ายและทางขวาของ node หนึ่ง",
        "parent = node แม่ที่ชี้ลงมาหา node นั้น",
        "leaf (ใบ) = node ที่ไม่มีลูกเลย (ทั้งซ้ายและขวาเป็น None) — เหมือนปลายกิ่ง",
        "ความสูง (height) = จำนวนชั้นจาก root ลงไปถึง leaf ที่ลึกสุด",
      ] },
      { t: "p", c: "ลองดูรูปนี้ ต้นไม้กลับหัว — root อยู่บน ใบอยู่ล่าง เส้น / กับ \\ คือลิงก์จากแม่ลงไปหาลูกซ้ายและลูกขวา" },
      { t: "code", lang: "python", c: `          3          <- root (ชั้น 1)
         / \\
        9   20         <- ชั้น 2
           /  \\
          15   7       <- ชั้น 3 (ทั้ง 9, 15, 7 คือ leaf เพราะไม่มีลูก)

# ต้นนี้ความสูง = 3 ชั้น
# 20 มีลูกซ้าย = 15, ลูกขวา = 7
# 9 ไม่มีลูก จึงเป็น leaf` },

      { t: "h2", c: "หน้าตาของ node ในโค้ด: คลาส TreeNode" },
      { t: "p", c: "LeetCode สร้าง node แต่ละตัวจากคลาส TreeNode หน้าตาแบบนี้ ตัวมันเองเก็บค่า val และตัวชี้ไปหาลูกสองตัวคือ left กับ right ถ้าไม่มีลูกฝั่งไหน ตัวชี้ฝั่งนั้นจะเป็น None" },
      { t: "code", lang: "python", c: `class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val      # ค่าที่เก็บใน node นี้
        self.left = left    # ลูกซ้าย (เป็น TreeNode หรือ None)
        self.right = right  # ลูกขวา (เป็น TreeNode หรือ None)

# ประกอบต้นไม้ตามรูปด้านบนด้วยมือ
root = TreeNode(3)
root.left = TreeNode(9)
root.right = TreeNode(20, TreeNode(15), TreeNode(7))
# root.right.left.val == 15` },

      { t: "h2", c: "DFS คืออะไร และทำไมใช้ recursion" },
      { t: "p", c: "DFS ย่อมาจาก Depth-First Search แปลตรงตัวว่า \"ค้นแบบลงลึกก่อน\" ไอเดียคือเลือกกิ่งหนึ่งแล้วเดินดิ่งลงไปให้สุดก่อน จนชนใบ แล้วค่อยถอยกลับมาลองกิ่งอื่น ต่างจากการไล่ทีละชั้น (นั่นคือ BFS ซึ่งเราจะเรียนหน้าถัดไป)" },
      { t: "p", c: "สาเหตุที่ recursion เข้ากับ tree ได้พอดิบพอดี เพราะต้นไม้มีธรรมชาติแบบ \"เรียกซ้อนตัวเอง\" อยู่แล้ว: ลูกซ้ายของ node ก็คือ root ของต้นไม้ย่อยอีกต้นหนึ่ง ลูกขวาก็เป็น root ของอีกต้นหนึ่งเช่นกัน ดังนั้นทุก node จึงมองเป็น \"ปัญหาย่อยหน้าตาเหมือนกันเป๊ะ\" เราแค่เขียนวิธีแก้ที่ node เดียว แล้วสั่งให้มันเรียกตัวเองกับลูกซ้ายและลูกขวา" },
      { t: "callout", title: "หัวใจของ recursion บน tree", c: "ทุกฟังก์ชัน recursion ต้องมี 2 ส่วน: (1) base case = เงื่อนไขหยุด มักเป็น \"ถ้า node เป็น None ให้คืนค่าเริ่มต้น\" กันไม่ให้เรียกทะลุปลายกิ่ง และ (2) recursive case = แก้ที่ node ปัจจุบันโดยอาศัยคำตอบของลูกซ้ายและลูกขวา" },
      { t: "code", lang: "python", c: `# template recursion บน binary tree ที่ใช้ได้แทบทุกข้อ
def dfs(node):
    if node is None:        # (1) base case: ตกขอบ ปลายกิ่งแล้ว
        return              #     คืนค่าเริ่มต้น (0, None, [] แล้วแต่โจทย์)

    # (2) recursive case: ถามคำตอบจากสองต้นย่อยก่อน
    left = dfs(node.left)
    right = dfs(node.right)

    # แล้วเอา left, right มารวมกับ node.val เป็นคำตอบของต้นนี้
    return combine(node.val, left, right)` },

      { t: "h2", c: "ลำดับการเดิน: preorder / inorder / postorder" },
      { t: "p", c: "เวลาเดิน DFS เราแตะ 3 อย่างที่แต่ละ node คือ ตัวมันเอง (N), ต้นย่อยซ้าย (L), และต้นย่อยขวา (R) ลำดับที่เราเลือกทำ N เทียบกับ L, R มีชื่อเรียกต่างกัน แต่ทั้งหมดคือ DFS เหมือนกัน" },
      { t: "table", head: ["ชื่อ", "ลำดับ", "จำง่าย ๆ / ใช้ตอนไหน"], rows: [
        ["preorder", "N → L → R", "ทำตัวเองก่อนค่อยลงลูก เช่น ก็อปปี้/พิมพ์โครงต้นไม้"],
        ["inorder", "L → N → R", "ซ้ายก่อนค่อยตัวเอง — บน BST จะได้ค่าเรียงจากน้อยไปมาก"],
        ["postorder", "L → R → N", "ลูกเสร็จหมดก่อนค่อยสรุปตัวเอง เช่น คิดความสูง/ผลรวม"],
      ] },
      { t: "p", c: "ในโจทย์ส่วนใหญ่ของหน้านี้เราใช้แนว postorder คือ \"ให้ลูกสองฝั่งคำนวณเสร็จก่อน แล้วค่อยเอาผลมารวมที่ตัวเอง\" เพราะคำตอบของ node หนึ่งมักต้องพึ่งคำตอบของลูกทั้งสอง" },

      { t: "h2", c: "ข้อ 1 — ความลึกมากสุดของต้นไม้ (LC104) 🟢" },
      { t: "p", c: "ให้ root ของ binary tree มา ให้หาความลึกมากสุด = จำนวน node บนเส้นทางที่ยาวสุดจาก root ลงไปถึง leaf เช่นต้นไม้ [3,9,20,null,null,15,7] (ตามรูปตัวอย่างด้านบน) คำตอบคือ 3 เพราะเส้นทางยาวสุดคือ 3 → 20 → 15 (หรือ 3 → 20 → 7)" },
      { t: "callout", title: "คำใบ้", c: "ความลึกของต้นหนึ่ง = 1 (นับตัว root เอง) บวกกับความลึกที่มากกว่าของสองต้นย่อย ลองปล่อยให้ลูกซ้ายและลูกขวาบอกความลึกของมันมา" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `# นิยาม node ที่ LeetCode ใช้ (โจทย์ tree ทุกข้ออิงหน้าตานี้)
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

def maxDepth(root):
    if root is None:        # ต้นว่าง ลึก 0 ชั้น (base case)
        return 0
    left = maxDepth(root.left)    # ความลึกของต้นย่อยซ้าย
    right = maxDepth(root.right)  # ความลึกของต้นย่อยขวา
    return 1 + max(left, right)   # บวก 1 นับตัวเอง เลือกฝั่งที่ลึกกว่า` },
        { t: "p", c: "คิดแบบ postorder: เราไม่รู้ความลึกของต้นตอนนี้จนกว่าจะรู้ความลึกของลูกสองฝั่งก่อน จึงเรียก recursion ลงลูกซ้ายและลูกขวาให้บอกความลึกกลับมา แล้วต้นปัจจุบันแค่หยิบฝั่งที่ลึกกว่ามาบวก 1 (บวกสำหรับนับตัว root เองในเส้นทางนั้น) base case สำคัญมาก: ถ้า node เป็น None แปลว่าตกขอบไปแล้ว คืน 0 ไม่งั้นจะเรียกทะลุ None แล้ว error" },
        { t: "p", c: "Time O(n) แตะทุก node ครั้งเดียว (n = จำนวน node) · Space O(h) จากความลึกของ call stack ที่เรียกซ้อนกันตามความสูง h ของต้นไม้ กรณีแย่สุด (ต้นเอียงเป็นเส้นตรง) h เท่ากับ n" },
      ] },

      { t: "h2", c: "ข้อ 2 — ต้นไม้ใบเหมือนกันไหม (LC872) 🟢" },
      { t: "p", c: "ให้ root ของต้นไม้สองต้น ให้ตอบว่าลำดับของ \"ค่าที่ leaf จากซ้ายไปขวา\" ของสองต้นเหมือนกันหรือไม่ เช่นถ้าต้นแรกอ่านใบจากซ้ายไปขวาได้ [6,7,4,9,8] และต้นที่สองก็ได้ [6,7,4,9,8] เท่ากัน → True แต่ถ้าลำดับใบต่างกันแม้แต่นิดเดียว → False (สนแค่ใบ ไม่สนโครงหรือ node ภายใน)" },
      { t: "callout", title: "คำใบ้", c: "ถ้าเราเก็บค่าของ leaf เรียงจากซ้ายไปขวาของแต่ละต้นออกมาเป็นลิสต์ได้ ก็แค่เทียบสองลิสต์ว่าเท่ากันไหม การเดินซ้ายก่อนขวาเสมอจะรับประกันลำดับซ้าย-ไป-ขวา" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def leafSimilar(root1, root2):
    def leaves(node):
        if node is None:
            return []
        # ไม่มีลูกทั้งสองฝั่ง = เป็น leaf เก็บค่าไว้
        if node.left is None and node.right is None:
            return [node.val]
        # ต่อใบฝั่งซ้ายก่อน แล้วตามด้วยฝั่งขวา -> ได้ลำดับซ้ายไปขวา
        return leaves(node.left) + leaves(node.right)

    return leaves(root1) == leaves(root2)` },
        { t: "p", c: "ฟังก์ชันย่อย leaves เดินแบบ DFS เก็บค่าเฉพาะ node ที่เป็นใบ (ไม่มีลูกทั้งซ้ายและขวา) เคล็ดของการได้ลำดับ \"ซ้ายไปขวา\" คือเราต่อผลของฝั่งซ้ายก่อนฝั่งขวาเสมอ จุดพลาดที่พบบ่อยคือลืมเช็คว่าเป็น leaf จริง ๆ แล้วไปเก็บค่า node ภายในด้วย หรือสลับลำดับซ้ายขวาทำให้ลำดับใบผิด" },
        { t: "p", c: "Time O(n1 + n2) แตะทุก node ของทั้งสองต้น · Space O(n1 + n2) จากลิสต์ใบที่เก็บไว้ บวกความลึก call stack ตามความสูงต้นไม้" },
      ] },

      { t: "h2", c: "ข้อ 3 — นับ node ที่ดี (LC1448) 🟡" },
      { t: "p", c: "node หนึ่งถือว่า \"ดี\" (good) ถ้าบนเส้นทางจาก root ลงมาถึง node นั้น ไม่มี node ไหนที่ค่ามากกว่ามันเลย (พูดอีกแบบคือค่าของมันมากกว่าหรือเท่ากับทุก node ที่ผ่านมาก่อนหน้าบนเส้นทางนั้น) ให้นับว่ามี good node กี่ตัว หมายเหตุ: root เป็น good เสมอ เพราะไม่มีใครอยู่ก่อนหน้ามัน เช่นต้น [3,1,4,3,null,1,5] คำตอบคือ 4" },
      { t: "callout", title: "คำใบ้", c: "ระหว่างเดินลงจาก root ให้พก \"ค่ามากสุดที่เจอมาตลอดเส้นทาง\" ติดตัวไปด้วย ถ้า node ปัจจุบันมีค่ามากกว่าหรือเท่ากับตัวนั้น ก็นับว่าดี แล้วอัปเดตค่ามากสุดก่อนส่งต่อให้ลูก" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def goodNodes(root):
    def dfs(node, max_so_far):
        if node is None:
            return 0
        # node นี้ดีไหม? ดีถ้าไม่มีใครบนเส้นทางค่ามากกว่ามัน
        good = 1 if node.val >= max_so_far else 0
        # อัปเดตค่ามากสุดก่อนส่งต่อให้ลูกสองฝั่ง
        new_max = max(max_so_far, node.val)
        return good + dfs(node.left, new_max) + dfs(node.right, new_max)

    # เริ่มด้วย -inf เพื่อให้ root ถูกนับเป็น good เสมอ
    return dfs(root, float('-inf'))` },
        { t: "p", c: "นี่คือ DFS แบบ preorder ที่ \"ส่งข้อมูลลงล่าง\" (top-down): เราพกค่ามากสุดบนเส้นทางลงไปเรื่อย ๆ ตัดสินที่ node ปัจจุบันก่อน แล้วค่อยลงลูก ต่างจากข้อ 1-2 ที่รอผลจากลูกขึ้นมา จุดสำคัญคือต้องอัปเดต new_max ก่อนเรียกลูก และแต่ละกิ่งมีสำเนา max_so_far ของตัวเอง (ส่งเป็นพารามิเตอร์ ไม่ใช่ตัวแปรร่วม) เพื่อไม่ให้เส้นทางฝั่งซ้ายไปกวนฝั่งขวา การเริ่มด้วย float('-inf') ทำให้ root ผ่านเงื่อนไข >= เสมอ" },
        { t: "p", c: "Time O(n) แตะทุก node ครั้งเดียว · Space O(h) จากความลึก call stack ตามความสูงต้นไม้" },
      ] },

      { t: "h2", c: "ข้อ 4 — นับเส้นทางที่ผลรวมเท่าเป้า (LC437) 🟡" },
      { t: "p", c: "ให้ต้นไม้และเลข targetSum ให้นับจำนวนเส้นทางที่ผลรวมของค่าบนเส้นทางเท่ากับ targetSum โดยเส้นทางต้องเดินลงล่างเท่านั้น (จากแม่ไปหาลูก) และต่อเนื่องกัน แต่ไม่จำเป็นต้องเริ่มที่ root หรือจบที่ leaf เช่นต้น [10,5,-3,3,2,null,11,3,-2,null,1] กับ targetSum = 8 คำตอบคือ 3 (ค่าติดลบก็มีได้ ทำให้ต้องระวัง)" },
      { t: "callout", title: "คำใบ้", c: "ถ้าเก็บ prefix sum (ผลรวมสะสมจาก root ลงมาถึง node ปัจจุบัน) ไว้ในตัวนับ (hash map) เราจะรู้ทันทีว่ามีจุดเริ่มต้นก่อนหน้ากี่จุดที่ทำให้ช่วงระหว่างมันถึงตอนนี้รวมได้เท่า targetSum — เทคนิคเดียวกับ prefix sum บนลิสต์ แต่ทำระหว่างเดิน DFS" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import defaultdict

def pathSum(root, targetSum):
    prefix = defaultdict(int)   # นับว่าผลรวมสะสมค่าหนึ่ง ๆ เคยเจอมากี่ครั้ง
    prefix[0] = 1               # ผลรวม 0 มีอยู่ 1 ครั้ง (จุดก่อนเริ่มเดิน)

    def dfs(node, curr):
        if node is None:
            return 0
        curr += node.val                    # ผลรวมสะสมถึง node นี้
        # มีจุดเริ่มก่อนหน้ากี่จุดที่ทำให้ช่วง (จุดนั้น, ตอนนี้] = targetSum
        count = prefix[curr - targetSum]
        prefix[curr] += 1                   # บันทึกผลรวมสะสมนี้ก่อนลงลูก
        count += dfs(node.left, curr)
        count += dfs(node.right, curr)
        prefix[curr] -= 1                   # ถอยออก (backtrack) ก่อนกลับขึ้น
        return count

    return dfs(root, 0)` },
        { t: "p", c: "หลักการ prefix sum: ถ้าผลรวมสะสมจาก root ถึง node ปัจจุบันคือ curr และเราอยากได้ช่วงที่รวมเป็น targetSum ก็แค่ถามว่า \"เคยมีผลรวมสะสม curr - targetSum อยู่ก่อนหน้ากี่ครั้งบนเส้นทางนี้\" ทุกครั้งที่เคยเจอ = หนึ่งเส้นทางที่ใช้ได้ ที่ต้อง prefix[0] = 1 เพราะครอบกรณีเส้นทางที่เริ่มตั้งแต่ root พอดี" },
        { t: "p", c: "จุดพลาดสำคัญที่สุดคือบรรทัด backtrack (prefix[curr] -= 1) เมื่อเดินลูกของ node นี้เสร็จหมดแล้วต้องกำลังจะถอยขึ้น เราต้องลบผลรวมสะสมนี้ออก ไม่งั้นมันจะไปปนกับเส้นทางฝั่งพี่น้องอีกกิ่งที่ไม่ได้อยู่บนเส้นทางเดียวกัน ทำให้นับเกิน อีกจุดคือค่าติดลบทำให้ใช้วิธี early-stop แบบ two-sum ตรง ๆ ไม่ได้ ต้องพึ่ง hash map นับความถี่แบบนี้" },
        { t: "p", c: "Time O(n) แตะทุก node ครั้งเดียว การค้น/อัปเดต hash map เป็น O(1) เฉลี่ย · Space O(n) จากขนาด hash map และความลึก call stack" },
      ] },

      { t: "h2", c: "ข้อ 5 — เส้นทางซิกแซกยาวสุด (LC1372) 🟡" },
      { t: "p", c: "เส้นทางซิกแซก (ZigZag) คือเดินจาก node หนึ่ง โดยสลับทิศทุกก้าว: ถ้าก้าวนี้ไปลูกซ้าย ก้าวหน้าต้องไปลูกขวา แล้วซ้าย สลับไปเรื่อย ๆ ให้หาความยาว (นับเป็นจำนวนก้าว/เส้นเชื่อม ไม่ใช่จำนวน node) ของเส้นทางซิกแซกที่ยาวสุดในต้นไม้ ต้นที่มีแค่ root เดียวตอบ 0 เพราะเดินไม่ได้เลย" },
      { t: "callout", title: "คำใบ้", c: "ที่แต่ละ node ให้จำ 2 อย่าง: ทิศที่กำลังจะไปต่อ กับ ความยาวสะสมมาถึงตอนนี้ ถ้าเดินตามทิศที่ควรไปก็ +1 ต่อความยาว ถ้าเดินอีกทางก็ถือว่าเริ่มซิกแซกเส้นใหม่ที่ความยาว 1" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def longestZigZag(root):
    ans = 0

    def dfs(node, go_left, length):
        nonlocal ans
        if node is None:
            return
        ans = max(ans, length)          # อัปเดตคำตอบทุก node ที่แวะ
        if go_left:
            dfs(node.left, False, length + 1)  # ไปซ้ายตามแผน -> ต่อความยาว, หน้าไปขวา
            dfs(node.right, True, 1)            # ไปขวาแทน = เริ่มเส้นใหม่ยาว 1
        else:
            dfs(node.right, True, length + 1)  # ไปขวาตามแผน -> ต่อความยาว, หน้าไปซ้าย
            dfs(node.left, False, 1)           # ไปซ้ายแทน = เริ่มเส้นใหม่ยาว 1

    # เริ่มจาก root ได้ทั้งเริ่มด้วยการไปซ้าย และเริ่มด้วยการไปขวา
    dfs(root, True, 0)
    dfs(root, False, 0)
    return ans` },
        { t: "p", c: "พารามิเตอร์ go_left บอกว่า \"ก้าวต่อไปที่จะทำให้ซิกแซกยังต่อเนื่องคือไปทางซ้าย\" ถ้าเราไปตามทางนั้นจริงก็ต่อความยาว (length + 1) แล้วสลับความคาดหวังเป็นไปขวา (False) ในก้าวถัดไป แต่ถ้าดันไปอีกทาง เส้นซิกแซกเก่าขาด กลายเป็นเริ่มต้นเส้นใหม่ที่ยาว 1 เราเรียก dfs จาก root สองครั้งเพราะเส้นซิกแซกที่ดีที่สุดอาจเริ่มด้วยการก้าวไปซ้ายหรือขวาก็ได้" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือสับสนว่านับ node หรือนับเส้นเชื่อม โจทย์นับเส้นเชื่อม (ก้าว) ดังนั้นเริ่มที่ length 0 ที่ root และอัปเดต ans ที่ทุก node ที่แวะ (ไม่ใช่เฉพาะตอนจบ) เพราะเส้นซิกแซกที่ดีสุดอาจจบกลางต้นไม้ก็ได้" },
        { t: "p", c: "Time O(n) แตะทุก node ครั้งเดียว · Space O(h) จากความลึก call stack ตามความสูงต้นไม้" },
      ] },

      { t: "h2", c: "ข้อ 6 — บรรพบุรุษร่วมต่ำสุด (LC236) 🟡" },
      { t: "p", c: "ให้ root และ node สองตัว p, q (รับประกันว่าอยู่ในต้นไม้ทั้งคู่) ให้หา lowest common ancestor (LCA) = node ที่เป็นบรรพบุรุษร่วมของทั้ง p และ q ที่อยู่ต่ำสุด (ลึกสุด) โดยนิยามให้ node หนึ่งเป็นบรรพบุรุษของตัวเองได้ เช่นถ้า p อยู่ใต้ q พอดี คำตอบคือ q เอง ค่าใน node ทุกตัวไม่ซ้ำกัน" },
      { t: "callout", title: "คำใบ้", c: "เดิน DFS ค้นหา p และ q ถ้า node ปัจจุบันเจอ p (หรือ q) ให้รายงานตัวเองขึ้นไป จากนั้นถ้า node ไหนได้รับรายงานว่า \"เจอของอยู่ทั้งฝั่งซ้ายและฝั่งขวา\" node นั้นแหละคือจุดที่สองคนมาบรรจบกัน = LCA" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def lowestCommonAncestor(root, p, q):
    # base case: ต้นว่าง หรือเจอ p/q พอดี ให้รายงาน node นี้ขึ้นไป
    if root is None or root is p or root is q:
        return root

    left = lowestCommonAncestor(root.left, p, q)    # ฝั่งซ้ายเจออะไรไหม
    right = lowestCommonAncestor(root.right, p, q)  # ฝั่งขวาเจออะไรไหม

    # เจอทั้งสองฝั่ง -> node นี้คือจุดบรรจบ = LCA
    if left and right:
        return root
    # เจอฝั่งเดียว -> ส่งฝั่งที่เจอต่อขึ้นไป (อีกฝั่งเป็น None)
    return left if left else right` },
        { t: "p", c: "คิดแบบ postorder: ให้ลูกสองฝั่งรายงานก่อนว่าเจอ p หรือ q ไหม base case คือถ้า node ปัจจุบันคือ p หรือ q เอง (หรือเป็น None) ก็คืน node นั้นขึ้นไปเป็นสัญญาณว่า \"เจอที่นี่\" เมื่อ node หนึ่งได้ผลว่าฝั่งซ้ายเจอหนึ่งตัวและฝั่งขวาเจออีกหนึ่งตัว แสดงว่า p กับ q แยกกันอยู่คนละฝั่งของ node นี้พอดี node นี้จึงเป็นบรรพบุรุษร่วมที่ต่ำสุด" },
        { t: "p", c: "ถ้าเจอแค่ฝั่งเดียว (อีกฝั่ง None) แปลว่าทั้ง p และ q อยู่ในฝั่งนั้นทั้งคู่ (หรือเจอแค่ตัวเดียว) เราแค่ส่งผลฝั่งที่เจอต่อขึ้นไปให้บรรพบุรุษที่สูงกว่าตัดสิน กรณีที่ p เป็นบรรพบุรุษของ q อยู่แล้ว จะได้ผลถูกเพราะเราเจอ p ก่อนแล้วคืนขึ้นไปเลย ไม่เดินลึกต่อ" },
        { t: "p", c: "Time O(n) กรณีแย่สุดแตะทุก node ครั้งเดียว · Space O(h) จากความลึก call stack ตามความสูงต้นไม้" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Binary Tree — BFS →", slug: "lc75-tree-bfs", desc: "ไล่ต้นไม้ทีละชั้นด้วย queue" },
        { title: "← Linked List", slug: "lc75-linked-list" },
      ] },
    ],
  },
};
