import type { Page } from "@/lib/types";

export const dpMultiPages: Record<string, Page> = {
  "lc75-dp-multi": {
    slug: "lc75-dp-multi",
    title: "Dynamic Programming — หลายมิติ",
    lead: "ยกระดับ DP ขึ้นเป็นตาราง 2 มิติ dp[i][j] สำหรับโจทย์สองสตริง สองลำดับ หรือกริด",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "ในหน้าก่อน DP ของเราใช้ state แค่ตัวเลขเดียว (dp[i]) แต่หลายโจทย์มีตัวแปรที่เปลี่ยนไปพร้อมกันสองอย่าง เช่น กำลังดูตัวอักษรตำแหน่งที่ i ของสตริงแรก และตำแหน่งที่ j ของสตริงที่สอง หรือกำลังยืนอยู่ที่แถว i คอลัมน์ j ของกริด กรณีแบบนี้ state เดียวไม่พอ เราต้องใช้ตาราง 2 มิติ dp[i][j] แทน แต่ไอเดียหลักยังเหมือนเดิมเป๊ะ: นิยาม state ให้ชัด หา transition แล้วไล่เติมตาราง" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "DP หลายมิติคือการขยาย state จากเส้นเดียวเป็นตาราง dp[i][j] หมายถึงคำตอบของปัญหาย่อยที่ระบุด้วยดัชนีสองตัว i และ j โจทย์ที่เข้าข่ายมักมีสองหน้าตา: (1) สองสตริง/สองลำดับ เช่นเทียบ word1 กับ word2 โดย i วิ่งบน word1 และ j วิ่งบน word2 หรือ (2) กริด เช่นเดินบนตาราง m x n โดย i คือแถว j คือคอลัมน์" },
      { t: "p", c: "วิธีคิด transition คือถามว่า ช่อง dp[i][j] คำนวณได้จากช่องข้างเคียงตัวไหน โดยทั่วไปเป็นเพื่อนบ้านสามช่อง: บน dp[i-1][j], ซ้าย dp[i][j-1], และทแยงบนซ้าย dp[i-1][j-1] เราไล่เติมตารางจากซ้ายบนไปขวาล่าง เพื่อให้ตอนคำนวณช่องหนึ่ง ช่องที่มันต้องพึ่งพาถูกเติมไว้ก่อนแล้ว" },
      { t: "code", lang: "python", c: `# template DP 2 มิติ (นิยมเผื่อแถว/คอลัมน์ที่ 0 ไว้เป็น base case)
m, n = len(a), len(b)
dp = [[0] * (n + 1) for _ in range(m + 1)]   # ตาราง (m+1) x (n+1)

# เติม base case ที่ขอบ (dp[0][*] และ dp[*][0]) ตามโจทย์
# ...

for i in range(1, m + 1):
    for j in range(1, n + 1):
        # dp[i][j] คำนวณจากเพื่อนบ้าน: บน / ซ้าย / ทแยง
        dp[i][j] = f(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])

answer = dp[m][n]   # มุมขวาล่างมักเป็นคำตอบ` },
      { t: "callout", title: "เคล็ดลับสร้างตาราง 2 มิติใน Python", warn: true, c: "ต้องใช้ [[0]*n for _ in range(m)] เท่านั้น อย่าเขียน [[0]*n]*m เด็ดขาด เพราะแบบหลังจะสร้างแถวที่เป็น object เดียวกันซ้ำ m ครั้ง พอแก้ช่องในแถวหนึ่ง แถวอื่นจะเปลี่ยนตามหมด กลายเป็นบั๊กที่หายาก" },
      { t: "callout", title: "ทำไมเผื่อขอบ index 0", c: "การทำตารางให้ใหญ่กว่าข้อมูลจริงหนึ่งแถวหนึ่งคอลัมน์ (m+1 x n+1) แล้วให้แถว/คอลัมน์แรกแทน กรณีสตริงว่าง ช่วยให้ไม่ต้องเขียนเงื่อนไขพิเศษตอน i=0 หรือ j=0 สูตร transition จึงสะอาดขึ้นมาก" },

      { t: "h2", c: "ข้อ 1 — นับทางเดินบนตาราง (LC62) 🟡" },
      { t: "p", c: "หุ่นยนต์อยู่มุมบนซ้ายของตาราง m x n ต้องเดินไปมุมล่างขวา โดยแต่ละก้าวเดินได้แค่ ลง หรือ ขวา เท่านั้น ให้นับว่ามีเส้นทางต่างกันทั้งหมดกี่แบบ เช่น m = 3, n = 7 ตอบ 28 อีกตัวอย่าง m = 3, n = 2 ตอบ 3 (จากมุมบนซ้ายไปล่างขวาของตาราง 3 แถว 2 คอลัมน์ มี 3 ทาง)" },
      { t: "callout", title: "คำใบ้", c: "นิยาม dp[i][j] = จำนวนทางเดินจากมุมเริ่มมาถึงช่อง (i, j) จะมาถึงช่องนี้ได้ต้องมาจากช่องบน (i-1, j) หรือช่องซ้าย (i, j-1) เท่านั้น จำนวนทางจึงเท่ากับผลบวกของสองช่องนั้น ส่วนแถวบนสุดและคอลัมน์ซ้ายสุดมีทางเดียวเสมอ (เดินตรงอย่างเดียว)" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def unique_paths(m, n):
    # dp[i][j] = จำนวนทางเดินมาถึงช่อง (i, j)
    dp = [[1] * n for _ in range(m)]   # แถวบนสุด/คอลัมน์ซ้ายสุด = 1 ทาง
    for i in range(1, m):
        for j in range(1, n):
            dp[i][j] = dp[i - 1][j] + dp[i][j - 1]  # จากบน + จากซ้าย
    return dp[m - 1][n - 1]

print(unique_paths(3, 7))  # 28
print(unique_paths(3, 2))  # 3` },
        { t: "p", c: "transition คือ dp[i][j] = dp[i-1][j] + dp[i][j-1] เพราะทุกเส้นทางที่มาถึงช่อง (i, j) ก้าวสุดท้ายต้องเป็นการเดินลงมาจากช่องบน หรือเดินขวามาจากช่องซ้าย จำนวนทางทั้งหมดจึงเป็นผลบวกของทั้งสองแหล่ง เราตั้งค่าเริ่มต้นทั้งตารางเป็น 1 ซึ่งจัดการ base case ให้เลย: แถวบนสุด (i=0) และคอลัมน์ซ้ายสุด (j=0) มีทางเดียวเสมอเพราะเดินได้ทิศเดียว" },
        { t: "p", c: "จุดที่ต้องระวังคือลำดับการไล่ ต้องเริ่ม i และ j จาก 1 เพื่อไม่ไปทับค่า base case ที่ขอบ และเพราะเราเติมจากบนลงล่าง ซ้ายไปขวา ตอนคำนวณ dp[i][j] ช่องบนและช่องซ้ายจึงถูกเติมเรียบร้อยแล้ว" },
        { t: "p", c: "Time O(m·n) เติมทุกช่องในตาราง · Space O(m·n) จากตาราง dp (ลดเหลือ O(n) ได้ด้วยการเก็บแค่แถวเดียว)" },
      ] },

      { t: "h2", c: "ข้อ 2 — ลำดับย่อยร่วมที่ยาวที่สุด (LC1143) 🟡" },
      { t: "p", c: "ให้สตริงสองตัว text1 กับ text2 หา subsequence (ลำดับย่อยที่ได้จากการลบตัวอักษรบางตัวออกโดยไม่สลับลำดับที่เหลือ) ที่ ปรากฏในทั้งสองสตริง และยาวที่สุด แล้วตอบความยาวของมัน เช่น text1 = \"abcde\", text2 = \"ace\" ตอบ 3 (subsequence ร่วมที่ยาวสุดคือ \"ace\") อีกตัวอย่าง text1 = \"abc\", text2 = \"def\" ตอบ 0 (ไม่มีตัวร่วมเลย)" },
      { t: "callout", title: "คำใบ้", c: "นิยาม dp[i][j] = ความยาว LCS ของ text1 ตัวแรก i ตัว กับ text2 ตัวแรก j ตัว ถ้าตัวอักษรตำแหน่งนี้ตรงกัน คำตอบ = ทแยง dp[i-1][j-1] + 1 ถ้าไม่ตรงกัน ให้ตัดตัวใดตัวหนึ่งทิ้ง เลือก max ระหว่างช่องบนกับช่องซ้าย" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def longest_common_subsequence(text1, text2):
    m, n = len(text1), len(text2)
    # dp[i][j] = LCS ของ text1[:i] กับ text2[:j] ; ขอบ = 0 (สตริงว่าง)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if text1[i - 1] == text2[j - 1]:   # ตัวอักษรตรงกัน
                dp[i][j] = dp[i - 1][j - 1] + 1
            else:                              # ไม่ตรง: ตัดตัวใดตัวหนึ่ง
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])
    return dp[m][n]

print(longest_common_subsequence("abcde", "ace"))  # 3
print(longest_common_subsequence("abc", "def"))    # 0` },
        { t: "p", c: "หัวใจของ LCS คือการเทียบตัวอักษรทีละคู่ ที่ช่อง dp[i][j] เราพิจารณาตัวอักษร text1[i-1] กับ text2[j-1] (ต้องลบ 1 เพราะ dp เผื่อขอบ index 0 ไว้แทนสตริงว่าง) ถ้าสองตัวนี้ตรงกัน เราได้ตัวร่วมเพิ่มหนึ่งตัว จึงต่อยอดจาก dp[i-1][j-1] (คำตอบก่อนรวมสองตัวนี้) แล้ว +1 ถ้าไม่ตรง แปลว่าอย่างน้อยหนึ่งในสองตัวนี้ไม่ได้อยู่ใน LCS จึงลองตัดทิ้งทีละตัวแล้วเลือกผลที่ดีกว่าระหว่าง dp[i-1][j] กับ dp[i][j-1]" },
        { t: "p", c: "ข้อควรระวังคืออย่าสับสนระหว่าง subsequence (ลบได้แต่ห้ามสลับ ไม่ต้องติดกัน) กับ substring (ต้องติดกัน) โจทย์นี้เป็นแบบแรก การเผื่อแถว/คอลัมน์ที่ 0 เป็น 0 คือ base case ที่ถูกต้อง เพราะ LCS กับสตริงว่างย่อมมีความยาว 0" },
        { t: "p", c: "Time O(m·n) เติมทุกช่อง · Space O(m·n) จากตาราง (ลดเหลือ O(n) ได้ด้วยการเก็บสองแถว)" },
      ] },

      { t: "h2", c: "ข้อ 3 — ซื้อขายหุ้นแบบมีค่าธรรมเนียม (LC714) 🟡" },
      { t: "p", c: "ให้ลิสต์ราคาหุ้นรายวัน prices และค่าธรรมเนียม fee ต่อการซื้อขายหนึ่งรอบ เราซื้อขายได้ไม่จำกัดจำนวนครั้ง แต่ต้องถือได้ทีละหุ้นเดียว (ขายก่อนถึงจะซื้อใหม่ได้) และเสียค่าธรรมเนียม fee ทุกครั้งที่ทำครบหนึ่งรอบซื้อ-ขาย ให้หากำไรสูงสุด เช่น prices = [1,3,2,8,4,9], fee = 2 ตอบ 8 (ซื้อที่ 1 ขายที่ 8 กำไร 8-1-2 = 5 แล้วซื้อที่ 4 ขายที่ 9 กำไร 9-4-2 = 3 รวม 8)" },
      { t: "callout", title: "คำใบ้", c: "state มีสองมิติ: วันที่ i และ สถานะว่าตอนนี้ถือหุ้นอยู่หรือไม่ ใช้สองตัวแปร cash (กำไรมากสุดเมื่อวันนี้ไม่ถือหุ้น) กับ hold (กำไรมากสุดเมื่อวันนี้ถือหุ้นอยู่) แล้วอัปเดตทั้งคู่ทุกวัน" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def max_profit(prices, fee):
    cash = 0             # กำไรมากสุดเมื่อ "ไม่ถือ" หุ้น (เริ่มวันแรก)
    hold = -prices[0]    # กำไรมากสุดเมื่อ "ถือ" หุ้น (ซื้อวันแรก จ่ายไปแล้ว)
    for price in prices[1:]:
        # วันนี้ไม่ถือ: อยู่เฉย ๆ หรือ ขายหุ้นที่ถืออยู่ (จ่าย fee ตอนขาย)
        cash = max(cash, hold + price - fee)
        # วันนี้ถือ: ถืออยู่แล้ว หรือ เพิ่งซื้อวันนี้ด้วยเงิน cash
        hold = max(hold, cash - price)
    return cash          # จบเกมต้องไม่ถือหุ้น กำไรจึงอยู่ที่ cash

print(max_profit([1, 3, 2, 8, 4, 9], 2))  # 8
print(max_profit([1, 3, 7, 5, 10, 3], 3)) # 6` },
        { t: "p", c: "แม้เขียนด้วยตัวแปรสองตัว แต่แท้จริงนี่คือ DP สองมิติ dp[i][ถือ/ไม่ถือ] แค่ยุบมิติวัน (i) ให้เหลือค่าปัจจุบัน เพราะแต่ละวันใช้แค่คำตอบของวันก่อนหน้า transition ของ cash คือ วันนี้ไม่ถือ ได้จากเมื่อวานก็ไม่ถือ (อยู่เฉย) หรือเมื่อวานถือแล้ววันนี้ขาย (บวก price ลบ fee) ส่วน hold คือ วันนี้ถือ ได้จากเมื่อวานก็ถือ หรือวันนี้เพิ่งซื้อ (เอา cash เมื่อวานมาลบ price)" },
        { t: "p", c: "เราหักค่าธรรมเนียม fee ตอนขายเพียงครั้งเดียวต่อรอบ (จะหักตอนซื้อก็ได้ แต่ต้องเลือกทำที่เดียวให้สม่ำเสมอ) จุดสำคัญคือคำตอบสุดท้ายต้องอ่านจาก cash ไม่ใช่ hold เพราะการจบเกมโดยยังถือหุ้นค้างไว้ย่อมไม่ใช่กำไรที่แท้จริง (ยังไม่ได้ขายเป็นเงิน)" },
        { t: "p", c: "Time O(n) ไล่ราคาครั้งเดียว · Space O(1) ใช้สองตัวแปรแทนตารางเต็ม" },
      ] },

      { t: "h2", c: "ข้อ 4 — ระยะการแก้ไขสตริง (LC72) 🟡" },
      { t: "p", c: "ให้สตริงสองตัว word1 กับ word2 หาจำนวน การแก้ไข น้อยที่สุดที่จะเปลี่ยน word1 ให้กลายเป็น word2 โดยการแก้ไขหนึ่งครั้งทำได้สามแบบ: แทรกตัวอักษร, ลบตัวอักษร, หรือแทนที่ตัวอักษร เช่น word1 = \"horse\", word2 = \"ros\" ตอบ 3 (horse→rorse แทน h เป็น r, →rose ลบ r, →ros ลบ e) อีกตัวอย่าง word1 = \"intention\", word2 = \"execution\" ตอบ 5" },
      { t: "callout", title: "คำใบ้", c: "นิยาม dp[i][j] = จำนวนการแก้น้อยสุดเพื่อเปลี่ยน word1 ตัวแรก i ตัว ให้เป็น word2 ตัวแรก j ตัว ถ้าตัวอักษรตรงกันไม่ต้องแก้ (ลอกทแยง) ถ้าไม่ตรงเลือก 1 + min ของสามทาง: ลบ (บน), แทรก (ซ้าย), แทนที่ (ทแยง) อย่าลืมตั้งขอบ: เปลี่ยนสตริงยาว i ให้เป็นสตริงว่างต้องลบ i ครั้ง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def min_distance(word1, word2):
    m, n = len(word1), len(word2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    # base case: เทียบกับสตริงว่าง
    for i in range(m + 1):
        dp[i][0] = i    # ลบ i ตัวให้กลายเป็นสตริงว่าง
    for j in range(n + 1):
        dp[0][j] = j    # แทรก j ตัวจากสตริงว่าง
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if word1[i - 1] == word2[j - 1]:   # ตรงกัน ไม่ต้องแก้
                dp[i][j] = dp[i - 1][j - 1]
            else:
                dp[i][j] = 1 + min(
                    dp[i - 1][j],       # ลบตัวจาก word1
                    dp[i][j - 1],       # แทรกตัวเข้า word1
                    dp[i - 1][j - 1],   # แทนที่ตัว
                )
    return dp[m][n]

print(min_distance("horse", "ros"))            # 3
print(min_distance("intention", "execution"))  # 5` },
        { t: "p", c: "Edit Distance คือ DP สองมิติต้นแบบเลย ที่ช่อง dp[i][j] ถ้าตัวอักษรท้ายสุดของทั้งสองส่วนตรงกัน (word1[i-1] == word2[j-1]) เราไม่ต้องเสียการแก้ที่ตำแหน่งนี้ จึงลอกค่าทแยง dp[i-1][j-1] มาตรง ๆ ถ้าไม่ตรง เราต้องแก้หนึ่งครั้ง แล้วเลือกทางที่ถูกที่สุดจากสามแบบ: ลบตัวท้ายของ word1 (มาจาก dp[i-1][j]), แทรกตัวให้ตรง (มาจาก dp[i][j-1]), หรือแทนที่ตัวท้าย (มาจาก dp[i-1][j-1]) แล้ว +1 สำหรับการแก้ครั้งนั้น" },
        { t: "p", c: "base case สำคัญมากและคนมักลืม: dp[i][0] = i หมายถึงเปลี่ยนสตริงยาว i ให้เป็นสตริงว่างต้องลบ i ครั้ง และ dp[0][j] = j หมายถึงสร้างสตริงยาว j จากสตริงว่างต้องแทรก j ครั้ง ถ้าไม่เติมขอบเหล่านี้ คำตอบจะผิดทันที การจับคู่ทิศทางกับความหมาย (บน=ลบ, ซ้าย=แทรก, ทแยง=แทนที่) ช่วยให้ไม่งงเวลาเขียนสูตร" },
        { t: "p", c: "Time O(m·n) เติมทุกช่องในตาราง · Space O(m·n) จากตาราง dp (ลดเหลือ O(n) ได้ด้วยการเก็บสองแถว)" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Bit Manipulation →", slug: "lc75-bit", desc: "เล่นกับบิตเพื่อแก้โจทย์ให้เร็วและประหยัด" },
        { title: "← Dynamic Programming — 1 มิติ", slug: "lc75-dp-1d" },
      ] },
    ],
  },
};
