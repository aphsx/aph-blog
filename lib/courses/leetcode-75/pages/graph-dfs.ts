import type { Page } from "@/lib/types";

export const graphDfsPages: Record<string, Page> = {
  "lc75-graph-dfs": {
    slug: "lc75-graph-dfs",
    title: "Graphs — DFS (ลุยลึก)",
    lead: "รู้จักกราฟตั้งแต่ศูนย์ แล้วสำรวจมันด้วย DFS ลุยลึกไปทางหนึ่งจนสุดก่อนค่อยถอยกลับ",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "หัวข้อนี้เราจะรู้จักกราฟ (graph) ซึ่งเป็นโครงสร้างข้อมูลที่ยืดหยุ่นที่สุดอันหนึ่ง ถ้าคุณยังไม่เคยเรียนกราฟมาก่อนไม่ต้องกังวล เราจะเริ่มจากศูนย์ ค่อย ๆ ปูตั้งแต่กราฟคืออะไร เก็บมันในโค้ดยังไง แล้วจึงเรียนเทคนิคสำรวจกราฟตัวแรกคือ DFS (Depth-First Search) หรือการลุยลึก" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "h3", c: "กราฟคืออะไร" },
      { t: "p", c: "กราฟคือชุดของ จุด (เรียกว่า node หรือ vertex) ที่มี เส้นเชื่อม (เรียกว่า edge) โยงระหว่างกัน ลองนึกถึงแผนที่เมือง เมืองแต่ละเมืองคือ node ถนนที่เชื่อมสองเมืองคือ edge หรือนึกถึงเพื่อนใน social network คนคือ node ความเป็นเพื่อนคือ edge อะไรก็ตามที่เป็นความสัมพันธ์ ระหว่างของหลาย ๆ ชิ้น เอามาเป็นกราฟได้หมด" },
      { t: "p", c: "ต้นไม้ (tree) ที่เราเรียนไปก่อนหน้านี้จริง ๆ ก็เป็นกราฟชนิดพิเศษ (กราฟที่ไม่มีวงและเชื่อมกันหมด) กราฟทั่วไปอิสระกว่านั้น มันมีวง (cycle) ได้ มีหลายกลุ่มที่ไม่เชื่อมกันได้ และ node หนึ่งจะมีเส้นเชื่อมกี่เส้นก็ได้" },
      { t: "h3", c: "มีทิศ vs ไม่มีทิศ (directed vs undirected)" },
      { t: "p", c: "กราฟแบบไม่มีทิศ (undirected) เส้นเชื่อมเดินได้สองทาง เช่นถ้า A เป็นเพื่อนกับ B แล้ว B ก็เป็นเพื่อนกับ A ด้วย ส่วนกราฟแบบมีทิศ (directed) เส้นเชื่อมเดินได้ทางเดียวตามหัวลูกศร เช่นถนนวันเวย์ ไปจาก A ถึง B ได้ แต่ย้อนกลับไม่ได้ หรือการติดตามบน social ที่ A ติดตาม B ไม่ได้แปลว่า B ติดตาม A การรู้ว่าโจทย์เป็นกราฟแบบไหนสำคัญมาก เพราะมันเปลี่ยนวิธีเก็บและวิธีเดิน" },
      { t: "h3", c: "เก็บกราฟยังไง — adjacency list" },
      { t: "p", c: "วิธีเก็บกราฟที่นิยมและใช้ง่ายสุดคือ adjacency list (ลิสต์เพื่อนบ้าน) ไอเดียคือใช้ dict ที่ key เป็น node และ value เป็นลิสต์ของ node ที่มันเชื่อมถึงโดยตรง พูดง่าย ๆ คือ สำหรับแต่ละ node เราจดไว้ว่ามันเดินไปหาใครได้บ้าง" },
      { t: "code", lang: "python", c: `# กราฟไม่มีทิศ วาดเป็นภาพ
#     0 --- 1
#     |     |
#     2 --- 3
#           |
#           4

from collections import defaultdict

edges = [(0, 1), (0, 2), (1, 3), (2, 3), (3, 4)]

graph = defaultdict(list)
for a, b in edges:
    graph[a].append(b)
    graph[b].append(a)   # ไม่มีทิศ ต้องใส่ทั้งสองทาง

# graph[0] = [1, 2]  -> node 0 เชื่อมกับ 1 และ 2
# graph[3] = [1, 2, 4]

# ถ้าเป็นกราฟมีทิศ (a -> b เท่านั้น) ใส่ทางเดียว
# graph[a].append(b)   # ไม่ต้องใส่ graph[b].append(a)` },
      { t: "h3", c: "DFS — ลุยลึก" },
      { t: "p", c: "DFS (Depth-First Search) คือวิธีสำรวจกราฟแบบ ลุยลึกไปทางหนึ่งให้สุดก่อน แล้วค่อยถอยกลับมาลองทางอื่น เหมือนเดินในเขาวงกตแล้วยึดกำแพงขวาไว้ตลอด เดินลึกเข้าไปเรื่อย ๆ จนตัน ค่อยถอยกลับ วิธีที่เขียนง่ายที่สุดคือใช้ recursion" },
      { t: "p", c: "สิ่งที่ขาดไม่ได้เลยในการเดินกราฟคือ set visited ที่จำว่าเราเคยไป node ไหนมาแล้วบ้าง เพราะกราฟมีวงได้ ถ้าไม่จำ เราจะเดินวนกลับมาที่เดิมไม่รู้จบ (infinite loop) กฎคือ ก่อนจะเดินเข้า node ไหน เช็คก่อนว่าเคยไปหรือยัง ถ้ายังค่อยไป และทันทีที่ไปถึงให้ทำเครื่องหมายว่าเยี่ยมแล้ว" },
      { t: "code", lang: "python", c: `# template DFS ด้วย recursion จำโครงนี้ไว้ใช้ได้ทุกข้อ
def dfs(node, graph, visited):
    visited.add(node)              # ทำเครื่องหมายว่ามาถึงแล้ว
    # ... ทำอะไรกับ node ตรงนี้ เช่น นับ, เก็บค่า ...
    for nxt in graph[node]:        # ลองเพื่อนบ้านทีละตัว
        if nxt not in visited:     # ถ้ายังไม่เคยไป
            dfs(nxt, graph, visited)   # ลุยลึกต่อ

visited = set()
dfs(0, graph, visited)             # เริ่มจาก node 0` },
      { t: "callout", title: "หัวใจของ DFS", c: "จำแค่สามอย่าง หนึ่ง ทำเครื่องหมาย visited ทันทีที่มาถึง node สอง วนดูเพื่อนบ้านทุกตัว สาม ตัวไหนยังไม่ visited ค่อยเรียก dfs ซ้ำเข้าไปลึก ๆ ถ้าลืม visited จะวนไม่จบเมื่อกราฟมีวง" },

      { t: "h2", c: "ข้อ 1 — เข้าห้องได้ครบทุกห้องไหม (LC841) 🟡" },
      { t: "p", c: "มี n ห้องเลข 0 ถึง n-1 ตอนแรกทุกห้องล็อกหมด ยกเว้นห้อง 0 ที่เปิดอยู่ ในแต่ละห้องมีพวงกุญแจ rooms[i] เป็นลิสต์ของเลขห้องที่กุญแจในห้อง i เปิดได้ เริ่มจากห้อง 0 เราเดินเก็บกุญแจไปเปิดห้องอื่นต่อได้เรื่อย ๆ ให้ตอบว่าเราจะเข้าถึงทุกห้องได้ครบหรือไม่ เช่น rooms = [[1],[2],[3],[]] ตอบ True (0 ให้กุญแจ 1, 1 ให้ 2, 2 ให้ 3 ครบทุกห้อง) แต่ rooms = [[1,3],[3,0,1],[2],[0]] ตอบ False เพราะไม่มีกุญแจห้อง 2 เลย" },
      { t: "callout", title: "คำใบ้", c: "มองห้องเป็น node กุญแจในห้องเป็น edge ที่ชี้ไปห้องอื่น ทำ DFS เริ่มจากห้อง 0 แล้วนับว่าเยี่ยมได้กี่ห้อง ถ้าเท่ากับจำนวนห้องทั้งหมดแปลว่าครบ" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def can_visit_all_rooms(rooms):
    visited = set()

    def dfs(room):
        visited.add(room)              # เข้าห้องนี้แล้ว
        for key in rooms[room]:        # กุญแจแต่ละดอกในห้องนี้
            if key not in visited:     # ถ้ายังไม่เคยเข้าห้องนั้น
                dfs(key)               # เข้าไปเลย

    dfs(0)                             # เริ่มจากห้อง 0
    return len(visited) == len(rooms)  # เข้าครบทุกห้องไหม

print(can_visit_all_rooms([[1], [2], [3], []]))          # True
print(can_visit_all_rooms([[1, 3], [3, 0, 1], [2], [0]]))  # False` },
        { t: "p", c: "โจทย์นี้จริง ๆ คือการถามว่ากราฟ เชื่อมถึงกันหมดจากจุดเริ่มไหม (reachability) เรามองห้องเป็น node และกุญแจในห้องเป็น edge แบบมีทิศที่ชี้ไปยังห้องที่มันเปิดได้ rooms เองก็เป็น adjacency list อยู่แล้ว ไม่ต้องแปลงอะไร ทำ DFS จากห้อง 0 เก็บทุกห้องที่ไปถึงลง visited แล้วสุดท้ายเทียบจำนวนห้องที่เยี่ยมกับจำนวนห้องทั้งหมด" },
        { t: "p", c: "set visited ทำสองหน้าที่พร้อมกัน ทั้งกันเดินวนซ้ำ (บางห้องมีกุญแจชี้กลับไปห้องเดิม) และใช้นับจำนวนห้องที่เข้าได้ จุดพลาดที่พบบ่อยคือลืมเช็ค if key not in visited ทำให้ recursion วนไม่จบเมื่อกุญแจชี้วนกัน · Time O(V + E) เยี่ยมทุก node หนึ่งครั้งและไล่ทุก edge หนึ่งครั้ง V คือจำนวนห้อง E คือจำนวนกุญแจทั้งหมด · Space O(V) จาก visited และความลึกของ call stack" },
      ] },

      { t: "h2", c: "ข้อ 2 — นับจำนวนแคว้น (LC547) 🟡" },
      { t: "p", c: "มี n เมือง ให้ตารางสี่เหลี่ยม isConnected ขนาด n x n โดย isConnected[i][j] = 1 แปลว่าเมือง i กับ j เชื่อมกันโดยตรง ถ้าเป็น 0 คือไม่เชื่อม เมืองที่เชื่อมถึงกัน (ไม่ว่าโดยตรงหรือผ่านเมืองอื่น) นับเป็นแคว้น (province) เดียวกัน ให้นับว่ามีทั้งหมดกี่แคว้น เช่น isConnected = [[1,1,0],[1,1,0],[0,0,1]] ตอบ 2 (เมือง 0 กับ 1 เป็นแคว้นเดียว เมือง 2 อยู่คนเดียวอีกแคว้น)" },
      { t: "callout", title: "คำใบ้", c: "นี่คือการนับ connected components ในกราฟ วน node ทุกตัว ถ้าเจอ node ที่ยังไม่ visited แปลว่าเจอแคว้นใหม่ นับเพิ่มหนึ่ง แล้ว DFS ไปเยี่ยมทุก node ในแคว้นนั้นให้หมดกันนับซ้ำ" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def find_circle_num(is_connected):
    n = len(is_connected)
    visited = set()

    def dfs(city):
        visited.add(city)
        for other in range(n):
            # เชื่อมกันโดยตรง และยังไม่เคยไป
            if is_connected[city][other] == 1 and other not in visited:
                dfs(other)

    provinces = 0
    for city in range(n):
        if city not in visited:        # เจอเมืองที่ยังไม่อยู่แคว้นไหน
            provinces += 1             # นับเป็นแคว้นใหม่
            dfs(city)                  # กวาดทุกเมืองในแคว้นนี้
    return provinces

print(find_circle_num([[1, 1, 0], [1, 1, 0], [0, 0, 1]]))  # 2
print(find_circle_num([[1, 0, 0], [0, 1, 0], [0, 0, 1]]))  # 3` },
        { t: "p", c: "connected components คือ กลุ่มก้อน ของ node ที่เชื่อมถึงกันได้ในกราฟไม่มีทิศ โจทย์นี้ให้กราฟมาในรูปตาราง (adjacency matrix) แทน list โดย is_connected[i][j] บอกว่าเมือง i กับ j เชื่อมกันไหม เราจึงหาเพื่อนบ้านของเมืองด้วยการไล่ทั้งแถวดูว่าช่องไหนเป็น 1" },
        { t: "p", c: "เทคนิคนับกลุ่มคือ วน node ทุกตัวจากนอก ถ้าเจอตัวที่ยังไม่ visited แสดงว่าเราเพิ่งสะดุดเข้าแคว้นใหม่ที่ยังไม่เคยแตะ บวกตัวนับหนึ่ง แล้วยิง DFS เข้าไปกวาดทุกเมืองในแคว้นนั้นให้ visited จนหมด พอ loop นอกเดินต่อ เมืองที่อยู่แคว้นเดิมจะถูก visited แล้วจึงไม่ถูกนับซ้ำ จุดพลาดที่พบบ่อยคือนับ province ทุกครั้งที่เข้า dfs ซึ่งจะเกิน ต้องนับเฉพาะตอนเริ่มแคว้นใหม่ใน loop นอกเท่านั้น · Time O(n^2) เพราะต้องไล่ตาราง n x n · Space O(n) จาก visited และ call stack" },
      ] },

      { t: "h2", c: "ข้อ 3 — กลับทิศถนนให้ไปเมือง 0 ได้ (LC1466) 🟡" },
      { t: "p", c: "มี n เมืองเลข 0 ถึง n-1 เชื่อมกันด้วยถนน n-1 เส้นเป็นรูปต้นไม้ (ทุกเมืองถึงกันได้ถ้าไม่สนทิศ) แต่ถนนเป็นแบบมีทิศ connections[i] = [a,b] แปลว่ามีถนนจาก a ไป b ให้หาจำนวนถนนน้อยที่สุดที่ต้องกลับทิศ เพื่อให้ทุกเมืองสามารถเดินทางไปถึงเมือง 0 ได้ เช่น n = 6, connections = [[0,1],[1,3],[2,3],[4,0],[4,5]] ตอบ 3 (ต้องกลับทิศถนน 0->1, 1->3, 4->5 เพื่อให้ทุกเมืองมุ่งหน้าสู่เมือง 0)" },
      { t: "callout", title: "คำใบ้", c: "สร้างกราฟไม่มีทิศเพื่อเดินให้ทั่ว แต่จำไว้ด้วยว่า edge ไหนคือทิศ ต้นฉบับ (ชี้ออกจากเมือง 0) เดิน DFS จากเมือง 0 ออกไป ทุกครั้งที่เดินไปตามถนนที่ทิศเดิมชี้ ออก จากเมือง 0 แปลว่าต้องกลับทิศ นับเพิ่มหนึ่ง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import defaultdict

def min_reorder(n, connections):
    graph = defaultdict(list)
    for a, b in connections:
        graph[a].append((b, 1))   # ทิศจริง a->b : ถ้าเดินทางนี้จาก 0 ออกไป ต้องกลับ (cost 1)
        graph[b].append((a, 0))   # ทิศปลอมเพิ่มเข้ามาเพื่อเดินได้ทั่ว (cost 0)

    visited = set()
    changes = 0

    def dfs(city):
        nonlocal changes
        visited.add(city)
        for nxt, cost in graph[city]:
            if nxt not in visited:
                changes += cost    # ถ้า edge นี้เป็นทิศจริง (ชี้ออกจาก 0) ต้องกลับ
                dfs(nxt)

    dfs(0)                         # เริ่มเดินจากเมือง 0 ออกไปทุกทิศ
    return changes

print(min_reorder(6, [[0, 1], [1, 3], [2, 3], [4, 0], [4, 5]]))  # 3
print(min_reorder(5, [[1, 0], [1, 2], [3, 2], [3, 4]]))          # 2` },
        { t: "p", c: "กุญแจของข้อนี้คือ มองกลับด้าน เราอยากให้ทุกเมืองไปถึงเมือง 0 แต่คิดแบบนั้นตรง ๆ ยาก จึงกลับมุมเป็น ถ้าเราออกเดินจากเมือง 0 ไปหาทุกเมือง ถนนเส้นไหนที่เราเดินสวนทิศของมัน เส้นนั้นแหละที่ต้องกลับทิศ เพราะในสภาพจริงเมืองปลายทางจะเดินย้อนกลับมาหาเมือง 0 ไม่ได้" },
        { t: "p", c: "ทริกในการเก็บกราฟคือ ใส่ทั้งสอง edge แบบไม่มีทิศเพื่อให้ DFS เดินได้ทั่วต้นไม้ แต่ติดป้าย cost ไว้ด้วย edge ทิศจริง (a->b ตามที่โจทย์ให้) ติด cost 1 ส่วน edge ปลอมที่เราเติมเพื่อเดินย้อน (b->a) ติด cost 0 พอ DFS ออกจากเมือง 0 ถ้าเราวิ่งไปตาม edge cost 1 แปลว่ากำลังเดินตามทิศที่ชี้ออกจากเมือง 0 ซึ่งจริง ๆ เมืองนั้นควรชี้เข้าหา 0 จึงต้องกลับทิศ บวก changes" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมใส่ edge ย้อนกลับ ทำให้ DFS เดินไปไม่ทั่วเพราะกราฟมีทิศ อีกจุดคือสับสนว่าจะนับ cost ของ edge ไหน จำง่าย ๆ ว่านับเฉพาะ edge ที่ชี้ ออกจาก ต้นทาง (เมือง 0) · Time O(n) เยี่ยมทุกเมืองและ edge หนึ่งครั้ง (edge มี n-1 เส้น) · Space O(n) จากกราฟ visited และ call stack" },
      ] },

      { t: "h2", c: "ข้อ 4 — คำนวณอัตราส่วนจากสมการ (LC399) 🟡" },
      { t: "p", c: "ให้สมการอัตราส่วนมาชุดหนึ่ง เช่น a/b = 2.0 และ b/c = 3.0 แล้วถามคำถามว่าอัตราส่วนของตัวแปรคู่อื่น ๆ เป็นเท่าไร เช่นถาม a/c ต้องตอบ 6.0 (เพราะ a/c = a/b x b/c = 2 x 3) ถ้าตัวแปรไหนไม่รู้จัก หรือเชื่อมโยงกันไม่ได้ ให้ตอบ -1.0 เช่นจากข้อมูลข้างต้น ถาม a/e ตอบ -1.0 (ไม่มี e) ถาม x/x ตอบ -1.0 (ไม่รู้จัก x) แต่ถาม a/a ตอบ 1.0 (a รู้จักและหารตัวเองได้ 1)" },
      { t: "callout", title: "คำใบ้", c: "มองแต่ละตัวแปรเป็น node และ a/b = 2.0 เป็น edge สองทาง จาก a ไป b น้ำหนัก 2.0 และจาก b ไป a น้ำหนัก 1/2.0 การหา x/y คือการเดิน DFS จาก x ไป y แล้วคูณน้ำหนัก edge ตลอดทาง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import defaultdict

def calc_equation(equations, values, queries):
    graph = defaultdict(dict)
    for (a, b), val in zip(equations, values):
        graph[a][b] = val          # a/b = val
        graph[b][a] = 1 / val      # b/a = 1/val

    def dfs(src, dst, visited):
        if src not in graph or dst not in graph:
            return -1.0            # มีตัวแปรที่ไม่รู้จัก
        if src == dst:
            return 1.0             # x/x = 1 (ต้องรู้จัก x ด้วย)
        visited.add(src)
        for nbr, weight in graph[src].items():
            if nbr not in visited:
                result = dfs(nbr, dst, visited)
                if result != -1.0:        # เจอทางถึง dst
                    return weight * result  # คูณน้ำหนักสะสม
        return -1.0                # ลองทุกทางแล้วไปไม่ถึง

    answers = []
    for a, b in queries:
        answers.append(dfs(a, b, set()))
    return answers

eq = [["a", "b"], ["b", "c"]]
vals = [2.0, 3.0]
q = [["a", "c"], ["b", "a"], ["a", "e"], ["a", "a"], ["x", "x"]]
print(calc_equation(eq, vals, q))  # [6.0, 0.5, -1.0, 1.0, -1.0]` },
        { t: "p", c: "ข้อนี้สอนกราฟถ่วงน้ำหนัก (weighted graph) คือ edge มีตัวเลขกำกับ ไอเดียคือแต่ละตัวแปรเป็น node สมการ a/b = 2.0 บอกว่าจาก a เดินไป b คูณ 2.0 และเพราะ b/a = 1/(a/b) เราจึงเพิ่ม edge ย้อนกลับจาก b ไป a คูณ 1/2.0 ด้วย ทำให้เดินได้สองทาง การหาคำตอบ x/y คือเดินจาก x ไป y แล้วคูณน้ำหนัก edge ที่ผ่านทั้งหมดสะสมกันไป" },
        { t: "p", c: "DFS ที่นี่ต่างจากข้อก่อน ๆ ตรงที่มันต้อง คืนค่า (ผลคูณสะสม) กลับขึ้นมา ไม่ใช่แค่ทำเครื่องหมาย เมื่อเดินไปเจอ dst เราคืน 1.0 แล้วระหว่างถอย recursion กลับ แต่ละชั้นคูณน้ำหนัก edge ของตัวเองเข้าไป ผลลัพธ์ที่โผล่กลับมาถึงจุดเริ่มจึงเป็นผลคูณตลอดเส้นทางพอดี ถ้าลองทุกเพื่อนบ้านแล้วไม่มีทางไหนถึง dst ก็คืน -1.0" },
        { t: "p", c: "edge case ที่ต้องระวังคือ ตัวแปรที่ไม่มีในสมการเลย (เช็ค src not in graph หรือ dst not in graph คืน -1.0) และกรณี a/a ที่ต้องคืน 1.0 เฉพาะเมื่อ a รู้จัก (ถ้า x ไม่รู้จักแม้ถาม x/x ก็ต้องเป็น -1.0 โค้ดจึงเช็คว่ารู้จักก่อนแล้วค่อยเช็ค src == dst) · Time O(Q x (V + E)) แต่ละ query ทำ DFS หนึ่งครั้ง Q คือจำนวนคำถาม · Space O(V + E) จากกราฟและ visited" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Graphs — BFS →", slug: "lc75-graph-bfs", desc: "สำรวจกราฟเป็นชั้น ๆ หาระยะสั้นสุด" },
        { title: "← Binary Search Tree", slug: "lc75-bst" },
      ] },
    ],
  },
};
