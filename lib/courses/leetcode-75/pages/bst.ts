import type { Page } from "@/lib/types";

export const bstPages: Record<string, Page> = {
  "lc75-intro-bst": {
    slug: "lc75-intro-bst",
    title: "Binary Search Tree — พื้นฐาน & แนวคิด",
    lead: "ต้นไม้ที่จัดค่าไว้เป็นระเบียบ ซ้ายเล็กกว่า ขวาใหญ่กว่า จึงค้นหา แทรก ลบ ได้เร็ว O(h)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "หมวดนี้ว่าด้วย Binary Search Tree (BST) หรือต้นไม้ค้นหาแบบทวิภาค เป็นโครงสร้างข้อมูลที่ออกแบบมาเพื่อ ค้นหา แทรก และลบ ค่าได้เร็ว เจอบ่อยเวลาต้องเก็บข้อมูลที่ต้องค้นหาซ้ำ ๆ และอยากได้ความเร็วดีกว่าลิสต์ธรรมดา ถ้าคุณรู้จักการค้นหาแบบ binary search ในลิสต์ที่เรียงแล้วมาก่อน BST ก็คือไอเดียเดียวกันแต่ทำบนต้นไม้" },

      { t: "h2", c: "BST คืออะไร" },
      { t: "p", c: "BST เป็นต้นไม้ที่แต่ละ node มีลูกได้มากสุด 2 ตัว (ซ้ายกับขวา) แต่จุดเด่นที่ทำให้มันพิเศษกว่าต้นไม้ทั่วไปคือ มันจัดเรียงค่าไว้เป็นระเบียบตามกฎ ทำให้เราค้นหาค่าได้เร็วเหมือนตอนเปิดพจนานุกรม คือเปิดกลาง ๆ แล้วตัดสินใจว่าจะไปซ้ายหรือขวา ไม่ต้องไล่ดูทุก node" },
      { t: "p", c: "BST มีกฎเดียวที่ต้องจำ สำหรับทุก node ค่าใน subtree ทางซ้ายทั้งหมดต้อง น้อยกว่า ค่าของ node นั้น และค่าใน subtree ทางขวาทั้งหมดต้อง มากกว่า ค่าของ node นั้น พูดสั้น ๆ คือ left < node < right และกฎนี้เป็นจริงทุก node ไม่ใช่แค่รากต้นเดียว" },
      { t: "code", lang: "python", c: `# BST หน้าตาแบบนี้ (root = 5)
#         5
#        / \\
#       3   8
#      / \\   \\
#     2   4   9
#
# ทางซ้ายของ 5 (คือ 3,2,4) น้อยกว่า 5 ทั้งหมด
# ทางขวาของ 5 (คือ 8,9) มากกว่า 5 ทั้งหมด
# และกฎนี้จริงกับทุก node เช่น 3 มีซ้าย 2 (< 3) ขวา 4 (> 3)` },
      { t: "p", c: "เพราะมีกฎนี้ เวลาจะหาค่าสักตัว เราเริ่มที่ราก แล้วเทียบ ถ้าค่าที่หาน้อยกว่า node ปัจจุบัน แปลว่ามันต้องอยู่ทางซ้าย (ทางขวาใหญ่กว่าหมดอยู่แล้ว ไม่ต้องดู) ถ้ามากกว่าก็ไปทางขวา ทำแบบนี้ไปเรื่อย ๆ แต่ละก้าวเราตัดครึ่งที่ต้องดูทิ้งไปเลย จึงใช้เวลาเท่ากับความสูงของต้นไม้ ไม่ใช่จำนวน node ทั้งหมด" },

      { t: "h2", c: "นิยาม node และ Big-O" },
      { t: "code", lang: "python", c: `# นิยาม node ของต้นไม้ (LeetCode ให้มาแบบนี้)
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right` },
      { t: "table", head: ["operation", "เฉลี่ย (สมดุล)", "แย่สุด (เอียง)"], rows: [
        ["ค้นหา (search)", "O(log n)", "O(n)"],
        ["แทรก (insert)", "O(log n)", "O(n)"],
        ["ลบ (delete)", "O(log n)", "O(n)"],
      ] },
      { t: "code", lang: "python", c: `# template ค้นหาค่า target ใน BST แบบวนลูป — จำโครงนี้ไว้ใช้ได้
def search(root, target):
    node = root
    while node:
        if target == node.val:
            return node          # เจอแล้ว
        elif target < node.val:
            node = node.left     # ค่าเล็กกว่า ไปซ้าย
        else:
            node = node.right    # ค่าใหญ่กว่า ไปขวา
    return None                  # หาไม่เจอ` },
      { t: "callout", title: "ทำไม O(h) ไม่ใช่ O(n)", c: "h คือความสูงของต้นไม้ (จำนวนชั้น) แต่ละก้าวเราลงลึกไปหนึ่งชั้นเสมอ ไม่เคยย้อนกลับ ถ้าต้นไม้สมดุลดี h ประมาณ log n การค้นหาจึงเร็วมาก แต่ถ้าต้นไม้เอียงเป็นเส้นตรง (เช่นแทรกค่าเรียงจากน้อยไปมาก) h จะกลายเป็น n และช้าลงเท่ากับลิสต์ธรรมดา" },
      { t: "callout", c: "หมวดนี้มี 2 ข้อ ค้นหาใน BST และลบ node ใน BST พร้อมแล้วกดถัดไปเริ่มข้อแรกได้เลย" },
    ],
  },

  "lc75-p41": {
    slug: "lc75-p41",
    title: "ข้อ 41 · LC700 — ค้นหาใน BST 🟢",
    lead: "ใช้กฎ left < node < right เดินลงต้นไม้ ตัดครึ่งที่ไม่เกี่ยวทิ้งทุกก้าว หาเจอใน O(h)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์ ให้รากของ BST มา และค่า val หนึ่งค่า ให้หา node ที่มีค่าเท่ากับ val แล้วคืน subtree ที่มี node นั้นเป็นราก (คือคืนตัว node นั้นทั้งก้อน รวมลูกหลานของมัน) ถ้าไม่มีให้คืน None" },
      { t: "ul", c: [
        "root = [4,2,7,1,3], val = 2 → คืน subtree ที่รากคือ node 2 (มีลูก 1 กับ 3)",
        "root = [4,2,7,1,3], val = 5 → คืน None เพราะไม่มีค่า 5 ในต้นไม้",
      ] },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "โครงสร้างที่ใช้คือ BST เอง และเทคนิคคือเดินลงต้นไม้โดยอาศัยกฎ left < node < right เลือกใช้วิธีนี้เพราะโจทย์การันตีว่าค่าถูกจัดเรียงไว้แล้ว เราจึงไม่ต้องเสียแรงดูทุก node" },
      { t: "p", c: "วิธีช้าแบบ brute force คือไล่ดูทุก node ในต้นไม้ (traversal ธรรมดา) จนกว่าจะเจอค่า ซึ่งเป็น O(n) แต่มันไม่ใช้ประโยชน์จากการที่ค่าเรียงอยู่แล้วเลย ทั้งที่ทุกครั้งที่เทียบค่าเราตัดครึ่งของต้นไม้ที่เป็นไปไม่ได้ทิ้งได้ทันที นั่นนำไปสู่วิธีเดินลงข้างเดียวที่เร็วกว่ามาก" },
      { t: "ol", c: [
        "ตั้งตัวชี้ node ไว้ที่ราก",
        "ถ้า node ยังไม่หลุดขอบ (ไม่ใช่ None) ให้เทียบค่า",
        "ถ้า val เท่ากับค่า node ปัจจุบัน เจอแล้ว คืน node นั้นทั้งก้อน",
        "ถ้า val น้อยกว่า ให้ขยับ node ไปลูกซ้าย (ค่าเล็กกว่าอยู่ฝั่งซ้ายเสมอ)",
        "ถ้า val มากกว่า ให้ขยับ node ไปลูกขวา",
        "ถ้าเดินจนหลุดขอบต้นไม้ (node เป็น None) แปลว่าไม่มี คืน None",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ลืมคืน None ตอนเดินจนตกขอบ (node กลายเป็น None) ทำให้โปรแกรม error โครงลูป while node จัดการให้แล้วเพราะมันหยุดเองเมื่อ node เป็น None" },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `# class TreeNode:
#     def __init__(self, val=0, left=None, right=None): ...

def search_bst(root, val):
    node = root                  # เริ่มที่ราก
    while node:                  # ยังไม่หลุดขอบต้นไม้
        if val == node.val:
            return node          # เจอ node ที่ต้องการ คืนทั้ง subtree
        elif val < node.val:
            node = node.left     # ค่าที่หาเล็กกว่า ต้องอยู่ทางซ้าย
        else:
            node = node.right    # ค่าที่หาใหญ่กว่า ต้องอยู่ทางขวา
    return None                  # เดินจนตกขอบต้นไม้ แปลว่าไม่มี` },
        { t: "p", c: "ข้อนี้คือการนำกฎ BST มาใช้ตรง ๆ แทนที่จะไล่ดูทุก node แบบต้นไม้ทั่วไป (ซึ่งจะเป็น O(n)) เราใช้ความจริงที่ว่าค่าถูกจัดเรียงไว้แล้ว ทุกครั้งที่เทียบ เราตัดทิ้งครึ่งหนึ่งของต้นไม้ที่เป็นไปไม่ได้ทันที เช่นถ้า val น้อยกว่า node ปัจจุบัน เราไม่มีทางเจอมันในฝั่งขวา (ฝั่งขวาใหญ่กว่าหมด) จึงไปซ้ายอย่างเดียว" },
        { t: "p", c: "จะเขียนแบบ recursion ก็ได้ ผลเหมือนกัน แต่แบบวนลูปประหยัด memory กว่าเพราะไม่ต้องใช้ call stack ถ้าเปลี่ยนเป็น recursion โค้ดจะสั้นลงแต่กิน memory เพิ่มตามความสูง" },
        { t: "p", c: "Time O(h) เดินลงจากรากถึงใบมากสุดเท่าความสูง h · Space O(1) แบบวนลูปใช้ตัวแปรเดียว (ถ้าเขียน recursion จะเป็น O(h) จาก call stack)" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เมื่อโจทย์ให้ BST มา ให้คิดถึงการเดินลงต้นไม้โดยเทียบค่าแล้วเลือกทิศ วิธีนี้ตัดปัญหาลงครึ่งทุกก้าว เอาไปใช้กับการค้นหา แทรก และลบ ใน BST ได้ทั้งหมด" },
    ],
  },

  "lc75-p42": {
    slug: "lc75-p42",
    title: "ข้อ 42 · LC450 — ลบโหนดใน BST 🟡",
    lead: "ลบ node แล้วยังคงกฎ BST แยกจัดการ 3 กรณี ไม่มีลูก มีลูกตัวเดียว และมีลูกสองตัว",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์ ให้รากของ BST และค่า key ให้ลบ node ที่มีค่าเท่ากับ key ออกจากต้นไม้ โดยยังต้องคงกฎ BST ไว้ (left < node < right) แล้วคืนรากของต้นไม้หลังลบ ถ้าไม่มี key ในต้นไม้ก็คืนต้นไม้เดิม" },
      { t: "ul", c: [
        "root = [5,3,6,2,4,null,7], key = 3 → หลังลบต้องได้ต้นไม้ที่ยังเป็น BST ถูกต้อง เช่นเอา 4 ขึ้นมาแทนที่ 3",
        "root = [5,3,6,2,4,null,7], key = 0 → ไม่มีค่า 0 คืนต้นไม้เดิมทั้งต้น",
      ] },
      { t: "callout", c: "การลบมี 3 กรณีที่ต้องคิดให้ครบ ไม่มีลูกเลย มีลูกตัวเดียว และมีลูกสองตัว แต่ละกรณีจัดการต่างกัน" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "ใช้ BST + recursion เดินหา node ที่จะลบ แล้วต่อ subtree กลับด้วยการเขียน root.left = delete(root.left, key) เทคนิคนี้ทำให้การเชื่อม node ใหม่หลังลบเกิดขึ้นเองอัตโนมัติ ไม่ต้องเก็บ pointer ของ node พ่อไว้เอง" },
      { t: "p", c: "หัวใจอยู่ที่กรณี node มีลูกสองตัว ถ้าลบตรง ๆ จะเหลือลูกกำพร้าสองก้อนซ่อมต้นไม้ไม่ได้ เราจึงไม่ลบ node นั้นจริง แต่หา successor คือค่าที่น้อยที่สุดในฝั่งขวา (เดินขวาหนึ่งก้าวแล้วเดินซ้ายจนสุด) เอาค่ามันมาแทน แล้วไปลบ successor ตัวเดิมออกจากฝั่งขวาแทน ซึ่งจะกลายเป็นกรณีง่ายเพราะ successor ไม่มีลูกซ้ายแน่นอน" },
      { t: "ol", c: [
        "ถ้าต้นไม้ว่าง (root เป็น None) ไม่มีอะไรให้ลบ คืน None",
        "ถ้า key น้อยกว่าค่าราก เข้าไปลบในฝั่งซ้าย แล้วรับผลกลับมาต่อกับ root.left",
        "ถ้า key มากกว่าค่าราก เข้าไปลบในฝั่งขวา แล้วต่อกับ root.right",
        "ถ้า key เท่ากับค่าราก คือเจอ node ที่จะลบ แยก 3 กรณี",
        "ไม่มีลูกซ้าย คืนลูกขวาขึ้นไปแทน (ถ้าไม่มีลูกเลย ลูกขวาก็เป็น None พอดี เท่ากับลบทิ้ง)",
        "ไม่มีลูกขวา คืนลูกซ้ายขึ้นไปแทน",
        "มีลูกสองตัว หา successor ในฝั่งขวา เอาค่ามาแทนค่าราก แล้วลบ successor ออกจากฝั่งขวาต่อ",
        "คืน root กลับขึ้นไปให้ชั้นบนต่อ node",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ลืมอัปเดตการเชื่อม node กลับ (ลืม root.left = ... หรือ root.right = ...) ทำให้ต้นไม้ไม่เปลี่ยน อีกจุดคือกรณีสองลูกแล้วลืมลบ successor ตัวเดิมออก ทำให้เกิดค่าซ้ำในต้นไม้" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "ลบ key = 3 จาก root = [5,3,6,2,4,null,7] (node 3 มีลูกสองตัวคือ 2 กับ 4)" },
      { t: "table", head: ["ขั้น", "node ปัจจุบัน", "ทำอะไร"], rows: [
        ["1", "5", "3 < 5 ลงไปลบในฝั่งซ้าย"],
        ["2", "3", "เจอ key มีลูกสองตัว หา successor ในฝั่งขวา"],
        ["3", "4", "เดินขวาจาก 3 ได้ 4 ไม่มีลูกซ้าย → successor = 4"],
        ["4", "3", "เอาค่า 4 มาแทน (node กลายเป็น 4) แล้วลบ 4 เดิมออกจากฝั่งขวา"],
        ["5", "4 (ตัวเดิม)", "ไม่มีลูก คืน None เท่ากับลบทิ้ง เชื่อมกลับเสร็จ"],
      ] },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `# class TreeNode:
#     def __init__(self, val=0, left=None, right=None): ...

def delete_node(root, key):
    if not root:
        return None                      # ต้นไม้ว่าง ไม่มีอะไรให้ลบ

    if key < root.val:
        root.left = delete_node(root.left, key)   # key อยู่ฝั่งซ้าย
    elif key > root.val:
        root.right = delete_node(root.right, key) # key อยู่ฝั่งขวา
    else:
        # เจอ node ที่จะลบแล้ว แยก 3 กรณี
        if not root.left:
            return root.right            # ไม่มีลูกซ้าย ยกลูกขวาขึ้นมาแทน
        if not root.right:
            return root.left             # ไม่มีลูกขวา ยกลูกซ้ายขึ้นมาแทน

        # กรณีมีลูกสองตัว หาตัวที่น้อยสุดในฝั่งขวา (successor)
        succ = root.right
        while succ.left:
            succ = succ.left
        root.val = succ.val              # เอาค่า successor มาแทนค่าปัจจุบัน
        # แล้วลบ successor ออกจาก subtree ขวา
        root.right = delete_node(root.right, succ.val)

    return root` },
        { t: "p", c: "ขั้นแรกเราต้องเดินไปหา node ที่จะลบก่อน โดยใช้กฎ BST เหมือนการค้นหา ถ้า key น้อยกว่ารากปัจจุบันก็เข้าไปลบในฝั่งซ้าย (แล้วรับผลลัพธ์กลับมาต่อกับ root.left) ถ้ามากกว่าก็ทำกับฝั่งขวา เทคนิคการเขียน root.left = delete_node(root.left, key) ช่วยให้การเชื่อม node ใหม่หลังลบเกิดขึ้นเองอัตโนมัติ ไม่ต้องเก็บ pointer ตัวพ่อไว้เอง" },
        { t: "p", c: "พอเจอ node ที่จะลบ (key == root.val) แยกเป็น 3 กรณี กรณีไม่มีลูก หรือมีลูกตัวเดียว จัดการง่าย แค่คืนลูกอีกฝั่งขึ้นไปแทนตำแหน่งของมัน (ถ้าไม่มีลูกเลย ฝั่งที่คืนก็เป็น None พอดี เท่ากับลบทิ้ง) กรณียากคือมีลูกสองตัว เราลบตรง ๆ ไม่ได้เพราะจะเหลือลูกกำพร้าสองก้อน วิธีแก้คือหา successor คือค่าที่น้อยที่สุดในฝั่งขวา (เดินขวาหนึ่งก้าวแล้วเดินซ้ายจนสุด) ค่านี้มากกว่าทุกตัวในฝั่งซ้าย และน้อยกว่าทุกตัวที่เหลือในฝั่งขวา จึงเอามาแทนที่แล้วต้นไม้ยังเป็น BST อยู่ จากนั้นลบ successor ตัวเดิมออก (ซึ่งจะเข้ากรณีง่ายเพราะ successor ไม่มีลูกซ้ายแน่นอน)" },
        { t: "p", c: "Time O(h) เดินหา node แล้วเดินหา successor รวมแล้วไม่เกินความสูง · Space O(h) จาก call stack ของ recursion เท่าความสูง" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "การลบใน BST = เดินหา node ด้วยกฎ BST แล้วต่อ subtree กลับด้วย root.child = recurse(...) เทคนิคแทนที่ด้วย successor (ค่าน้อยสุดฝั่งขวา) เป็นลูกเล่นที่ใช้ได้ทุกครั้งที่ต้องลบ node ที่มีลูกสองตัว" },
    ],
  },
};
