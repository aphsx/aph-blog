import type { Page } from "@/lib/types";

export const twoPointersPages: Record<string, Page> = {
  "lc75-two-pointers": {
    slug: "lc75-two-pointers",
    title: "Two Pointers — ตัวชี้สองตัว",
    lead: "เทคนิคใช้ตัวชี้สองตัววิ่งไล่กันบนลิสต์หรือสตริง ลดเวลาจาก O(n^2) เหลือ O(n)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "Two Pointers (ตัวชี้สองตัว) คือเทคนิคพื้นฐานที่เจอบ่อยสุด ๆ ในโจทย์เกี่ยวกับลิสต์และสตริง ไอเดียคือแทนที่จะวน loop ซ้อนกันเพื่อเทียบทุกคู่ (ซึ่งช้า O(n^2)) เราใช้ตัวชี้สองตัวเดินไปพร้อมกันในรอบเดียว ทำให้เหลือ O(n) ได้บ่อยครั้ง" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "ปกติถ้าเราอยากเทียบทุกคู่ในลิสต์ ต้องใช้ loop ซ้อนสองชั้น O(n^2) ซึ่งช้าเมื่อข้อมูลใหญ่ แต่ในหลายโจทย์เราไม่จำเป็นต้องดูทุกคู่จริง ๆ เราแค่ต้องเดินตัวชี้สองตัวอย่างชาญฉลาด แล้วตัดสินใจว่าจะขยับตัวไหน จึงเหลือแค่รอบเดียว O(n)" },
      { t: "p", c: "Two Pointers มีสองแบบหลักที่ต้องแยกให้ออก" },
      { t: "ul", c: [
        "แบบซ้าย/ขวาวิ่งเข้าหากัน (opposite ends): ตัวหนึ่งเริ่มหัวลิสต์ อีกตัวเริ่มท้ายลิสต์ แล้วขยับเข้าหากันจนมาชนกลาง มักใช้กับลิสต์ที่เรียงแล้ว หรือโจทย์จับคู่หัวกับท้าย เช่น หาคู่ผลรวมเป้าหมาย หรือกักน้ำ",
        "แบบช้า/เร็ว (slow/fast): ตัวชี้สองตัวเริ่มจากด้านเดียวกัน แต่วิ่งด้วยจังหวะต่างกัน ตัวเร็ววิ่งสำรวจไปข้างหน้า ตัวช้าคอยชี้ตำแหน่งที่จะเขียนผลลัพธ์ มักใช้ตอนต้องจัดเรียง/กรองของในลิสต์เดิม เช่น ย้ายของ ลบซ้ำ",
      ] },
      { t: "p", c: "เทมเพลตแบบซ้าย/ขวาวิ่งเข้าหากัน จำโครงนี้ไว้แล้วปรับเงื่อนไขตามโจทย์" },
      { t: "code", lang: "python", c: `left, right = 0, len(nums) - 1
while left < right:
    # ตัดสินใจจากค่าที่ตัวชี้สองตัวชี้อยู่
    if condition:
        left += 1       # ต้องการค่าฝั่งซ้ายมากขึ้น
    else:
        right -= 1      # ต้องการค่าฝั่งขวาน้อยลง` },
      { t: "p", c: "เทมเพลตแบบช้า/เร็ว ตัว fast ไล่ดูทุกตัว ส่วน slow ขยับเฉพาะตอนเจอของที่อยากเก็บ" },
      { t: "code", lang: "python", c: `slow = 0
for fast in range(len(nums)):
    if keep(nums[fast]):          # ตัวนี้ควรเก็บไว้
        nums[slow] = nums[fast]   # เขียนไปที่ตำแหน่ง slow
        slow += 1` },
      { t: "callout", title: "ใช้ตอนไหน", c: "เห็นคำว่า \"ลิสต์เรียงแล้ว\", \"หาคู่ผลรวม\", \"แก้ในที่เดิม (in-place)\", \"ย้าย/ลบของในลิสต์\", หรือ \"เทียบสองสตริงว่าตัวหนึ่งเป็นส่วนหนึ่งของอีกตัว\" ให้นึกถึง Two Pointers ไว้ก่อนเลย" },

      { t: "h2", c: "ข้อ 1 — ย้ายเลขศูนย์ไปท้าย (LC283) 🟢" },
      { t: "p", c: "มีลิสต์ตัวเลข ให้ย้ายเลข 0 ทุกตัวไปอยู่ท้ายลิสต์ โดยคงลำดับเดิมของตัวที่ไม่ใช่ศูนย์ไว้ และต้องแก้ในที่เดิม (in-place) ห้ามสร้างลิสต์ใหม่ เช่น [0, 1, 0, 3, 12] → [1, 3, 12, 0, 0]" },
      { t: "callout", title: "คำใบ้", c: "ใช้ตัวชี้ช้า/เร็ว ตัวช้าชี้ตำแหน่งที่จะวางเลขไม่ใช่ศูนย์ตัวถัดไป ตัวเร็วไล่หาเลขที่ไม่ใช่ศูนย์" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def move_zeroes(nums):
    insert = 0                     # ตำแหน่งที่จะวางตัวไม่ใช่ศูนย์ตัวถัดไป (ตัวช้า)
    for i in range(len(nums)):     # i คือตัวเร็ว ไล่ดูทุกตำแหน่ง
        if nums[i] != 0:
            # สลับตัวไม่ใช่ศูนย์มาไว้ข้างหน้า ดัน 0 ไปทางท้ายเอง
            nums[insert], nums[i] = nums[i], nums[insert]
            insert += 1
    return nums

print(move_zeroes([0, 1, 0, 3, 12]))  # [1, 3, 12, 0, 0]` },
        { t: "p", c: "ตัวชี้ insert คอยชี้ตำแหน่งว่างถัดไปสำหรับเลขที่ไม่ใช่ศูนย์ พอตัวเร็ว i เจอเลขไม่ใช่ศูนย์ ก็สลับมันมาไว้ที่ตำแหน่ง insert แล้วเลื่อน insert ไปหนึ่งช่อง ทำแบบนี้ไปเรื่อย ๆ เลขไม่ใช่ศูนย์จะเรียงชิดซ้ายตามลำดับเดิม ส่วนเลข 0 จะถูกดันไปกองท้ายเองโดยอัตโนมัติ" },
        { t: "p", c: "จุดที่คนพลาดบ่อยคือพยายาม \"ลบ 0 แล้วเติมท้าย\" ซึ่งทำให้ลำดับเพี้ยนหรือช้า วิธีสลับนี้ปลอดภัยกว่า และเมื่อ i == insert (ยังไม่เจอ 0 เลย) การสลับก็แค่สลับตัวมันเองไม่มีผลเสีย" },
        { t: "p", c: "Time O(n) ไล่ลิสต์รอบเดียว · Space O(1) แก้ในที่เดิมไม่สร้างลิสต์ใหม่" },
      ] },

      { t: "h2", c: "ข้อ 2 — เป็นซับซีเควนซ์ไหม (LC392) 🟢" },
      { t: "p", c: "ให้สตริง s และ t ตอบว่า s เป็น subsequence ของ t หรือไม่ subsequence คือการเลือกตัวอักษรบางตัวจาก t ออกมาโดยไม่สลับลำดับ (จะข้ามตัวไหนก็ได้แต่ห้ามย้อน) เช่น s = \"abc\", t = \"ahbgdc\" → True (เลือก a, b, c ได้ตามลำดับ) แต่ s = \"axc\", t = \"ahbgdc\" → False เพราะหา x ไม่เจอ" },
      { t: "callout", title: "คำใบ้", c: "ใช้ตัวชี้สองตัวเริ่มด้านซ้ายทั้งคู่ ตัวหนึ่งไล่ไปบน t เสมอ อีกตัวขยับบน s เฉพาะตอนตัวอักษรตรงกัน ถ้าขยับครบทุกตัวของ s แสดงว่าเจอครบ" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def is_subsequence(s, t):
    i = 0                     # ตัวชี้บน s
    for c in t:               # ไล่ทุกตัวอักษรของ t
        if i < len(s) and s[i] == c:
            i += 1            # ตัวอักษรของ s ตัวนี้เจอแล้ว ขยับไปตัวถัดไป
    return i == len(s)        # ขยับครบทุกตัวของ s = เจอครบตามลำดับ

print(is_subsequence("abc", "ahbgdc"))  # True
print(is_subsequence("axc", "ahbgdc"))  # False` },
        { t: "p", c: "หัวใจคือเราไล่ไปบน t ทีละตัวไม่มีวันย้อน และมีตัวชี้ i คอยบอกว่าตอนนี้กำลังตามหาตัวอักษรตัวที่เท่าไรของ s อยู่ ทุกครั้งที่ตัวอักษรใน t ตรงกับ s[i] เราถือว่าจับคู่ได้แล้วจึงเลื่อน i ไปข้างหน้า ถ้าไม่ตรงก็แค่ปล่อยผ่านตัวนั้นของ t (คือการ \"ข้าม\" นั่นเอง)" },
        { t: "p", c: "เงื่อนไข i < len(s) ต้องเช็คก่อน s[i] เสมอ เพื่อกันการอ้าง index เกินขอบเมื่อ i ขยับครบแล้ว จุดนี้คือ edge case ที่พลาดง่าย ถ้า s ว่าง (\"\") ผลลัพธ์จะเป็น True ทันทีเพราะ i == len(s) ตั้งแต่แรก ซึ่งถูกต้อง (สตริงว่างเป็น subsequence ของทุกอย่าง)" },
        { t: "p", c: "Time O(n) โดย n คือความยาวของ t (เราไล่ t รอบเดียว) · Space O(1) ใช้แค่ตัวชี้ตัวเดียว" },
      ] },

      { t: "h2", c: "ข้อ 3 — ภาชนะที่กักน้ำได้มากสุด (LC11) 🟡" },
      { t: "p", c: "มีลิสต์ความสูง height โดย height[i] คือความสูงของเส้นแนวตั้งที่ตำแหน่ง i ให้เลือกเส้นสองเส้นมาเป็นผนังซ้ายขวาเพื่อกักน้ำ ปริมาณน้ำที่กักได้ = ความกว้าง (ระยะห่างของสองเส้น) คูณ ความสูงที่ต่ำกว่าของสองเส้น (น้ำล้นออกทางเส้นที่เตี้ยกว่า) หาค่ามากสุดที่กักได้ เช่น [1, 8, 6, 2, 5, 4, 8, 3, 7] → 49 (เลือกเส้นสูง 8 ที่ index 1 กับเส้นสูง 7 ที่ index 8 ได้ min(8,7) x (8-1) = 7 x 7 = 49)" },
      { t: "callout", title: "คำใบ้", c: "เริ่มตัวชี้ที่หัวและท้ายลิสต์ให้กว้างสุดก่อน แล้วขยับ \"เส้นที่เตี้ยกว่า\" เข้ามา เพราะการขยับเส้นสูงเข้ามามีแต่จะทำให้แคบลงและก็ยังถูกจำกัดด้วยเส้นเตี้ยอยู่ดี" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def max_area(height):
    left, right = 0, len(height) - 1   # เริ่มกว้างสุด
    best = 0
    while left < right:
        h = min(height[left], height[right])   # น้ำสูงได้เท่าเส้นเตี้ยกว่า
        best = max(best, h * (right - left))    # ความสูง x ความกว้าง
        # ขยับเส้นที่เตี้ยกว่าเข้ามา หวังเจอเส้นที่สูงกว่า
        if height[left] < height[right]:
            left += 1
        else:
            right -= 1
    return best

print(max_area([1, 8, 6, 2, 5, 4, 8, 3, 7]))  # 49` },
        { t: "p", c: "ทำไมต้องขยับเส้นเตี้ย มาคิดกัน ปริมาณน้ำถูกจำกัดด้วยเส้นที่เตี้ยกว่าเสมอ ถ้าเราขยับเส้นที่สูงกว่าเข้ามา ความกว้างลดลงแน่ ๆ ส่วนความสูงก็ยังถูกกดด้วยเส้นเตี้ยตัวเดิมอยู่ดี ผลลัพธ์มีแต่จะแย่ลงหรือเท่าเดิม ตรงกันข้าม การขยับเส้นเตี้ยเข้ามาเป็นโอกาสเดียวที่จะได้เส้นสูงกว่ามาแทน ทำให้น้ำอาจสูงขึ้นและชดเชยความกว้างที่หายไปได้" },
        { t: "p", c: "นี่คือเหตุผลว่าทำไม Two Pointers ถึงถูกต้องโดยไม่ต้องลองทุกคู่ เราค่อย ๆ ตัดคู่ที่เป็นไปไม่ได้ทิ้งอย่างมั่นใจในแต่ละก้าว จุดพลาดที่พบบ่อยคือเผลอไปคิดว่า \"เลือกสองเส้นสูงสุด\" ซึ่งผิด เพราะสองเส้นสูงอาจอยู่ชิดกันจนความกว้างน้อยเกินไป" },
        { t: "p", c: "Time O(n) ตัวชี้สองตัวเดินเข้าหากันรวมกันแค่ n ก้าว · Space O(1) ใช้ตัวแปรไม่กี่ตัว" },
      ] },

      { t: "h2", c: "ข้อ 4 — จำนวนคู่ผลรวมเท่ากับ k มากสุด (LC1679) 🟡" },
      { t: "p", c: "มีลิสต์ nums และเลข k แต่ละครั้งเราหยิบเลขสองตัวที่รวมกันได้ k ออกจากลิสต์ นับเป็น 1 การจับคู่ ให้หาว่าจับคู่ได้มากสุดกี่ครั้ง (เลขแต่ละตัวใช้ได้ครั้งเดียว) เช่น nums = [3, 1, 3, 4, 3], k = 6 → 1 (จับคู่ 3 กับ 3 ได้หนึ่งคู่ ที่เหลือรวมกันไม่ได้ 6 แล้ว) หรือ nums = [1, 2, 3, 4], k = 5 → 2 (จับคู่ 1+4 และ 2+3)" },
      { t: "callout", title: "คำใบ้", c: "เรียงลิสต์ก่อน แล้วใช้ตัวชี้ซ้าย/ขวาวิ่งเข้าหากัน ถ้าผลรวมมากไปให้ลดฝั่งขวา ถ้าน้อยไปให้เพิ่มฝั่งซ้าย ถ้าพอดี k ก็นับคู่แล้วหุบทั้งสองฝั่ง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def max_operations(nums, k):
    nums.sort()                        # เรียงก่อน เพื่อใช้ตัวชี้ซ้าย/ขวาได้
    left, right = 0, len(nums) - 1
    count = 0
    while left < right:
        s = nums[left] + nums[right]
        if s == k:
            count += 1                 # เจอคู่พอดี นับแล้วหุบทั้งสองฝั่ง
            left += 1
            right -= 1
        elif s < k:
            left += 1                  # ผลรวมน้อยไป ต้องการค่ามากขึ้น
        else:
            right -= 1                 # ผลรวมมากไป ต้องการค่าน้อยลง
    return count

print(max_operations([3, 1, 3, 4, 3], 6))  # 1
print(max_operations([1, 2, 3, 4], 5))      # 2` },
        { t: "p", c: "หลังเรียงลิสต์แล้ว ค่าน้อยสุดอยู่ซ้าย ค่ามากสุดอยู่ขวา การรวมสองปลายนี้ทำให้เราคุมผลรวมได้ตรง ๆ ถ้าน้อยไปแปลว่าต้องดันฝั่งซ้ายให้ค่ามากขึ้น (left += 1) ถ้ามากไปก็หุบฝั่งขวาให้ค่าน้อยลง (right -= 1) พอเจอคู่ที่รวมได้ k พอดี เราเก็บคู่นั้น (นับ count) แล้วหุบเข้ามาทั้งสองฝั่งเพราะเลขสองตัวนั้นถูกใช้ไปแล้ว" },
        { t: "p", c: "อีกวิธีที่นิยมคือใช้ hash map นับความถี่ของแต่ละเลข แล้วมองหาคู่เติมเต็ม k - x วิธีนั้นได้ O(n) ไม่ต้องเรียง แต่ในหน้านี้เราโชว์แบบ Two Pointers เพื่อฝึก pattern เรียง-แล้ว-วิ่งเข้าหากัน จุดพลาดที่พบบ่อยคือลืมว่าเลขใช้ซ้ำไม่ได้ ต้องขยับตัวชี้ออกจากทั้งสองตัวที่จับคู่ไปแล้ว" },
        { t: "p", c: "Time O(n log n) จากการเรียง (ส่วนการวิ่งตัวชี้เป็น O(n)) · Space O(1) ถึง O(n) ขึ้นกับว่า sort ใช้หน่วยความจำเพิ่มแค่ไหน (Python ใช้ประมาณ O(n))" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Sliding Window →", slug: "lc75-sliding-window", desc: "หน้าต่างเลื่อนหาช่วงต่อเนื่องที่ดีที่สุด" },
        { title: "← Array / String", slug: "lc75-array-string" },
      ] },
    ],
  },
};
