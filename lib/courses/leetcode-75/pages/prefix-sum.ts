import type { Page } from "@/lib/types";

export const prefixSumPages: Record<string, Page> = {
  "lc75-prefix-sum": {
    slug: "lc75-prefix-sum",
    title: "Prefix Sum — ผลรวมสะสม",
    lead: "เก็บผลรวมสะสมไว้ล่วงหน้า เพื่อตอบผลรวมของช่วงใด ๆ ได้ในเวลา O(1)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "Prefix Sum (ผลรวมสะสม) เป็นเทคนิคง่าย ๆ แต่ทรงพลังมาก ไอเดียคือ ถ้าเราต้องถูกถามผลรวมของช่วงต่าง ๆ ในลิสต์บ่อย ๆ แทนที่จะบวกใหม่ทุกครั้ง เรา เตรียมผลรวมสะสมไว้ล่วงหน้า แล้วตอบคำถามได้ทันทีในเวลาคงที่ นอกจากนี้ตัว prefix sum เองก็มีประโยชน์ในโจทย์ที่ต้องรู้ค่า สะสม ณ แต่ละจุด เช่น ระดับความสูงที่ไต่ขึ้นไปได้" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "สมมติเรามีลิสต์ nums แล้วอยากรู้ผลรวมของช่วง nums[i..j] (รวมทั้งสองปลาย) ถ้าถามครั้งเดียวก็บวกตรง ๆ ได้ แต่ถ้าถูกถามหลาย ๆ ช่วง การบวกใหม่ทุกครั้งจะเสียเวลา O(n) ต่อคำถาม รวมแล้วช้า" },
      { t: "p", c: "ทริกคือสร้างลิสต์ prefix ที่เก็บผลรวมสะสมตั้งแต่ต้นจนถึงแต่ละตำแหน่ง โดยนิยมทำให้ prefix ยาวกว่า nums หนึ่งช่อง (มีช่อง 0 นำหน้า) เพื่อให้สูตรสวย: prefix[k] = ผลรวมของ nums[0..k-1]" },
      { t: "code", lang: "python", c: `nums = [3, 1, 4, 1, 5]

# สร้างผลรวมสะสม โดยให้ prefix[0] = 0 นำหน้า
prefix = [0] * (len(nums) + 1)
for i in range(len(nums)):
    prefix[i + 1] = prefix[i] + nums[i]

# prefix = [0, 3, 4, 8, 9, 14]

# ผลรวมของช่วง nums[i..j] (รวมสองปลาย) = prefix[j + 1] - prefix[i]
# เช่น ผลรวมของ nums[1..3] = 1 + 4 + 1 = 6
i, j = 1, 3
print(prefix[j + 1] - prefix[i])  # 6` },
      { t: "p", c: "ทำไมมันเร็วขึ้น? เพราะเราจ่ายเวลา O(n) แค่ครั้งเดียวตอนสร้าง prefix จากนั้นทุกคำถามช่วงตอบได้ในเวลา O(1) ด้วยการลบกันแค่ครั้งเดียว ผลรวมช่วง = ผลรวมสะสมถึงปลายขวา ลบ ผลรวมสะสมก่อนถึงปลายซ้าย" },
      { t: "callout", title: "จำสูตรนี้ไว้", c: "sum(nums[i..j]) = prefix[j+1] - prefix[i] เมื่อ prefix มีช่อง 0 นำหน้า การมีช่อง 0 นำหน้าช่วยไม่ให้ต้องเขียนเงื่อนไขพิเศษตอน i = 0" },

      { t: "h2", c: "ข้อ 1 — หาความสูงสูงสุดที่ไปถึง (LC1732) 🟢" },
      { t: "p", c: "นักปั่นจักรยานเริ่มที่ระดับความสูง 0 มีลิสต์ gain โดย gain[i] คือ ค่าต่างของระดับความสูง ระหว่างจุดที่ i กับ i+1 ให้หาว่าจุดสูงสุดที่นักปั่นไปถึงมีความสูงเท่าไร เช่น gain = [-5,1,5,0,-7] ระดับความสูงตามจุดคือ 0, -5, -4, 1, 1, -6 จุดสูงสุดคือ 1 อีกตัวอย่าง gain = [-4,-3,-2,-1,4,3,2] ได้ระดับ 0,-4,-7,-9,-10,-6,-3,-1 จุดสูงสุดคือ 0" },
      { t: "callout", title: "คำใบ้", c: "ระดับความสูงแต่ละจุดคือผลรวมสะสมของ gain ตั้งแต่ต้น ค่อย ๆ บวกสะสมไปแล้วเก็บค่ามากสุดที่เคยเจอ (อย่าลืมว่าเริ่มที่ 0)" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def largest_altitude(gain):
    altitude = 0        # ระดับความสูงปัจจุบัน (เริ่มที่จุด 0)
    highest = 0         # ค่าสูงสุดที่เคยไปถึง (จุดเริ่มก็ 0)
    for g in gain:
        altitude += g   # สะสมค่าต่างระดับ = ผลรวมสะสม
        highest = max(highest, altitude)
    return highest

print(largest_altitude([-5, 1, 5, 0, -7]))       # 1
print(largest_altitude([-4, -3, -2, -1, 4, 3, 2]))  # 0` },
        { t: "p", c: "โจทย์นี้คือ prefix sum ในรูปแบบพื้นฐานที่สุด ระดับความสูงของจุดที่ k ก็คือผลรวมของ gain[0..k-1] เราไม่จำเป็นต้องเก็บลิสต์ prefix ทั้งหมด เพราะต้องการแค่ค่าสูงสุด จึงบวกสะสมในตัวแปรเดียว altitude แล้วอัปเดต highest ไปเรื่อย ๆ" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมนับจุดเริ่มต้นที่ความสูง 0 ทำให้ตอบผิดเวลาค่าความสูงติดลบทั้งหมด การตั้ง highest = 0 ตั้งแต่แรกช่วยครอบคลุมกรณีนี้ได้" },
        { t: "p", c: "Time O(n) ไล่ลิสต์รอบเดียว · Space O(1) ใช้ตัวแปรสะสมแค่สองตัว ไม่สร้างลิสต์ใหม่" },
      ] },

      { t: "h2", c: "ข้อ 2 — หา index จุดสมดุล (LC724) 🟢" },
      { t: "p", c: "มีลิสต์ตัวเลข nums ให้หา index ที่เป็น จุดสมดุล (pivot) นั่นคือ index ที่ผลรวมของเลขทางซ้ายทั้งหมด เท่ากับ ผลรวมของเลขทางขวาทั้งหมด (ตัวที่ index นั้นไม่นับทั้งสองฝั่ง) ให้คืน index ที่เจอตัวแรก (ซ้ายสุด) ถ้าไม่มีให้คืน -1 เช่น nums = [1,7,3,6,5,6] ตอบ 3 เพราะซ้าย 1+7+3 = 11 เท่ากับขวา 5+6 = 11 ส่วน nums = [1,2,3] ตอบ -1 เพราะไม่มี index ไหนสมดุล" },
      { t: "callout", title: "คำใบ้", c: "ถ้ารู้ผลรวมทั้งหมด total และผลรวมทางซ้าย left_sum แล้ว ผลรวมทางขวาที่ index i ก็คือ total - left_sum - nums[i] ไม่ต้องบวกซ้ำทุกครั้ง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def pivot_index(nums):
    total = sum(nums)   # ผลรวมทั้งหมด
    left_sum = 0        # ผลรวมของตัวที่อยู่ทางซ้ายของ i
    for i, n in enumerate(nums):
        # ผลรวมขวา = total - left_sum - ตัวปัจจุบัน
        right_sum = total - left_sum - n
        if left_sum == right_sum:
            return i
        left_sum += n   # เลื่อน i ไปขวา ตัวปัจจุบันจึงกลายเป็นฝั่งซ้าย
    return -1

print(pivot_index([1, 7, 3, 6, 5, 6]))  # 3
print(pivot_index([1, 2, 3]))           # -1
print(pivot_index([2, 1, -1]))          # 0` },
        { t: "p", c: "หัวใจคือเราไม่อยากคำนวณผลรวมซ้าย-ขวาใหม่ทุก index (จะกลายเป็น O(n^2)) จึงใช้ไอเดีย prefix sum เก็บ left_sum ที่สะสมมาจากซ้าย ส่วนผลรวมขวาคำนวณได้จาก total ที่รู้ล่วงหน้า ลบด้วยฝั่งซ้ายและตัวปัจจุบันออก" },
        { t: "p", c: "ลำดับสำคัญมาก ต้องเช็คสมดุลก่อน แล้วค่อยบวก n เข้า left_sum เพราะตอนอยู่ที่ index i ตัว nums[i] ยังไม่นับเป็นฝั่งซ้าย edge case ที่ต้องระวังคือ pivot อยู่ที่หัวหรือท้ายลิสต์ เช่น [2,1,-1] ตอบ 0 เพราะฝั่งซ้ายว่าง (ผลรวม 0) เท่ากับฝั่งขวา 1 + (-1) = 0 โค้ดนี้รองรับเพราะ left_sum เริ่มที่ 0" },
        { t: "p", c: "Time O(n) หา total หนึ่งรอบ แล้วไล่อีกหนึ่งรอบ · Space O(1) ใช้ตัวแปรสะสมเท่านั้น" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Hash Map / Set →", slug: "lc75-hashmap", desc: "นับความถี่และจับคู่ด้วย dict กับ set" },
        { title: "← Sliding Window", slug: "lc75-sliding-window" },
      ] },
    ],
  },
};
