import type { Page } from "@/lib/types";

export const prefixSumPages: Record<string, Page> = {
  "lc75-intro-prefix-sum": {
    slug: "lc75-intro-prefix-sum",
    title: "Prefix Sum — พื้นฐาน & แนวคิด",
    lead: "เก็บผลรวมสะสมไว้ล่วงหน้า เพื่อตอบผลรวมของช่วงใด ๆ ได้ในเวลา O(1)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "Prefix Sum (ผลรวมสะสม) เป็นเทคนิคง่าย ๆ แต่ทรงพลังมาก ไอเดียคือ ถ้าเราถูกถามผลรวมของช่วงต่าง ๆ ในลิสต์บ่อย ๆ แทนที่จะบวกใหม่ทุกครั้ง เรา เตรียมผลรวมสะสมไว้ล่วงหน้า แล้วตอบคำถามได้ทันทีในเวลาคงที่ นอกจากนี้ตัว prefix sum เองก็มีประโยชน์ในโจทย์ที่ต้องรู้ค่า สะสม ณ แต่ละจุด เช่น ระดับความสูงที่ไต่ขึ้นไปได้ หรือหาจุดสมดุลของลิสต์" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "สมมติเรามีลิสต์ nums แล้วอยากรู้ผลรวมของช่วง nums[i..j] (รวมทั้งสองปลาย) ถ้าถามครั้งเดียวก็บวกตรง ๆ ได้ แต่ถ้าถูกถามหลาย ๆ ช่วง การบวกใหม่ทุกครั้งจะเสียเวลา O(n) ต่อคำถาม รวมแล้วช้า" },
      { t: "p", c: "ทริกคือสร้างลิสต์ prefix ที่เก็บผลรวมสะสมตั้งแต่ต้นจนถึงแต่ละตำแหน่ง โดยนิยมทำให้ prefix ยาวกว่า nums หนึ่งช่อง (มีช่อง 0 นำหน้า) เพื่อให้สูตรสวย: prefix[k] = ผลรวมของ nums[0..k-1]" },
      { t: "p", c: "ทำไมมันเร็วขึ้น? เพราะเราจ่ายเวลา O(n) แค่ครั้งเดียวตอนสร้าง prefix จากนั้นทุกคำถามช่วงตอบได้ในเวลา O(1) ด้วยการลบกันแค่ครั้งเดียว ผลรวมช่วง = ผลรวมสะสมถึงปลายขวา ลบ ผลรวมสะสมก่อนถึงปลายซ้าย" },
      { t: "code", lang: "python", c: `nums = [3, 1, 4, 1, 5]

# สร้างผลรวมสะสม โดยให้ prefix[0] = 0 นำหน้า
prefix = [0] * (len(nums) + 1)
for i in range(len(nums)):
    prefix[i + 1] = prefix[i] + nums[i]

# prefix = [0, 3, 4, 8, 9, 14]

# ผลรวมของช่วง nums[i..j] (รวมสองปลาย) = prefix[j + 1] - prefix[i]
# เช่น ผลรวมของ nums[1..3] = 1 + 4 + 1 = 6
i, j = 1, 3
print(prefix[j + 1] - prefix[i])  # 6` },
      { t: "callout", title: "จำสูตรนี้ไว้", c: "sum(nums[i..j]) = prefix[j+1] - prefix[i] เมื่อ prefix มีช่อง 0 นำหน้า การมีช่อง 0 นำหน้าช่วยไม่ให้ต้องเขียนเงื่อนไขพิเศษตอน i = 0 และบ่อยครั้งเราไม่ต้องสร้างลิสต์ prefix ทั้งก้อนด้วยซ้ำ ใช้ตัวแปรเดียวบวกสะสมไปก็พอ" },

      { t: "callout", title: "พร้อมแล้วไปต่อ", c: "หมวดนี้มี 2 ข้อ ทั้งสองข้อเป็น prefix sum แบบสะสมด้วยตัวแปรเดียว กดถัดไปเริ่มข้อแรกได้เลย" },
    ],
  },

  "lc75-p18": {
    slug: "lc75-p18",
    title: "ข้อ 18 · LC1732 — ความสูงมากสุด 🟢",
    lead: "ไต่ระดับตาม gain ที่ให้มา แล้วหาว่าจุดสูงสุดที่ไปถึงคือเท่าไร",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "นักปั่นจักรยานเริ่มที่ระดับความสูง 0 มีลิสต์ gain โดย gain[i] คือ ค่าต่างของระดับความสูง ระหว่างจุดที่ i กับ i+1 ให้หาว่าจุดสูงสุดที่นักปั่นไปถึงมีความสูงเท่าไร" },
      { t: "ul", c: [
        "gain = [-5, 1, 5, 0, -7] → ระดับตามจุดคือ 0, -5, -4, 1, 1, -6 จุดสูงสุดคือ 1",
        "gain = [-4, -3, -2, -1, 4, 3, 2] → ระดับคือ 0, -4, -7, -9, -10, -6, -3, -1 จุดสูงสุดคือ 0 (จุดเริ่มต้น)",
      ] },
      { t: "callout", c: "จุดเริ่มต้นความสูง 0 นับด้วยเสมอ ถ้า gain ทำให้ระดับติดลบตลอด คำตอบก็ยังเป็น 0" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "นี่คือ prefix sum ในรูปแบบพื้นฐานที่สุด ระดับความสูงของจุดที่ k ก็คือผลรวมสะสมของ gain[0..k-1] เพราะแต่ละ gain คือส่วนต่างที่บวกเข้าไปทีละก้อน" },
      { t: "p", c: "วิธีตรงตัวคือสร้างลิสต์ prefix เก็บทุกระดับแล้วค่อยหาค่ามากสุด แต่เราไม่ต้องเก็บทั้งลิสต์เลย เพราะต้องการแค่ ค่ามากสุด จึงบวกสะสมในตัวแปรเดียวแล้วจำค่าสูงสุดที่เคยเจอไว้ ประหยัดหน่วยความจำจาก O(n) เหลือ O(1)" },
      { t: "ol", c: [
        "ตั้งตัวแปร altitude = 0 เก็บระดับความสูงปัจจุบัน (เริ่มที่จุด 0)",
        "ตั้งตัวแปร highest = 0 เก็บค่าสูงสุดที่เคยไปถึง (จุดเริ่มก็คือ 0)",
        "ไล่ทีละค่า g ใน gain: บวก g เข้า altitude (สะสมผลรวม)",
        "อัปเดต highest ให้เป็นค่ามากกว่าระหว่าง highest เดิมกับ altitude ล่าสุด",
        "จบลูปแล้วคืน highest",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ลืมนับจุดเริ่มต้นที่ความสูง 0 ทำให้ตอบผิดเวลา gain ทำให้ระดับติดลบทั้งหมด การตั้ง highest = 0 ตั้งแต่แรกครอบคลุมกรณีนี้ให้เอง" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "ลองไล่ gain = [-5, 1, 5, 0, -7] ดูค่าตัวแปรเปลี่ยนทีละรอบ" },
      { t: "table", head: ["g", "altitude (หลังบวก)", "highest"], rows: [
        ["(เริ่มต้น)", "0", "0"],
        ["-5", "-5", "0"],
        ["1", "-4", "0"],
        ["5", "1", "1"],
        ["0", "1", "1"],
        ["-7", "-6", "1"],
      ] },
      { t: "p", c: "จบลูป highest = 1 ตรงกับคำตอบ" },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `def largest_altitude(gain):
    altitude = 0        # ระดับความสูงปัจจุบัน (เริ่มที่จุด 0)
    highest = 0         # ค่าสูงสุดที่เคยไปถึง (จุดเริ่มก็ 0)
    for g in gain:      # ไล่ค่าต่างระดับทีละก้อน
        altitude += g   # สะสมค่าต่างระดับ = ผลรวมสะสม (prefix sum)
        highest = max(highest, altitude)  # จำค่ามากสุดที่เคยเจอ
    return highest      # คืนจุดสูงสุด

print(largest_altitude([-5, 1, 5, 0, -7]))          # 1
print(largest_altitude([-4, -3, -2, -1, 4, 3, 2]))  # 0` },
        { t: "p", c: "หัวใจอยู่ที่บรรทัด altitude += g ซึ่งก็คือการสร้าง prefix sum ทีละสเต็ป ตัว altitude ณ รอบใด ๆ คือระดับความสูงจริงของจุดนั้น ถ้าเราอยากได้ทุกระดับก็เก็บลงลิสต์ได้ แต่โจทย์ถามแค่ค่ามากสุดจึงเก็บแค่ highest พอ" },
        { t: "p", c: "ถ้าลองเปลี่ยนบรรทัด highest = 0 เป็น highest = altitude แล้วอัปเดตในลูปเฉย ๆ โดยไม่ max กับ 0 จะพังทันทีเมื่อ gain ติดลบทั้งหมด เพราะจะไม่มีรอบไหนกลับมาแตะ 0 อีกเลย การตั้ง 0 ไว้ก่อนจึงเป็นการการันตีว่าจุดเริ่มต้นถูกนับ" },
        { t: "p", c: "Time O(n) ไล่ลิสต์รอบเดียว · Space O(1) ใช้ตัวแปรสะสมแค่สองตัว ไม่สร้างลิสต์ใหม่" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เมื่อโจทย์พูดถึง ค่าต่าง/ส่วนเพิ่มทีละก้อน แล้วถามค่าสะสม ให้คิดถึง prefix sum และถ้าต้องการแค่ค่าสุดขั้ว (มากสุด/น้อยสุด) ก็บวกสะสมด้วยตัวแปรเดียวแล้ว track ค่าไป ไม่ต้องเก็บทั้งลิสต์" },
    ],
  },

  "lc75-p19": {
    slug: "lc75-p19",
    title: "ข้อ 19 · LC724 — หา pivot index 🟢",
    lead: "หา index ที่ผลรวมฝั่งซ้ายเท่ากับฝั่งขวา โดยไม่บวกซ้ำทุกครั้ง",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "มีลิสต์ตัวเลข nums ให้หา index ที่เป็น จุดสมดุล (pivot) นั่นคือ index ที่ผลรวมของเลขทางซ้ายทั้งหมด เท่ากับ ผลรวมของเลขทางขวาทั้งหมด (ตัวที่ index นั้นไม่นับทั้งสองฝั่ง) ให้คืน index ที่เจอตัวแรก (ซ้ายสุด) ถ้าไม่มีให้คืน -1" },
      { t: "ul", c: [
        "nums = [1, 7, 3, 6, 5, 6] → ตอบ 3 เพราะซ้าย 1+7+3 = 11 เท่ากับขวา 5+6 = 11",
        "nums = [1, 2, 3] → ตอบ -1 เพราะไม่มี index ไหนสมดุล",
        "nums = [2, 1, -1] → ตอบ 0 เพราะฝั่งซ้ายว่าง (ผลรวม 0) เท่ากับฝั่งขวา 1+(-1) = 0",
      ] },
      { t: "callout", c: "ต้องคืน index ซ้ายสุดที่สมดุล และฝั่งที่ว่าง (ก่อนตัวแรกหรือหลังตัวสุดท้าย) ถือว่ามีผลรวมเป็น 0" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "ใช้ไอเดีย prefix sum แต่ครั้งนี้เราสนใจผลรวม สองฝั่ง ของแต่ละ index วิธี naive คือทุก index ก็บวกฝั่งซ้ายใหม่และฝั่งขวาใหม่ ซึ่งกลายเป็น O(n^2) ช้าเกินไปเมื่อลิสต์ยาว" },
      { t: "p", c: "ทริกคือรู้ผลรวมทั้งหมด total ไว้ก่อน แล้วสะสมเฉพาะฝั่งซ้าย left_sum ระหว่างเดิน เมื่ออยู่ที่ index i ผลรวมฝั่งขวาหาได้ทันทีจาก total - left_sum - nums[i] ไม่ต้องบวกฝั่งขวาใหม่เลย" },
      { t: "ol", c: [
        "คำนวณ total = ผลรวมทั้งลิสต์ (หนึ่งรอบ)",
        "ตั้ง left_sum = 0 เก็บผลรวมของตัวที่อยู่ทางซ้ายของ i",
        "ไล่ทีละ index i พร้อมค่า n = nums[i]: คำนวณ right_sum = total - left_sum - n",
        "ถ้า left_sum == right_sum แปลว่าเจอ pivot คืน i ทันที (ซ้ายสุดโดยธรรมชาติ)",
        "ถ้ายังไม่สมดุล บวก n เข้า left_sum แล้วไปตัวถัดไป",
        "จบลูปยังไม่เจอ คืน -1",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ลำดับสำคัญมาก ต้องเช็คสมดุล ก่อน แล้วค่อยบวก n เข้า left_sum เพราะตอนอยู่ที่ index i ตัว nums[i] ยังไม่นับเป็นฝั่งซ้าย ถ้าบวกก่อนเช็คจะได้คำตอบผิด" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "ไล่ nums = [1, 7, 3, 6, 5, 6] ซึ่ง total = 28" },
      { t: "table", head: ["i", "n", "left_sum (ก่อนเช็ค)", "right_sum", "สมดุล?"], rows: [
        ["0", "1", "0", "28-0-1 = 27", "ไม่"],
        ["1", "7", "1", "28-1-7 = 20", "ไม่"],
        ["2", "3", "8", "28-8-3 = 17", "ไม่"],
        ["3", "6", "11", "28-11-6 = 11", "ใช่ → คืน 3"],
      ] },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `def pivot_index(nums):
    total = sum(nums)   # ผลรวมทั้งหมด (จ่าย O(n) ครั้งเดียว)
    left_sum = 0        # ผลรวมของตัวที่อยู่ทางซ้ายของ i
    for i, n in enumerate(nums):
        # ผลรวมขวา = total - ฝั่งซ้าย - ตัวปัจจุบัน
        right_sum = total - left_sum - n
        if left_sum == right_sum:  # เช็คก่อนบวก n
            return i               # เจอ pivot ซ้ายสุด
        left_sum += n   # เลื่อน i ไปขวา ตัวปัจจุบันจึงกลายเป็นฝั่งซ้าย
    return -1           # ไล่จนจบไม่เจอ

print(pivot_index([1, 7, 3, 6, 5, 6]))  # 3
print(pivot_index([1, 2, 3]))           # -1
print(pivot_index([2, 1, -1]))          # 0` },
        { t: "p", c: "หัวใจคือเราไม่อยากคำนวณผลรวมซ้าย-ขวาใหม่ทุก index (จะกลายเป็น O(n^2)) จึงเก็บ left_sum ที่สะสมมาจากซ้าย ส่วนผลรวมขวาคำนวณได้จาก total ที่รู้ล่วงหน้า ลบด้วยฝั่งซ้ายและตัวปัจจุบันออก จึงเหลือแค่การไล่รอบเดียว" },
        { t: "p", c: "edge case ที่ต้องระวังคือ pivot อยู่ที่หัวหรือท้ายลิสต์ เช่น [2, 1, -1] ตอบ 0 เพราะฝั่งซ้ายว่าง (ผลรวม 0) เท่ากับฝั่งขวา 1+(-1) = 0 โค้ดนี้รองรับเพราะ left_sum เริ่มที่ 0 อยู่แล้ว ถ้าเผลอสลับไปบวก n ก่อนเช็ค index 0 จะไม่มีทางถูกตรวจในสภาพฝั่งซ้ายว่าง" },
        { t: "p", c: "Time O(n) หา total หนึ่งรอบ แล้วไล่อีกหนึ่งรอบ · Space O(1) ใช้ตัวแปรสะสมเท่านั้น" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เมื่อโจทย์ถามความสัมพันธ์ระหว่าง ผลรวมฝั่งซ้าย กับ ฝั่งขวา ที่แต่ละจุด ให้รู้ total ก่อนแล้วสะสมฝั่งเดียว ฝั่งตรงข้ามได้มาจากการลบ เทคนิคนี้เปลี่ยน O(n^2) ให้เหลือ O(n) ในหลายโจทย์" },
    ],
  },
};
