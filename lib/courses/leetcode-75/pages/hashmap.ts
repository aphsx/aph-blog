import type { Page } from "@/lib/types";

export const hashMapPages: Record<string, Page> = {
  "lc75-intro-hashmap": {
    slug: "lc75-intro-hashmap",
    title: "Hash Map / Set — พื้นฐาน & แนวคิด",
    lead: "ใช้ dict และ set เพื่อเช็คสมาชิก นับความถี่ และจับคู่ข้อมูลได้เร็วในเวลา O(1)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์จำนวนมากถามว่า มีตัวนี้อยู่ไหม, ตัวนี้ปรากฏกี่ครั้ง, หรือ สองกองข้อมูลมีตัวไหนซ้ำกันบ้าง คำถามพวกนี้ตอบได้เร็วมากด้วย hash map (ใน Python คือ dict) และ set ซึ่งแลกหน่วยความจำเพิ่มขึ้นเล็กน้อยเพื่อลดเวลาจาก O(n) เหลือ O(1) ต่อการค้นหา" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "dict และ set ใน Python ทำงานเบื้องหลังด้วย hash table ทำให้การเช็คว่า มีคีย์นี้ไหม (key in d) การหยิบค่า (d[key]) และการเพิ่ม/ลบสมาชิก ทำได้ในเวลาเฉลี่ย O(1) ไม่ต้องไล่ทีละตัวเหมือนการค้นใน list ที่เป็น O(n)" },
      { t: "table", head: ["operation", "list", "set / dict"], rows: [
        ["เช็คว่ามีสมาชิก (x in ...)", "O(n)", "O(1) เฉลี่ย"],
        ["หยิบค่าด้วยคีย์ (d[k])", "—", "O(1) เฉลี่ย"],
        ["เพิ่มสมาชิก", "O(1) ต่อท้าย", "O(1) เฉลี่ย"],
        ["ตัดตัวซ้ำ", "ต้องไล่เอง", "set(...) ให้อัตโนมัติ"],
      ] },
      { t: "h3", c: "set — เก็บสมาชิกไม่ซ้ำ" },
      { t: "code", lang: "python", c: `nums = [1, 2, 2, 3, 3, 3]

s = set(nums)          # {1, 2, 3} ตัดตัวซ้ำออกให้อัตโนมัติ
print(2 in s)          # True  — เช็คสมาชิกเร็ว O(1)
print(len(s))          # 3     — มีค่าที่ต่างกันกี่ตัว

# หา intersection / union / difference ได้ง่าย
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)           # {2, 3}   อยู่ทั้งสองฝั่ง
print(a | b)           # {1, 2, 3, 4} รวมกัน
print(a - b)           # {1}      อยู่ใน a แต่ไม่อยู่ใน b` },
      { t: "h3", c: "dict — นับความถี่ / จับคู่คีย์กับค่า" },
      { t: "p", c: "เวลาต้องนับว่าแต่ละค่าปรากฏกี่ครั้ง วิธีที่นิยมที่สุดคือใช้ collections.Counter ซึ่งเป็น dict ที่ทำหน้าที่นับให้เลย หรือจะนับเองด้วย dict.get ก็ได้" },
      { t: "code", lang: "python", c: `from collections import Counter

words = ["a", "b", "a", "c", "a", "b"]

# วิธีที่ 1 — Counter นับให้อัตโนมัติ
count = Counter(words)         # Counter({'a': 3, 'b': 2, 'c': 1})
print(count["a"])             # 3

# วิธีที่ 2 — นับเองด้วย dict.get (ค่าเริ่มต้น 0)
count = {}
for w in words:
    count[w] = count.get(w, 0) + 1
print(count)                  # {'a': 3, 'b': 2, 'c': 1}` },
      { t: "callout", title: "เลือกใช้ยังไง", c: "ต้องการแค่ เช็คว่ามีไหม / ตัดซ้ำ / หา intersection ใช้ set ต้องการ นับจำนวน หรือ จับคู่คีย์กับค่า ใช้ dict หรือ Counter ทั้งคู่ค้นหาในเวลา O(1) โดยเฉลี่ย" },

      { t: "callout", title: "พร้อมแล้วไปต่อ", c: "หมวดนี้มี 4 ข้อ ไล่จาก set ตรง ๆ ไปจนถึงการนับความถี่แล้วจับคู่ tuple กดถัดไปเริ่มข้อแรกได้เลย" },
    ],
  },

  "lc75-p20": {
    slug: "lc75-p20",
    title: "ข้อ 20 · LC2215 — ผลต่างของสองอาเรย์ 🟢",
    lead: "หาค่าที่อยู่ในลิสต์หนึ่งแต่ไม่อยู่ในอีกลิสต์ ด้วยการลบ set",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "มีลิสต์ nums1 และ nums2 ให้คืนคำตอบเป็นลิสต์สองชั้น answer โดย answer[0] คือค่าที่ ไม่ซ้ำกัน (distinct) ที่อยู่ใน nums1 แต่ไม่อยู่ใน nums2 และ answer[1] คือค่าที่ไม่ซ้ำกันที่อยู่ใน nums2 แต่ไม่อยู่ใน nums1 ลำดับในผลลัพธ์ไม่สำคัญ" },
      { t: "ul", c: [
        "nums1 = [1, 2, 3], nums2 = [2, 4, 6] → [[1, 3], [4, 6]] (1, 3 อยู่ใน nums1 อย่างเดียว, 4, 6 อยู่ใน nums2 อย่างเดียว)",
        "nums1 = [1, 2, 3, 3], nums2 = [1, 1, 2, 2] → [[3], []] (มีแต่ 3 ที่ต่าง และผลลัพธ์ตัดตัวซ้ำแล้ว)",
      ] },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "โจทย์นี้พูดถึง ค่าไม่ซ้ำ และ อยู่ในกองนี้แต่ไม่อยู่ในอีกกอง ซึ่งตรงกับนิยามของ set เป๊ะ ๆ เพราะ set ตัดตัวซ้ำให้อัตโนมัติและมีตัวดำเนินการลบ (difference) ในตัว" },
      { t: "p", c: "ถ้าไม่ใช้ set แล้วเช็คด้วย if x in nums2 บน list ตรง ๆ การเช็คแต่ละครั้งจะเป็น O(n) ทำให้รวมกลายเป็น O(n^2) การแปลงเป็น set ก่อนจึงคุ้มมาก เพราะเช็คสมาชิกเหลือ O(1)" },
      { t: "ol", c: [
        "แปลง nums1 เป็น set s1 และ nums2 เป็น set s2 (ตัดตัวซ้ำในตัว)",
        "หา s1 - s2 = ค่าที่อยู่ใน s1 แต่ไม่อยู่ใน s2",
        "หา s2 - s1 = ค่าที่อยู่ใน s2 แต่ไม่อยู่ใน s1",
        "คืนผลลัพธ์เป็นลิสต์สองชั้น โดยแปลง set กลับเป็น list",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "อย่าลืมว่าต้องทำ difference ทั้ง สองทิศทาง (s1-s2 และ s2-s1) คนละค่ากัน และเพราะโจทย์บอกว่าลำดับไม่สำคัญ จึงไม่ต้องกังวลว่า list(set) จะเรียงยังไง" },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `def find_difference(nums1, nums2):
    s1, s2 = set(nums1), set(nums2)  # ตัดตัวซ้ำของแต่ละกอง
    # s1 - s2 = อยู่ใน s1 แต่ไม่อยู่ใน s2
    # s2 - s1 = อยู่ใน s2 แต่ไม่อยู่ใน s1
    return [list(s1 - s2), list(s2 - s1)]

print(find_difference([1, 2, 3], [2, 4, 6]))        # [[1, 3], [4, 6]]
print(find_difference([1, 2, 3, 3], [1, 1, 2, 2]))  # [[3], []]` },
        { t: "p", c: "เมื่อแปลงเป็น set ปัญหาเรื่อง ค่าไม่ซ้ำ ถูกจัดการให้อัตโนมัติ ที่เหลือคือใช้ตัวดำเนินการ difference (เครื่องหมายลบระหว่าง set) ซึ่งคืนสมาชิกที่อยู่ในฝั่งซ้ายแต่ไม่อยู่ฝั่งขวา ทำสองทิศทางก็ได้คำตอบครบ" },
        { t: "p", c: "ถ้าลองเปลี่ยนไปทำบน list โดยไม่แปลงเป็น set ก่อน นอกจากจะช้าเป็น O(n^2) แล้ว ยังต้องเขียนโค้ดตัดตัวซ้ำเองอีก ซึ่ง set จัดการให้หมดในบรรทัดเดียว" },
        { t: "p", c: "Time O(n + m) แปลงสองลิสต์เป็น set และทำ difference · Space O(n + m) เก็บสอง set" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เจอโจทย์ที่พูดถึง อยู่ในกองนี้แต่ไม่อยู่ในกองนั้น หรือ ค่าที่ต่างกัน ให้นึกถึง set difference ทันที และตัวดำเนินการ & | - ของ set ช่วยเขียนตรรกะเซตให้สั้นและเร็ว" },
    ],
  },

  "lc75-p21": {
    slug: "lc75-p21",
    title: "ข้อ 21 · LC1207 — จำนวนครั้งไม่ซ้ำกัน 🟢",
    lead: "นับความถี่ของแต่ละค่า แล้วเช็คว่าจำนวนครั้งทั้งหมดไม่ซ้ำกัน",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "มีลิสต์ตัวเลข arr ให้เช็คว่า จำนวนครั้งที่ปรากฏ ของแต่ละค่าไม่ซ้ำกันเลยหรือไม่ (คืน True/False)" },
      { t: "ul", c: [
        "arr = [1, 2, 2, 1, 1, 3] → 1 ปรากฏ 3 ครั้ง, 2 ปรากฏ 2 ครั้ง, 3 ปรากฏ 1 ครั้ง จำนวนครั้ง {3, 2, 1} ไม่ซ้ำกัน → True",
        "arr = [1, 2] → ต่างก็ปรากฏ 1 ครั้งเท่ากัน จำนวนครั้งซ้ำกัน → False",
        "arr = [3, 5, 7, 7, 5, 5] → 3→1, 5→3, 7→2 จำนวนครั้ง {1, 3, 2} ไม่ซ้ำ → True",
      ] },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "โจทย์แบ่งเป็นสองชั้น ชั้นแรกต้อง นับความถี่ ของแต่ละค่า (ใช้ dict/Counter) ชั้นสองต้องเช็คว่า ค่าจำนวนครั้ง เหล่านั้นมีตัวซ้ำกันไหม" },
      { t: "p", c: "ทริกเช็คตัวซ้ำที่ใช้บ่อยคือเทียบ len ของ list เดิมกับ len ของ set ของมัน ถ้าเท่ากันแปลว่าไม่มีตัวไหนซ้ำ (เพราะ set ตัดตัวซ้ำออก ถ้ามีซ้ำ ขนาดจะหด)" },
      { t: "ol", c: [
        "นับความถี่ด้วย Counter(arr) ได้ dict ที่คีย์คือค่า ค่าคือจำนวนครั้ง",
        "ดึงเฉพาะ จำนวนครั้ง ออกมาด้วย .values()",
        "เทียบ len ของจำนวนครั้งทั้งหมด กับ len ของ set ของมัน",
        "เท่ากัน → ไม่มีจำนวนครั้งซ้ำ คืน True มิเช่นนั้นคืน False",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "อย่าเผลอเอา คีย์ (ตัวค่า) ไปเช็คแทน จำนวนครั้ง ต้องใช้ .values() ซึ่งเป็นจำนวนครั้ง ไม่ใช่ .keys() เพราะคีย์ย่อมไม่ซ้ำกันอยู่แล้วโดยธรรมชาติของ dict" },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def unique_occurrences(arr):
    counts = Counter(arr).values()   # จำนวนครั้งของแต่ละค่า เช่น [3, 2, 1]
    # ถ้าเอา values ใส่ set แล้วขนาดยังเท่าเดิม แปลว่าไม่มีตัวซ้ำ
    return len(counts) == len(set(counts))

print(unique_occurrences([1, 2, 2, 1, 1, 3]))  # True
print(unique_occurrences([1, 2]))              # False
print(unique_occurrences([3, 5, 7, 7, 5, 5]))  # True (3->1, 5->3, 7->2)` },
        { t: "p", c: "ชั้นแรก Counter(arr) นับให้อัตโนมัติ แล้ว .values() คืนจำนวนครั้งทั้งหมดออกมาเป็นลำดับหนึ่งชุด ชั้นสองแปลงชุดนั้นเป็น set แล้วเทียบขนาด ถ้ามีจำนวนครั้งที่ซ้ำกัน set จะยุบตัวลง ขนาดจึงไม่เท่ากับต้นฉบับ" },
        { t: "p", c: "ถ้าลองเขียนเองแบบไล่เช็คคู่ทุกคู่ของจำนวนครั้งจะเป็น O(k^2) แต่ทริก len เทียบ set ทำให้เหลือ O(k) และอ่านง่ายกว่ามาก" },
        { t: "p", c: "Time O(n) นับความถี่หนึ่งรอบ สร้าง set หนึ่งรอบ · Space O(n) เก็บ counter และ set ของความถี่" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "แพตเทิร์นสองชั้น นับก่อนด้วย Counter แล้วค่อยประมวลผลบน ค่าความถี่ พบบ่อยมาก และทริก len(x) == len(set(x)) คือวิธีเช็ค มีตัวซ้ำไหม ที่สั้นที่สุด" },
    ],
  },

  "lc75-p22": {
    slug: "lc75-p22",
    title: "ข้อ 22 · LC1657 — สองสตริงใกล้กันไหม 🟡",
    lead: "แปลปฏิบัติการสองแบบให้เป็นเงื่อนไขบนชุดตัวอักษรและกองความถี่",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "มีสองสตริง word1 และ word2 เรียกว่า ใกล้กัน (close) ถ้าเปลี่ยนจากสตริงหนึ่งไปอีกสตริงได้ด้วยการทำสองปฏิบัติการนี้กี่ครั้งก็ได้ 1) สลับตัวอักษรสองตำแหน่งใด ๆ (เช่น abcde กลายเป็น aecdb) 2) สลับความถี่ระหว่างตัวอักษรสองตัวที่ มีอยู่ในสตริง เช่น เปลี่ยน a ทุกตัวเป็น b และ b ทุกตัวเป็น a พร้อมกัน (aacabb กลายเป็น bbcbaa) ให้คืน True/False" },
      { t: "ul", c: [
        "word1 = abc, word2 = bca → True (แค่สลับตำแหน่ง)",
        "word1 = a, word2 = aa → False (ความยาวและชุดตัวอักษรต่างกัน)",
        "word1 = cabbba, word2 = abbccc → True (ชุดตัวอักษรเดียวกัน กองความถี่ตรงกัน)",
        "word1 = cabbba, word2 = aabbss → False (ความถี่เรียงเท่ากัน แต่ตัวอักษรคนละชุด)",
      ] },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "กุญแจคือแปลความหมายของปฏิบัติการทั้งสองให้เป็นเงื่อนไขที่เช็คได้ ปฏิบัติการ 1 (สลับตำแหน่ง) บอกว่าลำดับไม่มีผล เหลือแค่ ตัวอะไรมีกี่ตัว ปฏิบัติการ 2 (สลับความถี่) บอกว่าเราจับคู่ตัวอักษรกับจำนวนได้อิสระ ตราบใดที่ตัวอักษรนั้นมีอยู่แล้ว" },
      { t: "p", c: "เราจึงใช้ Counter นับความถี่ของแต่ละสตริง แล้วตรวจสองเงื่อนไข ไม่ต้องลองสลับจริง ๆ ซึ่งจะเป็นการค้นหาที่ระเบิดเป็นแฟกทอเรียล" },
      { t: "ol", c: [
        "สร้าง Counter ของ word1 และ word2 เป็น c1, c2",
        "เงื่อนไข 1: set(c1) == set(c2) — ต้องใช้ชุดตัวอักษรเดียวกัน (ปฏิบัติการ 2 สลับได้เฉพาะตัวที่มีอยู่ เสกตัวใหม่ไม่ได้)",
        "เงื่อนไข 2: sorted(c1.values()) == sorted(c2.values()) — กองของจำนวนความถี่ต้องจับคู่กันได้พอดี",
        "เป็นจริงทั้งสองข้อ → close คืน True",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "อย่าลืมเงื่อนไขข้อ 1 (ชุดตัวอักษรเดียวกัน) ถ้าเช็คแค่ความถี่เรียงเท่ากัน cabbba กับ aabbss จะถูกตอบผิดเป็น True ทั้งที่ตัว s ไม่มีใน word1 เลย ปฏิบัติการ 2 จึงเสกมันขึ้นมาไม่ได้" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "เทียบสองเงื่อนไขบนตัวอย่างทั้งสี่" },
      { t: "table", head: ["word1 / word2", "ชุดตัวอักษรเท่ากัน?", "ความถี่เรียงเท่ากัน?", "ผล"], rows: [
        ["abc / bca", "ใช่ {a,b,c}", "ใช่ [1,1,1]", "True"],
        ["a / aa", "ใช่ {a}", "ไม่ [1] vs [2]", "False"],
        ["cabbba / abbccc", "ใช่ {a,b,c}", "ใช่ [1,2,3]", "True"],
        ["cabbba / aabbss", "ไม่ {a,b,c} vs {a,b,s}", "(ใช่ [1,2,3])", "False"],
      ] },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def close_strings(word1, word2):
    c1, c2 = Counter(word1), Counter(word2)
    # เงื่อนไข 1: ต้องใช้ชุดตัวอักษรเดียวกัน (set ของคีย์เท่ากัน)
    # เงื่อนไข 2: กองของจำนวนความถี่ต้องตรงกัน (เรียงแล้วเท่ากัน)
    return (set(c1) == set(c2)
            and sorted(c1.values()) == sorted(c2.values()))

print(close_strings("abc", "bca"))        # True
print(close_strings("a", "aa"))           # False
print(close_strings("cabbba", "abbccc"))  # True
print(close_strings("cabbba", "aabbss"))  # False` },
        { t: "p", c: "set(c1) เอาเฉพาะคีย์ (ตัวอักษรที่ปรากฏ) มาเทียบกัน ส่วน c1.values() คือจำนวนครั้งของแต่ละตัว การ sorted ก่อนเทียบทำให้ไม่สนว่าตัวไหนจับกับจำนวนไหน สนแค่ว่ากองตัวเลขเหมือนกันไหม ซึ่งตรงกับอิสระของปฏิบัติการ 2 พอดี" },
        { t: "p", c: "ทำไมต้องมีทั้งสองเงื่อนไขคู่กัน? เงื่อนไข 2 อย่างเดียวยอมให้ตัวอักษรคนละชุดผ่านได้ (เช่น s แทน c) ส่วนเงื่อนไข 1 อย่างเดียวก็ไม่พอเพราะจำนวนแต่ละตัวอาจไม่จับคู่กันได้ ต้องครบทั้งสองข้อ" },
        { t: "p", c: "Time O(n + k log k) นับความถี่ O(n) และ sort ความถี่ที่มีอย่างมาก k = 26 ตัว · Space O(k) เก็บ Counter ของตัวอักษร (คงที่ 26 ตัว)" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "โจทย์ที่ให้ ปฏิบัติการแปลก ๆ มักแก้ด้วยการแปลปฏิบัติการเป็น invariant (สิ่งที่ไม่เปลี่ยนไม่ว่าทำกี่ครั้ง) แล้วเช็ค invariant นั้นแทนการจำลองจริง ที่นี่ invariant คือ ชุดตัวอักษร กับ กองความถี่" },
    ],
  },

  "lc75-p23": {
    slug: "lc75-p23",
    title: "ข้อ 23 · LC2352 — คู่แถว-คอลัมน์ที่เท่ากัน 🟡",
    lead: "นับความถี่ของแถวด้วย tuple แล้วยิงถามทีละคอลัมน์",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "มีตารางสี่เหลี่ยมจัตุรัส grid ขนาด n x n ให้นับจำนวนคู่ (แถว i, คอลัมน์ j) ที่แถว i และคอลัมน์ j มีค่าเหมือนกันเป๊ะทุกตำแหน่งตามลำดับ" },
      { t: "ul", c: [
        "grid = [[3,2,1],[1,7,6],[2,7,7]] → 1 คู่ (แถว 2 กับคอลัมน์ 1 ต่างก็เป็น [2,7,7])",
        "grid = [[3,1,2,2],[1,4,4,5],[2,4,2,2],[2,4,2,2]] → 3 คู่",
      ] },
      { t: "callout", c: "ลำดับของค่าในแถว/คอลัมน์สำคัญ [2,7,7] ตรงกับ [2,7,7] เท่านั้น ไม่ตรงกับ [7,2,7] และแถวที่หน้าตาซ้ำกันหลายแถวก็นับเป็นหลายคู่" },

      { t: "h2", c: "แนวทาง — ต้องใช้อะไร & คิดยังไง" },
      { t: "p", c: "วิธี naive คือเทียบทุกแถวกับทุกคอลัมน์ ซึ่งเป็น O(n^2) คู่ และแต่ละคู่เทียบ n ช่อง รวมเป็น O(n^3) ช้าเกินไปเมื่อ n ใหญ่" },
      { t: "p", c: "เร่งด้วย hash map ได้ ไอเดียคือ นับ ว่าแต่ละ หน้าตาแถว ปรากฏกี่ครั้ง เก็บใน Counter แล้วไล่ทีละคอลัมน์ ถามว่ามีแถวหน้าตาเดียวกันกี่แถว บวกสะสม เพราะแถวและคอลัมน์เป็นลำดับตัวเลข เราต้องแปลงเป็น tuple ก่อน (list เอาเป็นคีย์ dict ไม่ได้เพราะแก้ค่าได้ แต่ tuple แก้ไม่ได้จึง hash ได้)" },
      { t: "ol", c: [
        "อ่านขนาด n = len(grid)",
        "แปลงแต่ละแถวเป็น tuple แล้วนับด้วย Counter ได้ row_count ว่าหน้าตาแบบไหนมีกี่แถว",
        "ไล่ทีละคอลัมน์ j: ประกอบค่าคอลัมน์เป็น tuple col",
        "บวก row_count[col] เข้าตัวนับ pairs (มีแถวหน้าตาตรงกันกี่แถว จับคู่ได้ทุกแถว)",
        "จบลูปคืน pairs",
      ] },
      { t: "callout", title: "จุดพลาดที่พบบ่อย", c: "ต้อง บวกจำนวน row_count[col] ไม่ใช่บวกทีละหนึ่ง เพราะแถวที่เหมือนกันหลายแถวจับคู่กับคอลัมน์นี้ได้ทุกแถว ข้อดีของ Counter คือถ้าคีย์ไม่มีจะคืน 0 ให้เอง ไม่ error" },

      { t: "h2", c: "ไล่ทีละสเต็ป" },
      { t: "p", c: "ไล่ grid = [[3,2,1],[1,7,6],[2,7,7]] ซึ่ง row_count = {(3,2,1):1, (1,7,6):1, (2,7,7):1}" },
      { t: "table", head: ["j", "คอลัมน์ (tuple)", "row_count[col]", "pairs สะสม"], rows: [
        ["0", "(3, 1, 2)", "0", "0"],
        ["1", "(2, 7, 7)", "1", "1"],
        ["2", "(1, 6, 7)", "0", "1"],
      ] },
      { t: "p", c: "จบลูป pairs = 1 ตรงกับคำตอบ" },

      { t: "details", summary: "▶ เฉลยละเอียด (ลองเองก่อนนะ)", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def equal_pairs(grid):
    n = len(grid)
    # นับว่าแต่ละ "หน้าตาแถว" (แปลงเป็น tuple) ปรากฏกี่ครั้ง
    row_count = Counter(tuple(row) for row in grid)

    pairs = 0
    for j in range(n):
        # ประกอบคอลัมน์ที่ j เป็น tuple
        col = tuple(grid[i][j] for i in range(n))
        # มีแถวหน้าตาตรงกับคอลัมน์นี้กี่แถว บวกเข้าไปทั้งหมด
        pairs += row_count[col]
    return pairs

print(equal_pairs([[3, 2, 1], [1, 7, 6], [2, 7, 7]]))  # 1
print(equal_pairs([[3, 1, 2, 2], [1, 4, 4, 5],
                   [2, 4, 2, 2], [2, 4, 2, 2]]))        # 3` },
        { t: "p", c: "ขั้นแรกแปลงแต่ละแถวเป็น tuple แล้วนับด้วย Counter ว่าหน้าตาแบบไหนมีกี่แถว การใช้ tuple สำคัญเพราะ list เปลี่ยนแปลงได้จึงเป็นคีย์ dict ไม่ได้ แต่ tuple คงที่จึง hash ได้" },
        { t: "p", c: "ขั้นสองไล่ทีละคอลัมน์ ประกอบเป็น tuple แล้วถามจาก row_count ว่ามีแถวหน้าตาเดียวกันกี่แถว เพราะแถวที่เหมือนกันหลายแถวจับคู่กับคอลัมน์นี้ได้ทุกแถว จึงบวก จำนวน ที่ได้ ในตัวอย่างที่สอง คอลัมน์หนึ่งไปตรงกับแถวที่หน้าตาซ้ำกันสองแถว ก็บวก 2 ทีเดียว" },
        { t: "p", c: "Time O(n^2) แต่ละแถวและแต่ละคอลัมน์มี n ช่อง รวม n แถว/คอลัมน์ เป็น O(n^2) · Space O(n^2) เก็บ tuple ของแถวทั้งหมดใน Counter" },
      ] },

      { t: "callout", title: "💡 สรุป pattern", c: "เมื่อต้อง จับคู่ของที่เหมือนกัน จากสองกอง อย่าเทียบทุกคู่ (O(n^2) คู่) ให้ นับกองหนึ่งลง hash map ก่อน แล้วยิงถามอีกกองทีละตัว และจำไว้ว่าจะเอา list/แถว เป็นคีย์ต้องแปลงเป็น tuple ก่อน" },
    ],
  },
};
