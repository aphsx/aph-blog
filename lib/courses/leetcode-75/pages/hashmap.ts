import type { Page } from "@/lib/types";

export const hashMapPages: Record<string, Page> = {
  "lc75-hashmap": {
    slug: "lc75-hashmap",
    title: "Hash Map / Set — นับและจับคู่",
    lead: "ใช้ dict และ set เพื่อเช็คสมาชิก นับความถี่ และจับคู่ข้อมูลได้เร็วในเวลา O(1)",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "โจทย์จำนวนมากถามว่า มีตัวนี้อยู่ไหม, ตัวนี้ปรากฏกี่ครั้ง, หรือ สองกองข้อมูลมีตัวไหนซ้ำกันบ้าง คำถามพวกนี้ตอบได้เร็วมากด้วย hash map (ใน Python คือ dict) และ set ซึ่งแลกหน่วยความจำเพิ่มขึ้นเล็กน้อยเพื่อลดเวลาจาก O(n) เหลือ O(1) ต่อการค้นหา" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },
      { t: "p", c: "dict และ set ใน Python ทำงานเบื้องหลังด้วย hash table ทำให้การเช็คว่า มีคีย์นี้ไหม (key in d) การหยิบค่า (d[key]) และการเพิ่ม/ลบสมาชิก ทำได้ในเวลาเฉลี่ย O(1) ไม่ต้องไล่ทีละตัวเหมือนการค้นใน list ที่เป็น O(n)" },
      { t: "h3", c: "set — เก็บสมาชิกไม่ซ้ำ" },
      { t: "code", lang: "python", c: `nums = [1, 2, 2, 3, 3, 3]

s = set(nums)          # {1, 2, 3} ตัดตัวซ้ำออกให้อัตโนมัติ
print(2 in s)          # True  — เช็คสมาชิกเร็ว O(1)
print(len(s))          # 3     — มีค่าที่ต่างกันกี่ตัว

# หา intersection / union / difference ได้ง่าย
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)           # {2, 3}   อยู่ทั้งสองฝั่ง
print(a | b)           # {1, 2, 3, 4} รวมกัน
print(a - b)           # {1}      อยู่ใน a แต่ไม่อยู่ใน b` },
      { t: "h3", c: "dict — นับความถี่ / จับคู่คีย์กับค่า" },
      { t: "p", c: "เวลาต้องนับว่าแต่ละค่าปรากฏกี่ครั้ง วิธีที่นิยมที่สุดคือใช้ collections.Counter ซึ่งเป็น dict ที่ทำหน้าที่นับให้เลย หรือจะนับเองด้วย dict.get ก็ได้" },
      { t: "code", lang: "python", c: `from collections import Counter

words = ["a", "b", "a", "c", "a", "b"]

# วิธีที่ 1 — Counter นับให้อัตโนมัติ
count = Counter(words)         # Counter({'a': 3, 'b': 2, 'c': 1})
print(count["a"])             # 3

# วิธีที่ 2 — นับเองด้วย dict.get (ค่าเริ่มต้น 0)
count = {}
for w in words:
    count[w] = count.get(w, 0) + 1
print(count)                  # {'a': 3, 'b': 2, 'c': 1}` },
      { t: "callout", title: "เลือกใช้ยังไง", c: "ต้องการแค่ เช็คว่ามีไหม / ตัดซ้ำ / หา intersection ใช้ set ต้องการ นับจำนวน หรือ จับคู่คีย์กับค่า ใช้ dict หรือ Counter ทั้งคู่ค้นหาในเวลา O(1) โดยเฉลี่ย" },

      { t: "h2", c: "ข้อ 1 — หาตัวที่ต่างกันของสองลิสต์ (LC2215) 🟢" },
      { t: "p", c: "มีลิสต์ nums1 และ nums2 ให้คืนคำตอบเป็นลิสต์สองชั้น answer โดย answer[0] คือค่าที่ ไม่ซ้ำกัน (distinct) ที่อยู่ใน nums1 แต่ไม่อยู่ใน nums2 และ answer[1] คือค่าที่ไม่ซ้ำกันที่อยู่ใน nums2 แต่ไม่อยู่ใน nums1 ลำดับในผลลัพธ์ไม่สำคัญ เช่น nums1 = [1,2,3], nums2 = [2,4,6] ตอบ [[1,3],[4,6]] เพราะ 1 กับ 3 อยู่ใน nums1 อย่างเดียว ส่วน 4 กับ 6 อยู่ใน nums2 อย่างเดียว" },
      { t: "callout", title: "คำใบ้", c: "แปลงทั้งสองลิสต์เป็น set แล้วใช้การลบ set (difference) ตรง ๆ เพราะ set ตัดตัวซ้ำให้อยู่แล้ว และเช็คสมาชิกเร็ว O(1)" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def find_difference(nums1, nums2):
    s1, s2 = set(nums1), set(nums2)
    # s1 - s2 = อยู่ใน s1 แต่ไม่อยู่ใน s2
    return [list(s1 - s2), list(s2 - s1)]

print(find_difference([1, 2, 3], [2, 4, 6]))       # [[1, 3], [4, 6]]
print(find_difference([1, 2, 3, 3], [1, 1, 2, 2])) # [[3], []]` },
        { t: "p", c: "เมื่อแปลงเป็น set ปัญหาเรื่อง ค่าไม่ซ้ำ ถูกจัดการให้อัตโนมัติ ที่เหลือคือใช้ตัวดำเนินการ difference (เครื่องหมายลบระหว่าง set) ซึ่งคืนสมาชิกที่อยู่ในฝั่งซ้ายแต่ไม่อยู่ฝั่งขวา ทำสองทิศทางก็ได้คำตอบครบ" },
        { t: "p", c: "ถ้าไม่ใช้ set แล้วเช็คด้วย if x in nums2 บน list ตรง ๆ การเช็คแต่ละครั้งจะเป็น O(n) ทำให้รวมเป็น O(n^2) การแปลงเป็น set ก่อนจึงคุ้มมาก" },
        { t: "p", c: "Time O(n + m) แปลงสองลิสต์เป็น set และทำ difference · Space O(n + m) เก็บสอง set" },
      ] },

      { t: "h2", c: "ข้อ 2 — จำนวนครั้งที่ปรากฏไม่ซ้ำกันไหม (LC1207) 🟢" },
      { t: "p", c: "มีลิสต์ตัวเลข arr ให้เช็คว่า จำนวนครั้งที่ปรากฏ ของแต่ละค่าไม่ซ้ำกันเลยหรือไม่ (คืน True/False) เช่น arr = [1,2,2,1,1,3] ค่าปรากฏดังนี้ 1 ปรากฏ 3 ครั้ง, 2 ปรากฏ 2 ครั้ง, 3 ปรากฏ 1 ครั้ง จำนวนครั้ง {3,2,1} ไม่ซ้ำกัน ตอบ True แต่ arr = [1,2] ต่างก็ปรากฏ 1 ครั้งเท่ากัน จำนวนครั้งซ้ำกัน ตอบ False" },
      { t: "callout", title: "คำใบ้", c: "นับความถี่ของแต่ละค่าก่อน (dict/Counter) แล้วเอา ค่าจำนวนครั้ง ทั้งหมดมาเช็คว่ามีตัวซ้ำกันไหม เทียบจำนวนสมาชิกของ list กับ set ของมันได้เลย" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def unique_occurrences(arr):
    counts = Counter(arr).values()   # จำนวนครั้งของแต่ละค่า เช่น [3, 2, 1]
    # ถ้าเอา values ใส่ set แล้วขนาดยังเท่าเดิม แปลว่าไม่มีตัวซ้ำ
    return len(counts) == len(set(counts))

print(unique_occurrences([1, 2, 2, 1, 1, 3]))  # True
print(unique_occurrences([1, 2]))              # False
print(unique_occurrences([3, 5, 7, 7, 5, 5]))  # True (3->1, 5->3, 7->2)` },
        { t: "p", c: "โจทย์นี้ทำสองชั้น ชั้นแรก นับความถี่ ด้วย Counter ได้ dict ที่คีย์คือค่า ค่าคือจำนวนครั้ง ชั้นสอง เอาเฉพาะ จำนวนครั้ง มาเช็คว่าไม่ซ้ำ ทริกเช็คตัวซ้ำที่ใช้บ่อยคือเทียบ len ของ list เดิมกับ len ของ set ถ้าเท่ากันแปลว่าไม่มีตัวไหนซ้ำ (เพราะ set ตัดซ้ำออก)" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือเผลอเอา คีย์ (ตัวค่า) ไปเช็คแทน จำนวนครั้ง ต้องเอา .values() ซึ่งเป็นจำนวนครั้งมาเช็ค ไม่ใช่ .keys()" },
        { t: "p", c: "Time O(n) นับความถี่หนึ่งรอบ สร้าง set หนึ่งรอบ · Space O(n) เก็บ counter และ set ของความถี่" },
      ] },

      { t: "h2", c: "ข้อ 3 — สองสตริงใกล้กันไหม (LC1657) 🟡" },
      { t: "p", c: "มีสองสตริง word1 และ word2 เรียกว่า ใกล้กัน (close) ถ้าเปลี่ยนจากสตริงหนึ่งไปอีกสตริงได้ด้วยการทำสองปฏิบัติการนี้กี่ครั้งก็ได้ 1) สลับตัวอักษรสองตำแหน่งใด ๆ (เช่น abcde กลายเป็น aecdb) 2) สลับความถี่ระหว่างตัวอักษรสองตัวที่ มีอยู่ในสตริง เช่น เปลี่ยน a ทุกตัวเป็น b และ b ทุกตัวเป็น a พร้อมกัน (aacabb กลายเป็น bbcbaa) ให้คืน True/False เช่น word1 = abc, word2 = bca ตอบ True แต่ word1 = a, word2 = aa ตอบ False เพราะความยาวและชุดตัวอักษรต่างกัน" },
      { t: "callout", title: "คำใบ้", c: "ปฏิบัติการ 1 สลับตำแหน่งได้อิสระ แปลว่าลำดับไม่สำคัญ ปฏิบัติการ 2 สลับความถี่ระหว่างตัวที่มีอยู่ แปลว่า ชุดตัวอักษรที่ใช้ ต้องเหมือนกัน และ กองของจำนวนความถี่ ต้องตรงกัน (แต่ไม่จำเป็นต้องเป็นตัวเดียวกัน)" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def close_strings(word1, word2):
    c1, c2 = Counter(word1), Counter(word2)
    # เงื่อนไข 1: ต้องใช้ชุดตัวอักษรเดียวกัน (set ของคีย์เท่ากัน)
    # เงื่อนไข 2: กองของจำนวนความถี่ต้องตรงกัน (เรียงแล้วเท่ากัน)
    return (set(c1) == set(c2)
            and sorted(c1.values()) == sorted(c2.values()))

print(close_strings("abc", "bca"))       # True
print(close_strings("a", "aa"))          # False
print(close_strings("cabbba", "abbccc")) # True
print(close_strings("cabbba", "aabbss")) # False` },
        { t: "p", c: "กุญแจคือแปลความหมายของปฏิบัติการทั้งสองให้เป็นเงื่อนไขที่เช็คได้ ปฏิบัติการ 1 (สลับตำแหน่ง) บอกว่าลำดับไม่มีผล เหลือแค่ ตัวอะไรมีกี่ตัว ปฏิบัติการ 2 (สลับความถี่) บอกว่าเราจับคู่ตัวอักษรกับจำนวนได้อิสระ ตราบใดที่ ชุดตัวอักษรเหมือนกัน และ กองตัวเลขความถี่เหมือนกัน" },
        { t: "p", c: "ดังนั้นเงื่อนไขมีสองข้อ 1) set(c1) == set(c2) คือใช้ตัวอักษรชุดเดียวกัน (สำคัญ เพราะปฏิบัติการ 2 สลับได้เฉพาะตัวที่มีอยู่แล้ว จะเสกตัวใหม่ไม่ได้) 2) sorted ของค่าความถี่เท่ากัน คือจำนวนแต่ละก้อนจับคู่กันได้พอดี จุดพลาดที่พบบ่อยคือลืมเช็คเงื่อนไขข้อ 1 ทำให้ cabbba กับ aabbss ที่ความถี่เรียงเท่ากันแต่ตัวอักษรคนละชุด ถูกตอบผิดเป็น True" },
        { t: "p", c: "Time O(n + k log k) นับความถี่ O(n) และ sort ความถี่ที่มีอย่างมาก k = 26 ตัว · Space O(k) เก็บ Counter ของตัวอักษร (คงที่ 26 ตัว)" },
      ] },

      { t: "h2", c: "ข้อ 4 — นับคู่แถวและคอลัมน์ที่เหมือนกัน (LC2352) 🟡" },
      { t: "p", c: "มีตารางสี่เหลี่ยมจัตุรัส grid ขนาด n x n ให้นับจำนวนคู่ (แถว i, คอลัมน์ j) ที่แถว i และคอลัมน์ j มีค่าเหมือนกันเป๊ะทุกตำแหน่งตามลำดับ เช่น grid = [[3,2,1],[1,7,6],[2,7,7]] มีคู่ที่ตรงกัน 1 คู่ คือแถว 2 กับคอลัมน์ 1 ซึ่งต่างก็เป็น [2,7,7] ตอบ 1 อีกตัวอย่าง grid = [[3,1,2,2],[1,4,4,5],[2,4,2,2],[2,4,2,2]] ตอบ 3" },
      { t: "callout", title: "คำใบ้", c: "แถวและคอลัมน์เป็นลำดับของตัวเลข ถ้าแปลงแต่ละแถวเป็น tuple (ให้ hash ได้) แล้วนับความถี่ด้วย Counter จากนั้นไล่แต่ละคอลัมน์ ประกอบเป็น tuple แล้วเปิดดูว่ามีแถวหน้าตาแบบนี้กี่แถว บวกสะสมได้เลย" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from collections import Counter

def equal_pairs(grid):
    n = len(grid)
    # นับว่าแต่ละ "หน้าตาแถว" (แปลงเป็น tuple) ปรากฏกี่ครั้ง
    row_count = Counter(tuple(row) for row in grid)

    pairs = 0
    for j in range(n):
        # ประกอบคอลัมน์ที่ j เป็น tuple
        col = tuple(grid[i][j] for i in range(n))
        # มีแถวหน้าตาตรงกับคอลัมน์นี้กี่แถว บวกเข้าไปทั้งหมด
        pairs += row_count[col]
    return pairs

print(equal_pairs([[3, 2, 1], [1, 7, 6], [2, 7, 7]]))  # 1
print(equal_pairs([[3, 1, 2, 2], [1, 4, 4, 5],
                   [2, 4, 2, 2], [2, 4, 2, 2]]))        # 3` },
        { t: "p", c: "วิธีตรงไปตรงมาคือเทียบทุกแถวกับทุกคอลัมน์ ซึ่งเป็น O(n^2) คู่ และแต่ละคู่เทียบ n ช่อง รวมเป็น O(n^3) เราเร่งด้วย hash map ได้ ขั้นแรกแปลงแต่ละแถวเป็น tuple (list แปลงเป็นคีย์ dict ไม่ได้เพราะแก้ค่าได้ แต่ tuple ได้) แล้วนับด้วย Counter ว่าหน้าตาแบบไหนมีกี่แถว" },
        { t: "p", c: "ขั้นสองไล่ทีละคอลัมน์ ประกอบเป็น tuple แล้วถามจาก row_count ว่ามีแถวหน้าตาเดียวกันกี่แถว เพราะแถวที่เหมือนกันหลายแถวจับคู่กับคอลัมน์นี้ได้ทุกแถว จึง บวกจำนวน ไม่ใช่บวกหนึ่ง จุดที่ต้องระวังคือถ้าใช้ row_count[col] กับคีย์ที่ไม่มี Counter จะคืน 0 ให้เอง ไม่ error ซึ่งสะดวกพอดี" },
        { t: "p", c: "Time O(n^2) แต่ละแถวและแต่ละคอลัมน์มี n ช่อง รวม n แถว/คอลัมน์ เป็น O(n^2) · Space O(n^2) เก็บ tuple ของแถวทั้งหมดใน Counter" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Stack →", slug: "lc75-stack", desc: "โครงสร้างเข้าทีหลังออกก่อน (LIFO)" },
        { title: "← Prefix Sum", slug: "lc75-prefix-sum" },
      ] },
    ],
  },
};
