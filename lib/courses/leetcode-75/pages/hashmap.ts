import type { Page } from "@/lib/types";
export const hashMapPages: Record<string, Page> = {
  "lc75-intro-hashmap": {
    slug: "lc75-intro-hashmap",
    title: {
      th: "Hash Map / Set — พื้นฐาน & แนวคิด",
      en: "Hash Map / Set — Fundamentals & Mental Models",
    },
    lead: {
      th: "โครงสร้างข้อมูลที่ \"คำนวณตำแหน่งจากตัวค่าเอง\" จึงหาของเจอโดยไม่ต้องไล่ดูทีละตัว — ใน Python คือ set, dict, Counter, defaultdict หน้านี้แบ่งเป็น 7 ส่วน: กลไกเบื้องหลัง, กฎเรื่อง key, เครื่องมือ 4 ชิ้น, 6 แบบการใช้งานที่ต้องแยกให้ออก, และเมื่อไหร่ที่ห้ามใช้",
      en: 'A structure that "computes position from the value itself," so lookups skip scanning — in Python: set, dict, Counter, defaultdict. Seven parts: the mechanism, the key rule, four tools, six usage patterns, and when not to use it.',
    },
    group: "LeetCode 75",
    blocks: {
      th: [
              {
                t: "p",
                c: 'โจทย์ในหมวดนี้ถามคำถามหน้าตาต่างกัน แต่แก่นเหมือนกันหมด: "ตัวนี้เคยเจอมาก่อนไหม", "ตัวนี้โผล่มากี่ครั้ง", "สองกองนี้มีอะไรซ้ำกันบ้าง" — สามคำถามนี้ถ้าทำแบบตรงไปตรงมาคือไล่ดูทั้งกองทุกครั้งที่ถาม แต่ hash map ตอบได้ในเวลาเกือบคงที่ ไม่ว่ากองข้อมูลจะใหญ่แค่ไหน',
              },
              {
                t: "p",
                c: "และมันไม่ฟรี — สิ่งที่จ่ายคือ memory (หน่วยความจำ) เราสร้างโครงสร้างเพิ่มขึ้นมาอีกก้อนเพื่อ \"จำ\" ว่าเคยเห็นอะไรมาแล้ว การแลกนี้เรียกว่า space-time tradeoff และมันคือหัวใจของทั้งหมวดนี้",
              },
              {
                t: "callout",
                title: "หน้านี้อ่านยังไง",
                c: "ส่วนที่ 1-2 คือ \"ทำไมมันเร็ว\" ส่วนที่ 3-4 คือ \"เครื่องมือมีอะไรให้ใช้\" ส่วนที่ 5 คือหัวใจของหน้านี้ — 6 แบบการใช้งาน ที่ต้องแยกให้ออกจากกัน ถ้าเวลาน้อยให้อ่านส่วนที่ 5 กับ 7 เป็นอย่างน้อย",
              },

              { t: "h2", c: "ส่วนที่ 1 · ปัญหาที่ hash map มาแก้ — ดูของจริงก่อน" },
              {
                t: "p",
                c: 'โจทย์: มีสองกอง a กับ b อยากรู้ว่ามีตัวไหนใน b ที่ ไม่มีอยู่ใน a เลย วิธีตรงไปตรงมาคือหยิบตัวจาก b มาทีละตัว แล้วไล่เทียบกับทุกตัวใน a — ซึ่งจริง ๆ คือสิ่งที่ x in a ทำอยู่ข้างในเมื่อ a เป็น list เลยเขียนกางออกมาให้เห็นชัด ๆ พร้อมนับจำนวนครั้งที่ต้องเทียบ',
              },
              {
                t: "codeout",
                lang: "python",
                label: "เทียบสองวิธี พร้อมนับจำนวนครั้งที่ต้อง compare",
                code: `a = [4, 9, 5, 2, 7]
b = [9, 4, 9, 8, 4, 3]

# วิธีที่ 1 — เช็คด้วย list (กางไส้ในของ x in a ออกมาให้เห็น)
steps = 0
res = []
for x in b:
    found = False
    for y in a:                # ต้องไล่ทุกตัวใน a จนเจอ หรือจนหมดกอง
        steps += 1
        if x == y:
            found = True
            break
    if not found:
        res.append(x)
print("list :", res, "| เทียบ", steps, "ครั้ง")

# วิธีที่ 2 — เช็คด้วย set
a_set = set(a)                 # แปลงครั้งเดียว จ่าย memory เพิ่มหนึ่งก้อน
res2 = [x for x in b if x not in a_set]
print("set  :", res2, "| เทียบ", len(b), "ครั้ง")`,
                out: `list : [8, 3] | เทียบ 16 ครั้ง
set  : [8, 3] | เทียบ 6 ครั้ง`,
              },
              {
                t: "p",
                c: "16 ครั้งเหลือ 6 ครั้ง ยังไม่น่าตื่นเต้นเพราะกองเล็ก จุดสำคัญคือ ตัวเลขนี้โตต่างกันคนละแบบ: ฝั่ง list ยิ่ง a ใหญ่ขึ้น แต่ละครั้งที่ถามก็ต้องไล่นานขึ้นตามไปด้วย (จำนวนครั้ง ≈ ขนาด b × ขนาด a) แต่ฝั่ง set ไม่ว่า a จะมี 5 ตัวหรือ 5 ล้านตัว การถามหนึ่งครั้งใช้เวลาเท่าเดิม จำนวนครั้งจึงเป็นแค่ ขนาด b เท่านั้น ในภาษา Big-O คือ O(n·m) เทียบกับ O(n+m)",
              },
              {
                t: "callout",
                title: "ประโยคเดียวที่ต้องจำจากส่วนนี้",
                c: 'ถ้าในลูปมีการถามว่า "ตัวนี้อยู่ในกองนั้นไหม" หรือ "ตัวนี้เคยเจอมาก่อนไหม" ให้สงสัยทันทีว่ากองนั้นควรเป็น set หรือ dict ไม่ใช่ list — นี่คือสัญญาณที่พบบ่อยที่สุดของหมวดนี้',
              },
              {
                t: "h3",
                c: "ดูทีละขั้น (Interactive)",
              },
              {
                t: "p",
                c: "กด **Next ▶** ไล่ b = [9, 4, 9, 8, 4, 3] ถามว่ามีใน a = [4, 9, 5, 2, 7] ไหม · เฟสแรก list ไล่ a ใหม่ทุกตัว (ทอง = ช่วงที่เทียบ) · เฟสหลังแปลงเป็น set แล้วแต่ละตัวใน b ก้าวเดียว — นับจำนวนครั้งที่เทียบให้ทันว่าทำไม set ถึงคุ้ม",
              },
              { t: "viz", id: "hash-scan" },
              {
                t: "image",
                src: "/leetcode-75/hashmap-lookup.gif",
                alt: "Hash map two-sum style: store seen values, lookup complement in O(1)",
                caption:
                  "Pattern ที่เจอบ่อย: เก็บค่าที่เจอไว้ใน map แล้วถามว่า need = target−v มีไหม — เจอทันทีโดยไม่ต้องไล่ list",
              },

              { t: "h2", c: "ส่วนที่ 2 · มันหาเจอโดยไม่ต้องไล่ได้ยังไง" },
              {
                t: "p",
                c: "คำถามที่ต้องตอบให้ได้ก่อนจะเชื่อว่า O(1) เป็นเรื่องจริง: ถ้าไม่ไล่ดูทีละตัว แล้วมันรู้ได้ยังไงว่าของอยู่ไหน คำตอบคือ hash table ไม่ได้ \"ค้นหา\" ของ มัน คำนวณ ว่าของต้องอยู่ช่องไหน",
              },
              {
                t: "p",
                c: "ใน list ตำแหน่งของค่ากับตัวค่าไม่มีความสัมพันธ์กันเลย เลข 7 อาจอยู่ช่อง 0 หรือช่อง 900 ก็ได้ เราจึงไม่มีทางรู้ล่วงหน้า ต้องเดินดู แต่ hash table เปลี่ยนกฎข้อนี้: เอาตัวค่ามาผ่านฟังก์ชัน hash() เพื่อแปลงเป็นตัวเลข แล้ว mod ด้วยจำนวนช่องที่มี ผลลัพธ์คือหมายเลขช่องที่ของต้องไปอยู่ เวลาจะหาก็คำนวณสูตรเดียวกันอีกครั้ง แล้วเดินไปที่ช่องนั้นตรง ๆ",
              },
              {
                t: "codeout",
                lang: "python",
                label: "จาก key → ตัวเลข → หมายเลขช่อง (สมมติมี 8 ช่อง)",
                code: `for k in [7, 15, 23, 42, 100]:
    print(f"key {k:<4} hash = {hash(k):<4} -> ช่องที่ {hash(k) % 8}")`,
                out: `key 7    hash = 7    -> ช่องที่ 7
key 15   hash = 15   -> ช่องที่ 7
key 23   hash = 23   -> ช่องที่ 7
key 42   hash = 42   -> ช่องที่ 2
key 100  hash = 100  -> ช่องที่ 4`,
              },
              {
                t: "image",
                src: "/leetcode-75/hashmap-lookup.png",
                alt: "Hash table lookup: key ผ่าน hash() แล้ว mod ได้หมายเลขช่อง แล้วเดินไปช่องนั้นตรง ๆ",
                caption: "Lookup ไม่ใช่การค้น — คำนวณช่องจาก key แล้วเดินไปตรง ๆ (ตัวอย่าง key = 42 → ช่อง 2)",
              },
              {
                t: "p",
                c: "ที่เลือกใช้ int เป็นตัวอย่างเพราะ hash ของจำนวนเต็มไม่ติดลบที่ไม่ใหญ่มาก เท่ากับตัวมันเองพอดี จึงเห็นความสัมพันธ์ key → ช่อง ได้ชัดที่สุด (ไม่ใช่กฎทั่วไปนะ เช่น hash(-1) ได้ -2 ไม่ใช่ -1) ส่วน hash ของ str จะเป็นเลขยาว ๆ และ เปลี่ยนทุกครั้งที่รันโปรแกรมใหม่ ด้วยเหตุผลด้านความปลอดภัย ลอง print(hash(\"apple\")) จากสองการรันจะได้ค่าไม่เหมือนกัน — ห้ามเขียนโค้ดที่พึ่งพาค่า hash ตรง ๆ",
              },
              {
                t: "p",
                c: 'สังเกตผลลัพธ์ให้ดี: 7, 15, 23 ได้ช่องที่ 7 เหมือนกันหมด เหตุการณ์นี้เรียกว่า collision (การชนกัน) และมันเลี่ยงไม่ได้ เพราะเรากำลังยัดค่าที่เป็นไปได้ไม่จำกัด ลงในช่องจำนวนจำกัด เมื่อชนกัน Python จะไปหาช่องว่างถัดไปตามสูตรของมัน แล้วตอนค้นก็เดินตามสูตรเดิมพร้อมเทียบด้วย == ไปเรื่อย ๆ จนเจอตัวจริงหรือเจอช่องว่าง — แปลว่าการ lookup หนึ่งครั้ง จริง ๆ ไม่ใช่ "หนึ่งก้าว" เสมอไป',
              },
              {
                t: "image",
                src: "/leetcode-75/hashmap-collision.png",
                alt: "Hash collision: key หลายตัวได้ช่องเดียวกัน ต้องหาช่องว่างถัดไปแล้วเทียบด้วย ==",
                caption: "Collision: 7, 15, 23 ได้ช่อง 7 เหมือนกัน — ตัวแรกอยู่ช่อง 7 ที่เหลือถูกโยกไปช่องว่างถัดไป ตอนค้นต้องเทียบ == ด้วย ไม่ใช่เชื่อแค่หมายเลขช่อง",
              },
              {
                t: "p",
                c: "และนี่คือเหตุผลที่ทุกตารางเขียนว่า O(1) เฉลี่ย ไม่ใช่ O(1) เฉย ๆ Python คุมให้เฉลี่ยแล้วเร็วด้วยการขยายจำนวนช่องอัตโนมัติเมื่อของเริ่มแน่น (resize) เพื่อให้ยังมีที่ว่างเหลือเยอะ โอกาสชนจึงต่ำและโซ่การชนสั้น",
              },
              {
                t: "h3",
                c: "ดูทีละขั้น (Interactive)",
              },
              {
                t: "p",
                c: "กด **Next ▶** ใส่ 42, 100, 7, 15 ลงตาราง 8 ช่อง แล้วค้น 15 กับ 42 · ลูกศรทอง = ช่องที่สูตรชี้ · ส้ม = กำลังเขียน / ชนแล้วเดินต่อ · เขียว = เจอ — ดูจังหวะที่ 15 ชนช่อง 7 แล้วไปอยู่ช่อง 0 แต่ตอนค้นยังเริ่มที่ช่อง 7 ตามสูตรเดิม",
              },
              { t: "viz", id: "hash-slot" },

              { t: "h3", c: "worst case O(n) หน้าตาเป็นยังไง" },
              {
                t: "p",
                c: "ถ้า key ทุกตัว hash ออกมาได้เลขเดียวกันหมด ของก็จะไปกองอยู่บริเวณเดียวกัน แล้วการหาหนึ่งครั้งต้องเทียบเกือบทุกตัว = กลับไปเป็น O(n) เหมือน list สร้างสถานการณ์นี้ให้ดูได้จริงโดยเขียน class ที่ __hash__ คืนค่าเดิมทุกครั้ง แล้วนับว่าการหาหนึ่งครั้งเรียก == ไปกี่รอบ",
              },
              {
                t: "codeout",
                lang: "python",
                label: "หลักฐานว่า collision ทำให้ O(1) พังจริง",
                code: `class Key:
    calls = 0                          # ตัวนับกลาง ว่าถูกเทียบ == ไปกี่ครั้ง

    def __init__(self, v, h):
        self.v, self.h = v, h

    def __hash__(self):
        return self.h                  # กำหนดค่า hash เองได้ตามใจ

    def __eq__(self, other):
        Key.calls += 1
        return self.v == other.v


# กรณีปกติ — hash ต่างกัน ของกระจายไปคนละช่อง
s_good = {Key(i, i) for i in range(1000)}
Key.calls = 0                          # รีเซ็ตหลังสร้างเสร็จ นับแค่ตอนค้น
Key(999, 999) in s_good
print("hash กระจายดี  : เทียบ ==", Key.calls, "ครั้ง")

# กรณีแย่สุด — hash เป็น 0 ทุกตัว ของไปกองที่เดียว
s_bad = {Key(i, 0) for i in range(1000)}
Key.calls = 0
Key(999, 0) in s_bad
print("hash ชนกันหมด : เทียบ ==", Key.calls, "ครั้ง")`,
                out: `hash กระจายดี  : เทียบ == 1 ครั้ง
hash ชนกันหมด : เทียบ == 1345 ครั้ง`,
              },
              {
                t: "callout",
                title: "เรื่องนี้กระทบกับ LeetCode ไหม",
                c: 'ในทางปฏิบัติแทบไม่ต้องกังวล เพราะ hash ที่ Python ให้มาสำหรับ int, str, tuple กระจายดีอยู่แล้ว โจทย์ LC75 ทั้งหมวดใช้ O(1) เฉลี่ยได้อย่างสบายใจ แต่ต้องรู้ไว้เพราะ (1) เวลาตอบ interview ว่า "O(1)" ควรพูดคำว่า "average case" ต่อท้ายด้วย และ (2) ถ้าวันหนึ่งเขียน __hash__ ของ class ตัวเองแล้วเขียนพลาด นี่คืออาการที่จะเจอ',
              },

              { t: "h2", c: "ส่วนที่ 3 · กฎข้อเดียวเรื่อง key — ต้อง hashable" },
              {
                t: "p",
                c: "จากกลไกในส่วนที่ 2 จะเห็นว่าตำแหน่งที่เก็บของขึ้นอยู่กับค่า hash ของ key ดังนั้นถ้าค่าของ key เปลี่ยนได้หลังใส่ลงไปแล้ว ค่า hash จะเปลี่ยน ช่องที่ควรอยู่ก็เปลี่ยน ผลคือของหายไปจากตารางแบบหาไม่เจออีกเลย Python จึงตัดปัญหาด้วยการห้ามใช้ของที่แก้ไขได้ (mutable) เป็น key ตั้งแต่ต้น — คุณสมบัตินี้เรียกว่า hashable",
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของที่ใช้เป็น key ได้ กับที่ไม่ได้",
                code: `d = {}
d[3] = "int ได้"
d["abc"] = "str ได้"
d[(1, 2)] = "tuple ได้"        # tuple แก้ไม่ได้ -> ใช้ได้
print(d)

try:
    d[[1, 2]] = "list?"        # list แก้ได้ -> ใช้ไม่ได้
except TypeError as e:
    print("list  ->", type(e).__name__ + ":", e)

try:
    d[{1, 2}] = "set?"         # set ก็แก้ได้ -> ใช้ไม่ได้ (แต่ frozenset ได้)
except TypeError as e:
    print("set   ->", type(e).__name__ + ":", e)`,
                out: `{3: 'int ได้', 'abc': 'str ได้', (1, 2): 'tuple ได้'}
list  -> TypeError: unhashable type: 'list'
set   -> TypeError: unhashable type: 'set'`,
              },
              {
                t: "p",
                c: "คำว่า unhashable type: 'list' คือ error ที่จะเจอบ่อยมากในหมวดนี้ และทางแก้เกือบทุกครั้งคือประโยคเดียว: แปลง list เป็น tuple ก่อนใช้เป็น key เพราะ tuple สองตัวที่มีค่าเหมือนกันจะมี hash เท่ากัน จึงใช้เทียบกันได้ (เรื่องนี้จะกลายเป็นแบบที่ 4 ในส่วนถัดไป) — แต่มีข้อแม้ที่ต้องรู้: tuple ไม่ได้ hashable โดยอัตโนมัติ มันจะ hashable เฉพาะเมื่อ ของทุกชิ้นข้างในเป็น hashable ด้วย ดังนั้น tuple([1, 2]) ใช้ได้ แต่ tuple ที่มี list ซ้อนอยู่ข้างใน เช่น (1, [2]) ยังขึ้น unhashable type: 'list' เหมือนเดิม ถ้าเจอ list ซ้อนหลายชั้นต้องแปลงให้เป็น tuple ทุกชั้น",
              },
              {
                t: "codeout",
                lang: "python",
                label: "ข้อแม้ของ tuple — hashable เฉพาะเมื่อของข้างในทุกชิ้น hashable",
                code: `rows = [[1, 2], [3, 4], [1, 2]]
print("tuple ธรรมดา ใช้ได้      :", {tuple(r) for r in rows})

nested = [[1, [2]], [3, [4]]]
try:
    {tuple(r) for r in nested}
except TypeError as e:
    print("tuple ที่มี list ซ้อน    ->", type(e).__name__ + ":", e)

def deep(x):
    return tuple(deep(i) for i in x) if isinstance(x, list) else x

print("แปลงเป็น tuple ทุกชั้น    :", {deep(r) for r in nested})`,
                out: `tuple ธรรมดา ใช้ได้      : {(1, 2), (3, 4)}
tuple ที่มี list ซ้อน    -> TypeError: unhashable type: 'list'
แปลงเป็น tuple ทุกชั้น    : {(1, (2,)), (3, (4,))}`,
              },

              { t: "h3", c: "กับดักที่ไม่มีใครบอก: 1, True และ 1.0 คือ key เดียวกัน" },
              {
                t: "p",
                c: "การเป็น key เดียวกันไม่ได้ดูที่ type แต่ดูที่ hash เท่ากัน และ == เป็นจริง ซึ่ง 1, True, 1.0 เข้าเงื่อนไขนี้ทั้งสามตัว",
              },
              {
                t: "codeout",
                lang: "python",
                code: `d = {}
d[1] = "หนึ่ง"
d[True] = "จริง"
d[1.0] = "หนึ่งจุดศูนย์"
print(d)                                        # เหลือ key เดียว!
print("1 == True == 1.0 :", 1 == True == 1.0)
print("hash เท่ากันหมด  :", hash(1) == hash(True) == hash(1.0))`,
                out: `{1: 'หนึ่งจุดศูนย์'}
1 == True == 1.0 : True
hash เท่ากันหมด  : True`,
              },
              {
                t: "p",
                c: "สังเกตว่า key ที่แสดงคือ 1 (ตัวแรกที่ใส่) แต่ value เป็นของตัวสุดท้าย เพราะการใส่ครั้งหลังนับเป็นการ update ไม่ใช่การเพิ่ม key ใหม่ อาการนี้พังเงียบ ๆ ไม่มี error ให้เห็น",
              },

              { t: "h2", c: "ส่วนที่ 4 · เครื่องมือ 4 ชิ้น เลือกให้ถูกก่อน" },
              {
                t: "p",
                c: "ทุกอย่างในหมวดนี้ใช้ hash table เหมือนกันหมด ต่างกันแค่ \"เก็บอะไรพ่วงไปกับ key\" และ \"ทำอะไรให้เราฟรี\" ตารางนี้คือทั้งหมดที่ต้องรู้",
              },
              {
                t: "table",
                head: ["เครื่องมือ", "เก็บอะไร", "ได้อะไรฟรี", "หยิบมาใช้เมื่อ"],
                rows: [
                  [
                    "set",
                    "key เท่านั้น",
                    "ตัดตัวซ้ำ + operator เทียบสองกอง (& | - ^)",
                    'คำถามคือ "มีไหม" ไม่ใช่ "มีเท่าไหร่"',
                  ],
                  [
                    "dict",
                    "key → value อะไรก็ได้",
                    "จำลำดับที่ใส่ (Python 3.7+)",
                    "ต้องจำข้อมูลพ่วง เช่น index ที่เจอ",
                  ],
                  [
                    "Counter",
                    "key → จำนวนครั้ง",
                    "นับให้ในบรรทัดเดียว, key ที่ไม่มีคืน 0 ไม่ error, most_common()",
                    "โจทย์คือ \"นับ\" ล้วน ๆ",
                  ],
                  [
                    "defaultdict",
                    "key → ค่าเริ่มต้นตามชนิดที่บอก",
                    "ไม่ต้องเช็คก่อนว่ามี key ไหม",
                    "value เป็นกอง เช่น list หรือ set",
                  ],
                ],
              },
              {
                t: "codeout",
                lang: "python",
                label: "การนับความถี่ทำได้ 4 ท่า ผลเหมือนกันหมด",
                code: `from collections import defaultdict, Counter

words = ["a", "b", "a", "c", "a", "b"]

c1 = {}                          # 1) เช็คก่อนว่ามี key ไหม
for w in words:
    if w not in c1:
        c1[w] = 0
    c1[w] += 1

c2 = {}                          # 2) .get(key, ค่าเริ่มต้น) — สั้นกว่า ไม่ต้อง if
for w in words:
    c2[w] = c2.get(w, 0) + 1

c3 = defaultdict(int)            # 3) key ที่ไม่มี ให้เริ่มที่ int() = 0 ให้เอง
for w in words:
    c3[w] += 1

c4 = Counter(words)              # 4) นับให้เสร็จในบรรทัดเดียว

print("1) if/else       :", c1)
print("2) .get(w, 0)    :", c2)
print("3) defaultdict   :", dict(c3))
print("4) Counter       :", c4)
print("ผลเหมือนกันหมด    :", c1 == c2 == dict(c3) == dict(c4))`,
                out: `1) if/else       : {'a': 3, 'b': 2, 'c': 1}
2) .get(w, 0)    : {'a': 3, 'b': 2, 'c': 1}
3) defaultdict   : {'a': 3, 'b': 2, 'c': 1}
4) Counter       : Counter({'a': 3, 'b': 2, 'c': 1})
ผลเหมือนกันหมด    : True`,
              },
              {
                t: "callout",
                title: "จุดที่ต้องระวังที่สุดของ set",
                warn: true,
                c: "set ไม่รักษาลำดับ ถ้าเป็น str ลำดับจะสลับไปเรื่อย ๆ ทุกครั้งที่รันโปรแกรม (เพราะ hash ของ str สุ่มใหม่ทุกการรัน) ห้ามเขียนโค้ดที่พึ่งลำดับของ set เด็ดขาด — ถ้าต้องตัดซ้ำแต่ยังต้องคงลำดับเดิม ให้ใช้ list(dict.fromkeys(nums)) แทน เพราะ dict การันตีลำดับการใส่ และเพราะข้อจำกัดนี้ โจทย์ที่คำตอบเป็น set มักเขียนกำกับว่า \"ลำดับใน output ไม่สำคัญ\" (in any order) ซึ่งเป็นสัญญาณอย่างหนึ่งว่าใช้ set ได้",
              },

              { t: "h3", c: "อ่าน key ที่ไม่มี — สามเครื่องมือให้ผลต่างกัน" },
              {
                t: "codeout",
                lang: "python",
                code: `from collections import Counter

count = {"a": 3}
print('count.get("z", 0)     :', count.get("z", 0))      # ปลอดภัย คืนค่า default
print('Counter("aaa")["z"]   :', Counter("aaa")["z"])     # Counter คืน 0 ให้เอง
try:
    print(count["z"])                                     # dict ธรรมดา -> พัง
except KeyError as e:
    print('count["z"]            : KeyError', e)`,
                out: `count.get("z", 0)     : 0
Counter("aaa")["z"]   : 0
count["z"]            : KeyError 'z'`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "แต่ defaultdict สร้าง key ขึ้นมาเองแค่จากการ \"อ่าน\"",
                code: `from collections import defaultdict

d = defaultdict(int)
print('"z" in d ก่อนอ่าน :', "z" in d)
_ = d["z"]                          # แค่อ่านค่า ไม่ได้ assign อะไรเลย
print('"z" in d หลังอ่าน :', "z" in d, "<- แค่อ่านก็โดนสร้างแล้ว")
print("d =", dict(d), "| len(d) =", len(d))`,
                out: `"z" in d ก่อนอ่าน : False
"z" in d หลังอ่าน : True <- แค่อ่านก็โดนสร้างแล้ว
d = {'z': 0} | len(d) = 1`,
              },
              {
                t: "p",
                c: "ถ้าโจทย์ให้ตอบว่า \"มีค่าที่ต่างกันกี่ตัว\" แล้วเผลอ print(d[x]) เพื่อ debug ระหว่างทาง len(d) จะเพี้ยนขึ้นมาเงียบ ๆ ส่วน Counter ไม่มีปัญหานี้ — คืน 0 แต่ ไม่ สร้าง key",
              },

              { t: "h2", c: "ส่วนที่ 5 · 6 แบบการใช้งานที่ต้องแยกให้ออก" },
              {
                t: "p",
                c: 'นี่คือหัวใจของหน้านี้ เครื่องมือมีแค่ 4 ชิ้นตามส่วนที่ 4 แต่ คำถามที่เราเอาไปถามมัน มี 6 แบบที่ต่างกันชัดเจน คนที่ติดหมวดนี้ส่วนใหญ่ไม่ได้ติดเพราะไม่รู้จัก dict แต่ติดเพราะจำได้แค่ "โจทย์นี้ใช้ hash map" แล้วไม่รู้ว่าจะเอา hash map ไปถามอะไร',
              },
              {
                t: "p",
                c: "แต่ละแบบข้างล่างจะมี 3 ส่วนเหมือนกัน: คำถามที่โจทย์ถาม → โครงโค้ด (แม่แบบ ยังรันไม่ได้) → ของจริงที่รันได้ อ่านให้จบทั้ง 6 แบบก่อนเริ่มทำโจทย์ แล้วเวลาเจอโจทย์จะถามตัวเองได้ว่า \"นี่มันแบบไหน\"",
              },

              { t: "h3", c: "แบบที่ 1 · Membership — \"ตัวนี้อยู่ในกองนั้นไหม\"" },
              {
                t: "p",
                c: "แบบที่ง่ายและพบบ่อยที่สุด ใช้ตอนที่สนใจแค่ มี/ไม่มี ไม่สนจำนวนและไม่สนตำแหน่ง เครื่องมือคือ set ล้วน ๆ กุญแจอยู่ที่ต้องสร้าง set ให้เสร็จ ก่อน เข้าลูป ไม่ใช่สร้างใหม่ทุกรอบ",
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 1",
                c: `pool = set(กองที่จะเอาไว้เทียบ)      # สร้างครั้งเดียว ก่อนเข้าลูป
for x in data:
    if x in pool:                    # O(1) ทุกครั้งที่ถาม
        ...`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — หาตัวที่มีในกองหนึ่งแต่ไม่มีในอีกกอง",
                code: `a, b = [4, 9, 5], [9, 8, 4, 3]

seen = set(a)
print("ตัวใน b ที่ไม่มีใน a  :", [x for x in b if x not in seen])

# ถ้าโจทย์ไม่ต้องการตัวซ้ำในคำตอบ ใช้ operator ของ set ได้เลย สั้นกว่า
print("แบบตัดซ้ำด้วย set    :", set(b) - set(a))`,
                out: `ตัวใน b ที่ไม่มีใน a  : [8, 3]
แบบตัดซ้ำด้วย set    : {8, 3}`,
              },
              {
                t: "callout",
                title: "แบบที่ 1 · สรุป",
                c: "เจอในข้อ 20 · Find the Difference of Two Arrays (ทำสองทิศ: set(a)-set(b) และ set(b)-set(a)) · กับดัก: ลืมว่าผลลัพธ์เป็น set จึงไม่มีลำดับ ถ้าโจทย์ขอ list ต้อง list(...) ก่อน",
              },

              { t: "h3", c: "แบบที่ 2 · Frequency — \"แต่ละตัวโผล่กี่ครั้ง\"" },
              {
                t: "p",
                c: "ขยับจาก \"มี/ไม่มี\" เป็น \"มีเท่าไหร่\" พอต้องเก็บจำนวน set ก็ไม่พอแล้ว ต้องใช้ dict ที่ value เป็นตัวเลข ซึ่ง Counter ทำให้เสร็จในบรรทัดเดียว",
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 2",
                c: `from collections import Counter

count = Counter(data)      # นับทั้งกองให้เสร็จในบรรทัดเดียว
count[x]                   # x โผล่กี่ครั้ง — ถ้าไม่มีจะได้ 0 ไม่ error`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — นับตัวอักษร",
                code: `from collections import Counter

c = Counter("abracadabra")
print("Counter         :", c)              # เรียงจากบ่อยสุดให้เลย
print('c["a"] / c["z"]  :', c["a"], "/", c["z"])
print("บ่อยสุด 2 ตัว     :", c.most_common(2))`,
                out: `Counter         : Counter({'a': 5, 'b': 2, 'r': 2, 'c': 1, 'd': 1})
c["a"] / c["z"]  : 5 / 0
บ่อยสุด 2 ตัว     : [('a', 5), ('b', 2)]`,
              },
              {
                t: "callout",
                title: "แบบที่ 2 · สรุป",
                c: "เป็นฐานของแบบที่ 3 และ 4 · ใช้ Counter(data) ได้กับทุกอย่างที่วนได้ ทั้ง str, list, generator · กับดัก: Counter คืน 0 ให้ key ที่ไม่มี ซึ่งสะดวก แต่ทำให้เงื่อนไข if c[x] เป็นเท็จได้ทั้งกรณี \"ไม่มี\" และกรณี \"มี 0 ตัว\" ต้องแยกให้ออกถ้าโจทย์แยก",
              },

              { t: "h3", c: "แบบที่ 3 · Count of counts — \"รูปแบบของจำนวนครั้ง เป็นยังไง\"" },
              {
                t: "p",
                c: 'แบบนี้คือจุดที่คนพลาดมากที่สุดในหมวด เพราะโจทย์ไม่ได้ถามถึง ตัวข้อมูล อีกแล้ว แต่ถามถึง จำนวนครั้ง ที่นับได้ ท่าคือนับด้วย Counter ก่อน แล้ว โยน key ทิ้ง เหลือแต่ .values() มาถามคำถามชั้นที่สอง',
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 3",
                c: `from collections import Counter

count = Counter(data)
vals = list(count.values())      # ทิ้ง key ทั้งหมด เหลือแต่ "จำนวนครั้ง"

# แล้วค่อยถามคำถามชั้นที่สองกับ vals เช่น
len(set(vals)) == len(vals)      # จำนวนครั้งของทุกตัว ไม่ซ้ำกันเลย ใช่ไหม
sorted(vals) == sorted(other)    # รูปแบบความถี่ของสองกอง ตรงกันไหม`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — จำนวนครั้งของทุกตัวไม่ซ้ำกันเลยใช่ไหม",
                code: `from collections import Counter

for s in ["aabbbc", "aabbc"]:
    c = Counter(s)
    v = list(c.values())
    print(f"{s:<8} -> {dict(c)}")
    print(f"{'':8}    จำนวนครั้ง = {sorted(v)} | ไม่ซ้ำกันเลยไหม: {len(set(v)) == len(v)}")`,
                out: `aabbbc   -> {'a': 2, 'b': 3, 'c': 1}
            จำนวนครั้ง = [1, 2, 3] | ไม่ซ้ำกันเลยไหม: True
aabbc    -> {'a': 2, 'b': 2, 'c': 1}
            จำนวนครั้ง = [1, 2, 2] | ไม่ซ้ำกันเลยไหม: False`,
              },
              {
                t: "p",
                c: "อ่านบรรทัด len(set(v)) == len(v) ให้ออก: set(v) ตัดตัวซ้ำใน v ทิ้ง ถ้าความยาวยังเท่าเดิมแปลว่าไม่มีตัวซ้ำตั้งแต่แรก — นี่คือท่ามาตรฐานสำหรับเช็ค \"ทุกตัวไม่ซ้ำกันเลยไหม\" ซึ่งจะได้ใช้อีกหลายที่ สังเกตว่าเราใช้ set ซ้อนอยู่บน Counter คือใช้สองเครื่องมือคนละชั้นในโจทย์เดียว",
              },
              {
                t: "callout",
                title: "แบบที่ 3 · สรุป",
                c: "เจอในข้อ 21 · Unique Number of Occurrences (ใช้ len(set(v)) == len(v)) และข้อ 22 · Determine if Two Strings Are Close (ใช้ sorted(x.values()) == sorted(y.values()) คู่กับ set(x) == set(y)) · กุญแจของทั้งสองข้อคือรู้ว่าต้อง \"นับแล้วนับอีกชั้น\" ไม่ใช่นับชั้นเดียว",
              },

              { t: "h3", c: "แบบที่ 4 · Composite key — \"ของหลายชิ้นรวมเป็น key เดียว\"" },
              {
                t: "p",
                c: "ใช้ตอนที่สิ่งที่อยากเทียบไม่ใช่ค่าเดี่ยว แต่เป็นทั้งก้อน เช่นทั้งแถวของตาราง หรือคู่ (x, y) ปัญหาคือ list ใช้เป็น key ไม่ได้ (ส่วนที่ 3) ทางแก้คือแปลงเป็น tuple แล้วทั้งก้อนจะกลายเป็น key เดียวที่เทียบกันได้ในหนึ่ง lookup",
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 4",
                c: `from collections import Counter

count = Counter(tuple(item) for item in data)   # list -> tuple ก่อนใช้เป็น key
count[tuple(อีกก้อนที่อยากเทียบ)]                 # เทียบทั้งก้อนใน O(1) ครั้งเดียว`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — นับว่ามีคู่ (แถว, หลัก) ที่หน้าตาเหมือนกันกี่คู่",
                code: `from collections import Counter

grid = [[3, 2, 1],
        [1, 7, 6],
        [2, 7, 7]]

rowc = Counter(tuple(r) for r in grid)     # นับแถว โดยใช้ทั้งแถวเป็น key
print("นับแต่ละแถว :", rowc)

cols = list(zip(*grid))                    # zip(*grid) = พลิกตาราง ได้หลักออกมา
print("หลักทั้งหมด :", cols)

for col in cols:
    print(f"   หลัก {col} -> มีแถวหน้าตาเดียวกัน {rowc[col]} แถว")

print("คำตอบ =", sum(rowc[col] for col in cols))`,
                out: `นับแต่ละแถว : Counter({(3, 2, 1): 1, (1, 7, 6): 1, (2, 7, 7): 1})
หลักทั้งหมด : [(3, 1, 2), (2, 7, 7), (1, 6, 7)]
   หลัก (3, 1, 2) -> มีแถวหน้าตาเดียวกัน 0 แถว
   หลัก (2, 7, 7) -> มีแถวหน้าตาเดียวกัน 1 แถว
   หลัก (1, 6, 7) -> มีแถวหน้าตาเดียวกัน 0 แถว
คำตอบ = 1`,
              },
              {
                t: "p",
                c: "ทั้งข้อจบในโค้ดข้างบนแล้ว ที่มันดูง่ายเพราะย้ายภาระไปให้ hash map ทั้งหมด: ถ้าไม่ใช้ท่านี้ต้องเทียบทุกแถวกับทุกหลักทีละช่อง = สามลูปซ้อน แต่พอทำแถวให้เป็น key ก้อนเดียว การเทียบหนึ่งครั้งเหลือ O(1) เลยเหลือแค่ลูปเดียว",
              },
              {
                t: "callout",
                title: "แบบที่ 4 · สรุป",
                c: "เจอในข้อ 23 · Equal Row and Column Pairs · จำ: แปลงแถวเป็น tuple(...) เพื่อให้ hashable แล้วใช้ Counter นับ · ประกอบคอลัมน์ด้วย tuple(grid[i][j] for i in ...) หรือ zip(*grid) ก็ได้ · กับดัก: ต้องบวกด้วย rowc[col] ไม่ใช่บวก 1 เพราะแถวหน้าตาเดียวกันอาจมีหลายแถว แต่ละแถวนับเป็นหนึ่งคู่",
              },

              { t: "h3", c: "แบบที่ 5 · Seen-so-far — \"เพื่อนที่ฉันต้องการ เดินผ่านมาแล้วหรือยัง\"" },
              {
                t: "p",
                c: 'สี่แบบก่อนหน้าสร้าง hash map ให้เสร็จก่อนแล้วค่อยใช้ แบบที่ 5 ต่างออกไป — มัน ใช้ระหว่างที่ยังสร้างไม่เสร็จ วนข้อมูลรอบเดียว แต่ละก้าวถามว่า "ของที่ฉันต้องการ เคยเดินผ่านมาก่อนหรือยัง" ถ้าเคยก็จบ ถ้ายังก็จดตัวเองลงไปให้คนหลังมาเจอ คนเรียกท่านี้ว่า one-pass',
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 5",
                c: `seen = {}                       # หรือ set() ถ้าไม่ต้องจำ index
for i, x in enumerate(data):
    if สิ่งที่ต้องการ in seen:        # 1) ถามก่อน
        ...                         #    เจอแล้ว จบได้เลย
    seen[x] = i                     # 2) แล้วค่อยจดตัวเอง — สลับสองบรรทัดนี้ไม่ได้`,
              },
              {
                t: "p",
                c: 'ตัวอย่างที่ดีที่สุดคือ LC1 Two Sum ซึ่งเป็นข้อที่หน้า Two Pointers ทิ้งค้างไว้ว่า "ทำด้วย two pointers ไม่ได้เพราะโจทย์ขอ index แต่การ sort ทำลาย index เดิม" — แบบที่ 5 แก้ได้พอดี เพราะมันจำได้ว่าค่าไหนอยู่ index ไหน จึงไม่ต้อง sort เลย',
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — LC1 Two Sum วนรอบเดียว ไม่ sort จึงไม่เสีย index",
                code: `nums = [3, 9, 4, 1]
target = 12
seen = {}                       # ค่า -> index ที่เคยเจอ

for i, x in enumerate(nums):
    need = target - x           # ถ้าจะครบ target ต้องมีเพื่อนหน้าตาแบบนี้
    print(f"i={i} x={x} | ต้องการคู่ {need} | seen={seen} -> เคยเจอไหม {need in seen}")
    if need in seen:
        print("คำตอบ index:", (seen[need], i))
        break
    seen[x] = i                 # ยังไม่เจอ -> จดตัวเองไว้ให้คนหลังมาเจอ`,
                out: `i=0 x=3 | ต้องการคู่ 9 | seen={} -> เคยเจอไหม False
i=1 x=9 | ต้องการคู่ 3 | seen={3: 0} -> เคยเจอไหม True
คำตอบ index: (0, 1)`,
              },
              {
                t: "h3",
                c: "ดูทีละขั้น (Interactive)",
              },
              {
                t: "p",
                c: "กด **Next ▶** ไล่ nums = [3, 9, 4, 1] เป้า 12 · ทอง = ตัวที่กำลังยืน · เขียวใน seen = ค่าที่ need กำลังถาม · วงแหวนเขียว = คู่ที่เจอ — สังเกตว่าถามก่อน แล้วค่อยจด เลยไม่จับคู่กับตัวเอง",
              },
              { t: "viz", id: "hash-seen" },
              {
                t: "callout",
                title: "แบบที่ 5 · สรุป",
                warn: true,
                c: "กับดักสำคัญคือลำดับของสองบรรทัดสุดท้าย: ต้อง เช็คก่อน แล้วค่อย จด ถ้าสลับกัน ตัวที่กำลังยืนอยู่จะไปเจอ ตัวเอง ในกรณีที่ x บวกกับตัวเองได้ target พอดี เช่น nums = [6, 3], target = 12 จะตอบ (0, 0) ซึ่งผิด · แบบนี้ไม่มีในโจทย์ 4 ข้อของหมวดนี้ตรง ๆ แต่จะโผล่ในหมวด Prefix Sum, Sliding Window และโจทย์สัมภาษณ์บ่อยมาก",
              },

              { t: "h3", c: "แบบที่ 6 · Grouping — \"จัดของเข้ากลุ่มตาม key ที่คำนวณเอง\"" },
              {
                t: "p",
                c: "ห้าแบบก่อนหน้าใช้ value เป็นตัวเลขหรือไม่มี value เลย แบบที่ 6 ให้ value เป็น กอง (list หรือ set) แล้วโยนของที่ key เดียวกันไปกองรวมกัน จุดที่ต่างจากแบบอื่นคือ key ไม่ได้อยู่ในข้อมูลเดิม แต่เรา คำนวณขึ้นมา เอง — ของสองชิ้นที่คำนวณ key ได้เท่ากัน ถือว่าอยู่กลุ่มเดียวกัน",
              },
              {
                t: "code",
                lang: "python",
                label: "โครงของแบบที่ 6",
                c: `from collections import defaultdict

groups = defaultdict(list)          # key ที่ยังไม่มี จะเริ่มที่ [] ให้เอง
for item in data:
    key = คำนวณ key จาก item        # นี่คือส่วนที่ต้องคิดของแต่ละโจทย์
    groups[key].append(item)        # ของ key เดียวกันไปกองรวมกันเอง`,
              },
              {
                t: "codeout",
                lang: "python",
                label: "ของจริง — จัดกลุ่มคำที่สลับตัวอักษรกันได้ (anagram)",
                code: `from collections import defaultdict

words = ["eat", "tea", "tan", "ate", "nat", "bat"]
groups = defaultdict(list)

for w in words:
    key = "".join(sorted(w))        # เรียงตัวอักษร -> คำที่สลับกันได้จะได้ key เดียวกัน
    groups[key].append(w)
    print(f"{w} -> key '{key}'")

print("ผลลัพธ์:", list(groups.values()))`,
                out: `eat -> key 'aet'
tea -> key 'aet'
tan -> key 'ant'
ate -> key 'aet'
nat -> key 'ant'
bat -> key 'abt'
ผลลัพธ์: [['eat', 'tea', 'ate'], ['tan', 'nat'], ['bat']]`,
              },
              {
                t: "p",
                c: "หัวใจอยู่ที่บรรทัด key = \"\".join(sorted(w)) — การเลือกว่าจะคำนวณ key จากอะไร คือทั้งหมดของโจทย์แนวนี้ ถ้าเลือก key ถูก ที่เหลือคือ append เข้า defaultdict ซึ่งเหมือนกันทุกข้อ",
              },
              {
                t: "callout",
                title: "แบบที่ 6 · สรุป",
                c: "ไม่มีในโจทย์ 4 ข้อของหมวดนี้ แต่ต้องรู้ไว้เพราะมันคือรูปแบบเดียวกับการสร้าง adjacency list ในหมวด Graph (defaultdict(list) แล้ว append เพื่อนบ้าน) ซึ่งจะใช้หนักมากในหมวด 12-13 · ถ้าเข้าใจแบบนี้ตอนนี้ ตอนถึง Graph จะเหลือแค่เรื่องการเดิน ไม่ต้องมาเรียนเรื่องเก็บข้อมูลใหม่",
              },

              { t: "h3", c: "ตารางสรุป 6 แบบ" },
              {
                t: "table",
                head: ["แบบ", "คำถามที่โจทย์ถาม", "เครื่องมือ", "บรรทัดที่เป็นหัวใจ", "ข้อในคอร์ส"],
                rows: [
                  [
                    "1 · Membership",
                    "ตัวนี้อยู่ในกองนั้นไหม",
                    "set",
                    "x in pool / set(b) - set(a)",
                    "20",
                  ],
                  [
                    "2 · Frequency",
                    "แต่ละตัวโผล่กี่ครั้ง",
                    "Counter",
                    "Counter(data)[x]",
                    "ฐานของ 21-23",
                  ],
                  [
                    "3 · Count of counts",
                    "รูปแบบของจำนวนครั้ง เป็นยังไง",
                    "Counter + set/sorted",
                    "len(set(c.values())) == len(c)",
                    "21, 22",
                  ],
                  [
                    "4 · Composite key",
                    "ของทั้งก้อนนี้ ซ้ำกับก้อนไหน",
                    "Counter ที่ key เป็น tuple",
                    "Counter(tuple(r) for r in data)",
                    "23",
                  ],
                  [
                    "5 · Seen-so-far",
                    "เพื่อนที่ต้องการ เดินผ่านมาแล้วหรือยัง",
                    "dict (ค่า → index)",
                    "if need in seen: ... แล้วค่อย seen[x] = i",
                    "LC1, หมวด 4",
                  ],
                  [
                    "6 · Grouping",
                    "ของพวกนี้จัดกลุ่มกันยังไง",
                    "defaultdict(list)",
                    "groups[คำนวณ key].append(item)",
                    "หมวด 12-13",
                  ],
                ],
              },
              {
                t: "callout",
                title: "วิธีเลือกแบบตอนเจอโจทย์จริง",
                c: "ไล่คำถามตามลำดับนี้: ต้องรู้จำนวนไหม ถ้าไม่ต้อง จบที่แบบที่ 1 · ถ้าต้อง โจทย์ถามถึงตัวข้อมูลหรือถามถึงตัวเลขที่นับได้ ถ้าถามถึงตัวเลขที่นับได้คือแบบที่ 3 ถ้าถามถึงตัวข้อมูลคือแบบที่ 2 · สิ่งที่จะเอาไปเป็น key เป็นก้อนหรือเป็นค่าเดี่ยว ถ้าเป็นก้อนคือแบบที่ 4 · ต้องรู้ตำแหน่งหรือต้องทำให้จบในรอบเดียวไหม ถ้าใช่คือแบบที่ 5 · ผลลัพธ์เป็นกลุ่มก้อนไหม ถ้าใช่คือแบบที่ 6",
              },

              { t: "h2", c: "ส่วนที่ 6 · เมื่อไหร่ที่ hash map ไม่ใช่คำตอบ" },
              {
                t: "p",
                c: "hash map เร็วเพราะมันโยนข้อมูลเรื่อง \"ลำดับ\" และ \"ความใกล้เคียง\" ทิ้งไปทั้งหมด เก็บไว้แค่ \"มี/ไม่มี\" กับ \"เท่าไหร่\" ดังนั้นโจทย์ที่ต้องใช้สองอย่างที่มันทิ้งไป จะใช้ท่านี้ไม่ได้",
              },
              {
                t: "table",
                head: ["สถานการณ์", "ทำไมใช้ไม่ได้", "ใช้อะไรแทน"],
                rows: [
                  [
                    'ถามเป็นช่วง เช่น "มีค่าระหว่าง 3 ถึง 7 กี่ตัว"',
                    "hash ตอบได้แค่ค่าที่เท่ากันเป๊ะ ๆ ไม่รู้ว่าใครใกล้ใคร ต้องไล่ทุกตัว = เท่ากับไม่ได้ใช้",
                    "sorted array + binary search (หมวด 15)",
                  ],
                  [
                    "ต้องหยิบตัวน้อยสุด/มากสุดซ้ำ ๆ ระหว่างที่ข้อมูลเปลี่ยนไปเรื่อย",
                    "หา min/max ใน dict คือ O(n) ทุกครั้ง ไม่มีทางลัด",
                    "heap / priority queue (หมวด 14)",
                  ],
                  [
                    "ต้องการ output ที่เรียงลำดับ",
                    "set ไม่มีลำดับ dict มีแค่ลำดับที่ใส่เข้าไป",
                    "sorted(...) ตอนท้าย หรือเปลี่ยนไปใช้ sort ตั้งแต่ต้น",
                  ],
                  [
                    "key เป็น list หรือของที่แก้ไขได้",
                    "unhashable — ใส่เป็น key ไม่ได้เลย",
                    "แปลงเป็น tuple / frozenset ก่อน (แบบที่ 4)",
                  ],
                  [
                    "domain แคบและรู้แน่ เช่น a-z เท่านั้น",
                    "ใช้ได้ แต่เปลืองกว่าที่จำเป็น เพราะ hash table กิน memory หลายเท่า",
                    "list ขนาด 26 แล้ว index ด้วย ord(c) - ord('a')",
                  ],
                ],
              },
              {
                t: "codeout",
                lang: "python",
                label: "ราคาของ memory ที่จ่ายไป (CPython 3.10)",
                code: `import sys

n = 1000
print("list(range(1000)) :", sys.getsizeof(list(range(n))), "bytes")
print("set(range(1000))  :", sys.getsizeof(set(range(n))), "bytes")
print("dict.fromkeys     :", sys.getsizeof(dict.fromkeys(range(n))), "bytes")`,
                out: `list(range(1000)) : 8056 bytes
set(range(1000))  : 32984 bytes
dict.fromkeys     : 36960 bytes`,
              },
              {
                t: "p",
                c: "ประมาณ 4 เท่า และเหตุผลคือสิ่งที่อธิบายในส่วนที่ 2: hash table ต้องเผื่อช่องว่างไว้เยอะเพื่อกันการชน ถ้าเก็บแน่นเต็มพอดีเหมือน list มันจะไม่เร็วอีกต่อไป นี่คือรูปธรรมของคำว่า \"แลก memory ซื้อเวลา\" ในย่อหน้าแรกของหน้านี้ ตัวเลขนี้แทบไม่เคยเป็นปัญหาบน LeetCode แต่ควรรู้ว่ามันคือของที่จ่ายไป",
              },

              { t: "h2", c: "ส่วนที่ 7 · กับดักที่เจอบ่อย" },
              {
                t: "ul",
                c: [
                  "เขียน if x in list ไว้ข้างในลูป → กลายเป็น O(n²) แบบไม่รู้ตัว ทั้งที่แก้ได้ด้วยการ set(...) ครั้งเดียวก่อนเข้าลูป นี่คือกับดักอันดับหนึ่งของหมวดนี้",
                  "พึ่งลำดับของ set → ผลจะไม่คงที่ (สำหรับ str จะเปลี่ยนทุกครั้งที่รันโปรแกรม) ถ้าต้องตัดซ้ำแต่คงลำดับ ใช้ list(dict.fromkeys(nums))",
                  "อ่าน d[key] ทั้งที่ key อาจไม่มี → KeyError ให้ใช้ d.get(key, ค่าเริ่มต้น) หรือเช็ค if key in d ก่อน (Counter ไม่พัง แต่ dict ธรรมดาพัง)",
                  "เผลออ่าน d[key] ของ defaultdict เพื่อ debug → key ถูกสร้างขึ้นมาจริง ทำให้ len(d) และการวนลูปเพี้ยนแบบไม่มี error เตือน",
                  "ใช้ list เป็น key → TypeError: unhashable type: 'list' แปลงเป็น tuple ก่อนเสมอ (แบบที่ 4)",
                  "ในแบบที่ 5 เขียน seen[x] = i ก่อนเช็ค → ตัวที่ยืนอยู่จะไปจับคู่กับตัวเอง ต้องเช็คให้เสร็จก่อนจดตัวเองลงไป",
                  "ลืมว่า 1, True, 1.0 คือ key เดียวกัน และ key in d เช็คแต่ฝั่ง key ไม่ได้เช็ค value (จะเช็ค value ต้องเขียน v in d.values() ซึ่งเป็น O(n) ไม่ใช่ O(1))",
                  "เพิ่มหรือลบ key ระหว่างที่กำลังวน for k in d → RuntimeError: dictionary changed size during iteration ถ้าต้องแก้ระหว่างวน ให้วนบนสำเนา เช่น for k in list(d)",
                ],
              },

              {
                t: "callout",
                title: "พร้อมแล้วไปต่อ",
                c: "หมวดนี้มี 4 ข้อ และแต่ละข้อแมปกับแบบในส่วนที่ 5 ตรง ๆ: 20 · Difference of Two Arrays = แบบที่ 1 → 21 · Unique Number of Occurrences = แบบที่ 3 → 22 · Two Strings Are Close = แบบที่ 3 สองชั้น → 23 · Equal Row and Column Pairs = แบบที่ 4 กดถัดไปเริ่มข้อแรกได้เลย",
              },
      ],
      en: [
    {
      t: "p",
      c: 'Problems in this section look different on the surface, but they all ask the same core questions: "have I seen this before?", "how many times does this appear?", "what overlaps between these two groups?" — answered naively by scanning the whole collection every time. A hash map answers them in near-constant time, no matter how large the data gets.',
    },
    {
      t: "p",
      c: 'It is not free — you pay with memory. You build an extra structure to "remember" what you have already seen. That trade is called a space-time tradeoff, and it is the heart of this whole section.',
    },
    {
      t: "callout",
      title: "How to read this page",
      c: 'Parts 1–2 are "why it is fast". Parts 3–4 are "what tools you get". Part 5 is the core — 6 usage patterns you must tell apart. If short on time, read parts 5 and 7 at minimum.',
    },

    { t: "h2", c: "Part 1 · The problem hash maps solve — see it first" },
    {
      t: "p",
      c: "Task: given two piles a and b, find values in b that do not appear in a at all. The naive way is to take each value from b and scan every value in a — which is exactly what `x in a` does when a is a list. Let's expand that and count comparisons.",
    },
    {
      t: "codeout",
      lang: "python",
      label: "Compare two approaches and count comparisons",
      code: `a = [4, 9, 5, 2, 7]
b = [9, 4, 9, 8, 4, 3]

# Approach 1 — check with a list (expand what \`x in a\` does)
steps = 0
res = []
for x in b:
    found = False
    for y in a:                # scan every item in a until found or exhausted
        steps += 1
        if x == y:
            found = True
            break
    if not found:
        res.append(x)
print("list :", res, "| compared", steps, "times")

# Approach 2 — check with a set
a_set = set(a)                 # convert once; pay one chunk of extra memory
res2 = [x for x in b if x not in a_set]
print("set  :", res2, "| compared", len(b), "times")`,
      out: `list : [8, 3] | compared 16 times
set  : [8, 3] | compared 6 times`,
    },
    {
      t: "p",
      c: "16 down to 6 is not exciting on a tiny pile. The point is how these numbers grow: with a list, as a grows, every query gets slower (≈ size(b) × size(a)). With a set, whether a has 5 or 5 million items, one query costs the same — total work is just size(b). In Big-O: O(n·m) vs O(n+m).",
    },
    {
      t: "callout",
      title: "One sentence to remember from this part",
      c: 'If a loop asks "is this in that pile?" or "have I seen this before?", immediately suspect that pile should be a set or dict, not a list — the most common signal in this section.',
    },
    {
      t: "h3",
      c: "Step through (Interactive)",
    },
    {
      t: "p",
      c: "Hit **Next ▶** and ask whether each value in b = [9, 4, 9, 8, 4, 3] sits in a = [4, 9, 5, 2, 7]. Phase 1 scans the list (gold = cells compared). Phase 2 converts to a set, then each query is one hop — watch the comparison count to see why the set pays for itself.",
    },
    { t: "viz", id: "hash-scan" },

    { t: "h2", c: "Part 2 · How it finds things without scanning" },
    {
      t: "p",
      c: 'The question you need answered before trusting O(1): if it does not scan item by item, how does it know where something is? A hash table does not "search" — it computes which slot the item must live in.',
    },
    {
      t: "p",
      c: "In a list, position and value are unrelated. The number 7 might sit at index 0 or 900 — you cannot know ahead of time, so you walk. A hash table changes the rule: run the value through hash(), then mod by the number of slots. That gives the slot index. To look it up later, compute the same formula and go straight there.",
    },
    {
      t: "codeout",
      lang: "python",
      label: "From key → number → slot (assume 8 slots)",
      code: `for k in [7, 15, 23, 42, 100]:
    print(f"key {k:<4} hash = {hash(k):<4} -> slot {hash(k) % 8}")`,
      out: `key 7    hash = 7    -> slot 7
key 15   hash = 15   -> slot 7
key 23   hash = 23   -> slot 7
key 42   hash = 42   -> slot 2
key 100  hash = 100  -> slot 4`,
    },
    {
      t: "image",
      src: "/leetcode-75/hashmap-lookup.png",
      alt: "Hash table lookup: key through hash() then mod to a slot, then walk straight to that slot",
      caption: "Lookup is not a search — compute the slot from the key and go straight there (example: key = 42 → slot 2)",
    },
    {
      t: "p",
      c: 'We use ints here because for small non-negative integers, hash(k) equals k, so key → slot is easiest to see (not a general rule — e.g. hash(-1) is -2, not -1). For str, hash is a large number and changes every process for security. Try print(hash("apple")) across two runs — do not write code that depends on raw hash values.',
    },
    {
      t: "p",
      c: 'Notice: 7, 15, and 23 all land in slot 7. That is a collision, and it is unavoidable when you map unbounded keys into finitely many slots. Python then probes for the next open slot; lookup follows the same probe sequence comparing with == until it finds the real key or an empty slot — so one lookup is not always "one step".',
    },
    {
      t: "image",
      src: "/leetcode-75/hashmap-collision.png",
      alt: "Hash collision: several keys map to the same slot; probe for the next open slot and compare with ==",
      caption: "Collision: 7, 15, 23 all want slot 7 — the first stays in 7, the rest move to later open slots. Lookup must use ==, not slot number alone.",
    },
    {
      t: "p",
      c: "That is why every table says average O(1), not plain O(1). Python keeps the average fast by resizing (growing the table) when it gets full, so plenty of empty slots remain, collisions stay rare, and probe chains stay short.",
    },
    {
      t: "h3",
      c: "Step through (Interactive)",
    },
    {
      t: "p",
      c: "Hit **Next ▶** and insert 42, 100, 7, 15 into an 8-slot table, then look up 15 and 42. Gold arrow = computed slot. Orange = write / collision probe. Green = found. Watch 15 collide on slot 7, land in slot 0, then lookup still starts at slot 7 using the same formula.",
    },
    { t: "viz", id: "hash-slot" },

    { t: "h3", c: "What worst-case O(n) looks like" },
    {
      t: "p",
      c: "If every key hashes to the same number, everything piles into one region and one lookup compares almost every item — back to O(n) like a list. You can force this with a class whose __hash__ always returns the same value, then count how many == calls one lookup needs.",
    },
    {
      t: "codeout",
      lang: "python",
      label: "Proof that collisions can destroy O(1)",
      code: `class Key:
    calls = 0                          # shared counter for == calls

    def __init__(self, v, h):
        self.v, self.h = v, h

    def __hash__(self):
        return self.h                  # you control the hash

    def __eq__(self, other):
        Key.calls += 1
        return self.v == other.v


# Normal case — different hashes, items spread out
s_good = {Key(i, i) for i in range(1000)}
Key.calls = 0                          # reset after build; count only lookup
Key(999, 999) in s_good
print("well distributed : == called", Key.calls, "times")

# Worst case — every hash is 0, everything piles up
s_bad = {Key(i, 0) for i in range(1000)}
Key.calls = 0
Key(999, 0) in s_bad
print("all collide      : == called", Key.calls, "times")`,
      out: `well distributed : == called 1 times
all collide      : == called 1345 times`,
    },
    {
      t: "callout",
      title: "Does this matter on LeetCode?",
      c: 'In practice almost never — Python\'s hash for int, str, and tuple distributes well. All LC75 problems in this section can safely assume average O(1). Still know it because (1) in interviews, say "average case" after "O(1)", and (2) if you write a buggy __hash__ yourself, this is the failure mode.',
    },

    { t: "h2", c: "Part 3 · The one rule for keys — must be hashable" },
    {
      t: "p",
      c: "From part 2, storage location depends on the key's hash. If the key can change after insert, its hash changes, the slot it should live in changes, and the item is effectively lost. Python forbids mutable objects as keys up front — that property is called hashable.",
    },
    {
      t: "codeout",
      lang: "python",
      label: "What can be a key vs what cannot",
      code: `d = {}
d[3] = "int ok"
d["abc"] = "str ok"
d[(1, 2)] = "tuple ok"        # immutable -> ok
print(d)

try:
    d[[1, 2]] = "list?"        # mutable -> not ok
except TypeError as e:
    print("list  ->", type(e).__name__ + ":", e)

try:
    d[{1, 2}] = "set?"         # mutable -> not ok (frozenset is ok)
except TypeError as e:
    print("set   ->", type(e).__name__ + ":", e)`,
      out: `{3: 'int ok', 'abc': 'str ok', (1, 2): 'tuple ok'}
list  -> TypeError: unhashable type: 'list'
set   -> TypeError: unhashable type: 'set'`,
    },
    {
      t: "p",
      c: "unhashable type: 'list' is the error you will see often here. The fix is almost always one line: convert the list to a tuple before using it as a key — equal tuples share the same hash, so they compare correctly (this becomes pattern 4 next). But there is a catch worth knowing: a tuple is not automatically hashable. It is hashable only if every element inside it is hashable too. So tuple([1, 2]) works, but a tuple with a nested list such as (1, [2]) still raises unhashable type: 'list'. With nested lists you must convert every level to tuples.",
    },
    {
      t: "codeout",
      lang: "python",
      label: "The tuple caveat — hashable only if every element inside is hashable",
      code: `rows = [[1, 2], [3, 4], [1, 2]]
print("plain tuple works    :", {tuple(r) for r in rows})

nested = [[1, [2]], [3, [4]]]
try:
    {tuple(r) for r in nested}
except TypeError as e:
    print("tuple with a list    ->", type(e).__name__ + ":", e)

def deep(x):
    return tuple(deep(i) for i in x) if isinstance(x, list) else x

print("convert every level  :", {deep(r) for r in nested})`,
      out: `plain tuple works    : {(1, 2), (3, 4)}
tuple with a list    -> TypeError: unhashable type: 'list'
convert every level  : {(1, (2,)), (3, (4,))}`,
    },

    { t: "h3", c: "The trap nobody warns you about: 1, True, and 1.0 are the same key" },
    {
      t: "p",
      c: "Samness as a key is not about type — it is about equal hash and == being True. 1, True, and 1.0 all satisfy that.",
    },
    {
      t: "codeout",
      lang: "python",
      code: `d = {}
d[1] = "one"
d[True] = "true"
d[1.0] = "one-point-zero"
print(d)                                        # only one key left!
print("1 == True == 1.0 :", 1 == True == 1.0)
print("same hashes      :", hash(1) == hash(True) == hash(1.0))`,
      out: `{1: 'one-point-zero'}
1 == True == 1.0 : True
same hashes      : True`,
    },
    {
      t: "p",
      c: "The displayed key stays 1 (the first insert), but the value is the last write — later inserts update, they do not add a new key. Silent breakage, no exception.",
    },

    { t: "h2", c: "Part 4 · Four tools — pick the right one" },
    {
      t: "p",
      c: 'Everything in this section is a hash table. They differ only in "what rides along with the key" and "what you get for free". This table is all you need.',
    },
    {
      t: "table",
      head: ["Tool", "Stores", "Free benefits", "Reach for it when"],
      rows: [
        [
          "set",
          "keys only",
          "dedupe + set ops (& | - ^)",
          'question is "is it there?", not "how many?"',
        ],
        [
          "dict",
          "key → any value",
          "insertion order (Python 3.7+)",
          "you need side data, e.g. an index",
        ],
        [
          "Counter",
          "key → count",
          "count in one line, missing key → 0, most_common()",
          'the problem is pure "counting"',
        ],
        [
          "defaultdict",
          "key → typed default",
          "no need to check if key exists",
          "value is a collection, e.g. list or set",
        ],
      ],
    },
    {
      t: "codeout",
      lang: "python",
      label: "Four equivalent ways to count frequencies",
      code: `from collections import defaultdict, Counter

words = ["a", "b", "a", "c", "a", "b"]

c1 = {}                          # 1) check if key exists first
for w in words:
    if w not in c1:
        c1[w] = 0
    c1[w] += 1

c2 = {}                          # 2) .get(key, default) — shorter, no if
for w in words:
    c2[w] = c2.get(w, 0) + 1

c3 = defaultdict(int)            # 3) missing key starts at int() = 0
for w in words:
    c3[w] += 1

c4 = Counter(words)              # 4) count in one line

print("1) if/else       :", c1)
print("2) .get(w, 0)    :", c2)
print("3) defaultdict   :", dict(c3))
print("4) Counter       :", c4)
print("all equal        :", c1 == c2 == dict(c3) == dict(c4))`,
      out: `1) if/else       : {'a': 3, 'b': 2, 'c': 1}
2) .get(w, 0)    : {'a': 3, 'b': 2, 'c': 1}
3) defaultdict   : {'a': 3, 'b': 2, 'c': 1}
4) Counter       : Counter({'a': 3, 'b': 2, 'c': 1})
all equal        : True`,
    },
    {
      t: "callout",
      title: "The sharpest set pitfall",
      warn: true,
      c: 'Sets do not preserve order. For str keys, order can reshuffle every process (str hashes are randomized). Never rely on set order — to dedupe while keeping first-seen order use list(dict.fromkeys(nums)). Problems that return a set often say "in any order", which is itself a hint that a set is fine.',
    },

    { t: "h3", c: "Reading a missing key — three tools, three behaviors" },
    {
      t: "codeout",
      lang: "python",
      code: `from collections import Counter

count = {"a": 3}
print('count.get("z", 0)     :', count.get("z", 0))      # safe default
print('Counter("aaa")["z"]   :', Counter("aaa")["z"])     # Counter returns 0
try:
    print(count["z"])                                     # plain dict -> boom
except KeyError as e:
    print('count["z"]            : KeyError', e)`,
      out: `count.get("z", 0)     : 0
Counter("aaa")["z"]   : 0
count["z"]            : KeyError 'z'`,
    },
    {
      t: "codeout",
      lang: "python",
      label: 'defaultdict creates a key just from a "read"',
      code: `from collections import defaultdict

d = defaultdict(int)
print('"z" in d before read :', "z" in d)
_ = d["z"]                          # read only — no assignment
print('"z" in d after read  :', "z" in d, "<- created by reading")
print("d =", dict(d), "| len(d) =", len(d))`,
      out: `"z" in d before read : False
"z" in d after read  : True <- created by reading
d = {'z': 0} | len(d) = 1`,
    },
    {
      t: "p",
      c: 'If the problem asks "how many distinct values?" and you casually print(d[x]) while debugging, len(d) silently drifts. Counter does not — it returns 0 without creating the key.',
    },

    { t: "h2", c: "Part 5 · Six usage patterns you must separate" },
    {
      t: "p",
      c: 'This is the heart of the page. Only four tools (part 4), but six distinct questions you ask them. People stuck here usually know dict — they just remember "use a hash map" without knowing what to ask it.',
    },
    {
      t: "p",
      c: 'Each pattern below has the same three parts: the question → a skeleton (not runnable) → a runnable example. Read all six before the problems, then ask yourself "which pattern is this?"',
    },

    { t: "h3", c: 'Pattern 1 · Membership — "is this in that pile?"' },
    {
      t: "p",
      c: "Simplest and most common. You only care present/absent — not count, not position. Tool: set. Build the set once before the loop, not every iteration.",
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 1",
      c: `pool = set(the_collection_to_check)   # once, before the loop
for x in data:
    if x in pool:                     # O(1) each query
        ...`,
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — values in one pile but not the other",
      code: `a, b = [4, 9, 5], [9, 8, 4, 3]

seen = set(a)
print("in b but not a :", [x for x in b if x not in seen])

# if duplicates in the answer do not matter, set ops are shorter
print("set difference :", set(b) - set(a))`,
      out: `in b but not a : [8, 3]
set difference : {8, 3}`,
    },
    {
      t: "callout",
      title: "Pattern 1 · summary",
      c: "Appears in problem 20 · Find the Difference of Two Arrays (both directions: set(a)-set(b) and set(b)-set(a)). Trap: results are sets (unordered) — wrap with list(...) if a list is required.",
    },

    { t: "h3", c: 'Pattern 2 · Frequency — "how many times does each appear?"' },
    {
      t: "p",
      c: 'Step up from "yes/no" to "how many". A set is not enough — you need a dict of numbers. Counter does it in one line.',
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 2",
      c: `from collections import Counter

count = Counter(data)      # count the whole collection
count[x]                   # how many times x appears — 0 if missing, no error`,
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — count characters",
      code: `from collections import Counter

c = Counter("abracadabra")
print("Counter         :", c)              # ordered by frequency
print('c["a"] / c["z"]  :', c["a"], "/", c["z"])
print("top 2           :", c.most_common(2))`,
      out: `Counter         : Counter({'a': 5, 'b': 2, 'r': 2, 'c': 1, 'd': 1})
c["a"] / c["z"]  : 5 / 0
top 2           : [('a', 5), ('b', 2)]`,
    },
    {
      t: "callout",
      title: "Pattern 2 · summary",
      c: 'Base for patterns 3 and 4. Counter(data) works on any iterable — str, list, generator. Trap: Counter returns 0 for missing keys, so if c[x] is falsy for both "missing" and "zero count" — separate those cases when the problem does.',
    },

    { t: "h3", c: 'Pattern 3 · Count of counts — "what does the frequency pattern look like?"' },
    {
      t: "p",
      c: "The most missed pattern in this section. The problem no longer asks about the data values — it asks about the counts themselves. Count with Counter, then drop the keys and reason over .values() as a second layer.",
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 3",
      c: `from collections import Counter

count = Counter(data)
vals = list(count.values())      # drop keys; keep only "how many times"

# second-layer questions, e.g.
len(set(vals)) == len(vals)      # are all frequencies unique?
sorted(vals) == sorted(other)    # do two piles share the same frequency bag?`,
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — are all frequencies unique?",
      code: `from collections import Counter

for s in ["aabbbc", "aabbc"]:
    c = Counter(s)
    v = list(c.values())
    print(f"{s:<8} -> {dict(c)}")
    print(f"{'':8}    freqs = {sorted(v)} | all unique? {len(set(v)) == len(v)}")`,
      out: `aabbbc   -> {'a': 2, 'b': 3, 'c': 1}
            freqs = [1, 2, 3] | all unique? True
aabbc    -> {'a': 2, 'b': 2, 'c': 1}
            freqs = [1, 2, 2] | all unique? False`,
    },
    {
      t: "p",
      c: 'Read len(set(v)) == len(v): set(v) drops duplicates in v; if length is unchanged, there were none — the standard "are all unique?" check. Notice we stack set on top of Counter: two tools, two layers, one problem.',
    },
    {
      t: "callout",
      title: "Pattern 3 · summary",
      c: 'Problem 21 · Unique Number of Occurrences (len(set(v)) == len(v)) and problem 22 · Determine if Two Strings Are Close (sorted(x.values()) == sorted(y.values()) plus set(x) == set(y)). The key is "count, then count again" — not a single counting pass.',
    },

    { t: "h3", c: 'Pattern 4 · Composite key — "several pieces as one key"' },
    {
      t: "p",
      c: "When what you compare is a whole bundle — a matrix row, a pair (x, y) — not a single value. Lists cannot be keys (part 3). Convert to a tuple so the whole bundle becomes one lookup key.",
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 4",
      c: `from collections import Counter

count = Counter(tuple(item) for item in data)   # list -> tuple first
count[tuple(another_bundle)]                    # whole-bundle compare in O(1)`,
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — how many (row, column) pairs look the same",
      code: `from collections import Counter

grid = [[3, 2, 1],
        [1, 7, 6],
        [2, 7, 7]]

rowc = Counter(tuple(r) for r in grid)     # count rows; whole row is the key
print("row counts :", rowc)

cols = list(zip(*grid))                    # zip(*grid) = transpose → columns
print("columns    :", cols)

for col in cols:
    print(f"   col {col} -> matching rows: {rowc[col]}")

print("answer =", sum(rowc[col] for col in cols))`,
      out: `row counts : Counter({(3, 2, 1): 1, (1, 7, 6): 1, (2, 7, 7): 1})
columns    : [(3, 1, 2), (2, 7, 7), (1, 6, 7)]
   col (3, 1, 2) -> matching rows: 0
   col (2, 7, 7) -> matching rows: 1
   col (1, 6, 7) -> matching rows: 0
answer = 1`,
    },
    {
      t: "p",
      c: "The whole problem collapses into the code above because the hash map does the heavy lifting: without it you compare every row to every column cell-by-cell (three nested loops). With a row as one key, each compare is O(1) — one loop left.",
    },
    {
      t: "callout",
      title: "Pattern 4 · summary",
      c: "Problem 23 · Equal Row and Column Pairs. Remember: tuple(...) for hashability, then Counter. Build columns with tuple(grid[i][j] for i in ...) or zip(*grid). Trap: add rowc[col], not +1 — duplicate-looking rows each form their own pair.",
    },

    { t: "h3", c: 'Pattern 5 · Seen-so-far — "has the partner I need already walked by?"' },
    {
      t: "p",
      c: 'Patterns 1–4 build the map first, then use it. Pattern 5 uses it while still building — one pass. At each step ask "has the value I need already passed?" If yes, done; if not, record yourself for someone later. Often called one-pass.',
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 5",
      c: `seen = {}                       # or set() if you do not need the index
for i, x in enumerate(data):
    if needed_value in seen:        # 1) ask first
        ...                         #    found — done
    seen[x] = i                     # 2) then record yourself — order matters`,
    },
    {
      t: "p",
      c: 'Best example: LC1 Two Sum — the Two Pointers page left this hanging ("two pointers cannot keep original indices because sorting destroys them"). Pattern 5 solves it: it remembers which value sat at which index, so no sort is needed.',
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — LC1 Two Sum in one pass, no sort, indices intact",
      code: `nums = [3, 9, 4, 1]
target = 12
seen = {}                       # value -> index seen so far

for i, x in enumerate(nums):
    need = target - x           # the partner that would complete target
    print(f"i={i} x={x} | need {need} | seen={seen} -> seen? {need in seen}")
    if need in seen:
        print("answer indices:", (seen[need], i))
        break
    seen[x] = i                 # not found yet -> leave yourself for later`,
      out: `i=0 x=3 | need 9 | seen={} -> seen? False
i=1 x=9 | need 3 | seen={3: 0} -> seen? True
answer indices: (0, 1)`,
    },
    {
      t: "h3",
      c: "Step through (Interactive)",
    },
    {
      t: "p",
      c: "Hit **Next ▶** on nums = [3, 9, 4, 1], target 12. Gold = current value. Green in seen = the need we are asking for. Green rings = the pair. Notice we ask first, then record — so a value never pairs with itself.",
    },
    { t: "viz", id: "hash-seen" },
    {
      t: "callout",
      title: "Pattern 5 · summary",
      warn: true,
      c: "Critical trap: check first, then record. Swap those lines and the current value pairs with itself when 2*x == target — e.g. nums = [6, 3], target = 12 wrongly returns (0, 0). Not in the four problems of this section directly, but shows up in Prefix Sum, Sliding Window, and interviews constantly.",
    },

    { t: "h3", c: 'Pattern 6 · Grouping — "bucket items by a key you compute"' },
    {
      t: "p",
      c: "Patterns 1–5 use numeric values or no value. Pattern 6 uses a collection (list/set) as the value and dumps items that share a computed key into the same bucket. The key is not in the raw data — you invent it. Equal computed keys → same group.",
    },
    {
      t: "code",
      lang: "python",
      label: "Skeleton for pattern 6",
      c: `from collections import defaultdict

groups = defaultdict(list)          # missing key starts as []
for item in data:
    key = compute_key(item)         # the part you design per problem
    groups[key].append(item)        # same key → same bucket`,
    },
    {
      t: "codeout",
      lang: "python",
      label: "Live — group anagrams",
      code: `from collections import defaultdict

words = ["eat", "tea", "tan", "ate", "nat", "bat"]
groups = defaultdict(list)

for w in words:
    key = "".join(sorted(w))        # sorted letters → anagrams share a key
    groups[key].append(w)
    print(f"{w} -> key '{key}'")

print("result:", list(groups.values()))`,
      out: `eat -> key 'aet'
tea -> key 'aet'
tan -> key 'ant'
ate -> key 'aet'
nat -> key 'ant'
bat -> key 'abt'
result: [['eat', 'tea', 'ate'], ['tan', 'nat'], ['bat']]`,
    },
    {
      t: "p",
      c: 'The whole trick is key = "".join(sorted(w)) — choosing what to compute as the key is the problem. Once the key is right, append into defaultdict is identical every time.',
    },
    {
      t: "callout",
      title: "Pattern 6 · summary",
      c: "Not in the four problems of this section, but the same shape as building an adjacency list in Graph (defaultdict(list) then append neighbors) — heavy in sections 12–13. Learn it now and Graph becomes mostly about traversal.",
    },

    { t: "h3", c: "Summary table of the six patterns" },
    {
      t: "table",
      head: ["Pattern", "Question asked", "Tool", "Key line", "Course #"],
      rows: [
        ["1 · Membership", "is this in that pile?", "set", "x in pool / set(b) - set(a)", "20"],
        ["2 · Frequency", "how many times each?", "Counter", "Counter(data)[x]", "base of 21–23"],
        ["3 · Count of counts", "what do the frequencies look like?", "Counter + set/sorted", "len(set(c.values())) == len(c)", "21, 22"],
        ["4 · Composite key", "does this whole bundle match?", "Counter of tuples", "Counter(tuple(r) for r in data)", "23"],
        ["5 · Seen-so-far", "has my partner already passed?", "dict (value → index)", "if need in seen: ... then seen[x] = i", "LC1, §4"],
        ["6 · Grouping", "how do these group together?", "defaultdict(list)", "groups[computed_key].append(item)", "§12–13"],
      ],
    },
    {
      t: "callout",
      title: "How to pick a pattern on a real problem",
      c: "Ask in order: need counts? if not → pattern 1. If yes: does it ask about the data or about the counts? counts → 3, data → 2. Is the key a bundle or a single value? bundle → 4. Need positions / one pass? → 5. Is the answer groups? → 6.",
    },

    { t: "h2", c: "Part 6 · When a hash map is the wrong tool" },
    {
      t: "p",
      c: 'Hash maps are fast because they throw away "order" and "nearness" — they only keep "present/absent" and "how many". Problems that need what they threw away cannot use this move.',
    },
    {
      t: "table",
      head: ["Situation", "Why not", "Use instead"],
      rows: [
        [
          'range queries, e.g. "how many values between 3 and 7?"',
          "hash answers exact equality only; scanning all keys defeats the point",
          "sorted array + binary search (§15)",
        ],
        [
          "repeatedly take min/max while data changes",
          "min/max over a dict is O(n) every time",
          "heap / priority queue (§14)",
        ],
        [
          "need sorted output",
          "sets are unordered; dicts only keep insertion order",
          "sorted(...) at the end, or sort up front",
        ],
        [
          "key is a list / mutable object",
          "unhashable — cannot be a key at all",
          "convert to tuple / frozenset first (pattern 4)",
        ],
        [
          "tiny fixed domain, e.g. a–z only",
          "works but wastes memory vs a plain array",
          "list of size 26 indexed by ord(c) - ord('a')",
        ],
      ],
    },
    {
      t: "codeout",
      lang: "python",
      label: "Memory cost (CPython 3.10)",
      code: `import sys

n = 1000
print("list(range(1000)) :", sys.getsizeof(list(range(n))), "bytes")
print("set(range(1000))  :", sys.getsizeof(set(range(n))), "bytes")
print("dict.fromkeys     :", sys.getsizeof(dict.fromkeys(range(n))), "bytes")`,
      out: `list(range(1000)) : 8056 bytes
set(range(1000))  : 32984 bytes
dict.fromkeys     : 36960 bytes`,
    },
    {
      t: "p",
      c: 'About 4× — because of part 2: hash tables keep many empty slots to avoid collisions. Pack them like a list and they stop being fast. That is the concrete "trade memory for time" from the opening. Almost never a LeetCode issue, but know what you are paying.',
    },

    { t: "h2", c: "Part 7 · Common traps" },
    {
      t: "ul",
      c: [
        "Writing if x in some_list inside a loop → accidental O(n²). Fix: set(...) once before the loop. #1 trap in this section.",
        "Relying on set order → unstable (for str, changes every process). Dedupe keeping order: list(dict.fromkeys(nums)).",
        "Reading d[key] when key may be missing → KeyError. Use d.get(key, default) or if key in d (Counter is safe; plain dict is not).",
        "Reading d[key] on a defaultdict while debugging → key is created for real; len(d) and loops drift with no error.",
        "Using a list as a key → TypeError: unhashable type: 'list'. Always tuple first (pattern 4).",
        "In pattern 5, writing seen[x] = i before the check → current item pairs with itself. Check first, then record.",
        "Forgetting 1, True, 1.0 are the same key; and key in d checks keys only, not values (v in d.values() is O(n)).",
        "Mutating keys while iterating for k in d → RuntimeError: dictionary changed size during iteration. Iterate a copy: for k in list(d).",
      ],
    },

    {
      t: "callout",
      title: "Ready for the problems",
      c: "This section has 4 problems, each mapped to a pattern in part 5: 20 · Difference of Two Arrays = pattern 1 → 21 · Unique Number of Occurrences = pattern 3 → 22 · Two Strings Are Close = pattern 3 (two layers) → 23 · Equal Row and Column Pairs = pattern 4. Hit next to start problem 20.",
    },
  ],
    },
  },

  "lc75-p20": {
    slug: "lc75-p20",
    title: {
      th: "ข้อ 20 · LC2215 Find the Difference of Two Arrays 🟢",
      en: "2215. Find the Difference of Two Arrays",
    },
    lead: {
      th: "หาค่าที่อยู่ใน array หนึ่งแต่ไม่อยู่ในอีก array ด้วย set difference (การหาผลต่างเซต) — แปลงเป็น set ครั้งเดียว ตัดตัวซ้ำและค้นหาได้ในเวลา O(1)",
      en: "Find values in one array but not the other with set difference in O(1) lookup time.",
    },
    group: "LeetCode 75",
    blocks: {
      th: [
        {
          t: "p",
          c: "Given two 0-indexed integer arrays nums1 and nums2, return a list answer of size 2 where:\n\n• answer[0] is a list of all distinct integers in nums1 which are not present in nums2.\n• answer[1] is a list of all distinct integers in nums2 which are not present in nums1.\n\nNote that the integers in the lists may be returned in any order.",
        },
        {
          t: "p",
          c: "มี array ตัวเลขจำนวนเต็ม `nums1` และ `nums2` มาให้สองชุด — ให้คืนค่า list ขนาด 2 ช่อง `[answer[0], answer[1]]` โดยที่:\n\n• `answer[0]` คือ list ของตัวเลขทั้งหมดที่ไม่ซ้ำกันใน `nums1` ซึ่งไม่ปรากฏอยู่ใน `nums2` เลย\n• `answer[1]` คือ list ของตัวเลขทั้งหมดที่ไม่ซ้ำกันใน `nums2` ซึ่งไม่ปรากฏอยู่ใน `nums1` เลย\n\nหมายเหตุ: ตัวเลขในผลลัพธ์จะเรียงลำดับอย่างไรก็ได้",
        },
        {
          t: "example",
          c: [
            {
              input: "nums1 = [1,2,3], nums2 = [2,4,6]",
              output: "[[1,3],[4,6]]",
              explain:
                "สำหรับ nums1: เลข 2 มีอยู่ใน nums2 (ตัดออก) เหลือ 1 กับ 3 ซึ่งไม่มีใน nums2 → answer[0] = [1, 3]\nสำหรับ nums2: เลข 2 มีอยู่ใน nums1 (ตัดออก) เหลือ 4 กับ 6 ซึ่งไม่มีใน nums1 → answer[1] = [4, 6]",
            },
            {
              input: "nums1 = [1,2,3,3], nums2 = [1,1,2,2]",
              output: "[[3],[]]",
              explain:
                "สำหรับ nums1: เลข 1 กับ 2 มีใน nums2 เหลือเลข 3 (แม้ใน nums1 จะมี 3 สองตัว แต่โจทย์ขอ distinct จึงตอบ [3])\nสำหรับ nums2: ทั้ง 1 และ 2 ล้วนมีใน nums1 แล้ว จึงไม่เหลือเลขใดเลย → answer[1] = []",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= nums1.length, nums2.length <= 1000",
            "-1000 <= nums1[i], nums2[i] <= 1000",
          ],
        },
        {
          t: "callout",
          title: "⏸ ลองเองก่อน",
          c: "อ่านโจทย์แล้วคิดตาม: ทำไมการใช้ list วนหา `x in nums2` ถึงช้า? ลองเขียนโค้ดด้วย Set ด้วยตัวเองก่อนเปิดเฉลย",
        },

        {
          t: "solution",
          summary: "เฉลยเต็ม · ซ่อนไว้ให้ลองเองก่อน",
          c: [
            { t: "h3", c: "ขั้นที่ 1 · โจทย์นี้ขออะไร" },
            {
              t: "p",
              c: "โจทย์ขอของสองอย่างพร้อมกัน: (1) หาตัวเลขในกองแรกที่ไม่อยู่ในกองสอง และ (2) หาตัวเลขในกองสองที่ไม่อยู่ในกองแรก โดยผลลัพธ์แต่ละกองต้อง **ไม่มีตัวเลขซ้ำ (distinct)** เลย",
            },
            {
              t: "p",
              c: "สังเกตคำสองคำในโจทย์: 'distinct' (ไม่ซ้ำ) กับ 'not present' (ไม่อยู่ในอีกกอง) สองคำนี้คือคำบอกใบ้ตรงตัวของโครงสร้างข้อมูลที่ชื่อ **Set (เซต)** ในภาษาโปรแกรม",
            },

            { t: "h3", c: "ขั้นที่ 2 · ทำให้ได้ด้วยมือ" },
            {
              t: "p",
              c: "ลองใช้ Example 2: nums1 = [1, 2, 3, 3], nums2 = [1, 1, 2, 2]",
            },
            {
              t: "ul",
              c: [
                "ก้าวแรก — ยุบตัวซ้ำในแต่ละกองทิ้งก่อน: nums1 กลายเป็นถุง {1, 2, 3} และ nums2 กลายเป็นถุง {1, 2}",
                "ก้าวสอง — หา answer[0] (อยู่ใน nums1 แต่ไม่อยู่ใน nums2): ดูทีละตัวใน {1, 2, 3} → 1 มีใน nums2 (ข้าม), 2 มีใน nums2 (ข้าม), 3 ไม่มี! → เก็บ [3]",
                "ก้าวสาม — หา answer[1] (อยู่ใน nums2 แต่ไม่อยู่ใน nums1): ดูทีละตัวใน {1, 2} → 1 มีใน nums1 (ข้าม), 2 มีใน nums1 (ข้าม) → ไม่มีตัวเหลือเลย → เก็บ []",
                "รวมผลลัพธ์เป็นสองมิติ: [[3], []]",
              ],
            },

            { t: "h3", c: "ขั้นที่ 3 · วิธีทำ" },
            {
              t: "p",
              c: "ภาพรวม: แปลง array ทั้งสองเป็น Set เพื่อยุบตัวซ้ำทิ้งและทำให้เช็คการมีอยู่ได้ในเวลา O(1) จากนั้นใช้ตัวดำเนินการลบเซต (`-`) หาผลต่างสองทิศทาง แล้วแปลงกลับเป็น List",
            },
            {
              t: "p",
              c: "เครื่องมือและตัวแปรที่ต้องใช้:",
            },
            {
              t: "ul",
              c: [
                "s1 = set(nums1) — ถุงเซตของ nums1 ตัดตัวซ้ำทิ้งทั้งหมด",
                "s2 = set(nums2) — ถุงเซตของ nums2 ตัดตัวซ้ำทิ้งทั้งหมด",
                "s1 - s2 — เซตผลต่าง (difference): ของที่มีใน s1 แต่ไม่มีใน s2",
                "s2 - s1 — เซตผลต่างอีกฝั่ง: ของที่มีใน s2 แต่ไม่มีใน s1 (การลบเซตไม่สลับที่!)",
                "list(...) — แปลงก้อนผลลัพธ์จากเซตกลับเป็น list ตามที่ LeetCode กำหนด",
              ],
            },
            {
              t: "p",
              c: "เจาะลึก: ทำไมต้อง set? ถ้าเราเขียนวนลูป `[x for x in nums1 if x not in nums2]` โดยที่ nums2 ยังเป็น list คำสั่ง `x not in nums2` จะต้องกวาดสายตามองทั้ง nums2 ทุกรอบ รวมเป็น O(n × m) ซึ่งช้ามาก แต่ถ้าแปลง nums2 เป็น set แล้ว การเช็ค `x not in s2` จะคำนวณตำแหน่งผ่าน Hash Function ได้ในเสี้ยววินาที O(1) ทันที!",
            },

            { t: "h3", c: "ดูทีละขั้น (Interactive)" },
            {
              t: "p",
              c: "กด **Next ▶** แปลง nums1 = [1, 2, 3, 3] กับ nums2 = [1, 1, 2, 2] เป็น set ทีละตัว · ส้ม = กำลังใส่ · ช่องจาง = ตัวซ้ำที่ถูกยุบ · เขียว = รอดจาก difference — ต้องลบสองทิศ คนละคำตอบ",
            },
            { t: "viz", id: "hash-diff" },

            { t: "h3", c: "โค้ดสำหรับวางใน LeetCode" },
            {
              t: "code",
              lang: "python",
              c: `class Solution:
    def findDifference(self, nums1: List[int], nums2: List[int]) -> List[List[int]]:
        # 1. แปลงเป็น set เพื่อตัดตัวซ้ำและค้นหาได้เร็ว O(1)
        s1 = set(nums1)
        s2 = set(nums2)

        # 2. หาผลต่างของเซตทั้งสองทิศทาง
        # s1 - s2 คือตัวเลขที่มีใน s1 แต่ไม่ปรากฏใน s2
        # s2 - s1 คือตัวเลขที่มีใน s2 แต่ไม่ปรากฏใน s1
        diff1 = list(s1 - s2)
        diff2 = list(s2 - s1)

        # 3. ส่งคืนคำตอบเป็น list สองชั้น
        return [diff1, diff2]`,
            },

            { t: "h3", c: "อ่านโค้ดทีละส่วน" },
            {
              t: "table",
              head: ["ส่วนของโค้ด", "หน้าที่", "ตัวอย่างค่า (nums1=[1,2,3,3], nums2=[1,1,2,2])"],
              rows: [
                ["s1 = set(nums1)", "แปลง nums1 เป็นเซต กำจัดตัวซ้ำ", "{1, 2, 3}"],
                ["s2 = set(nums2)", "แปลง nums2 เป็นเซต กำจัดตัวซ้ำ", "{1, 2}"],
                ["diff1 = list(s1 - s2)", "หาของที่ s1 มีแต่ s2 ไม่มี แล้วทำเป็น list", "[3]"],
                ["diff2 = list(s2 - s1)", "หาของที่ s2 มีแต่ s1 ไม่มี แล้วทำเป็น list", "[]"],
                ["return [diff1, diff2]", "ประกอบผลลัพธ์เป็น list ขนาด 2", "[[3], []]"],
              ],
            },

            { t: "h3", c: "ต้นทุน" },
            {
              t: "p",
              c: "• **เวลา (Time Complexity)**: O(n + m) โดย n คือความยาว nums1 และ m คือความยาว nums2 การแปลงเป็น set ใช้ O(n) และ O(m) ส่วนการลบเซตใช้เวลาแปรผันตามขนาดของเซต รวมแล้วทำงานเป็นเส้นตรงรอบเดียว เร็วกว่าแบบลูปซ้อน O(n × m) มหาศาล\n\n• **พื้นที่ (Space Complexity)**: O(n + m) สร้างเซตใหม่ 2 ก้อนเพื่อเก็บสมาชิกที่ไม่ซ้ำกันของทั้งสอง array",
            },
          ],
        },
      ],
      en: [
        {
          t: "p",
          c: "Given two 0-indexed integer arrays nums1 and nums2, return a list answer of size 2 where:\n\n• answer[0] is a list of all distinct integers in nums1 which are not present in nums2.\n• answer[1] is a list of all distinct integers in nums2 which are not present in nums1.\n\nNote that the integers in the lists may be returned in any order.",
        },
        {
          t: "example",
          c: [
            {
              input: "nums1 = [1,2,3], nums2 = [2,4,6]",
              output: "[[1,3],[4,6]]",
              explain:
                "For nums1, nums1[1] = 2 is present at index 0 of nums2, whereas nums1[0] = 1 and nums1[2] = 3 are not present in nums2. Therefore, answer[0] = [1,3]. For nums2, nums2[0] = 2 is present at index 1 of nums1, whereas nums2[1] = 4 and nums2[2] = 6 are not present in nums1. Therefore, answer[1] = [4,6].",
            },
            {
              input: "nums1 = [1,2,3,3], nums2 = [1,1,2,2]",
              output: "[[3],[]]",
              explain:
                "For nums1, nums1[2] and nums1[3] are not present in nums2. Since nums1[2] == nums1[3], their value is only included once and answer[0] = [3]. Every integer in nums2 is present in nums1. Therefore, answer[1] = [].",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= nums1.length, nums2.length <= 1000",
            "-1000 <= nums1[i], nums2[i] <= 1000",
          ],
        },
        {
          t: "callout",
          title: "⏸ Try it yourself first",
          c: "Think about why scanning with `x in nums2` on a list is slow. Try writing the set solution yourself before opening the solution.",
        },

        {
          t: "solution",
          summary: "Full solution · hidden so you can try first",
          c: [
            { t: "h3", c: "Step 1 · What does the problem ask?" },
            {
              t: "p",
              c: "The problem asks for two things: (1) values in the first pile not in the second, and (2) values in the second pile not in the first, with both results being **distinct** (no duplicates).",
            },
            {
              t: "p",
              c: "Notice the keywords: 'distinct' and 'not present'. These are the exact hints for **Set**.",
            },

            { t: "h3", c: "Step 2 · Walk through by hand" },
            {
              t: "p",
              c: "Example 2: nums1 = [1, 2, 3, 3], nums2 = [1, 1, 2, 2]",
            },
            {
              t: "ul",
              c: [
                "First — collapse duplicates: nums1 becomes {1, 2, 3} and nums2 becomes {1, 2}.",
                "Second — find answer[0] (in nums1 but not nums2): check {1, 2, 3} → 1 is in nums2 (skip), 2 is in nums2 (skip), 3 is not! → keep [3].",
                "Third — find answer[1] (in nums2 but not nums1): check {1, 2} → 1 and 2 are both in nums1 → nothing left → keep [].",
                "Result: [[3], []]",
              ],
            },

            { t: "h3", c: "Step 3 · Approach" },
            {
              t: "p",
              c: "Overview: Convert both arrays to Sets to drop duplicates and enable O(1) membership lookup. Then use set difference (`-`) in both directions, and convert back to Lists.",
            },
            {
              t: "p",
              c: "Tools used:",
            },
            {
              t: "ul",
              c: [
                "s1 = set(nums1) — set of nums1 with duplicates removed.",
                "s2 = set(nums2) — set of nums2 with duplicates removed.",
                "s1 - s2 — set difference: elements in s1 but not s2.",
                "s2 - s1 — set difference the other way (difference is not commutative!).",
                "list(...) — convert the result back to a list as required by LeetCode.",
              ],
            },
            {
              t: "p",
              c: "Deep dive: Why sets? If we wrote `[x for x in nums1 if x not in nums2]` with nums2 as a list, `x not in nums2` scans the entire list every time, taking O(n × m). Converting nums2 to a set makes `x not in s2` an O(1) hash lookup via hash function!",
            },

            { t: "h3", c: "Step through (Interactive)" },
            {
              t: "p",
              c: "Hit **Next ▶** and convert nums1 = [1, 2, 3, 3] and nums2 = [1, 1, 2, 2] into sets one value at a time. Orange = inserting. Dim = collapsed duplicate. Green = survived the difference.",
            },
            { t: "viz", id: "hash-diff" },

            { t: "h3", c: "LeetCode Solution Code" },
            {
              t: "code",
              lang: "python",
              c: `class Solution:
    def findDifference(self, nums1: List[int], nums2: List[int]) -> List[List[int]]:
        s1 = set(nums1)
        s2 = set(nums2)
        return [list(s1 - s2), list(s2 - s1)]`,
            },

            { t: "h3", c: "Code breakdown" },
            {
              t: "table",
              head: ["Code snippet", "What it does", "Example (nums1=[1,2,3,3], nums2=[1,1,2,2])"],
              rows: [
                ["s1 = set(nums1)", "Convert nums1 to set, removing duplicates", "{1, 2, 3}"],
                ["s2 = set(nums2)", "Convert nums2 to set, removing duplicates", "{1, 2}"],
                ["list(s1 - s2)", "Find elements in s1 but not s2, convert to list", "[3]"],
                ["list(s2 - s1)", "Find elements in s2 but not s1, convert to list", "[]"],
                ["return [diff1, diff2]", "Return result as list of two lists", "[[3], []]"],
              ],
            },

            { t: "h3", c: "Complexity" },
            {
              t: "p",
              c: "• **Time Complexity**: O(n + m) where n and m are lengths of nums1 and nums2. Building sets takes O(n + m) and set difference takes time proportional to set sizes. Much faster than O(n × m) nested loops.\n\n• **Space Complexity**: O(n + m) to store the two sets.",
            },
          ],
        },
      ],
    },
  },

  "lc75-p21": {
    slug: "lc75-p21",
    title: {
      th: "ข้อ 21 · LC1207 Unique Number of Occurrences 🟢",
      en: "1207. Unique Number of Occurrences",
    },
    lead: {
      th: "นับความถี่ (occurrences) ของแต่ละค่า แล้วตรวจสอบว่าจำนวนครั้งที่นับได้ไม่ซ้ำกันเลยแม้แต่คู่เดียว — นับด้วย Counter แล้วตรวจตัวซ้ำด้วย Set",
      en: "Count occurrences of each value, then check that all frequency counts are distinct using a set.",
    },
    group: "LeetCode 75",
    blocks: {
      th: [
        {
          t: "p",
          c: "Given an array of integers arr, return true if the number of occurrences of each value in the array is unique or false otherwise.",
        },
        {
          t: "p",
          c: "มี array ของจำนวนเต็ม `arr` มาให้ — ให้คืนค่า `true` ถ้าจำนวนครั้งที่แต่ละตัวเลขปรากฏ (occurrences) ใน array นี้ไม่ซ้ำกันเลย หรือคืน `false` ถ้ามีตัวเลขสองตัวใดที่มีจำนวนครั้งเท่ากัน",
        },
        {
          t: "example",
          c: [
            {
              input: "arr = [1,2,2,1,1,3]",
              output: "true",
              explain:
                "เลข 1 โผล่มา 3 ครั้ง\nเลข 2 โผล่มา 2 ครั้ง\nเลข 3 โผล่มา 1 ครั้ง\nจำนวนครั้งคือ 3, 2, 1 ซึ่งไม่มีเลขใดซ้ำกันเลย → true",
            },
            {
              input: "arr = [1,2]",
              output: "false",
              explain:
                "เลข 1 โผล่มา 1 ครั้ง\nเลข 2 โผล่มา 1 ครั้ง\nจำนวนครั้งคือ 1, 1 ซึ่งซ้ำกัน! → false",
            },
            {
              input: "arr = [-3,0,1,-3,1,1,1,-3,10,0]",
              output: "true",
              explain:
                "ความถี่ของแต่ละค่าคือ: -3 โผล่ 3 ครั้ง, 0 โผล่ 2 ครั้ง, 1 โผล่ 4 ครั้ง, 10 โผล่ 1 ครั้ง → ความถี่คือ 3, 2, 4, 1 ไม่ซ้ำกันเลย → true",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= arr.length <= 1000",
            "-1000 <= arr[i] <= 1000",
          ],
        },

        {
          t: "callout",
          title: "⏸ ลองเองก่อน",
          c: "ระวังอย่าสับสนระหว่าง 'ตัวเลขใน array ซ้ำไหม' กับ 'จำนวนครั้ง (ความถี่) ซ้ำไหม' — โจทย์ถามเรื่องความถี่ซ้ำ!",
        },

        {
          t: "solution",
          summary: "เฉลยเต็ม · ซ่อนไว้ให้ลองเองก่อน",
          c: [
            { t: "h3", c: "ขั้นที่ 1 · โจทย์นี้ขออะไร" },
            {
              t: "p",
              c: "โจทย์ไม่ได้สนใจว่าตัวเลขใน array ซ้ำกันหรือไม่ แต่สนใจ **จำนวนครั้งที่โผล่มา (frequency)** ของตัวเลขแต่ละตัว ว่าตัวเลขความถี่เหล่านั้นมีคู่ไหนที่ซ้ำกันหรือไม่",
            },
            {
              t: "p",
              c: "ยกตัวอย่าง: ถ้าในห้องมีคนชื่อ A 2 คน และคนชื่อ B 2 คน → จำนวนครั้งของ A คือ 2, ของ B คือ 2 → เลข 2 ชนกัน แบบนี้ถือว่าไม่ Unique (ตอบ false)",
            },

            { t: "h3", c: "ขั้นที่ 2 · ทำให้ได้ด้วยมือ" },
            {
              t: "p",
              c: "ใช้ Example 1: arr = [1, 2, 2, 1, 1, 3]",
            },
            {
              t: "ul",
              c: [
                "ขั้นตอนที่ 1 (นับคะแนน): ขีดนับตัวเลขแต่ละตัวใส่ตาราง\n• เลข 1 มี 3 ตัว\n• เลข 2 มี 2 ตัว\n• เลข 3 มี 1 ตัว",
                "ขั้นตอนที่ 2 (ดึงเฉพาะจำนวนครั้ง): ตารางบอกว่ายอดนับคือ [3, 2, 1]",
                "ขั้นตอนที่ 3 (ตรวจตัวซ้ำ): เอา [3, 2, 1] ไปใส่ถุง Set → ได้ {3, 2, 1} ขนาดถุงยังเป็น 3 เท่าเดิม แปลว่าไม่มีเลขไหนซ้ำกันเลย → ตอบ True",
              ],
            },
            {
              t: "p",
              c: "เทียบกับ Example 2: arr = [1, 2] → ยอดนับคือ 1 มี 1 ตัว และ 2 มี 1 ตัว → ยอดนับคือ [1, 1] เมื่อเทใส่ Set ตัวซ้ำจะถูกยุบเหลือ {1} ขนาดหดจาก 2 เหลือ 1 แปลว่ามีตัวซ้ำ → ตอบ False",
            },

            { t: "h3", c: "ขั้นที่ 3 · วิธีทำ" },
            {
              t: "p",
              c: "ภาพรวม: ใช้ Hash Map (หรือ Counter ในภาษา Python) นับความถี่ของตัวเลขทุกตัว จากนั้นดึงเฉพาะค่ายอดนับ (`values`) ออกมา แล้วใช้คุณสมบัติของ Set ตรวจสอบว่ามียอดนับที่ซ้ำกันหรือไม่",
            },
            {
              t: "p",
              c: "เครื่องมือที่ต้องใช้:",
            },
            {
              t: "ul",
              c: [
                "Counter(arr) — ตัวนับอัตโนมัติจากโมดูล collections ที่จะแปลง list ให้กลายเป็นพจนานุกรม {ตัวเลข: จำนวนครั้ง}",
                "counts.values() — ดึงเฉพาะตัวเลขจำนวนครั้งออกมาเป็นชุดข้อมูล",
                "len(values) == len(set(values)) — ทริกคลาสสิก: ถ้าแปลงเป็น Set แล้วจำนวนสมาชิกลดลง แปลว่ามีสมาชิกที่ซ้ำกัน!",
              ],
            },
            {
              t: "p",
              c: "เจาะลึก: ทำไมทริก `len(set)` ถึงทำงานได้? Set มีกฎเหล็กว่า **ห้ามมีสมาชิกซ้ำ** ถ้าเรามี `[3, 2, 1]` ซึ่งมี 3 ตัว แปลงเป็น set จะได้ `{1, 2, 3}` มี 3 ตัวเท่าเดิม แต่ถ้ามี `[1, 1]` ซึ่งมี 2 ตัว แปลงเป็น set จะเหลือ `{1}` สมาชิกหายไป 1 ตัวทันที ดังนั้นเทียบความยาวก่อนและหลังใส่ set บรรทัดเดียวรู้เรื่อง!",
            },

            { t: "h3", c: "ดูทีละขั้น (Interactive)" },
            {
              t: "p",
              c: "กด **Next ▶** นับ frequency ของ arr = [1, 2, 2, 1, 1, 3] ลง Counter · เฟส 1 ขีดนับความถี่ · เฟส 2 ดึงเฉพาะ values มาเทใส่ Set · เขียว = ขนาดเท่าเดิม ไม่ซ้ำ",
            },
            { t: "viz", id: "hash-freq" },

            { t: "h3", c: "โค้ดสำหรับวางใน LeetCode" },
            {
              t: "code",
              lang: "python",
              c: `from collections import Counter
from typing import List

class Solution:
    def uniqueOccurrences(self, arr: List[int]) -> bool:
        # 1. นับว่าแต่ละตัวเลขโผล่มากี่ครั้ง
        counts = Counter(arr)

        # 2. ดึงเฉพาะจำนวนครั้ง (values) ออกมา
        freq_list = counts.values()

        # 3. เทียบความยาว: ถ้าเทใส่ set แล้วความยาวไม่ลด แสดงว่าไม่มีความถี่ใดซ้ำกัน
        return len(freq_list) == len(set(freq_list))`,
            },

            { t: "h3", c: "อ่านโค้ดทีละส่วน" },
            {
              t: "table",
              head: ["บรรทัดโค้ด", "ความหมาย", "ค่าที่ได้ (ตัวอย่าง [1,2,2,1,1,3])"],
              rows: [
                ["counts = Counter(arr)", "นับความถี่ของแต่ละเลขใส่พจนานุกรม", "{1: 3, 2: 2, 3: 1}"],
                ["freq_list = counts.values()", "ดึงเฉพาะยอดนับความถี่", "[3, 2, 1]"],
                ["set(freq_list)", "นำยอดนับใส่ set เพื่อกำจัดตัวซ้ำ", "{1, 2, 3}"],
                ["len(...) == len(...)", "ตรวจว่าความยาวก่อนและหลังยุบเท่ากันไหม", "3 == 3 → True"],
              ],
            },

            { t: "h3", c: "ต้นทุน" },
            {
              t: "p",
              c: "• **เวลา (Time Complexity)**: O(n) โดย n คือความยาวของ `arr` — เราวนอ่านตัวเลขใน arr รอบเดียวเพื่อสร้าง Counter จากนั้นอ่านยอดนับที่มีไม่เกิน n ตัวเพื่อสร้าง set ทั้งหมดทำงานเป็น O(n)\n\n• **พื้นที่ (Space Complexity)**: O(n) เพื่อเก็บตารางความถี่และ set ของยอดนับในหน่วยความจำ",
            },
          ],
        },
      ],
      en: [
        {
          t: "p",
          c: "Given an array of integers arr, return true if the number of occurrences of each value in the array is unique or false otherwise.",
        },
        {
          t: "example",
          c: [
            {
              input: "arr = [1,2,2,1,1,3]",
              output: "true",
              explain:
                "The value 1 has 3 occurrences, 2 has 2 and 3 has 1. No two values have the same number of occurrences.",
            },
            {
              input: "arr = [1,2]",
              output: "false",
            },
            {
              input: "arr = [-3,0,1,-3,1,1,1,-3,10,0]",
              output: "true",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= arr.length <= 1000",
            "-1000 <= arr[i] <= 1000",
          ],
        },
      ],
    },
  },

  "lc75-p22": {
    slug: "lc75-p22",
    title: {
      th: "ข้อ 22 · LC1657 Determine if Two Strings Are Close 🟡",
      en: "1657. Determine if Two Strings Are Close",
    },
    lead: {
      th: "ตัดสินว่าสตริงสองตัวแปลงหากันได้หรือไม่ — แปลงกฎการสลับที่ซับซ้อนให้กลายเป็นด่านตรวจ 3 ด่าน: ความยาว, ชุดตัวอักษร, และกองความถี่",
      en: "Check if two strings are close by breaking the allowed operations into three checkpoints: length, character set, and frequency multiset.",
    },
    group: "LeetCode 75",
    blocks: {
      th: [
        {
          t: "p",
          c: "Two strings are considered close if you can attain one from the other using the following operations:\n\n• Operation 1: Swap any two existing characters.\n  - For example, abcde -> aecdb\n• Operation 2: Transform every occurrence of one existing character into another existing character, and do the same with the other character.\n  - For example, aacabb -> bbcbaa (all a's turn into b's, and all b's turn into a's)\n\nYou can use the operations on either string as many times as necessary.\n\nGiven two strings, word1 and word2, return true if word1 and word2 are close, and false otherwise.",
        },
        {
          t: "p",
          c: "สตริงสองตัวถือว่า \"ใกล้กัน\" (close) ถ้าคุณสามารถเปลี่ยนจากสตริงหนึ่งไปเป็นอีกสตริงหนึ่งได้โดยใช้การกระทำ 2 แบบนี้กี่ครั้งก็ได้:\n\n• **Operation 1 (สลับที่)**: สลับตำแหน่งตัวอักษร 2 ตัวใดก็ได้ที่มีอยู่ในข้อความ เช่น `abcde` -> `aecdb`\n• **Operation 2 (สลับตัวตน)**: เปลี่ยนตัวอักษรชนิดหนึ่งทั้งหมดให้เป็นอีกชนิดหนึ่ง และสลับตัวอักษรชนิดนั้นกลับมาด้วย เช่น `aacabb` -> `bbcbaa` (a ทั้งหมดกลายเป็น b และ b ทั้งหมดกลายเป็น a)\n\nกำหนดสตริง `word1` และ `word2` มาให้ — ให้คืนค่า `true` ถ้าทั้งสองตัว close กัน หรือ `false` ในกรณีอื่น",
        },
        {
          t: "example",
          c: [
            {
              input: 'word1 = "abc", word2 = "bca"',
              output: "true",
              explain:
                "ใช้ Operation 1 สลับตำแหน่งตัวอักษรได้อิสระ: 'abc' -> 'acb' -> 'bca' ได้เลย",
            },
            {
              input: 'word1 = "a", word2 = "aa"',
              output: "false",
              explain:
                "ความยาวไม่เท่ากัน ไม่ว่าจะสลับอย่างไรก็ไม่มีทางแปลงได้",
            },
            {
              input: 'word1 = "cabbba", word2 = "abbccc"',
              output: "true",
              explain:
                "word1 มี c:1, a:2, b:3\nword2 มี a:1, b:2, c:3\nใช้ Op 1 จัดตำแหน่ง และใช้ Op 2 สลับบทบาทความถี่ระหว่างตัวอักษรได้ทั้งหมด",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= word1.length, word2.length <= 10^5",
            "word1 และ word2 ประกอบด้วยตัวอักษรภาษาอังกฤษพิมพ์เล็กเท่านั้น",
          ],
        },
        {
          t: "callout",
          title: "⏸ ลองเองก่อน",
          c: "อย่าเขียนโปรแกรมจำลองการสลับ (Simulation) จริง ๆ เด็ดขาด เพราะวิธีสลับมีนับล้านแบบจนโปรแกรมค้าง! จงคิดว่า: 'การสลับ 2 แบบนี้ยอมให้เราทำอะไรได้บ้าง และอะไรที่ทำไม่ได้เลย?'",
        },

        {
          t: "solution",
          summary: "เฉลยเต็ม · ซ่อนไว้ให้ลองเองก่อน",
          c: [
            { t: "h3", c: "ขั้นที่ 1 · โจทย์นี้ขออะไร" },
            {
              t: "p",
              c: "โจทย์ถามว่าคำสองคำแปลงร่างหากันได้ไหมด้วยกติกา 2 ข้อ ถ้าเรามองลึก ๆ ถึงผลลัพธ์ของกติกาแต่ละข้อ:\n\n1. **Operation 1 (สลับที่)**: หมายความว่า **ลำดับตัวอักษรไม่สำคัญเลย** เราจะเรียงตัวอักษรใหม่ยังไงก็ได้ ขอแค่มีตัวอักษรครบ\n2. **Operation 2 (สลับบทบาท)**: หมายความว่า **กองจำนวนความถี่สามารถสลับเจ้าของกันได้** เช่น ถ้าคำแรกมี (ตัวหนึ่งมี 3 ตัว, อีกตัวมี 1 ตัว) คำสองก็ขอแค่มี (ตัวหนึ่ง 3 ตัว, อีกตัว 1 ตัว) ไม่สำคัญว่าใครจะถือเลข 3 หรือเลข 1!",
            },
            {
              t: "p",
              c: "แต่สิ่งสำคัญคือ: **ห้ามเสกตัวอักษรชนิดใหม่ขึ้นมา** ตัวอักษรที่มีใน word1 ต้องเป็นชนิดเดียวกับใน word2 เป๊ะ ๆ",
            },

            { t: "h3", c: "ขั้นที่ 2 · ทำให้ได้ด้วยมือ" },
            {
              t: "p",
              c: "เราสรุปเป็นด่านตรวจ 3 ด่าน ถอดรหัสคำว่า Close:\n\n• **ด่านที่ 1 (ความยาว)**: `len(word1) == len(word2)` ถ้าความยาวไม่เท่ากัน ตกทันที\n• **ด่านที่ 2 (ชนิดตัวอักษร)**: `set(word1) == set(word2)` ต้องมีตัวอักษรชุดเดียวกันเป๊ะ เช่น ถ้าฝั่งหนึ่งมี 'x' แต่อีกฝั่งไม่มี 'x' ก็แปลงไม่ได้แน่นอน\n• **ด่านที่ 3 (กองความถี่)**: เอาจำนวนครั้งของตัวอักษรแต่ละตัวมาเรียงลำดับจากน้อยไปมาก แล้วเทียบกัน ต้องได้ตัวเลขชุดเดียวกันเป๊ะ!",
            },
            {
              t: "p",
              c: "ทดสอบกับ 'cabbba' vs 'abbccc':\n• ด่าน 1: ยาว 6 เท่ากัน ✅ ผ่าน\n• ด่าน 2: ทั้งสองคำประกอบด้วย {'a', 'b', 'c'} เหมือนกันเป๊ะ ✅ ผ่าน\n• ด่าน 3: \n  - word1 มี c:1, a:2, b:3 → กองความถี่คือ [1, 2, 3]\n  - word2 มี a:1, b:2, c:3 → กองความถี่คือ [1, 2, 3]\n  - กองความถี่ [1, 2, 3] เหมือนกันเป๊ะ! ✅ ผ่าน → ตอบ True",
            },

            { t: "h3", c: "ขั้นที่ 3 · วิธีทำ" },
            {
              t: "p",
              c: "ภาพรวม: เขียนเงื่อนไข if ตรวจสอบด่านทั้งสามตามลำดับ ถ้าผ่านทั้ง 3 ด่านตอบ `True` ถ้าตกด่านใดด่านหนึ่งให้ตอบ `False` ทันที",
            },
            {
              t: "p",
              c: "เครื่องมือที่ต้องใช้:",
            },
            {
              t: "ul",
              c: [
                "len(...) — ตรวจสอบความยาวตัวอักษร",
                "set(...) — ดึงเฉพาะชนิดตัวอักษรที่มี เพื่อตรวจสอบว่ามีตัวแปลกปลอมไหม",
                "Counter(...) — นับจำนวนครั้งของตัวอักษรแต่ละชนิด",
                "sorted(c.values()) — ดึงเฉพาะตัวเลขความถี่แล้วนำมาเรียงลำดับ เพื่อนำมาเทียบว่า pattern ของกองตัวเลขตรงกันไหม",
              ],
            },
            {
              t: "p",
              c: "กับดักที่พบบ่อย: หลายคนลืมเช็คด่านที่ 2 (`set(word1) == set(word2)`) ทำให้เคสอย่าง `word1 = 'uau'`, `word2 = 'ssx'` ตอบผิด! เพราะ word1 มีความถี่ [1, 2] และ word2 ก็มีความถี่ [1, 2] เหมือนกัน แต่ตัวอักษรเป็นคนละพวกกันเลย ('u', 'a' กับ 's', 'x') ซึ่ง Operation 2 ไม่สามารถเสกตัวอักษรใหม่ได้ ด่านที่ 2 จึงห้ามขาดเด็ดขาด!",
            },

            { t: "h3", c: "ดูทีละขั้น (Interactive)" },
            {
              t: "p",
              c: "กด **Next ▶** ไล่สามด่านกับ word1 = 'cabbba', word2 = 'abbccc' · เขียว = ด่านผ่าน · Gate 2 คือชุดตัวอักษร (เสกตัวใหม่ไม่ได้) · Gate 3 คือกองจำนวนหลังเรียง (ย้ายกองได้)",
            },
            { t: "viz", id: "hash-close" },

            { t: "h3", c: "โค้ดสำหรับวางใน LeetCode" },
            {
              t: "code",
              lang: "python",
              c: `from collections import Counter

class Solution:
    def closeStrings(self, word1: str, word2: str) -> bool:
        # ด่านที่ 1: ความยาวต้องเท่ากัน
        if len(word1) != len(word2):
            return False

        # ด่านที่ 2: ชุดตัวอักษรที่มีต้องเหมือนกันเป๊ะ (เสกตัวใหม่ไม่ได้)
        if set(word1) != set(word2):
            return False

        # ด่านที่ 3: กองตัวเลขความถี่เมื่อเรียงลำดับแล้วต้องเท่ากัน
        count1 = Counter(word1)
        count2 = Counter(word2)
        return sorted(count1.values()) == sorted(count2.values())`,
            },

            { t: "h3", c: "อ่านโค้ดทีละส่วน" },
            {
              t: "table",
              head: ["ท่อนโค้ด", "ทำไมต้องมี", "ผลลัพธ์ถ้าลืมใส่"],
              rows: [
                ["if len(word1) != len(word2): return False", "ตัดกรณีความยาวไม่เท่ากันออกทันที", "ช้าลง และอาจคำนวณต่อโดยไม่จำเป็น"],
                ["if set(word1) != set(word2): return False", "เช็คว่าทั้งสองคำมีชนิดตัวอักษรตรงกัน", "ตอบ True ผิดในกรณี 'uau' กับ 'ssx'"],
                ["sorted(count1.values()) == sorted(...)", "เทียบว่ามีกองจำนวนความถี่รูปแบบเดียวกันไหม", "ไม่สามารถตรวจสอบ Op 2 ได้"],
              ],
            },

            { t: "h3", c: "ต้นทุน" },
            {
              t: "p",
              c: "• **เวลา (Time Complexity)**: O(n) โดย n คือความยาวของสตริง — การนับตัวอักษรด้วย Counter และหา set ใช้เวลา O(n) ส่วนการเรียงลำดับ `sorted(values)` ตัวอักษรภาษาอังกฤษมีไม่เกิน 26 ตัว การ sort อาเรย์ขนาดคงที่ 26 ตัวจึงใช้เวลาคงที่ O(26 log 26) = O(1)\n\n• **พื้นที่ (Space Complexity)**: O(1) เนื่องจากเก็บความถี่ของตัวอักษรภาษาอังกฤษพิมพ์เล็กไม่เกิน 26 ชนิด ขนาดของ memory จึงคงที่",
            },
          ],
        },
      ],
      en: [
        {
          t: "p",
          c: "Two strings are considered close if you can attain one from the other using the following operations:\n\n• Operation 1: Swap any two existing characters.\n  - For example, abcde -> aecdb\n• Operation 2: Transform every occurrence of one existing character into another existing character, and do the same with the other character.\n  - For example, aacabb -> bbcbaa (all a's turn into b's, and all b's turn into a's)\n\nYou can use the operations on either string as many times as necessary.\n\nGiven two strings, word1 and word2, return true if word1 and word2 are close, and false otherwise.",
        },
        {
          t: "example",
          c: [
            {
              input: 'word1 = "abc", word2 = "bca"',
              output: "true",
              explain:
                'You can attain word2 from word1 in 2 operations. Apply Operation 1: "abc" -> "acb". Apply Operation 1: "acb" -> "bca".',
            },
            {
              input: 'word1 = "a", word2 = "aa"',
              output: "false",
              explain:
                "It is impossible to attain word2 from word1, or vice versa, in any number of operations.",
            },
            {
              input: 'word1 = "cabbba", word2 = "abbccc"',
              output: "true",
              explain:
                'You can attain word2 from word1 in 3 operations. Apply Operation 1: "cabbba" -> "caabbb". Apply Operation 2: "caabbb" -> "baaccc". Apply Operation 2: "baaccc" -> "abbccc".',
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "1 <= word1.length, word2.length <= 10^5",
            "word1 and word2 contain only lowercase English letters.",
          ],
        },
      ],
    },
  },

  "lc75-p23": {
    slug: "lc75-p23",
    title: {
      th: "ข้อ 23 · LC2352 Equal Row and Column Pairs 🟡",
      en: "2352. Equal Row and Column Pairs",
    },
    lead: {
      th: "นับจำนวนคู่ระหว่างแถวแนวนอน (row) และแถวแนวตั้ง (column) ที่มีตัวเลขเหมือนกันทุกช่อง — จดบัญชีแถวด้วย Counter แล้วเอาแต่ละคอลัมน์มาตรวจ",
      en: "Count matching row and column pairs by recording row tuples in a Counter, then querying each column against it.",
    },
    group: "LeetCode 75",
    blocks: {
      th: [
        {
          t: "p",
          c: "Given a 0-indexed n x n integer matrix grid, return the number of pairs (ri, cj) such that row ri and column cj are equal.\n\nA row and column pair is considered equal if they contain the same elements in the same order (i.e., an equal array).",
        },
        {
          t: "p",
          c: "กำหนดตารางตัวเลขสี่เหลี่ยมจัตุรัส `grid` ขนาด n x n มาให้ — จงหาจำนวนคู่ `(ri, cj)` ทั้งหมดที่ แถวแนวนอนที่ `ri` และ คอลัมน์แนวตั้งที่ `cj` มีตัวเลขเหมือนกันทุกตำแหน่งและเรียงตามลำดับเดียวกันเป๊ะ ๆ",
        },
        {
          t: "example",
          c: [
            {
              input: "grid = [[3,2,1],[1,7,6],[2,7,7]]",
              output: "1",
              explain:
                "แนวนอน (Rows):\n• แถว 0: [3, 2, 1]\n• แถว 1: [1, 7, 6]\n• แถว 2: [2, 7, 7]\n\nแนวตั้ง (Columns):\n• คอลัมน์ 0: [3, 1, 2]\n• คอลัมน์ 1: [2, 7, 7]\n• คอลัมน์ 2: [1, 6, 7]\n\nคู่ที่เหมือนกันคือ แถว 2 กับ คอลัมน์ 1 คือ [2, 7, 7] ตรงกัน 1 คู่",
            },
            {
              input: "grid = [[3,1,2,2],[1,4,4,5],[2,4,2,2],[2,4,2,2]]",
              output: "3",
              explain:
                "คู่ที่เท่ากันมี 3 คู่:\n• แถว 0 กับ คอลัมน์ 0: [3, 1, 2, 2]\n• แถว 2 กับ คอลัมน์ 2: [2, 4, 2, 2]\n• แถว 3 กับ คอลัมน์ 2: [2, 4, 2, 2]",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "n == grid.length == grid[i].length",
            "1 <= n <= 200",
            "1 <= grid[i][j] <= 10^5",
          ],
        },
        {
          t: "callout",
          title: "⏸ ลองเองก่อน",
          c: "ใน Python โครงสร้างข้อมูลอย่าง List ไม่สามารถนำมาเป็น key ของ Dictionary หรือ Counter ได้... ทำไมนะ? แล้วต้องแปลงเป็นอะไรก่อน?",
        },

        {
          t: "solution",
          summary: "เฉลยเต็ม · ซ่อนไว้ให้ลองเองก่อน",
          c: [
            { t: "h3", c: "ขั้นที่ 1 · โจทย์นี้ขออะไร" },
            {
              t: "p",
              c: "โจทย์ให้ตารางขนาด n × n มา และอยากรู้ว่ามีกี่คู่ที่เส้นแนวนอน (Row) หน้าตาเหมือนกับเส้นแนวตั้ง (Column) ทุกตัวเลขตามลำดับ",
            },
            {
              t: "p",
              c: "จุดสำคัญ: ถ้ามีแนวนอนที่หน้าตาเหมือนกัน 2 แถว และมีแนวตั้งที่หน้าตาตรงกับแถวนั้น 1 คอลัมน์ จะเกิดคู่ขึ้น **2 คู่** ไม่ใช่ 1 คู่! ดังนั้นเราต้อง 'นับจำนวนครั้ง' ของแต่ละแถวไว้ด้วย ไม่ใช่แค่จำว่าเคยเจอไหม",
            },

            { t: "h3", c: "ขั้นที่ 2 · ทำให้ได้ด้วยมือ" },
            {
              t: "p",
              c: "ลองใช้ตาราง 3×3: grid = [[3,2,1], [1,7,6], [2,7,7]]",
            },
            {
              t: "ul",
              c: [
                "เฟสที่ 1 (จดบัญชีแถวแนวนอนลงสมุด): กวาดมองทีละแถว\n• แถวที่ 0: (3, 2, 1) → บันทึกไว้ 1 ครั้ง\n• แถวที่ 1: (1, 7, 6) → บันทึกไว้ 1 ครั้ง\n• แถวที่ 2: (2, 7, 7) → บันทึกไว้ 1 ครั้ง",
                "เฟสที่ 2 (ดึงแนวตั้งมาตรวจยอด): ประกอบตัวเลขตามแนวตั้งทีละคอลัมน์\n• คอลัมน์ที่ 0: อ่านดิ่งลงมาได้ (3, 1, 2) → ไปเปิดสมุดดู... ไม่มีในสมุด (ได้ 0)\n• คอลัมน์ที่ 1: อ่านดิ่งลงมาได้ (2, 7, 7) → ไปเปิดสมุดดู... มีในสมุด 1 ครั้ง! → บวกคะแนน 1\n• คอลัมน์ที่ 2: อ่านดิ่งลงมาได้ (1, 6, 7) → ไปเปิดสมุดดู... ไม่มีในสมุด (ได้ 0)",
                "รวมคะแนนทั้งหมด: ได้ 1 คู่",
              ],
            },

            { t: "h3", c: "ขั้นที่ 3 · วิธีทำ" },
            {
              t: "p",
              c: "ภาพรวม: แปลงแถวแนวนอนทุกแถวให้เป็น `tuple` เพื่อใช้เป็น key ใน `Counter` จากนั้นลูปประกอบคอลัมน์แนวตั้งทีละเส้น แล้วถาม `Counter` ว่าเส้นนี้มีในแถวแนวนอนกี่แถว แล้วบวกจำนวนนั้นเข้ากับผลรวม",
            },
            {
              t: "p",
              c: "เครื่องมือที่ต้องใช้:",
            },
            {
              t: "ul",
              c: [
                "tuple(row) — กฎเหล็กของ Python: List เปลี่ยนแปลงค่าได้ (mutable) จึงคำนวณ hash ไม่ได้ (unhashable) ทำให้เป็น key ของ dict ไม่ได้ ต้องแปลงเป็น tuple ที่ล็อกค่าคงที่ไว้ก่อน",
                "row_counts = Counter(...) — สมุดจดบัญชีที่จำว่าแต่ละแถว (tuple) ปรากฏมากี่ครั้ง",
                "col = tuple(grid[r][c] for r in range(n)) — การดึงตัวเลขในคอลัมน์ c โดยตรึง c ไว้แล้ววิ่งตัวแปรแถว r จาก 0 ถึง n-1",
                "pairs += row_counts[col] — ถ้ามีในบัญชีกี่แถวก็บวกเพิ่มเท่านั้น (Counter จะคืนค่า 0 อัตโนมัติถ้าไม่เจอ key)",
              ],
            },
            {
              t: "p",
              c: "ข้อผิดพลาดที่พบบ่อย: อย่าเขียน `pairs += 1` ตอนเจอคู่! ต้องเขียน `pairs += row_counts[col]` เพราะถ้ามีแถวหน้าตาเหมือนกัน 3 แถว คอลัมน์นี้จะต้องจับคู่ได้ถึง 3 ครั้ง",
            },

            { t: "h3", c: "ดูทีละขั้น (Interactive)" },
            {
              t: "p",
              c: "กด **Next ▶** เฟส 1 จดแถวลง ledger เป็น tuple · เฟส 2 ประกอบคอลัมน์ทีละเส้นไปถาม · ทอง = แถว/คอลัมน์ที่กำลังดู · เขียว = คู่ที่หน้าตาตรงกัน — สังเกตว่าบวกด้วยจำนวนครั้งในบัญชี ไม่ใช่บวก 1",
            },
            { t: "viz", id: "hash-pairs" },

            { t: "h3", c: "โค้ดสำหรับวางใน LeetCode" },
            {
              t: "code",
              lang: "python",
              c: `from collections import Counter
from typing import List

class Solution:
    def equalPairs(self, grid: List[List[int]]) -> int:
        n = len(grid)

        # เฟส 1: จดบัญชีแถวแนวนอนลง Counter
        # ต้องแปลง list เป็น tuple เพราะ list นำมาเป็น key ของ dict ไม่ได้
        row_counts = Counter(tuple(row) for row in grid)

        pairs = 0

        # เฟส 2: ดึงแนวตั้งทีละคอลัมน์มาเปิดบัญชีเทียบ
        for c in range(n):
            # ดึงสมาชิกในคอลัมน์ c จากบนลงล่าง
            col = tuple(grid[r][c] for r in range(n))
            # ถ้าคอลัมน์นี้ตรงกับแถวใดในบัญชี ให้บวกจำนวนแถวนั้นเพิ่มเข้าไป
            pairs += row_counts[col]

        return pairs`,
            },

            { t: "h3", c: "อ่านโค้ดทีละส่วน" },
            {
              t: "table",
              head: ["โค้ด", "หน้าที่", "ตัวอย่าง"],
              rows: [
                ["tuple(row) for row in grid", "แปลงทุกแถวแนวนอนเป็น tuple", "(3, 2, 1), (1, 7, 6), (2, 7, 7)"],
                ["row_counts = Counter(...)", "นับว่าแต่ละแถวโผล่มากี่ครั้ง", "{(3,2,1): 1, (1,7,6): 1, (2,7,7): 1}"],
                ["col = tuple(grid[r][c] for r in range(n))", "อ่านตัวเลขคอลัมน์ c แนวดิ่ง", "เมื่อ c=1 จะได้ (2, 7, 7)"],
                ["pairs += row_counts[col]", "นำคอลัมน์ไปถาม Counter แล้วบวกยอด", "เจอ (2,7,7) ใน Counter มีค่า 1 → pairs += 1"],
              ],
            },

            { t: "h3", c: "ต้นทุน" },
            {
              t: "p",
              c: "• **เวลา (Time Complexity)**: O(n²) โดย n คือขนาดมิติของตาราง — มี n แถว แต่ละแถวยาว n การแปลงเป็น tuple ใช้ O(n²) และมี n คอลัมน์ แต่ละคอลัมน์ยาว n การสร้างและค้นหาใน hash map ใช้ O(n²) รวมเป็น O(n²)\n\n• **พื้นที่ (Space Complexity)**: O(n²) สำหรับเก็บ tuple ของแถวแนวนอนทั้งหมดใน Counter",
            },
          ],
        },
      ],
      en: [
        {
          t: "p",
          c: "Given a 0-indexed n x n integer matrix grid, return the number of pairs (ri, cj) such that row ri and column cj are equal.\n\nA row and column pair is considered equal if they contain the same elements in the same order (i.e., an equal array).",
        },
        {
          t: "example",
          c: [
            {
              input: "grid = [[3,2,1],[1,7,6],[2,7,7]]",
              output: "1",
              explain:
                "There is 1 equal row and column pair: (Row 2, Column 1): [2,7,7]",
            },
            {
              input: "grid = [[3,1,2,2],[1,4,4,5],[2,4,2,2],[2,4,2,2]]",
              output: "3",
              explain:
                "There are 3 equal row and column pairs:\n• (Row 0, Column 0): [3,1,2,2]\n• (Row 2, Column 2): [2,4,2,2]\n• (Row 3, Column 2): [2,4,2,2]",
            },
          ],
        },
        {
          t: "constraints",
          c: [
            "n == grid.length == grid[i].length",
            "1 <= n <= 200",
            "1 <= grid[i][j] <= 10^5",
          ],
        },
      ],
    },
  },
};
