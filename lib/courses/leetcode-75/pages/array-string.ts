import type { Page } from "@/lib/types";

export const arrayStringPages: Record<string, Page> = {
  "lc75-array-string": {
    slug: "lc75-array-string",
    title: "Array / String — จัดการลิสต์และข้อความ",
    lead: "หัวข้อวอร์มอัพว่าด้วยการเข้าถึง วน และแปลงลิสต์กับสตริงให้คล่องก่อนลุยเทคนิคจริง",
    group: "LeetCode 75",
    blocks: [
      { t: "p", c: "หัวข้อ Array / String คือด่านแรกของ LeetCode 75 เป็นหัวข้อวอร์มอัพที่ยังไม่ต้องใช้เทคนิคหวือหวา แต่ต้องคล่องเรื่องพื้นฐาน คือการหยิบสมาชิกในลิสต์ การตัดช่วง (slice) การวนลูป และการแปลงไปมาระหว่าง string กับ list ให้ได้ ถ้าพื้นฐานพวกนี้แน่น โจทย์ในหัวข้อต่อ ๆ ไปจะง่ายขึ้นเยอะ" },

      { t: "h2", c: "แนวคิดของหัวข้อนี้" },

      { t: "h3", c: "เข้าถึงสมาชิกด้วย index" },
      { t: "p", c: "ลิสต์ใน Python เข้าถึงด้วยเลข index เริ่มที่ 0 และใช้เลขติดลบเพื่อนับจากท้ายได้ nums[-1] คือตัวสุดท้าย nums[-2] คือตัวรองสุดท้าย ตรงนี้ใช้บ่อยมากเวลาต้องดูตัวท้าย ๆ" },
      { t: "code", lang: "python", c: `nums = [10, 20, 30, 40]
print(nums[0])    # 10  ตัวแรก
print(nums[-1])   # 40  ตัวสุดท้าย
print(nums[-2])   # 30  ตัวรองสุดท้าย
print(len(nums))  # 4   จำนวนสมาชิก` },

      { t: "h3", c: "การตัดช่วง (slicing)" },
      { t: "p", c: "รูปแบบคือ nums[a:b] จะได้สมาชิกตั้งแต่ index a ถึง b-1 (รวม a ไม่รวม b) ถ้าละตัวหน้าไว้จะเริ่มที่ 0 ถ้าละตัวหลังจะไปจนจบ และ nums[::-1] คือการกลับลิสต์ทั้งหมด (step เป็น -1) เทคนิคนี้ทำงานได้ทั้งกับ list และ string" },
      { t: "code", lang: "python", c: `nums = [10, 20, 30, 40, 50]
print(nums[1:4])   # [20, 30, 40]  ตั้งแต่ index 1 ถึง 3
print(nums[:3])    # [10, 20, 30]  ตั้งแต่ต้นถึง index 2
print(nums[2:])    # [30, 40, 50]  ตั้งแต่ index 2 ถึงจบ
print(nums[::-1])  # [50, 40, 30, 20, 10]  กลับลิสต์
print("hello"[::-1])  # "olleh"  กลับสตริงก็ได้` },

      { t: "h3", c: "การวนลิสต์" },
      { t: "p", c: "วนตามค่าโดยตรงด้วย for x in nums ถ้าต้องการทั้ง index และค่าให้ใช้ enumerate(nums) และถ้าอยากได้เลข index อย่างเดียวใช้ range(len(nums)) เลือกใช้ตามงาน" },
      { t: "code", lang: "python", c: `nums = [10, 20, 30]

for x in nums:              # วนตามค่า
    print(x)                # 10, 20, 30

for i, x in enumerate(nums):  # ได้ทั้ง index และค่า
    print(i, x)             # 0 10, 1 20, 2 30

for i in range(len(nums)):  # ได้เฉพาะ index
    print(nums[i])` },

      { t: "h3", c: "string เป็น immutable" },
      { t: "p", c: "จุดที่คนพลาดบ่อย คือ string ใน Python แก้ทีละตัวไม่ได้ การเขียน s[0] = 'x' จะ error เพราะ string เป็น immutable (เปลี่ยนแปลงในที่เดิมไม่ได้) ถ้าต้องแก้ตัวอักษรหลายตำแหน่ง วิธีมาตรฐานคือแปลงเป็น list ก่อน แก้ให้เสร็จ แล้วค่อย join กลับเป็น string" },
      { t: "code", lang: "python", c: `s = "hello"
# s[0] = "H"   # ❌ error: 'str' object does not support item assignment

chars = list(s)   # ['h', 'e', 'l', 'l', 'o']  แปลงเป็น list ก่อน
chars[0] = "H"    # แก้ได้
s = "".join(chars)  # "Hello"  join กลับเป็น string` },

      { t: "h3", c: "ฟังก์ชันที่ใช้บ่อย" },
      { t: "p", c: "len(x) นับจำนวน · ''.join(list_of_str) ต่อ list ของ string เป็น string เดียว (ตัวคั่นคือ string ที่อยู่หน้า .join) · s.split() ตัด string เป็น list ของคำ ถ้าไม่ใส่อาร์กิวเมนต์จะตัดด้วยช่องว่างและตัดช่องว่างเกินให้อัตโนมัติด้วย" },
      { t: "code", lang: "python", c: `words = ["I", "love", "python"]
print(" ".join(words))   # "I love python"  คั่นด้วยช่องว่าง
print("-".join(words))   # "I-love-python"  คั่นด้วยขีด

s = "  hello   world  "
print(s.split())          # ['hello', 'world']  ตัดช่องว่างเกินให้เอง
print("a,b,c".split(","))  # ['a', 'b', 'c']  ระบุตัวคั่นเองได้` },

      { t: "callout", title: "เคล็ดลับ", c: "จำไว้ 2 คู่หูนี้จะช่วยได้เยอะในหัวข้อ String คือ list(s) เพื่อแตกสตริงเป็นตัวอักษรที่แก้ได้ และ ''.join(chars) เพื่อประกอบกลับ" },

      { t: "h2", c: "ข้อ 1 — สลับสองสตริง (LC1768) 🟢" },
      { t: "p", c: "มีสตริงสองตัว word1 กับ word2 ให้สร้างสตริงใหม่โดยหยิบตัวอักษรสลับกันทีละตัว เริ่มจาก word1 ก่อน ถ้าตัวไหนหมดก่อนให้เอาส่วนที่เหลือของอีกตัวต่อท้าย เช่น word1 = \"abc\", word2 = \"pqr\" → \"apbqcr\" และ word1 = \"ab\", word2 = \"pqrs\" → \"apbqrs\" (word2 ยาวกว่า ตัว rs ที่เหลือต่อท้าย)" },
      { t: "callout", title: "คำใบ้", c: "วนด้วย index ไปจนถึงความยาวของตัวที่ยาวกว่า แล้วเช็คว่ายังมีตัวอักษรในแต่ละสตริงที่ index นั้นไหม" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def merge_alternately(word1, word2):
    result = []
    n = max(len(word1), len(word2))  # วนถึงตัวที่ยาวกว่า
    for i in range(n):
        if i < len(word1):           # ยังมีตัวใน word1 ที่ index นี้ไหม
            result.append(word1[i])
        if i < len(word2):           # ยังมีตัวใน word2 ที่ index นี้ไหม
            result.append(word2[i])
    return "".join(result)           # ประกอบ list กลับเป็น string

print(merge_alternately("abc", "pqr"))  # apbqcr
print(merge_alternately("ab", "pqrs"))  # apbqrs` },
        { t: "p", c: "ไอเดียคือวน index ตั้งแต่ 0 ถึงความยาวของสตริงที่ยาวกว่า ในแต่ละรอบลองหยิบตัวจาก word1 ก่อน แล้วตามด้วย word2 แต่ต้องเช็คก่อนว่า index นั้นยังไม่เกินความยาวของแต่ละสตริง ไม่งั้นจะ index ออกนอกช่วง การเก็บผลลัพธ์ใส่ list แล้วค่อย join ตอนท้ายเร็วกว่าเอา string มาบวกกันในลูป เพราะ string เป็น immutable การบวกทุกครั้งจะสร้าง string ใหม่" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมกรณีที่สองสตริงยาวไม่เท่ากัน ถ้าวนแค่ min(len) จะตกส่วนหางที่เหลือไป การวนถึง max(len) พร้อมเช็คขอบเขตทีละตัวจึงครอบคลุมทุกกรณีในลูปเดียว" },
        { t: "p", c: "Time O(n+m) วนตามความยาวรวมของสองสตริง · Space O(n+m) สำหรับสตริงผลลัพธ์" },
      ] },

      { t: "h2", c: "ข้อ 2 — ตัวหารร่วมมากของสตริง (LC1071) 🟢" },
      { t: "p", c: "มีสตริงสองตัว str1 กับ str2 นิยามว่า t \"หาร\" s ได้ ถ้า s เกิดจากการเอา t มาต่อกันซ้ำ ๆ (เช่น \"abab\" เกิดจาก \"ab\" ต่อกัน 2 ครั้ง) ให้หาสตริง t ที่ยาวที่สุดที่หารได้ทั้ง str1 และ str2 เช่น str1 = \"ABCABC\", str2 = \"ABC\" → \"ABC\" และ str1 = \"ABABAB\", str2 = \"ABAB\" → \"AB\" ถ้าไม่มีเลยให้คืนสตริงว่าง" },
      { t: "callout", title: "คำใบ้", c: "ถ้ามี t ที่หารได้ทั้งคู่ การต่อสตริงสองแบบ str1 + str2 กับ str2 + str1 ต้องได้ผลเท่ากัน และความยาวของ t จะเท่ากับ gcd ของความยาวสองสตริง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `from math import gcd

def gcd_of_strings(str1, str2):
    # ถ้าต่อกันสองแบบแล้วไม่เท่ากัน แปลว่าไม่มีตัวหารร่วม
    if str1 + str2 != str2 + str1:
        return ""
    # ความยาวของคำตอบคือ gcd ของความยาวสองสตริง
    g = gcd(len(str1), len(str2))
    return str1[:g]

print(gcd_of_strings("ABCABC", "ABC"))   # ABC
print(gcd_of_strings("ABABAB", "ABAB"))  # AB
print(gcd_of_strings("LEET", "CODE"))    # "" (ว่าง)` },
        { t: "p", c: "กุญแจสำคัญมี 2 อย่าง อย่างแรกคือเช็คว่า \"มี\" ตัวหารร่วมไหม ทริกคลาสสิกคือ ถ้าทั้งสองสตริงประกอบขึ้นจากบล็อกเดียวกัน การต่อ str1 + str2 กับ str2 + str1 ต้องได้สตริงเดียวกันเป๊ะ ถ้าต่างกันแสดงว่าไม่มีทางมีตัวหารร่วม อย่างที่สองคือ \"ความยาว\" ของตัวหารร่วมที่ยาวที่สุด ต้องหารความยาวของทั้งสองสตริงลงตัว ตัวที่ยาวที่สุดจึงเท่ากับ gcd (ห.ร.ม.) ของสองความยาวพอดี แล้วตัดหัวมา g ตัวก็เป็นคำตอบ" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือพยายามไล่ลองทีละความยาวโดยไม่เช็คเงื่อนไข str1 + str2 == str2 + str1 ก่อน ทำให้โค้ดยุ่งและพลาดกรณีที่ไม่มีคำตอบ การใช้สองทริกนี้ร่วมกันทำให้โค้ดสั้นและถูกต้อง" },
        { t: "p", c: "Time O(n+m) การต่อและเทียบสตริงเป็นเชิงเส้นตามความยาว · Space O(n+m) สำหรับสตริงที่ต่อขึ้นมาเทียบ" },
      ] },

      { t: "h2", c: "ข้อ 3 — เด็กที่ได้ลูกอมมากที่สุด (LC1431) 🟢" },
      { t: "p", c: "มีลิสต์ candies บอกจำนวนลูกอมของเด็กแต่ละคน และมีลูกอมพิเศษ extra เม็ด ให้ตอบว่าถ้าเอา extra ทั้งหมดไปเพิ่มให้เด็กคนที่ i แล้ว เด็กคนนั้นจะมีลูกอมมากที่สุด (มากกว่าหรือเท่ากับทุกคน) หรือไม่ คืนลิสต์ของ True/False เช่น candies = [2,3,5,1,3], extra = 3 → [true, true, true, false, true]" },
      { t: "callout", title: "คำใบ้", c: "หาค่ามากที่สุดในลิสต์ก่อนหนึ่งครั้ง แล้วเทียบทีละคนว่า candies[i] + extra ถึงค่ามากสุดนั้นไหม" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def kids_with_candies(candies, extra):
    biggest = max(candies)  # หาค่ามากสุดครั้งเดียวก่อน
    # เด็กคนนี้ถ้าได้ extra เพิ่มแล้วถึง biggest ไหม
    return [c + extra >= biggest for c in candies]

print(kids_with_candies([2, 3, 5, 1, 3], 3))
# [True, True, True, False, True]` },
        { t: "p", c: "หัวใจอยู่ที่การหาค่ามากที่สุดของลิสต์ \"ครั้งเดียว\" ไว้ก่อน เพราะการเพิ่ม extra ให้เด็กคนหนึ่งไม่ทำให้ลูกอมของคนอื่นเปลี่ยน ดังนั้นค่าที่ต้องแข่งด้วยคือค่ามากสุดเดิมเสมอ จากนั้นแค่เทียบทีละคนว่า candies[i] + extra >= biggest หรือไม่ ใช้ list comprehension เขียนได้ในบรรทัดเดียว" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือไปหา max ใหม่ในทุกรอบของลูป ทำให้กลายเป็น O(n^2) โดยไม่จำเป็น ที่จริงหา max รอบเดียวก็พอ" },
        { t: "p", c: "Time O(n) หา max หนึ่งรอบและวนเทียบอีกหนึ่งรอบ · Space O(n) สำหรับลิสต์ผลลัพธ์" },
      ] },

      { t: "h2", c: "ข้อ 4 — ปลูกดอกไม้ (LC605) 🟢" },
      { t: "p", c: "มีแปลงดอกไม้เป็นลิสต์ flowerbed ที่ 0 = ช่องว่าง 1 = ปลูกไว้แล้ว กติกาคือปลูกดอกไม้สองต้นติดกันไม่ได้ ให้ตอบว่าปลูกเพิ่มได้อีก n ต้นโดยไม่ผิดกติกาหรือไม่ (True/False) เช่น flowerbed = [1,0,0,0,1], n = 1 → true (ปลูกช่องกลางได้) แต่ n = 2 → false" },
      { t: "callout", title: "คำใบ้", c: "วิ่งไล่ทีละช่อง ช่องไหนว่างและซ้าย-ขวาว่างด้วย ให้ปลูกเลย (greedy) แล้วนับว่าปลูกครบ n ไหม เทคนิคจัดการขอบคือมองว่านอกแปลงเป็นช่องว่าง" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def can_place_flowers(flowerbed, n):
    count = 0
    length = len(flowerbed)
    for i in range(length):
        if flowerbed[i] == 0:
            # เช็คซ้ายและขวา ถ้าอยู่ริมให้ถือว่าด้านนอกว่าง (0)
            left_empty = (i == 0) or (flowerbed[i - 1] == 0)
            right_empty = (i == length - 1) or (flowerbed[i + 1] == 0)
            if left_empty and right_empty:
                flowerbed[i] = 1   # ปลูกเลย
                count += 1
                if count >= n:     # ปลูกครบแล้ว รีบตอบ
                    return True
    return count >= n

print(can_place_flowers([1, 0, 0, 0, 1], 1))  # True
print(can_place_flowers([1, 0, 0, 0, 1], 2))  # False` },
        { t: "p", c: "วิธีนี้คือ greedy (โลภมาก) วิ่งจากซ้ายไปขวา เจอช่องว่างที่ปลูกได้ก็ปลูกทันที เพราะการปลูกให้เร็วที่สุดจากซ้ายไม่ทำให้เสียโอกาสปลูกช่องขวา เงื่อนไขที่ปลูกได้คือช่องปัจจุบันเป็น 0 และช่องซ้าย-ขวาต้องว่างด้วย ทริกสำคัญคือการจัดการขอบ ถ้าอยู่ช่องแรกให้ถือว่าด้านซ้ายเป็นช่องว่าง และถ้าอยู่ช่องสุดท้ายให้ถือว่าด้านขวาว่าง จะได้ไม่ต้อง index ออกนอกลิสต์" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมเช็คขอบแล้วไปอ่าน flowerbed[-1] หรือ flowerbed[length] จนพัง หรือลืมอัปเดต flowerbed[i] = 1 หลังปลูก ทำให้ช่องถัดไปเข้าใจผิดว่าซ้ายยังว่าง" },
        { t: "p", c: "Time O(n) วิ่งไล่ลิสต์รอบเดียว · Space O(1) ปลูกในลิสต์เดิมไม่ใช้พื้นที่เพิ่ม" },
      ] },

      { t: "h2", c: "ข้อ 5 — กลับตำแหน่งสระ (LC345) 🟢" },
      { t: "p", c: "ให้สตริง s กลับตำแหน่งเฉพาะตัวอักษรที่เป็นสระ (a, e, i, o, u ทั้งพิมพ์เล็กและใหญ่) ส่วนพยัญชนะอยู่ที่เดิม เช่น \"hello\" → \"holle\" (สระ e กับ o สลับที่กัน) และ \"leetcode\" → \"leotcede\"" },
      { t: "callout", title: "คำใบ้", c: "ใช้ตัวชี้สองตัว ตัวหนึ่งจากซ้ายตัวหนึ่งจากขวา วิ่งหาสระของแต่ละฝั่ง เจอทั้งคู่แล้วสลับ เพราะ string แก้ทีละตัวไม่ได้ ให้แปลงเป็น list ก่อน" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def reverse_vowels(s):
    vowels = set("aeiouAEIOU")  # เช็คสมาชิกใน set เร็ว O(1)
    chars = list(s)             # string แก้ทีละตัวไม่ได้ ต้องเป็น list
    left, right = 0, len(chars) - 1
    while left < right:
        if chars[left] not in vowels:   # ซ้ายยังไม่ใช่สระ ขยับเข้า
            left += 1
        elif chars[right] not in vowels:  # ขวายังไม่ใช่สระ ขยับเข้า
            right -= 1
        else:  # ทั้งซ้ายและขวาเป็นสระ สลับได้
            chars[left], chars[right] = chars[right], chars[left]
            left += 1
            right -= 1
    return "".join(chars)

print(reverse_vowels("hello"))     # holle
print(reverse_vowels("leetcode"))  # leotcede` },
        { t: "p", c: "เพราะเราสนใจแค่สระ จึงใช้ตัวชี้สองตัวจากปลายทั้งสองข้างวิ่งเข้าหากัน ตัวซ้ายจะขยับเข้าจนกว่าจะเจอสระ ตัวขวาก็ขยับเข้าจนเจอสระเช่นกัน เมื่อทั้งคู่ชี้สระพร้อมกันก็สลับที่กันแล้วขยับเข้าทั้งคู่ ทำจนตัวชี้ชนกันตรงกลาง การเก็บสระไว้ใน set ช่วยให้เช็คว่าเป็นสระไหมได้เร็วแบบ O(1) และอย่าลืมว่าต้องรวมทั้งพิมพ์เล็กและพิมพ์ใหญ่" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลืมแปลง s เป็น list ก่อน (string assign ทีละตัวไม่ได้) หรือเผลอขยับตัวชี้ทั้งสองในกรณีที่ยังไม่เจอสระครบทั้งคู่ ต้องแยกเงื่อนไขให้ชัดว่าขยับเฉพาะฝั่งที่ยังไม่ใช่สระ" },
        { t: "p", c: "Time O(n) ตัวชี้สองตัวรวมกันเดินผ่านทุกตัวไม่เกินหนึ่งรอบ · Space O(n) สำหรับ list ของตัวอักษร (ใน Python เพราะ string immutable)" },
      ] },

      { t: "h2", c: "ข้อ 6 — กลับลำดับคำในประโยค (LC151) 🟡" },
      { t: "p", c: "ให้สตริง s กลับลำดับของคำ โดยตัดช่องว่างเกินออกด้วย (ช่องว่างหน้า-หลัง และช่องว่างซ้อนกันระหว่างคำเหลือช่องเดียว) เช่น \"the sky is blue\" → \"blue is sky the\" และ \"  hello   world  \" → \"world hello\"" },
      { t: "callout", title: "คำใบ้", c: "split() แบบไม่ใส่อาร์กิวเมนต์จะตัดคำและกำจัดช่องว่างเกินให้อัตโนมัติ เหลือแค่กลับลำดับ list แล้ว join กลับ" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def reverse_words(s):
    words = s.split()      # ตัดคำ + กำจัดช่องว่างเกินให้อัตโนมัติ
    words.reverse()        # กลับลำดับ (หรือใช้ words[::-1] ก็ได้)
    return " ".join(words) # ต่อกลับด้วยช่องว่างเดียว

print(reverse_words("the sky is blue"))     # blue is sky the
print(reverse_words("  hello   world  "))   # world hello` },
        { t: "p", c: "ข้อนี้ดูเหมือน Medium แต่ถ้ารู้จัก split() จะง่ายมาก เพราะ s.split() แบบไม่ใส่ตัวคั่นจะฉลาดพอที่จะแยกคำตามช่องว่าง และกำจัดช่องว่างหน้า-หลัง-ซ้อนกันให้เองทั้งหมด เหลือแค่ list ของคำล้วน ๆ จากนั้นกลับลำดับด้วย reverse() หรือ slice [::-1] แล้ว join กลับด้วยช่องว่างเดียว" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือไปใช้ s.split(\" \") ที่ระบุตัวคั่นเป็นช่องว่างตายตัว แบบนี้ช่องว่างซ้อนกันจะกลายเป็นคำว่าง \"\" ปนอยู่ใน list ทำให้ผลลัพธ์มีช่องว่างเกิน ต้องใช้ split() เปล่า ๆ เท่านั้นถึงจะกำจัดช่องว่างเกินได้" },
        { t: "p", c: "Time O(n) split, reverse และ join ล้วนเป็นเชิงเส้นตามความยาว · Space O(n) สำหรับ list ของคำและสตริงผลลัพธ์" },
      ] },

      { t: "h2", c: "ข้อ 7 — ผลคูณยกเว้นตัวเอง (LC238) 🟡" },
      { t: "p", c: "ให้ลิสต์ nums คืนลิสต์ answer โดย answer[i] คือผลคูณของทุกตัวใน nums ยกเว้น nums[i] เอง ข้อจำกัดคือห้ามใช้การหาร และต้องทำใน O(n) เช่น nums = [1,2,3,4] → [24,12,8,6] (เช่น ตัวแรก 24 = 2*3*4)" },
      { t: "callout", title: "คำใบ้", c: "คิดเป็นสองรอบ รอบแรกเก็บผลคูณของทุกตัวทางซ้ายของแต่ละตำแหน่ง รอบสองคูณด้วยผลคูณของทุกตัวทางขวา" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def product_except_self(nums):
    n = len(nums)
    answer = [1] * n

    # รอบแรก: answer[i] = ผลคูณของทุกตัวทางซ้ายของ i
    prefix = 1
    for i in range(n):
        answer[i] = prefix
        prefix *= nums[i]

    # รอบสอง: คูณด้วยผลคูณของทุกตัวทางขวาของ i
    suffix = 1
    for i in range(n - 1, -1, -1):
        answer[i] *= suffix
        suffix *= nums[i]

    return answer

print(product_except_self([1, 2, 3, 4]))  # [24, 12, 8, 6]` },
        { t: "p", c: "ไอเดียคือ ผลคูณของทุกตัวยกเว้นตัวเอง = (ผลคูณของทุกตัวทางซ้าย) คูณ (ผลคูณของทุกตัวทางขวา) รอบแรกวิ่งจากซ้ายไปขวา เก็บ prefix (ผลคูณสะสมของตัวที่อยู่ก่อนหน้า) ใส่ไว้ใน answer[i] ก่อนแล้วค่อยคูณ nums[i] เข้า prefix รอบสองวิ่งจากขวาไปซ้าย เก็บ suffix (ผลคูณสะสมของตัวที่อยู่ถัดไป) แล้วคูณเข้ากับ answer[i] ที่มีค่า prefix อยู่แล้ว ได้คำตอบครบโดยไม่ต้องหารเลย" },
        { t: "p", c: "จุดพลาดที่พบบ่อยคือลำดับการอัปเดต ต้องกำหนด answer[i] = prefix \"ก่อน\" แล้วค่อยคูณ nums[i] เข้า prefix ไม่งั้นจะเผลอนับตัวเองเข้าไปด้วย และเหตุที่ห้ามใช้การหารเพราะถ้ามีเลข 0 อยู่ในลิสต์ การหารจะพัง วิธี prefix/suffix นี้จึงทนต่อเลข 0 โดยธรรมชาติ" },
        { t: "p", c: "Time O(n) วิ่งสองรอบ · Space O(1) ไม่นับลิสต์ผลลัพธ์ (ใช้ตัวแปร prefix/suffix แค่สองตัว)" },
      ] },

      { t: "h2", c: "ข้อ 8 — ลำดับสามตัวที่เพิ่มขึ้น (LC334) 🟡" },
      { t: "p", c: "ให้ลิสต์ nums ตอบว่ามีสาม index i < j < k ที่ nums[i] < nums[j] < nums[k] หรือไม่ (ไม่จำเป็นต้องติดกัน) ต้องทำใน O(n) เวลา และ O(1) พื้นที่ เช่น [1,2,3,4,5] → true, [5,4,3,2,1] → false, [2,1,5,0,4,6] → true (คู่ 1 < 4 < 6 หรือ 1 < 5 < 6)" },
      { t: "callout", title: "คำใบ้", c: "เก็บ \"ตัวเล็กสุดที่เจอมา\" และ \"ตัวกลางที่เล็กที่สุดที่เจอมา\" ไว้สองตัว ถ้าเจอตัวที่ใหญ่กว่าตัวกลางเมื่อไหร่ แสดงว่ามีครบสามตัว" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def increasing_triplet(nums):
    first = float("inf")   # ตัวเล็กสุดที่เจอมา (ตัวที่ i)
    second = float("inf")  # ตัวกลางที่เล็กสุดที่เจอมา (ตัวที่ j)
    for x in nums:
        if x <= first:
            first = x        # อัปเดตตัวเล็กสุด
        elif x <= second:
            second = x        # x มากกว่า first แล้ว เป็นตัวกลางที่ดีกว่า
        else:
            return True       # x มากกว่าทั้ง first และ second → ครบสามตัว
    return False

print(increasing_triplet([1, 2, 3, 4, 5]))   # True
print(increasing_triplet([5, 4, 3, 2, 1]))   # False
print(increasing_triplet([2, 1, 5, 0, 4, 6]))  # True` },
        { t: "p", c: "เทคนิคนี้เก็บตัวแปรแค่สองตัว first คือค่าน้อยสุดที่เคยเจอ และ second คือค่ากลางที่น้อยสุดที่เคยเจอ (โดยที่มันต้องเคยมีตัวเล็กกว่ามาก่อนหน้าเสมอ) เมื่อวิ่งเจอค่าใหม่ ถ้ามันน้อยกว่าหรือเท่ากับ first ก็อัปเดต first ถ้ามันมากกว่า first แต่ยังน้อยกว่าหรือเท่ากับ second ก็อัปเดต second แต่ถ้ามันมากกว่าทั้งคู่ แปลว่าเราเจอ nums[i] < nums[j] < nums[k] ครบแล้ว ตอบ True ได้ทันที" },
        { t: "p", c: "จุดที่คนงงคือ ถึง second ถูกอัปเดต แต่ first ที่คู่กับมันอาจถูกเขียนทับด้วยค่าเล็กกว่าไปแล้วในภายหลัง จะยังถูกไหม คำตอบคือถูก เพราะการที่ second มีค่าได้ แปลว่า \"เคย\" มี first ที่เล็กกว่ามาก่อนหน้าจริง ๆ ในอดีต ลำดับ index จึงยังถูกต้องเสมอ นี่คือเหตุผลที่ตอบได้ใน O(1) พื้นที่โดยไม่ต้องจำ index" },
        { t: "p", c: "Time O(n) วิ่งรอบเดียว · Space O(1) ใช้ตัวแปรแค่สองตัว" },
      ] },

      { t: "h2", c: "ข้อ 9 — บีบอัดสตริง (LC443) 🟡" },
      { t: "p", c: "ให้ลิสต์ตัวอักษร chars บีบอัดในที่เดิม (in-place) โดยแทนกลุ่มตัวอักษรที่ซ้ำติดกันด้วยตัวอักษรนั้นตามด้วยจำนวน (ถ้าซ้ำแค่ 1 ตัวไม่ต้องใส่เลข) แล้วคืนความยาวใหม่ เช่น [\"a\",\"a\",\"b\",\"b\",\"c\",\"c\",\"c\"] → [\"a\",\"2\",\"b\",\"2\",\"c\",\"3\"] คืน 6 และ [\"a\"] → [\"a\"] คืน 1 ข้อสำคัญคือถ้านับได้ 12 ต้องแยกเป็น \"1\",\"2\" (ตัวเลขหลายหลักแตกเป็นทีละตัวอักษร)" },
      { t: "callout", title: "คำใบ้", c: "ใช้ตัวชี้สองตัว ตัวอ่าน (read) ไล่นับกลุ่มที่ซ้ำ และตัวเขียน (write) คอยเขียนตัวอักษรกับจำนวนทับลงในลิสต์เดิม" },
      { t: "details", summary: "กดดูเฉลย + คำอธิบาย", c: [
        { t: "code", lang: "python", c: `def compress(chars):
    write = 0          # ตำแหน่งที่จะเขียนผลลัพธ์ทับลงไป
    read = 0           # ตำแหน่งที่กำลังอ่าน
    n = len(chars)
    while read < n:
        char = chars[read]
        count = 0
        # นับว่าตัวอักษรนี้ซ้ำติดกันกี่ตัว
        while read < n and chars[read] == char:
            read += 1
            count += 1
        # เขียนตัวอักษรลงไป
        chars[write] = char
        write += 1
        # ถ้าซ้ำมากกว่า 1 เขียนจำนวนต่อท้าย (แตกเป็นทีละหลัก)
        if count > 1:
            for digit in str(count):
                chars[write] = digit
                write += 1
    return write

data = ["a", "a", "b", "b", "c", "c", "c"]
length = compress(data)
print(length, data[:length])  # 6 ['a', '2', 'b', '2', 'c', '3']` },
        { t: "p", c: "ข้อนี้ต้องแก้ในที่เดิม จึงใช้ตัวชี้สองตัว read สำหรับไล่อ่านและนับกลุ่มตัวซ้ำ กับ write สำหรับเขียนผลลัพธ์ทับลงในลิสต์เดิม เพราะผลลัพธ์ที่บีบอัดแล้วสั้นกว่าหรือเท่ากับของเดิมเสมอ ตัว write จึงไม่มีทางวิ่งแซง read ทำให้ไม่เขียนทับข้อมูลที่ยังไม่ได้อ่าน วิธีนับคือ loop ในตัววิ่ง read ไปเรื่อย ๆ ตราบใดที่ตัวอักษรยังเหมือนเดิม แล้วเขียนตัวอักษรกับจำนวนลงไป" },
        { t: "p", c: "จุดพลาดที่พบบ่อยมี 2 อย่าง อย่างแรกคือกรณีนับได้แค่ 1 ตัวต้องไม่เขียนเลข (เขียนแค่ตัวอักษร) อย่างที่สองคือเลขที่มากกว่า 9 เช่น 12 ต้องแตกเป็นตัวอักษร \"1\" กับ \"2\" ทีละหลัก ซึ่งเทคนิคง่าย ๆ คือแปลง count เป็น str(count) แล้ววนเขียนทีละตัว" },
        { t: "p", c: "Time O(n) read วิ่งผ่านทุกตัวครั้งเดียว · Space O(1) เขียนทับในลิสต์เดิม (str(count) ยาวไม่เกินไม่กี่หลัก ถือเป็นค่าคงที่)" },
      ] },

      { t: "links", c: [
        { title: "ถัดไป: Two Pointers →", slug: "lc75-two-pointers", desc: "ตัวชี้สองตัววิ่งไล่กัน" },
        { title: "← Big-O", slug: "lc75-bigo" },
      ] },
    ],
  },
};
